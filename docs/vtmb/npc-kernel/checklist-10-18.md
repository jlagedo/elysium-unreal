<!-- generated by `uv run elysium research kernel_ledger`; do not hand-edit -->
# NPC kernel — porting checklist, layers 10–18

_vampire.dll sha256 `c546f4de2003624d…`, corpus dumped 2026-08-21; closure depth 6. Rebuild: `uv run elysium research kernel_ledger`._

One row per *core* function (a family or helper class method, or a body touching an offset past `CBaseCombatCharacter`'s layout) whose `order.md` layer falls in this band, in build order. The **Verdict** column is the porting story's record of what it did with the body, and it is not stored here: it comes from `research/tooling/ghidra/driver/kernel_verdicts.tsv`, so regenerating this table from the corpus never loses one.

- **rule** — the body carries a formula, a threshold, an ordering or a state write a program can observe. It is ported verbatim onto the port's virtual, cites the retail address, and has one automation test derived from the decompiled C. *Target* names the port method; *Evidence* the test.
- **mechanism** — Unreal already supplies it (a trace, a physics query, an audio call, a string or CRT routine, a math or container helper). *Target* names the service seam; nothing is ported.
- **present** — the port already runs it. *Target* names the port function, which cites the retail address, so the Port column below turns true on the next run.
- **dead** — no caller in the closure and no slot. Recorded and left.
- **unsettled** — read and not settled; *Evidence* says why. Counted apart: it is a recorded failure to reach a verdict, not a verdict.

‼ marks a damaged decompilation; those rows are read from `corpus asm <addr>` one at a time and are never verdicted in bulk.

| Measure | Count |
|---|---|
| Core functions, layers 10–18 | 344 |
| … `rule` | 269 |
| … `mechanism` | 67 |
| … `present` | 8 |
| … `dead` | 0 |
| … `unsettled` | 0 |
| … no verdict yet | 0 |
| … damaged decompilation | 4 |
| … cited by the port | 271 |
| … cited by the oracle | 266 |
| … cited by neither and unverdicted | 0 |

| Address | Function | Size | Layer | Slots | Writes | Reads | Callers | Verdict | Target | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|
| `0x1009ebb0` | CAISound::FUN_1009ebb0 | 190 | 10 | `CAISound#109`, `CAI_Hint#109`, `CAI_InterestingPlace#109`, +2 more | — | `+0x01b8`, `+0x026c` | 1d/0v/0c | `rule` | FElysiumEntity::KeyValue | Slot 109 on the CAISound/CAI_Hint/CAI_InterestingPlace/CAI_StandoffGoal line, CBaseEntity::KeyValue(key, float): pushes one scope-trace frame naming CBaseEntity::KeyValue and the entity (NULL_ENTITY when this is null, m_iName otherwise, the empty string when unnamed), Q_snprintf's the float into a 256-byte stack buffer with the format at DAT_10554f28, dispatches its OWN slot 110 KeyValue(key, const char*) through vtable +0x1b8 with that buffer, then pops the frame; it writes no member and returns nothing of its own, so the forward is the whole behaviour. |
| `0x1009eca0` | CAISound::FUN_1009eca0 | 212 | 10 | `CAISound#108`, `CAI_Hint#108`, `CAI_InterestingPlace#108`, +2 more | — | `+0x01b8`, `+0x026c` | 1d/0v/0c | `rule` | FElysiumEntity::KeyValue | Slot 108 on the same line, CBaseEntity::KeyValue(key, Vector): identical shape to 0x1009ebb0 but formats three floats with the "%f %f %f" literal at 10555584 into the 256-byte buffer and forwards to slot 110 KeyValue(key, const char*) at vtable +0x1b8 under the same CBaseEntity::KeyValue scope-trace frame; writes no member. |
| `0x101c1480` | CAI_BaseNPC::FUN_101c1480 | 114 | 10 | `CAI_BaseHumanoid#110`, `CAI_BaseNPC#110`, `CAI_BaseNPCTroika#110`, +74 more | `+0x04fc`, `+0x0504` | — | 0d/0v/0c +2 outside | `rule` | hand:FElysiumNpc::KeyValue | Slot 110 KeyValue(key, value) on the Troika line (77 classes), read off the listing: __strcmpi against "lip" (10561fc0) stores atof(value) into m_flLip at +0x504 (FSTP at 101c14a4) and returns true; __strcmpi against "distance" (1053f4c4) stores atof(value) into m_flMoveDistance at +0x4fc (FSTP at 101c14d0) and returns true; anything else tails to CBaseEntity::KeyValue 0x1009e430 and returns its answer verbatim. Both compares are case-insensitive and a non-numeric value writes 0.0 and still answers true. Ported with the two words carried on the leaf and the tail running family Lifecycle's recovered ClassifyKeyValue cascade, whose DataMap arm is this runtime's class-chain field table. |
| `0x1025fa50` | CAI_BaseHumanoid::MaintainEyeDirection | 2226 | 10 | `CAI_BaseHumanoid#333` | `+0x0854`, `+0x5f74`, `+0x5f78`, `+0x5f7c`, +2 more | `+0x011c`, `+0x0304`, `+0x0454`, `+0x046c`, +14 more | 0d/0v/0c | `rule` | FElysiumNpc::BaseHumanoidMaintainEyeDirection | CAI_BaseHumanoid#333, 2226 bytes, read off the LISTING because the decompiler aliases six stack floats across the three passes. THREE corrections to the earlier walk. One: the gate at vt+0x928 is CAI_BaseHumanoid#586 = 0x1025f1a0, whose whole body is return the look-queue count is non-zero - HasActiveLookTargets, not an is-in-a-scene test - so the cycler/PickLookTarget block is an ACQUISITION taken when the queue is EMPTY. Two: the no-claimant fallback is not a velocity-projected point: 1026008c is VectorVectors(HeadDirection3D, right, up) and then eye + forward times 128.0 (_DAT_1046dcd0) + right times RandomFloat(-16,16) + up times RandomFloat(-32,32). Three: the dot floor _DAT_10497ca0 is a DOUBLE and reads -0.5, recovered from the pinned image; story 29c-1 carried it unrecovered at 0.0. Also recovered: the ACCEPTED arm calls slot 537 SetHeadDirection BEFORE writing +0x5f74 and the DECAY arm writes +0x5f74 first and then calls it, and only the decay arm re-normalises; the prune walk does not advance its index on a removal; the blink compare is strict. |
| `0x102709c0` | CAI_BaseNPC::FUN_102709c0 | 175 | 10 | `CAI_BaseHumanoid#544`, `CAI_BaseNPC#544`, `CAI_BaseNPCTroika#544`, +74 more | — | `+0x07b8`, `+0x0874`, `+0x5bb0`, `+0x5d34`, +1 more | 0d/2v/2c | `rule` | hand:FElysiumNpc::UpdateEnemyMemory | slot 544 UpdateEnemyMemory(enemy, pos, informer): GetEnemies (+0x874) null returns TRUE at once and writes nothing; with a non-null enemy it reads the enemy's NPC sub-object at enemy+0x94 (param_1[0x25], NOT +0xa8) and, when that exists AND our own m_iSquadDisconnected at +0x5bb0 is below 1 AND our squad word +0x5da4 is non-zero, forms the enemy's squad word as 0 when the enemy sub-object's own +0x5bb0 is above 0 and its +0x5da4 otherwise, then returns FALSE when that word equals ours and the enemy's +0x5bb0 is still below 1, so squadmates never enter each other's memory; then IsEluded 0x102e0210, called on the same GetEnemies list, fires FoundEnemySound slot 0x7b8; then it forwards unconditionally to CAI_Memory::UpdateMemory 0x102df700 with the node array at m_pNavigator+0x2c, the enemy, the position and the velocity, and returns that answer. NOTE the pack-01 row misnames two offsets: m_iSquadDisconnected is +0x5bb0 and +0x5d34 is m_pNavigator, and the enemy sub-object is +0x94 not +0xa8. The port's squad word is a seam answering 0, retail's own no-squad value, which is the admitting arm. |
| `0x10288710` | CAI_BaseNPC::FUN_10288710 | 78 | 10 | `CAI_BaseHumanoid#445`, `CAI_BaseNPC#445`, `CAI_BaseNPCTroika#445`, +74 more | — | `+0x068c`, `+0x0844`, `+0x08fc`, `+0x5bbc`, +2 more | 0d/0v/1c | `rule` | hand:FElysiumNpc::StartTaskOverlay | slot 445: gated entirely on IsCurTaskContinuousMove (slot 529, +0x844) - a false answer leaves the overlay untouched; then, when ShouldMoveAndShoot (slot 575, +0x8fc) is false, it disables the move-and-shoot overlay by writing FLT_MAX (0x7f7fffff) into m_MoveAndShootOverlay+0x18 through 0x102e8250 and returns; otherwise it calls UpdateBurstShootPause (slot 419, +0x68c) FIRST and then 0x102e8270 with m_flBurstShootPauseMin (+0x5bbc) and m_flBurstShootPauseMax (+0x5bc0), which re-derives the overlay's shot counts from the active weapon's data (+0x3a4/+0x3a8), stores the pause pair at overlay+0x24/+0x28 and re-arms overlay+0x18 = curtime + overlay+0x2c, falling back to the same 0x102e8250 disable when the NPC is in state 4, has no weapon, or lacks either the 0x11 or the 0x15 activity sequence. |
| `0x10299700` | CAI_BaseNPCTroika::Restore | 344 | 10 | `CAI_BaseNPCTroika#127`, `CGeneric_NPC#127`, `CNPCMaker#127`, +50 more | — | `+0x607c`, `+0x6084`, `+0x60b0`, `+0x60dc`, +18 more | 4d/0v/0c | `rule` | hand:FElysiumNpc::Restore | Slot 127, the Troika Restore. CAI_BaseNPC::Restore 0x1027c160 runs FIRST, its answer is kept in EBX and returned unchanged at 10299851; then IRestore vt+0x44 reads one bool whose truth gates two vt+0x3c reads into m_iRestorePedLinkNode +0x6310 and m_iRestorePedLinkDestNode +0x6314; then the same eleven stamps and nine CAISound expiry stamps the Troika Save encodes, decoded through 0x101cf2f0 and 0x101b9860 in the same order with the same modes. This body's decompiled C is unaliased and reads the eleven out field by field, which is the independent confirmation of 0x102993c0's decode list. Ported as story 29d family SaveRestore10 with five species arms (CScriptedTarget 0x1034e370, CNPC_VMingXiao 0x10396000, CNPC_VMingXiaoTentacle 0x1039eda0, CNPC_VTzimisceHeadClaw 0x103c2860, CNPC_VVampireBoss 0x103c5910). |
| `0x1029aa10` | CAI_BaseNPCTroika::ProcessTweakParam | 735 | 10 | `CAI_BaseNPCTroika#585`, `CGeneric_NPC#585`, `CNPCMaker#585`, +61 more | `+0x63b0`, `+0x63b4`, `+0x63bc`, `+0x65dc`, +2 more | `+0x63b0` | 0d/0v/1c +1 outside | `rule` | hand:FElysiumNpc::ProcessTweakParam | Slot 585's Troika-line body, so hand: applies. The tweak-file key dispatch in retail's own __strcmpi order, eleven literals. CAPABILITIES and GOALS are RECOGNISED, print their own DevMsg placeholder and then FALL THROUGH to the ignore message, so both print. NPCPERCEPTION atoi's into +0x63b0 and clamps twice, each clamp RE-READING the field (1029aabd) so the two cannot both fire, then runs 0x1028fb70 and 0x1028fc90. VISION and HEARING store BEFORE the branch (1029ab1d FST, 1029ab8e FST) and run the same two recomputes on BOTH the error path and the accepting path; the guard is value < _DAT_104454c4 AND value != _DAT_104492dc, which the pinned image reads as the FLOATS 0.0f (FCOM dword at 1029ab17) and -1.0f (FCOMP dword at 1029ab31). SQUAD, IPGROUPS and HINTGROUPS forward to the ported 0x1029a930, 0x10298910 and 0x102989e0; TPMOVETIMER is curtime + atof; IGNOREATTACK and NOALERTSTATE are atoi != 0 into +0x65f5 and +0x65f6. NAMED DIVERGENCE: retail's Error() does not return, so its clamps are dead code and the game exits; the port logs and clamps, a crash guard. |
| `0x1029adb0` | CAI_BaseNPCTroika::TaskFail | 410 | 10 | `CAI_BaseNPCTroika#448`, `CGeneric_NPC#448`, `CNPCMaker#448`, +50 more | `+0x14b8`, `+0x14bc`, `+0x5d8c`, `+0x6244`, +12 more | `+0x003c`, `+0x14b8`, `+0x14bc`, `+0x5d44`, +4 more | 9d/20v/43c | `present` | hand:FElysiumNpc::TaskFail | slot 448; the Troika body is FElysiumNpc::TaskFail (ported in story 13) and story 29d adds the SEVEN species arms in front of it as one prologue call on its first line. Every species body runs its arm and then chains 0x1029adb0 unconditionally, so the arms are a prologue and not a replacement: AsianVampire 0x10362390 sets m_bPathBlocked on codes 12..15, ChangBros 0x1036d1d0 writes m_failSchedule 0x15d on the same window, Gargoyle 0x10379060, Hengeyokai 0x10380510 and Tzimisce 0x103ba350 clear m_afMemory's top bit in combat with TASK_FAILED standing as an interrupt and then work the FINDING_BODY and CARRYING_BODY bits, MingXiao 0x10394090 switches on m_eThrowableObjectMode, and SheriffMan 0x103b0290 adds nothing at all. |
| `0x102a1650` | FUN_102a1650 | 455 | 10 | — | — | `+0x0364`, `+0x5d40`, `+0x6290`, `+0x6294`, +4 more | 2d/0v/0c | `rule` | FElysiumNpc::TraceMoveClearanceAtYaw | Ported. CORRECTED on two points from the listing. (1) The decompiler types the body void and it is NOT: 102a1807 tail-calls the move probe and RET 0xc hands back whatever it left in AL, which is why all four call sites read the byte - a blocked sweep is what refuses the task. (2) Both scale cells are RECOVERED out of the pinned image rather than unrecovered: _DAT_10462950 = 40.0 (the forward leg) and _DAT_10451acc = 64.0 (the right leg). End = GetAbsOrigin + cos(yaw) * m_vecForward * 40 * reach + sin(yaw) * m_vecRight * 64 * reach, swept through m_pMoveProbe +0x5d40 (0x102e6d70) with mask 0x202400b and the literal 100.0. The first argument is read by no arm. NOTE for family Schedule: _DAT_10451acc is the same cell ElysiumNpcKernelSchedule.cpp carries as GScheduleMeleeHeightDiffUnits and still reads UNRECOVERED 0.0. Walked into docs/vtmb/combat-and-damage.md. |
| `0x102b7cf0` | FUN_102b7cf0 | 462 | 10 | — | — | `+0x029c`?, `+0x0940`?, `+0x6435`? | 3d/0v/0c | `rule` | FElysiumNpc::SelectCombatReactionSchedule | Ported. CORRECTED: only the COND_LOST_ENEMY arm (0x47 giving schedule 0x10, line 23844) sits outside the enemy gate. 102b7d2a opens ONE block on GetEnemy() != 0 AND CanSeekCover() (slot 592, vt+0x940) and the cover offer 0x102b7690(1,1,1,1), the cvar gate DAT_109248f4, the 0x800 DODGING taunt arm and the SEE_ENEMY arm are ALL inside it - the previous walk read the cvar-gated half as a sibling of that gate. RECOVERED out of the pinned image: _DAT_10463584 = 15.0, the amount the 0x8f arm ADDS to m_flNextDodgeTime +0x65a4 (it is an add, not a re-base on curtime). RETARGET: the row named ElysiumNpcCombat::SelectCombatReactionSchedule; the body needs the NPC flag words, the frenzied word +0x5b84 and the dodge stamp, so it lands as a member of FElysiumNpc beside the three selectors that offer it. Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x102c5500` | CAI_BaseNPCTroika::FUN_102c5500 | 69 | 10 | `CAI_BaseNPCTroika#419`, `CGeneric_NPC#419`, `CNPCMaker#419`, +61 more | `+0x5bbc`, `+0x5bc0` | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::UpdateBurstShootPause | slot 419: GetActiveWeapon first; with a weapon m_flBurstShootPauseMin (+0x5bbc) takes 0x102c5780 = the scaled weapon word at wpndata+0x264 and m_flBurstShootPauseMax (+0x5bc0) the word at +0x268, both through 0x102c5570 with the weapon data from 0x102517e0; with NO weapon the two literals 0.3 (0x3e99999a) and 0.5 (0x3f000000) are written and the body returns. CORRECTION: the checklist walk says no port member carries the burst pause pair - FElysiumNpc::BurstShootPauseMin and BurstShootPauseMax are both bound and both in the shape map at those offsets. 0x102c5570 read from the listing: the scale seeds at 1.0 (_DAT_104454c0), stays 1.0 when wpndata+0x26c is at or below _DAT_104454c4, becomes sqrt(distance/range) past it and sqrt(1.0/range) with no enemy, and the answer is scale times (value minus wpndata+0x260) - the decompiler turned the x87 compare chain into a status-word ushort. |
| `0x102e14a0` | CAI_Motor::vfunc19 | 129 | 10 | `CAI_Motor#19` | — | `+0x0030`, `+0x003c`, `+0x0048` | 0d/0v/0c | `rule` | FElysiumNpc::MoveGroundExecute | PORTED 29d Motor10 (listing). CORRECTION: it RETURNS 0x102e1560's answer - CALL 0x10012116 then POP EDI/ESI/ECX and RET 0xc leaves EAX alone - so the decompiler's void signature is wrong and the row is an AIMoveResult_t. Four statements: slot 18 (+0x48) MoveFacing on the goal FIRST; speed = GetCurSpeed 0x102e12c0, whose whole body is JMP [[owner]+0x3e0], the owner's slot 248 GetIdealSpeed; step = (¦m_vecVelocity¦ + speed) * m_flMoveInterval (+0x30) * 0.5 (_DAT_10449270, a double); then 0x102e1560(goal, speed, step, arg2, arg3) - the two trailing arguments are slot 19's OWN arg2 and arg3 (the AIMoveTrace_t out param and the filter), which the decompiler mis-attributes as param_1 and param_2. 0x102e1560 is ported here too: step <= goal maxDist zeroes the interval and leaves the step; otherwise the goal flag 0x2 zeroes it and anything else scales it by (1.0 - maxDist/step), and either way the step is clamped to maxDist; m_vecVelocity (+0x3c) = goal dir * speed; above _DAT_1044fab0 (0.0, strict) it steps through 0x102e0bd0 from slot 220 GetOrigin with the goal's blocker +0x34, calling motor slot 10 (+0x28) and answering 0 on refusal; at or below it the LOCAL NAVIGATOR at motor+0x10 slot 6 (+0x18) answers, folded to 1 / 0. |
| `0x102e1760` | CAI_Motor::FUN_102e1760 ‼ | 506 | 10 | `CAI_Motor#20` | `+0x0030` | `+0x0004`, `+0x0030`, `+0x003c`, `+0x0048` | 0d/0v/0c | `rule` | FElysiumNpc::MotorMoveGroundStep | PORTED 29d Motor10 (listing). CORRECTION: 0x101cf5c0 is UTIL_SetOrigin, NOT NDebugOverlay::Line - its body is entity->slot 62 (+0xf8) SetLocalOrigin(vec) then PhysicsTouchTriggers when the third argument is non-zero, and docs/vtmb/npc-kernel/layout.md already names it the SetAbsOrigin helper - so the last arm MOVES the body to trace+0x04, the trace endpoint. Confirmed from the listing: the trace record is copied into the SECOND argument (EDI = [ESP+0x78] = argument 2) and the move goal is never written back; return 4 additionally requires the goal's expected blocker +0x34 to be NON-ZERO (MOV EBX,[EBX+0x34]; TEST EBX,EBX; JZ -> 0) before trace+0x1c is compared to it; vt+0x370 is slot 220 GetOrigin. Also recovered: the copy writes +0x00, +0x04, +0x08, +0x0c, +0x10, +0x14, +0x18, +0x1c, +0x20, +0x24, the EHANDLE at +0x28 through its assignment operator and +0x34 - +0x2c and +0x30 are NOT copied. The AIMoveTrace_t layout comes from 0x102e6d70's own initialiser: +0x00 fStatus, +0x04 vEndPosition seeded to the START, +0x10 vHitNormal = vec3_origin, +0x1c pObstruction, +0x20 flDistObstructed, +0x24 flTotalDist, and the trace answers fStatus >= 0. The sweep is kind 2, mask 0x202400b, extent 100.0 (0x42c80000), filter = argument 3. |
| `0x1032ce40` | CAI_BaseNPC::FUN_1032ce40 | 511 | 10 | `CAI_BaseHumanoid#386`, `CAI_BaseNPC#386`, `CAI_BaseNPCTroika#386`, +74 more | `+0x0ea4`, `+0x0ea8` | `+0x026c`, `+0x0ea4`, `+0x0ea8`, `+0x1589` | 0d/2v/1c | `rule` | hand:FElysiumNpc::Weapon_Drop | Ported as slot 386. Gated on m_bCantDropWeapons +0x1589 clear AND a non-null GetActiveWeapon AND thunk_FUN_102577e0 on it. With FL_NPC 0x2000 the ammunition is REROLLED first, i = 0 and 1 (iVar9 steps 0x1093c while below 0x21278, exactly twice) into weapon+0x74c+4*i: the weapon vt+0x454 true takes RandomInt(1, wpndata+0x2674+i*0x1093c) via 0x102517b0, false takes vt+0x450 as a cap and writes RandomInt(1, cap) or a FLAT ZERO when the cap is below 1. Then thunk_FUN_100b5190 gates vt+0x138; then m_hLastWeapon +0xea4 and m_hLastMeleeWeapon +0xea8 are each reset to 0xffffffff ONLY when they resolve to THIS weapon; then vt+0x4b0, Inventory_Remove, and Weapon_Detach only when FL_NPC is still set (retail re-reads GetFlags). GetActiveWeapon is no longer a seam: family Debug ActiveWeaponEntity now answers FElysiumInventory::Active. |
| `0x1034b7b0` | FUN_1034b7b0 | 850 | 10 | `CNPCMaker#617` | `+0x01f0`, `+0x6660`, `+0x66b0`, `+0x66b8` | `+0x0364`, `+0x09a8`, `+0x09ac`, `+0x09b0`, +23 more | 1d/0v/9c +1 outside | `rule` | FElysiumNpcMaker::TrySpawn | CORRECTIONS to the walk, from the listing. (1) The final write is 1034baee MOV byte ptr [EDI+0x65f4],0x1 - m_bCameFromSpawner on the CHILD, not "the child's +0x2c4 latch", which is Ghidra's this_00[0x17].field_0x2c4 view of the same byte. (2) The block at 1034ba0f..1034ba69 is NOT the script-state block: the listing copies plain offsets +0x6420/+0x6424/+0x6428/+0x642c/+0x6430 as dwords and +0x6434/+0x6435/+0x6436 as bytes, and the datamap names them m_iPercentOccludedWait/Cover/Walk/Flank/Chase, m_bAllowAlertLookaround, m_bStayEntrenched and m_bAllowKickHintUse - nine inherited words, and Ghidra's m_ScriptSavedMoveCollide / m_bScriptHidden / m_fScriptSavedEffects / m_hSoundOverrideEnt / m_bFakeSilence names come from a wrong struct applied to the child pointer. (3) 1034b9ee MOV ECX,ESI puts the MAKER, not the child, in the this register for thunk_FUN_1028fb70 InitPerceptionDistances and thunk_FUN_1028fc90, so the child inherits the three authored perception words at +0x63b0/+0x63b4/+0x63bc and NOTHING derives the resolved pair from them here - a retail oddity, reproduced and counted. (4) The +0x1584 copy is m_RelationshipString, a CBaseCombatCharacter string_t, and it happens BEFORE the OnSpawnNPC output at +0x6668 fires. Slots 619 ChildPreSpawn 0x1034af30 and 620 ChildPostSpawn 0x1034af50 are both empty on CNPCMaker, which is a fact and not a gap. Ported as story 29d family Lifecycle10. |
| `0x1034d0a0` | FUN_1034d0a0 | 122 | 10 | `CNPCMaker_Zombie#618` | — | `+0x0364`, `+0x76d8` | 0d/0v/7c | `rule` | FElysiumNpcMaker::CanMakeNpcZombie | CNPCMaker_Zombie's slot 618 override of CanMakeNPC, confirmed with its constant pinned: _DAT_10450a9c is 0.9, read out of the pinned vampire.dll at file offset 0x450a9c (base 0x10000000). A non-zero bypass answers true before anything else; otherwise, with a resolvable player, the MANHATTAN distance abs(dx)+abs(dy)+abs(dz) between the two GetAbsOrigins is scaled by 0.9 and the maker REFUSES when that product EXCEEDS +0x76d8 - a zombie maker refuses when the player is too FAR, the opposite sense of the base's Euclidean minimum-distance arm at +0x66c8; then it forwards to CNPCMaker::CanMakeNPC 0x1034b580 with a HARD-CODED false bypass, so the base's live limit, global no-spawn byte, npcclip, view cone, minimum distance and hull-occupancy trace all still run. Ported as story 29d family Lifecycle10. |
| `0x1034e370` | CScriptedTarget::Restore | 40 | 10 | `CScriptedTarget#127` | — | `+0x5f64` | 0d/0v/0c | `rule` | FElysiumNpc::Restore | CScriptedTarget slot 127: CAI_BaseNPC::Restore (0x1027c160) runs and ITS answer is returned unchanged; the only addition is one absolute-stamp fixup through 0x101cf2f0 on m_flPauseDoneTime (+0x5f64) with mode 3, which rewrites the stamp to 0.0 when it stands at or above _DAT_10482fac and leaves it alone otherwise. Nothing else is read or written. The port carries CScriptedTarget's Spawn words and its two debug overlays (ElysiumNpcKernelDebug.inl) but no Restore. CONVENTION: slot 127's port method is FElysiumNpc::Restore - both 0x1027c160 (band 5-9) and the Troika 0x10299700 target it - so CScriptedTarget's arm is keyed on IsRetailClass(CScriptedTarget) inside that one method rather than coined; relative to the base CAI_BaseNPC::Restore the arm ADDS exactly one absolute-stamp fixup on m_flPauseDoneTime +0x5f64 after the base runs and returns the base's answer unchanged. ARGUED: classes.md stands CScriptedTarget in the 77-class tree (CAI_BaseNPC, 583 slots, 8 own bodies) and ElysiumNpcKernelShape.cpp:4467 carries its census row, so Find, OverrideOf and BodyOf key on it; its Entity classnames column is empty and no CScriptedTarget_Classnames table exists, and RetailClass() is OfClassname(Def->Classname) (ElysiumNpcKernelClassLookup.cpp:156), so the arm cannot be SELECTED until a scripted_target spawn leaf is registered - the same gap CNPC_VCop, CNPC_VWerewolf, CNPC_VZombie, CNPC_VGhoulCroucher and CNPC_VSheriffMan share and band 0-9 retargeted anyway, so it is not a reason to keep a coined method. |
| `0x10363db0` | CNPC_VBach::GatherAttackConditions | 573 | 10 | `CNPC_VBach#561` | `+0x5b80`, `+0x6684`, `+0x6688`, `+0x66a5`, +1 more | `+0x02e0`, `+0x0378`, `+0x13bc`, `+0x13c0`, +4 more | 0d/0v/0c | `rule` | FElysiumNpc::GatherAttackConditions | CNPC_VBach slot 561, ported as BachGatherAttackConditions and run from the top of FElysiumNpc::GatherAttackConditions. The whole shield/teleport block is gated on HasCondition(0x4c) or (0x4d); inside, m_bCondTookDamage +0x5b80 is cleared first, then distance at or above DAT_1062d200[m_iBachTeleportState] OR m_flNextHolyLightTime past takes the SHIELD arm and anything else sets condition 0x7b. DAT_1062d200 RECOVERED out of the pinned image at 0x62d200: four floats 768.0, 768.0, 384.0, 384.0. The shield arm needs m_flNextShieldTime +0x6688 past, then CVStatList_t::SetBase(stat 0xd, 5) on the type-3 list, m_flNextShieldTime = curtime + 20.0, m_bShieldActive +0x66a5 = 1, m_flShieldTime +0x6684 = curtime + 6.0, and the sound - STRING CORRECTED, 0x1062ea6c is Character/Boss/Bach/bach_shield.wav, with the .wav - on channel 2 at volume 1.0 attenuation 0.8 pitch 100. +0x66a6 is cleared on either branch. Independently, when m_flNextWeaponSwitchTime +0x668c is past, condition 0x79 at or above _DAT_104704d0 and 0x7a below; VALUE RECOVERED at 0x4704d0: 72.0. The base 0x1026dd10 runs last, unmodified. |
| `0x10369fb0` | CNPC_VCameraSecurity::FInViewCone | 45 | 10 | `CNPC_VCameraSecurity#363` | — | — | 0d/0v/3c | `rule` | FElysiumNpc::FInViewCone | CNPC_VCameraSecurity slot 363, FInViewCone(CBaseEntity*): resolves the linked CSecCamera through 0x10369e70 (the port's FElysiumNpc::ResolveSecCameraLink), then answers true ONLY when the argument carries a non-null troika sub-object at +0xa8 AND the link resolved AND the camera's own cone test 0x1020cc60 passes; that test re-checks the argument's +0xa8, takes the camera's forward from its angles (camera slot +0x374 through 0x10139610), the normalised direction from the camera's GetAbsOrigin (+0x364) to the argument's WorldSpaceCenter (argument slot +0x300) via 0x101d1120, and requires the dot to be STRICTLY greater than the camera's cosine at +0x798. So a security-camera NPC sees through its camera and never through its own eyes, and answers false whenever the link is missing. The port cites 0x10369fb0 only in a comment at ElysiumNpcKernelDialogue.cpp:621; the slot itself is unwritten. CONVENTION: species arm of slot 363's FElysiumNpc::FInViewCone keyed on IsRetailClass(CNPC_VCameraSecurity); it REPLACES the base 0x102b4540 cone test outright - the NPC's own eyes, forward vector and cone cosine are never consulted, the answer comes from the linked CSecCamera and is false whenever the link is missing. |
| `0x1036cfa0` | CNPC_VChangBros::SelectLedgeNode | 261 | 10 | — | — | `+0x0364` | 1d/0v/0c | `rule` | FElysiumNpc::ChangBrosSelectLedgeNode | walks the global hint list DAT_10925450 through +0x5d8, keeps nodes whose type word +0x5dc is 0x4653 and that CheckJumpPathToHintNode accepts, scores each by the full 3-D distance from the node's GetAbsOrigin to its own, seeds the incumbent with -FLT_MAX and REPLACES on a strictly greater distance - so ChangBros picks the FARTHEST reachable ledge, the opposite comparison from CNPC_VSheriffMan::SelectLedgeNode 0x103b0ab0 that the port's SelectLedgeNodeRule carries. Unlike the Sheriff's twin there is no closest-player gate in front of it. Returns null when nothing qualifies. |
| `0x10372cc0` | CNPC_VCop::vfunc597 | 201 | 10 | `CNPC_VCop#597` | `+0x6664` | `+0x0740`, `+0x628c`, `+0x6664` | 0d/2v/4c | `rule` | FElysiumNpc::Slot597 | CNPC_VCop slot 597, ported as CopSlot597Prologue and run from the top of FElysiumNpc::Slot597. Gated on the argument being the very entity m_hClosestPlayer +0x628c resolves to; inside, when m_hPursuitPlayer +0x6664 does NOT resolve live and GetState (slot 464) is 2 COMBAT, it latches the player's handle, writing 0xffffffff when the argument carries no player record; then unconditionally InputSetRelationship with the literal Player D_HT 10 at priority 0; then the Troika base 0x102b4fb0 unchanged. CORRECTION: argument+0xa8 is m_pPlayer, the player self-downcast cache, not a troika sub-object, so the latch stores the PLAYER's handle. Family Debug10's CopPursuitPlayer seam now resolves the word this writes. |
| `0x1037e2d0` | FUN_1037e2d0 | 20 | 10 | — | `+0x6660`? | — | 2d/0v/0c | `rule` | FElysiumNpc::Guard1HatePlayer | CNPC_VGuard1's two-statement hate latch, reached from OnStateChange 0x1037d020 and six times from vfunc461 0x1037d290: sets the byte at +0x6660 to 1 and then calls InputSetRelationship 0x10273790 with the literal player D_HT 10 - lower case player, unlike CNPC_VCop's capital P - at priority 0. |
| `0x10385e40` | CNPC_VHuman::SelectScheduleMeleeCombat | 1449 | 10 | `CNPC_ProneDialog#604`, `CNPC_VAndreiBlood#604`, `CNPC_VBatSwarm#604`, +32 more | `+0x1b30`, `+0x1b34`, `+0x6274` | `+0x029c`, `+0x04d0`, `+0x095c`, `+0x0964`, +5 more | 1d/4v/8c | `rule` | FElysiumNpc::SelectScheduleMeleeCombat | CNPC_VHuman#604, 1449 bytes, slot 604 for 34 census classes; it REPLACES the Troika body 0x102b6c30 wholesale and never chains it. THREE float compares decoded off the listing because the decompiler renders them as an inequality against an equality: at 10385ee1 the scripted-combat far arm is twice the cvar AT OR BELOW the distance (the earlier walk said exceeds); at 10385f5b the not-engaged far arm needs cvar plus 200 STRICTLY below the distance; at 102862f6 the near pair 0xd1/0xd2 needs the distance STRICTLY below the bare cvar (the earlier walk said at or below). Also recovered: 10385f99 CMP EAX,0x19 JL means the roll must EXCEED 24, not reach 25; the common tail offers 0x102b7370 SelectDoorObstructionSchedule and THEN 0x102b6fe0, returning either non-zero answer; COND 0xf and COND 0xe are BOTH read before either is tested; and the height-diff retry stamp is ticked whatever the arms below it decide. |
| `0x10396050` | CNPC_VMingXiao::SelectScheduleMeleeCombat | 1522 | 10 | `CNPC_VMingXiao#604` | `+0x1b30`, `+0x1b34`, `+0x6274` | `+0x029c`, `+0x04d0`, `+0x0968`, `+0x6078`, +3 more | 0d/0v/8c | `rule` | FElysiumNpc::SelectScheduleMeleeCombat | CNPC_VMingXiao#604, 1522 bytes, the same skeleton as CNPC_VHuman's 0x10385e40 with four stated differences: the not-yet-engaged arm tests the distance against the cvar plus 200 FIRST and does not offer the melee failure gate at all; the common tail offers ONLY 0x102b7370; it opens with an extra COND 0x48 ENEMY_OCCLUDED arm that returns 0xe9 after slot 601 when slot 308 passes and 0xcd otherwise; and there is NO COND 0x0d SHOULD_BLOCK arm. It also re-fetches GetEnemy at every use rather than caching it, which is observable only in the dispatch count. |
| `0x103a0ab0` | FUN_103a0ab0 | 525 | 10 | — | `+0x6668`?, `+0x666c`?, `+0x667c`?, `+0x6680`?, +1 more | `+0x665c`?, `+0x6660`?, `+0x6668`?, `+0x6670`?, +2 more | 1d/0v/0c | `rule` | FElysiumNpc::LoadNewscasterStories | CNPC_VNewscaster's story load, reached from 0x103a0670. PATH CORRECTED: 0x1064aadc and 0x1064aac0 are the FORMAT strings %sNewscaster_Main.txt and %sNewscaster_Side.txt handed to UTIL_VarArgs 0x101d3730 with the one vararg at 0x105a0f80, which is vdata/system/ - so the files are vdata/system/Newscaster_Main.txt and _Side.txt, and the earlier walk read the %s as a backslash-s. Body: tear both queues down through 0x103a0d50 FIRST; open the main file as KeyValues and, for each child key, zero a ten-word 0x28 scratch row, fill it with 0x103a07f0 and append it only when that answers true, growing the array at +0x665c through 0x103a1320 and memmoving the tail in 0x28 strides; release the KeyValues; repeat verbatim for the side array at +0x6670; set the loaded byte +0x6690. PARSER RECOVERED (0x103a07f0): the key name must contain Story (else DevWarning Newscaster: invalid key!), the name is GetString(Name, STORY), each Version subkey contributes a dependency/filename pair at +0x04/+0x08 stepping by 8 up to four of them (a fifth warns too many versions), and the tail stores at +0x24 the index of the FIRST version whose dependency is empty or evaluates non-zero through 0x1000134d, which is PyRun_String(src, Py_eval_input) - answering FALSE and dropping the record when none does. The port now reads both files through ElysiumKeyValues and evaluates each dependency through FElysiumEntityWorld::EvalCondition, this runtime's PyRun_String. |
| `0x103aa960` | CNPC_VSabbatLeader::UpdateBloodSplash | 212 | 10 | — | `+0x66c4`, `+0x66c8` | `+0x03e0`, `+0x66c4`, `+0x66c8`, `+0x66d5` | 1d/0v/0c | `rule` | FElysiumNpc::SabbatLeaderUpdateBloodSplash | does nothing at all while m_bDiving +0x66d5 is set - not even the level copy; otherwise, when m_nWaterLevel +0x3e0 is above 0 AND either the previous level +0x66c4 was 0 or m_fLastSplashTime +0x66c8 plus _DAT_104c3cdc is strictly past curtime, it spawns bloodsplash_emitter through SpawnBloodPoolEmitter and, ONLY on the dry-to-wet edge, bloodbigsplash_emitter as well, then stamps +0x66c8 with curtime; the previous level is copied from the current one on every non-diving pass, so entering a dive freezes it and leaving one re-fires the big splash. VALUE RECOVERED at 0x4c3cdc: 0.25. |
| `0x103c6f40` | CNPC_VVampireBoss::SpawnBodyEmitters | 149 | 10 | — | — | `+0x66a0` | 3d/0v/0c | `rule` | FElysiumNpc::VampireBossSpawnBodyEmitters | KillBodyEmitters first, then a fixed four-iteration loop calling SpawnBodyEmitter(i, name) with the four attachment names from the pointer table at PTR_s_Bip01_L_Hand_1065e6d0 and storing each spawned entity's handle into m_hParticleEmitters[i] at +0x66a0; a spawn that answers null leaves that slot's PREVIOUS handle untouched rather than clearing it, which is why the kill has to run first. THE FOUR NAMES RECOVERED out of the pinned image at 0x65e6d0: Bip01 L Hand, Bip01 R Hand, Bip01 Spine, Bip01 Spine - the last two are the same string. |
| `0x103d6390` | CNPC_VWerewolf::GetHintEndEntity | 306 | 10 | — | — | `+0x026c`, `+0x6714`, `+0x6720` | 7d/0v/0c +1 outside | `rule` | FElysiumNpc::GetHintEndEntity | CORRECTS the pack walk. A linear scan of the +0x6714 record array (count +0x6720, stride 0x48) whose match is the record's word at +0x04 (puVar4[1]) and whose answer is its word at +0x00, a cached EHANDLE to the end entity. The hit arm does NOT return a second cached handle at the same slot: it re-reads base[i * 0x12], which is the SAME word +0x00, and re-validates it, so the redundancy is retail's and the answer is one handle. A row whose cached handle does not resolve is SKIPPED by the loop guard rather than answered, and a miss or an empty array falls through to FindHintEndEntity (0x103d6520), which family Hints already ported and which is called here. The record layout also corrects story 29c-1's FWerewolfHintGroundpoint comment: GetHintGroundpoint (0x103d6770) starts its cursor at field_0x6714 + 4, so the hint is at +0x04, the cached end entity at +0x00 and the groundpoint at +0x08. |
| `0x1004fbb0` | CAI_BaseNPC::FUN_1004fbb0 | 38 | 11 | `CAI_BaseHumanoid#108`, `CAI_BaseNPC#108`, `CAI_BaseNPCTroika#108`, +74 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::KeyValue | Slot 108 KeyValue(const char*, Vector), 38 bytes. CORRECTION to the earlier walk: the body formats NOTHING itself - it is a pure tail call into CAISound::FUN_1009eca0, which is what pushes the CBaseEntity::KeyValue scope-trace frame, Q_snprintf's "%f %f %f" (10555584) into a 256-byte buffer and dispatches this object's own slot 110 through vtable +0x1b8. CAI_BaseNPCTroika shares the body. Ported with 0x1009eca0 as FormatKeyValueVector and the forward into the string overload. |
| `0x1004fbf0` | CAI_BaseNPC::FUN_1004fbf0 | 13 | 11 | `CAI_BaseHumanoid#109`, `CAI_BaseNPC#109`, `CAI_BaseNPCTroika#109`, +74 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::KeyValue | Slot 109 KeyValue(const char*, float), 13 bytes and the same correction: a pure tail call into CAISound::FUN_1009ebb0, whose body carries the frame, the "%f" format at 10554f28 and the slot-110 dispatch. Ported as FormatKeyValueFloat plus the forward. |
| `0x10183120` | FUN_10183120 | 308 | 11 | — | — | `+0x13bc`, `+0x13c0`, `+0x1adc` | 1d/0v/0c | `rule` | FElysiumPlayer::RescaleActiveDisciplineDurations | RETARGETED and the walk is corrected. This is not an NPC body: +0x1adc is CBasePlayer::m_flClientVActiveDisciplineDurations[17] (vtmb_fields CBasePlayer), so the 17 floats are the CLIENT-side mirror of each compiled Discipline's running duration and not per-condition timers. RET 0x10, four stack arguments; the whole body is skipped when the first two are POINTER-EQUAL (1018312a CMP EAX,[ESP+0x28] / JZ), a no-op guard. Per slot it finds the class-info entry at +0x13bc/+0x13c0 whose type tag +0x10 is 3 - the Discipline stat list - or lazily constructs the global CVStatList_t singleton; asks 0x102012d0 whether the slot exists, builds a CVStat key (3, i) with 0x10230f00, tests membership with 0x100ce450 over DAT_106e7050 and reads the stored value with 0x100ce600. The rewrite is remaining = value - engine vtable+0x1dc(), elapsed = slot - remaining, delta = remaining * arg2 / arg3, and slot = elapsed + delta when 0x100ce630 accepts delta - a DURATION RESCALE of the remaining part, with the stored value widened to a double before the subtraction (101831e4 FLD dword / FSTP qword). No producer in this substrate: the record lookup is a seam answering nothing, so no slot is rewritten. |
| `0x1025e510` | CAI_BaseHumanoid::SetModel | 485 | 11 | `CAI_BaseHumanoid#105` | `+0x5fb8`, `+0x5fbc`, `+0x5fc0`, `+0x5fc4`, +22 more | — | 0d/0v/15c | `rule` | FElysiumNpc::SetModel | CAI_BaseHumanoid#105, 485 bytes. CORRECTION: the 26 cached indices are NOT one lookup repeated. The first THIRTEEN (body_trans_Y through head_roll, +0x5fb8 to +0x5fe8) go through CBaseAnimating LookupPoseParameter and the last THIRTEEN (move_rightleft through head_tilt, +0x5fec to +0x601c) through thunk_FUN_100b5d10, which is LookupFlexController - a body story 29c-1 ported and recovered as answering 0, not -1, on a miss. So the two halves of the cache take DIFFERENT miss values and a misspelt flex name caches controller zero. Indices 10 to 12 (head_yaw, head_pitch, head_roll) are the three 0x1025efc0 zeroes before every MaintainEyeDirection pass. UNREACHABLE in this runtime: CAI_BaseHumanoid carries no entity classname in the 77-class census. |
| `0x10260dc0` | FUN_10260dc0 | 11 | 11 | `CAI_BaseHumanoid#584`, `CAI_ExpressiveNPC#584` | — | `+0x5f48` | 0d/0v/2c | `rule` | FElysiumNpc::Slot584 | CAI_BaseHumanoid#584 and CAI_ExpressiveNPC#584 only. The whole 11-byte body is MOV ECX,[ECX+0x5f48] then JMP 0x100116d0 - a tail jump through the Expresser pointer into 0x10311c10 with the concept id and a modifier string, and no pre- or post-work at all. Ported as the species prologue of slot 584, whose Troika-line body 0x1028d910 is ResetAllThinkStamps and has nothing to do with it. TWO gaps named: the generated slot signature carries the Troika body's arity (one int) so the modifier has no way through and the arm speaks with an empty one, and neither CAI_BaseHumanoid nor CAI_ExpressiveNPC carries an entity classname in the census so the arm is UNREACHABLE today. |
| `0x10278650` | FUN_10278650 | 531 | 11 | — | — | `+0x029c`, `+0x0374`, `+0x0874`, `+0x5ba8` | 4d/0v/0c | `rule` | FElysiumNpc::GetShootTarget | TARGET CORRECTED 29d Motor10: this is CAI_BaseNPC::GetShootTarget(posSrc, bNoisy, bFlag2), NOT a standoff anchor. The reading that settles it - at 102786f1 slot 541 (vtable +0x874) is GetEnemies() and takes NO arguments, so the two pushes around it belong to the NEXT call, CAI_Enemies::GetLastKnownPosition(&lkp, enemy) at 0x102dfed0 (its own DevWarning is Asking LastKnownPosition for ene...); the enemy is slot 167 GetEnemy (+0x29c); the offset is enemy->slot 197 BodyTarget (+0x314); and +0x5ba8 is the shape map's own ShootTargetOverride. Arms in order: a resolving +0x5ba8 answers that entity's GetAbsOrigin and IGNORES all three arguments; no enemy answers AngleVectors(slot 221 GetAngles, &forward) 0x10139610 PLUS posSrc, and slot 221 returns a const QAngle& so it takes only the hidden pointer; an enemy answers lkp + (BodyTarget(posSrc,bNoisy,bFlag2) - enemy->GetAbsOrigin()), with the body target's Z first raised by _DAT_104994e0 = -30.0f (read out of the pinned image) when the enemy's first type-3 CVStatList_t answers exactly 5 for stat 0xb through GetValue 0x102012d0. Debug10's caller passes (out, eye, 0.0, 0.0), i.e. both bools false. |
| `0x1027ef20` | FUN_1027ef20 | 111 | 11 | — | `+0x5b50`, `+0x5b54` | `+0x5b50` | 2d/0v/0c +1 outside | `rule` | FElysiumNpc::AppendDebugLogLine | No slot. Appends a sprintf'd param_1 string into a per-entity ring buffer at +0x1b4e, cursor tracked at +0x5b50; when the cursor exceeds 0x3dff (16383 of a 0x4000-byte buffer) it zero-fills the remainder, sets a "wrapped" flag byte at +0x5b54, and resets the cursor to 0. Oracle cites docs/vtmb/npc-ai/shape.md:51; no port coverage found for this per-entity debug ring buffer. |
| `0x1028d990` | FUN_1028d990 | 906 | 11 | — | — | `+0x0660`, `+0x0664`, `+0x14b8`, `+0x5d8c` | 2d/0v/0c | `rule` | FElysiumNpc::BuildConditionDebugString | No slot; the 906-byte formatter slots 17 and 18 push every trace message through, and the real body behind family Debug10's TraceMessageFormat seam. A null out buffer or a size at or below zero writes NOTHING AT ALL. A null message becomes the empty string and a negative indent clamps to 0. The CONDS: block (0x105d8908 then 0x105a3060, which is a space then %s - the space is LEADING - then a newline 0x10547e40) is gated only by the schedule-debug ConVar DAT_10924a6c, whose vtable[4] must answer 0 and whose +0x2c int must be greater than 0, and walks 0 to GetLastSharedCondition() re-reading slot 409 every iteration. CORRECTION: the checklist walk says three bitmask-to-glyph ladders; there are TWO - 32 glyphs of PIS__PF_T_L__TTEPLM________ICCCC (0x105d88e0) over m_afMemory +0x5d8c and 30 of RSCPFCNFIPCDHVAEFSBDSLIAMFDPOIO_ (0x105d88b8) over m_bfAINPCFlags +0x14b8 - and the third buffer is only ever NUL-terminated, so the fifth %s of the full format always prints nothing. The NAV pair is NAV %s %s (0x105d888c) built only for nav type 3 or 1, with CLIMB (0x105d88a0) or five spaces (0x105d8898) first and JUMP (0x105d88b0) or four spaces (0x105d88a8) second, so a climber prints NAV CLIMB and a jumper NAV JUMP in aligned columns. The three format strings are 0x105d8868, 0x105d8854 and 0x105d8828, selected by the two BYTES DAT_10920534 and DAT_10920535, and the four optional blocks are built when 534 is clear or 535 is set - exactly the two arms that consume them. |
| `0x10290570` | CAI_BaseNPCTroika::FUN_10290570 | 145 | 11 | `CAI_BaseNPCTroika#532`, `CGeneric_NPC#532`, `CNPCMaker#532`, +61 more | `+0x5d24`, `+0x5d30`, `+0x644c` | `+0x0700`, `+0x5d34`, `+0x644c` | 0d/1v/2c | `rule` | hand:FElysiumNpc::Slot532 | slot 532, a jump table at 0x10290604 over param_1-1 in [0,7]. Case 1: m_eAlternateAI in [1,4] runs the navigator arm then clears. Cases 2 and 4: only m_eAlternateAI == 4, and vtable +0x700 is slot 448 - the call is TaskFail(0xe), which runs the whole failure chain from inside the door cleanup and BEFORE m_hOpeningDoor and m_bOpeningDoorWait are cleared. Case 8 CORRECTED against the listing at 102905ca (CMP EAX,0x3 / JLE 0x102905ea): m_eAlternateAI 1..3 DOES take the clear, 4 also takes the navigator arm, and only values below 1 or above 4 take no clear - the checklist walk said the opposite. The navigator arm is 0x102ee2e0 IsGoalSet (not 0x102ee680 IsGoalActive) gating 0x102bf7e0, which tests IsGoalSet a second time, stops the navigator and sets m_bShouldMove. Every path then chains CAI_BaseNPC 0x1027e0f0, whose whole body is the two door words and return 1. |
| `0x10295c20` | CAI_BaseNPCTroika::FValidateHintType | 544 | 11 | `CAI_BaseNPCTroika#566`, `CGeneric_NPC#566`, `CNPCMaker#566`, +48 more | — | `+0x026c`, `+0x62e4` | 1d/1v/5c | `rule` | hand:FElysiumNpc::FValidateHintType | Slot 566's Troika-line body, the canonical fill for ~51 census classes, so hand: applies. AUDITED at the listing 10295d80-10295e3d and BOTH inferred boundaries confirmed: < 0x64 refuses (10295d92), 0x64..0x65 takes 0x102974f0 (10295d97), < 0x283c refuses (10295df2), 0x283c..0x283d takes 0x10295ed0 (10295dfd), == 0x27d8 takes 0x10297430, == 0x28a0 takes 0x102961a0 and == 0x2774 is an outright MOV AL,1. Gate first: a null hint refuses at 10295c96; otherwise hint->m_iGroupID +0x470 AND this->m_iHintGroups +0x62e4 must be non-zero, and only when IsHintDebugNpc (DAT_10925444) is this body does it DevMsg 'Hint group id %s usable %s' (0x105d8dd0) from two bit lists built with the format at 0x105a1814, which the pinned image reads as ' %d' with ONE-BASED indices (10295cf0 LEA EDX,[ESI+0x1]). Ported as FElysiumNpc::FValidateHintType in ElysiumNpcKernelHints10.cpp; the four callees are family Hints' IsHintCoverValid / IsHintCoverValidLoose and family BaseHelpers' FUN_10295ed0 / FUN_102961a0, called and not restated. CONVENTION SETTLED: the generated void* parameter is a const FHintWords*, the only typed view of a CAI_Hint this substrate carries. |
| `0x102b5b20` | CAI_BaseNPCTroika::GiveBaseFightingItems | 56 | 11 | `CAI_BaseNPCTroika#304`, `CGeneric_NPC#304`, `CNPCMaker#304`, +55 more | — | `+0x04cc`, `+0x04d0` | 0d/1v/4c | `rule` | hand:FElysiumNpc::GiveBaseFightingItems | Ported as slot 304. 56 bytes, the whole body: slot 307 HasUsableMeleeWeapon (vt+0x4cc) false AND slot 308 HasUsableRangedWeapon (vt+0x4d0) false, then thunk_FUN_1021fe50(this, item_w_fists, 0) and AddMiscFlag(0x10). Both generated slots are stubs answering false, so the port reads the item catalogue capability word and names the two slots beside it, as ElysiumNpcKernelSchedule.cpp already does for slot 308. 0x1021fe50 is Weapon_Create plus the init 0x10258620 plus Inventory_Can_Insert plus the weapon vt+0x5fc equip and its vt+0x610 switch, which is the port FElysiumInventory::GiveNamedItem. |
| `0x102b5b70` | CAI_BaseNPCTroika::RemoveBaseFightingItems | 37 | 11 | `CAI_BaseNPCTroika#305`, `CGeneric_NPC#305`, `CNPCMaker#305`, +55 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::RemoveBaseFightingItems | Ported as slot 305. 37 bytes: GetMiscFlags() & 0x10, then thunk_FUN_1021fee0(this, item_w_fists) and RemoveMiscFlag(0x10). 0x1021fee0 is Inventory_Find plus Inventory_Remove plus UTIL_Remove on the item. Mirrors the slot-304 grant exactly in reverse, and with the flag clear it does nothing at all - not even a lookup. |
| `0x102c87a0` | CAI_StandoffGoal::vfunc241 | 105 | 11 | `CAI_StandoffGoal#241` | `+0x0484` | `+0x0484` | 0d/0v/0c | `rule` | FElysiumNpc::StandoffInputActivate | PORTED 29d Motor10. The clamp: m_aggressiveness +0x484 outside [0,4] and not the sentinel 5 DevMsgs Invalid aggressiveness value %d with the PRE-clamp value, a negative clamps to 0 and a value over 4 clamps to 4, and every path calls the backing routine exactly once. CAI_GoalEntity::InputActivate 0x102cd650 is ported in full beside it: an already-set ACTIVE bit (+0x480 bit 0) returns with NOTHING done, else insert into DAT_106eb5d8 through 0x100f6d80 (node +0x450), resolve actors once via 0x102cd4a0 setting bit 1 (0x2) or refresh via 0x102cd3b0, set bit 0, then EnableGoal (vtable +0x3d0) per actor over the +0x468 array with count +0x474. |
| `0x102c8830` | CAI_StandoffGoal::vfunc243 | 105 | 11 | `CAI_StandoffGoal#243` | `+0x0484` | `+0x0484` | 0d/1v/0c | `rule` | FElysiumNpc::StandoffInputDeactivate | PORTED 29d Motor10. Byte-for-byte the same clamp as slot 241, differing only in calling 0x102cdb70. CAI_GoalEntity::InputDeactivate is the exact inverse: nothing unless the ACTIVE bit stands, the same resolve-or-refresh, CLEAR the ACTIVE bit, DisableGoal (vtable +0x3d4) per actor, and the list removal (0x100f6e40) LAST. The two overrides are one clamp shared by both inputs, which is why the port carries the clamp once. |
| `0x1032d0c0` | CAI_BaseNPC::FUN_1032d0c0 | 431 | 11 | `CAI_BaseHumanoid#385`, `CAI_BaseNPC#385`, `CAI_BaseNPCTroika#385`, +74 more | `+0x0ea4`, `+0x0ea8` | `+0x026c`, `+0x0608`, `+0x0ea4`, `+0x0ea8` | 0d/0v/0c +2 outside | `rule` | hand:FElysiumNpc::Weapon_Drop | Ported as slot 385. CORRECTED: the second arm is a STACK SPLIT and not an ammo test. param_1[0x234] on an int pointer is BYTE offset 0x8d0, which is m_iItemCount, and the weapon vt+0x5cc beside it is the stackability gate; the real magazine +0x74c is untouched by either drop body except through the slot 386 reroll. Order: a null weapon does nothing; a weapon that IS GetActiveWeapon takes this vt+0x608, which is slot 386 (0x608/4 == 386) and is this class no-argument Weapon_Drop; then droppable OR the third argument forcing it; then the split - vt+0x5cc true and the count at two or more decrements, reads GetClassname off the ORIGINAL (vt+0x570), Weapon_Create plus 0x10258620 plus the NEW weapon vt+0x4b0, and RETURNS, and a FAILED create falls straight out of the body leaving the original decremented and NOT removed; otherwise the same two handle resets, vt+0x4b0, Inventory_Remove and Weapon_Detach under FL_NPC. The second argument is read by no arm. |
| `0x1032fe60` | CAI_BaseNPC::FUN_1032fe60 | 344 | 11 | `CAI_BaseHumanoid#348`, `CAI_BaseNPC#348`, `CAI_BaseNPCTroika#348`, +73 more | — | `+0x0208`, `+0x026c`, `+0x13bc`, `+0x13c0` | 0d/4v/1c +1 outside | `rule` | hand:FElysiumNpc::HealthToPercent | Ported as slot 348. Confirmed against the corpus: the retail scope-trace string names it CBaseCombatCharacter::HealthToPercent. Two walks of the +0x13bc count / +0x13c0 table for the first list whose record +0x10 is 0, each falling back to the lazily built empty global DAT_109f0b40, then GetValue(0x11) as the CAP and GetValue(0x0f) as the accumulated WOUND counter, then ((cap - wounds) * m_iMaxHealth) / cap as an INTEGER divide. The port needs no seam for this: FElysiumSheet IS the retail type-0 CVStatList_t, with ElysiumSlot::Health at 15 (0x0f, damage TAKEN) and ElysiumSlot::MaxHealth at 17 (0x11), which is why FElysiumCombatCharacter::SyncHealthFromSheet already cites this address. DIVERGENCE, named: a zero cap takes a crash guard answering 0 where retail faults. Walked into docs/vtmb/combat-and-damage.md. |
| `0x1034d140` | FUN_1034d140 | 307 | 11 | `CNPCMaker_Zombie#617` | — | `+0x0304`, `+0x0364`, `+0x036c`, `+0x76d0` | 0d/0v/9c | `rule` | FElysiumNpcMaker::EquipZombieFists | CORRECTIONS to the walk, from the listing. The walk stops at the hand-over and the body has four more steps: after Weapon_Equip it runs an ___RTDynamicCast on the child against the type name at 0x1062567c and, on a hit, thunk_FUN_103e0980(child, this->+0x76d0); creates "Zombies_spawning_emitter" (0x1062565c) at the maker's GetAbsOrigin and GetAbsAngles(-1.0) with a 15.0-second life (1034d249 PUSH 0x41700000); dispatches the child's vtable +0x10c, slot 67 Unhide; and calls thunk_FUN_1029f300(child, false), SetDisableAI(false), which OVERRIDES the copy MakeNPC made from the maker's own m_bDisableAI. Also: vtable +0x304 is slot 193 EyePosition, not GetAbsOrigin, so the item is placed at the maker's EYE; the item's +0xa0 word is read at 1034d1bd BEFORE the 0x40000000 flag is OR'd into +0x204 at 1034d1ca; and Weapon_OwnsThisType is asked of the MAKER (1034d16a MOV ECX,EDI) and not of the spawned zombie. RETAIL FAULT recorded: XOR ESI,ESI at entry and 1034d173 JNZ 0x1034d19c mean the true arm of Weapon_OwnsThisType reaches 1034d1a3 MOV EBP,[ESI] with ESI still null - a null dereference. The port guards it and takes the refusal instead, a named divergence; the arm is unreachable in retail because a maker owns no weapons. Ported as story 29d family Lifecycle10. |
| `0x10357800` | CNPC_Crow::vfunc511 | 11 | 11 | `CNPC_Crow#511` | — | — | 0d/0v/2c | `rule` | FElysiumNpc::StopLoopingSounds | Slot 511, CNPC_Crow#511 only; slot 511's Troika-line body is 0x1027caa0, whose port method ElysiumNpcKernelSlots.inl declares as StopLoopingSounds (layer 0, story 29c), so this species arm targets that same method with no hand: prefix and the coined StopLoopingSoundsCrow is dropped. AUDITED: the whole body is thunk_FUN_101b0d80(this, "NPC_Crow.Flap") and it does NOT chain to the base - CNPC_Crow's arm REPLACES the generic stop with a single StopSound of its flap loop, so nothing the base body stops is stopped for a crow. |
| `0x10362390` | CNPC_VAsianVampire::TaskFail | 117 | 11 | `CNPC_VAsianVampire#448` | `+0x66d4` | — | 0d/1v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VAsianVampire#448, a species arm of slot 448's port method. The whole arm is if (0xb < code && code < 0x10) m_bPathBlocked (+0x66d4) = 1, then the unconditional chain to 0x1029adb0. m_bPathBlocked has no port word - this port stands one leaf per classname - so it is carried as FElysiumNpc::bSpeciesPathBlocked, named for the retail field. |
| `0x10364080` | CNPC_VBach::SelectScheduleMeleeCombat | 395 | 11 | `CNPC_VBach#604` | `+0x1b30`, `+0x1b34`, `+0x6444`, `+0x6690` | `+0x0974`, `+0x5cc0` | 0d/0v/8c | `rule` | FElysiumNpc::SelectScheduleMeleeCombat | CNPC_VBach#604, 395 bytes, ported as a species arm of slot 604's one port method. Condition 0x7b stamps +0x6690 with curtime plus _DAT_10463584 (15.0f) and returns 0x15a; else the active weapon is fetched ONCE and condition 0x7a read immediately after it - unarmed with 0x7a returns 0x158 and unarmed with 0x79 returns 0x159; armed with 0x7a CLEAR and 0x79 set requires the classname item_w_rem_m_700_bach or returns 0x159, and on a match RETURNS slot 605 SelectScheduleRangedCombat's answer, the one arm that leaves the melee family; armed with 0x7a set requires item_w_katana or returns 0x158. The fall-through calls 0x10385e40 DIRECTLY, clears +0x6444 unless m_NPCState is 4 or 0xc - on every path, including the ones that answered non-zero - and forces 0x159 when that call returned 0. |
| `0x1036d1d0` | CNPC_VChangBros::TaskFail | 120 | 11 | `CNPC_VChangBros#448`, `CNPC_VChangBrosBlade#448`, `CNPC_VChangBrosClaw#448` | `+0x5c54` | — | 0d/2v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VChangBros#448, shared by CNPC_VChangBrosBlade and CNPC_VChangBrosClaw. The same 12..15 gate as the AsianVampire arm but the write is m_failSchedule (+0x5c54) = 0x15d, this runtime's FElysiumScheduleState::FailScheduleOverride through ScheduleFromRetailNumber; then the unconditional chain to 0x1029adb0. |
| `0x10379060` | CNPC_VGargoyle::TaskFail | 77 | 11 | `CNPC_VGargoyle#448` | `+0x5d8c`, `+0x6680` | `+0x5cc0`, `+0x5d8c` | 0d/0v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VGargoyle#448. In combat (m_NPCState == 2) with 0x10269d30 HasInterruptCondition(0x5c TASK_FAILED) standing it clears m_afMemory's top bit; then 0x10379040 and 0x10379000. CORRECTION, from the listing: the checklist walk flags 1037908c CALL 0x10006613 as reached with a lost this alias needing an asm check. 0x10379040 is four instructions (MOV EAX,[ECX+0x14b8] / SHR EAX,4 / AND AL,1 / RET) and never writes ECX, so ECX still holds this from 10379081 - there is no bug. Both bodies are plain m_bfAINPCFlags accessors for bit 0x10 FINDING_BODY: read, then clear. Finally m_iShunnedFindPillar (+0x6680) = 0, then the chain to 0x1029adb0. |
| `0x1037c090` | CNPC_VGhoulCroucher::BurnPlayer | 228 | 11 | — | — | `+0x026c` | 3d/0v/0c | `rule` | FElysiumNpc::BurnPlayer | no slot. With a non-null target it iterates every hitbox of the target's model (count via CBaseAnimating::GetModelPtr plus offsets 0x104/0x4) calling CBaseCombatCharacter::BurnHitbox(target, index, 5.0, 0.5), then builds a CTakeDamageInfo via thunk_FUN_101c26d0 with inflictor = my GetActiveWeapon, attacker = this, the CALLER's damage amount and bitsDamageType 8, plus the trailing 0/0/-1, and applies it with CBaseEntity::TakeDamage. CORRECTION: the walk read the 8 as the whole descriptor and lost the damage amount, which is the caller's 10.0. The hitbox loop goes through family Damage's HitboxSetCount seam, which answers 0 - retail's own zero-hitbox arm. |
| `0x1037d020` | CNPC_VGuard1::OnStateChange | 162 | 11 | `CNPC_VGuard1#463` | — | `+0x029c` | 0d/0v/1c | `rule` | FElysiumNpc::StateChangeSpeciesPreStep | Slot 463, CNPC_VGuard1#463 only. RETARGETED from FElysiumNpc::OnStateChange to the pre-step method the arm actually lands in: FElysiumNpc::OnStateChange's HolsterOnState case now calls StateChangeSpeciesPreStep before ApplyStateWeaponVisibility, which is retail's order. THE WALK NAMED THE WRONG THING ENTIRELY. vtable +0x29c is slot 167, and slot 167's body on the whole NPC line is CAI_BaseNPC::FUN_101a67e0 at 0x101a67e0, six instructions that resolve m_hEnemy through the global entity table: it is GetEnemy(), not a door reference. And 0x1037e2d0 is not an obstructing-door hook: it sets +0x6660 and calls InputSetRelationship(this, player D_HT 10, 0), the literal at 0x1063bc28 with spaces and a lower-case player (CNPC_VCop's is Player D_HT 10 at 0x106366f4). So arm (1) is: unconditionally on both states, if GetEnemy() is non-null and that entity's +0xa8 (m_pPlayer) is non-zero, hate the player at priority 10. GetEnemy() is dispatched TWICE (1037d026, 1037d034) and retail caches neither call, so the port makes both. 0x1037e2d0 itself is family SpeciesMisc10's Guard1HatePlayer (it has six more callers in vfunc461) and is called, not restated. Arm (2), the holster switch, was already carried by FElysiumNpc::ApplyStateWeaponVisibility. |
| `0x10380510` | CNPC_VHengeyokai::TaskFail | 130 | 11 | `CNPC_VHengeyokai#448` | `+0x5d8c`, `+0x6664`, `+0x6678` | `+0x5cc0`, `+0x5d8c`, `+0x6664` | 0d/1v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VHengeyokai#448. The memory-bit clear as above; then 0x10381be0 is (m_bfAINPCFlags >> 4) & 1 = FINDING_BODY and 0x10381ba0 sets or clears that same bit, so the pair is read-then-clear and NOT a species word. CORRECTION: 0x10382970 does not release the pickup target - it is a CUtlVector of BlacklistedEntity_t grow-and-append of (handle, curtime + 20.0) onto m_BlacklistedEntities (+0x66a4), the 20.0 being _DAT_1044eb0c, so the target is SHUNNED for twenty seconds. Then 0x10381c80 is (m_bfAINPCFlags >> 5) & 1 = CARRYING_BODY and the arm runs when it is CLEAR: 0x102c43b0(this, 0.75) re-arms m_flIgnoreCollisionTimer (+0x6458) and m_hPickupTarget goes to -1. Always m_iShunnedFindFish (+0x6678) = 0, then the chain. |
| `0x10388880` | CNPC_VHunter::OnStateChange | 268 | 11 | `CNPC_VHunter#463` | `+0x6664` | `+0x029c`, `+0x6664` | 0d/0v/1c | `rule` | FElysiumNpc::StateChangeSpeciesPreStep | Slot 463, CNPC_VHunter#463 only. RETARGETED as 0x1037d020 was, and carrying the same correction: vtable +0x29c is GetEnemy() (slot 167, CAI_BaseNPC::FUN_101a67e0), not a door reference. Two independent arms, in this order, and both can fire on one call. Arm A: if GetEnemy() is non-null (dispatched twice) and that entity's +0xa8 is non-zero and param_2 == 2 (COMBAT), call 0x10388c40 - which is 0x1037e2d0 MINUS the +0x6660 latch byte and is the only difference between the two 20-byte bodies, ported here as FElysiumNpc::HunterHatePlayer - then 0x1017f7b0(player), then m_hPursuitPlayer +0x6664 = the player's own EHANDLE. Arm B: if param_1 == 2 and m_hPursuitPlayer resolves live and its +0xa8 is non-zero, CLEAR m_hPursuitPlayer FIRST (1038891e) and release through 0x1017f830 second. 0x1017f7b0 and 0x1017f830 are the hunter-pursuit refcount pair on the PLAYER (+0x1d14), which family Conditions already ported as the static FElysiumNpc::OnHunterPursuitStart / OnHunterPursuitStop with its own receiver correction; they are called, not restated, and the count lives on FElysiumPoliceState::HuntersInPursuit. The tail chains 0x103871c0 (CNPC_VHumanCombatant::OnStateChange, the shared holster body) rather than 0x102ae140 directly, which is the same sequence either way. UNRECOVERED: the CSActs manager 0x1017fa40 / 0x1017fa70 fire into (0x1023dcd0's singleton, output list +0x4e0). |
| `0x1038e480` | CNPC_VManBat::FValidateHintType | 317 | 11 | `CNPC_VManBat#566` | — | `+0x6670` | 0d/0v/5c | `rule` | FElysiumNpc::FValidateHintType | CNPC_VManBat#566, a species arm of slot 566's port method keyed on the census body address; it REPLACES the base dispatcher rather than extending it, reading neither m_iGroupID nor the range switch. 1038e48b: only hint type 20000 is considered, everything else refuses at 1038e5b3. The mode is m_field_0x6670 through the ladder at 1038e49b (AND 0x710935, XOR 0x148739, ADD 0x4094ab, AND 0x18ef6ca, XOR the word, XOR 0x412a96ec) and then 0x1042fbf0; the jump table at 0x1038e5c0 covers modes 1..8 and sends 5, 6 and 7 to the default. Templates, read out of the pinned image: 1 'ManBat Landpoint' copied RAW by a byte loop with no index, 2 and 4 'ManBat Divepoint %d', 3 'ManBat Divepoint %d Bottom', 8 'ManBat Script Node %d', default 'ManBat %d', each %d being m_field_0x6674. The built name is matched against the hint's m_iName +0x26c with __strcmpi, or with __strnicmp over strlen-1 when the template ends in a star; an EMPTY template compares the hint's name POINTER against zero instead (1038e556), so an unnamed hint matches. Lands as FElysiumNpc::ManBatValidateHintType, dispatched from the slot's one port method. |
| `0x10394090` | CNPC_VMingXiao::TaskFail | 88 | 11 | `CNPC_VMingXiao#448` | `+0x6718` | `+0x5d44`, `+0x673c` | 0d/1v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VMingXiao#448, a switch on m_eThrowableObjectMode (+0x673c). Modes 3 and 4 call 0x102e0a60(m_pMotor, 0x43340000) - m_pMotor +0x1c = 180.0, the same steering reset the Troika body itself makes - and touch nothing else. CORRECTION: the default arm's 0x10398d90 is not a throwable-prop clear; its whole body is m_eThrowableObjectMode = arg, so the arm sets the MODE to 0 and then releases m_hThrowObject (+0x6718) to -1. Both arms chain 0x1029adb0. |
| `0x10396000` | CNPC_VMingXiao::vfunc127 | 53 | 11 | `CNPC_VMingXiao#127` | — | `+0x66f4` | 0d/0v/0c | `rule` | FElysiumNpc::Restore | Slot 127, CNPC_VMingXiao#127 only; slot 127's Troika-line body is 0x10299700, whose port method ElysiumNpcKernelSlots.inl declares as virtual int32 Restore(void*), so the species arm targets that same method with no hand: prefix and the coined RestoreMingXiao is dropped. registry:127 is rejected here: the body is a base call plus a loop, not a literal, keyfield or one member. Arm: call the base Restore 0x10299700 first, then walk the 6-float m_rflRegrowTimers array at +0x66f4 calling 0x101cf2f0(&timer[i], 4) on each in ascending order - the decode twin of this class's Save 0x10395f80, mode 4 turning the 1e+11 sentinel back into FLT_MAX against retail's FIELD_TIME rebase. Save games are disposable for this project, but the retail fact is recorded; no port coverage. |
| `0x103970d0` | CNPC_VMingXiao::vfunc348 | 532 | 11 | `CNPC_VMingXiao#348` | — | `+0x026c`, `+0x13bc`, `+0x13c0` | 0d/0v/1c | `rule` | FElysiumNpc::HealthToPercent | Ported as the CNPC_VMingXiao species arm of slot 348, dispatched from inside the one slot method with no coined name. It reuses the BASE scope-trace string (CBaseCombatCharacter::HealthToPercent), makes the same two stat reads in the same order (0x11 the cap, 0x0f the wounds) and adds a loop i = 0..5 over 0x10398000(this, i) folding one extra contribution in per true result before the final ftol - the regrown-limb count lowering the reported percent. The decompiler lost the FPU arithmetic entirely (three bare ftol calls with no operands); the loop, its bound and its guard are what the listing carries. SEAM: no port system stands the Ming Xiao severable limbs, so 0x10398000 answers false for all six and the arm equals the base, which is the retail answer for an intact boss. Walked into docs/vtmb/combat-and-damage.md. |
| `0x1039eda0` | CNPC_VMingXiaoTentacle::vfunc127 | 40 | 11 | `CNPC_VMingXiaoTentacle#127` | — | `+0x6674` | 0d/0v/0c | `rule` | FElysiumNpc::Restore | Slot 127, CNPC_VMingXiaoTentacle#127 only; retargeted to slot 127's port method FElysiumNpc::Restore (Troika body 0x10299700), no hand:, coined RestoreMingXiaoTentacle dropped; registry:127 rejected because the body is a base call plus a call, not a literal, keyfield or one member. Arm: the base Restore 0x10299700 first, then 0x101cf2f0(&m_flPhaseExpireTimer at +0x6674, 3) - mode 3, one float, the decode twin of this class's Save 0x1039ed50 which encodes the same field with the same mode; mode 3 swaps the 1e+11 sentinel back for a value exactly equal to _DAT_104454c4, recovered from the pinned image as 0.0f. No port coverage. |
| `0x103a0670` | FUN_103a0670 | 302 | 11 | — | `+0x6684`?, `+0x6688`?, `+0x668c`? | `+0x665c`?, `+0x6668`?, `+0x6670`?, `+0x667c`?, +4 more | 1d/0v/0c | `rule` | FElysiumNpc::PlayNextNewscasterStory | CLASS ATTRIBUTION CONFIRMED - CNPC_VNewscaster, not the MingXiao family. If the loaded flag +0x6690 is clear and 0x101cd9e0(1), which is UTIL_PlayerByIndex(1), answers an entity, call the loader then seed both cursors with RandomInt(0, count-1); a missing player returns WITHOUT loading. Then, unless IsInDialog 0x102c1170, and only when count0+count1 is non-zero, roll RandomInt(0, count0+count1) - an INCLUSIVE upper bound - and write 1 to +0x668c when the roll is below count0 else 0. A zero +0x668c or an empty main queue advances the side cursor +0x6688 modulo +0x667c (returning early on an empty side queue), otherwise the main cursor +0x6684 modulo +0x6668; the advanced row's SELECTED VERSION filename at base + i*0x28 + 8 + record[+0x24]*8 is played through 0x102c0520 when non-null. CORRECTION: the record's +0x24 is NOT a version count but the index 0x103a07f0's tail chose (see that row's evidence); there are FOUR version slots per record, not five alternates. |
| `0x103aaa80` | CNPC_VSabbatLeader::RecordPlayerHealth | 255 | 11 | — | `+0x66cc` | `+0x628c` | 2d/0v/0c | `rule` | FElysiumNpc::RecordPlayerHealth | no slot, recovered retail name. Does NOTHING without a live m_hClosestPlayer +0x628c - the mark keeps its previous value. Otherwise it finds that player's class-info-list entry with type tag +0x10 equal to 0 over +0x13bc/+0x13c0, falling back to the lazily constructed CVStatList_t singleton DAT_109f0b40, and reads stat 0xf into m_LastPlayerHealth +0x66cc. Stat 0xf is the accumulated wound counter, not current health - CBaseCombatCharacter::HealthToPercent 0x1032fe60 computes ((stat0x11 - stat0xf) * m_iMaxHealth) / stat0x11 - so this snapshots the player's damage TOTAL at the start of a round. Ported over family Combat10's typed stat-list seam, applied to another entity. |
| `0x103aabc0` | CNPC_VSabbatLeader::PlayerDamagedEnoughThisRound | 301 | 11 | — | — | `+0x628c`, `+0x66cc` | 1d/0v/0c | `rule` | FElysiumNpc::PlayerDamagedEnoughThisRound | no slot, recovered retail name. If m_hClosestPlayer does not resolve live it returns false; otherwise it re-derives that player's stat 0xf exactly as 0x103aaa80 does and returns true only when _DAT_104c3ce0 is at or below (float)(stat0xf - m_LastPlayerHealth). THRESHOLD CONFIRMED at file offset 0x4c3ce0 of the pinned vampire.dll: 2.0f. |
| `0x103b0290` | CNPC_VSheriffMan::TaskFail | 100 | 11 | `CNPC_VSheriffMan#448` | — | — | 0d/1v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VSheriffMan#448, leaf only. Apart from the scope-trace push and pop the entire body is the direct chain to 0x1029adb0 with param_1 unchanged. The recovered fact is the ABSENCE of a species arm; it is ported so a test can prove the sheriff adds nothing to the base. |
| `0x103ba350` | CNPC_VTzimisce::TaskFail | 130 | 11 | `CNPC_VTzimisce#448` | `+0x5d8c`, `+0x6670`, `+0x66b8` | `+0x5cc0`, `+0x5d8c`, `+0x6670` | 0d/2v/43c | `rule` | FElysiumNpc::TaskFail | CNPC_VTzimisce#448, the Hengeyokai arm with its own words. 0x103be090 and 0x103be130 are byte-identical to 0x10381be0 and 0x10381c80 - FINDING_BODY and CARRYING_BODY on m_bfAINPCFlags - and 0x103be050 is the same bit-0x10 writer. 0x103bf200 appends (handle, curtime + 20.0) to m_FailedPickupTargets (+0x6690) rather than releasing anything. m_hPickupTarget is +0x6670 and m_iShunnedFindBody is +0x66b8. Then the chain to 0x1029adb0. |
| `0x103c2860` | CNPC_VTzimisceHeadClaw::vfunc127 | 40 | 11 | `CNPC_VTzimisceHeadClaw#127` | — | `+0x6678` | 0d/0v/0c | `rule` | FElysiumNpc::Restore | Slot 127, CNPC_VTzimisceHeadClaw#127 only; retargeted to slot 127's port method FElysiumNpc::Restore (Troika body 0x10299700), no hand:, coined RestoreTzimisceHeadClaw dropped; registry:127 rejected for the same reason as 0x1039eda0. Arm: the base Restore 0x10299700, then 0x101cf2f0(&m_flSlowedExpire at +0x6678, 3) - the mode-3 decode twin of this class's Save 0x103c2810, which encodes the same one field with the same mode; _DAT_104454c4 recovered from the pinned image as 0.0f. No port coverage. |
| `0x103c5910` | CNPC_VVampireBoss::Restore | 140 | 11 | `CNPC_VVampireBoss#127` | `+0x6680`, `+0x6694` | — | 5d/0v/0c | `rule` | FElysiumNpc::Restore | Slot 127, CNPC_VVampireBoss#127 only; retargeted to slot 127's port method FElysiumNpc::Restore (Troika body 0x10299700), no hand:, coined RestoreVampireBoss dropped; registry:127 rejected because the body writes three things and calls a helper. This is also the body the other bosses' own slot-127 overrides call as their base (CNPC_VAndreiBlood 0x1035cf80, CNPC_VAsianVampire 0x10360e10, the ChangBros 0x1036b170, CNPC_VSabbatLeader 0x103a6e80, CNPC_VSheriffMan 0x103ae7f0 all chain through it), so it sits between the Troika body and those species rows. Arm: call the base Restore 0x10299700, null m_pMonsterModelName, call ClearBodyEmitterNames(this), and reset field_0x6694 to the literal "npc_VVampireBoss" - a post-load reset of the monster-model override and the body-emitter names back to the default classname. No port coverage. |
| `0x103c59d0` | CNPC_VVampireBoss::SelectHintNode | 190 | 11 | — | `+0x1b44`, `+0x1b48`, `+0x5ddc` | `+0x0700` | 1d/0v/0c | `present` | ElysiumNpcKernelHints.cpp:FElysiumNpc::FindHintNode | CONFIRMED present, and the evidence is corrected. Read again against 0x10365780, the address FindHintNode cites: the two bodies are identical arm for arm - FindHintNear(hint type, flags, 5000.0, null, null) through 0x102d1af0, the result written to m_pHintNode +0x5ddc on BOTH paths, a hit calling 0x10273e80(this, 0) and returning true, a miss writing an assert file string and line into +0x1b44/+0x1b48 and then vtable +0x700 (4) and returning false. The difference is NOT only the line number: the FILE string differs too - 0x1065ec20 npc_VVampireBoss.cpp line 0xd1 here against 0x1062eadc NPC_VBach.cpp line 0x48a there. Both halves are the same deliberately unmodelled assert pair: ElysiumNpcKernelShapeMap.cpp records +0x1b44 ABSENT port-wide, a decision already accepted for the sibling body. Stays present. |
| `0x103c6830` | CNPC_VVampireBoss::GetCurrHealthPercent | 356 | 11 | — | — | — | 3d/0v/0c | `rule` | FElysiumNpc::GetCurrHealthPercent | Ported. Confirmed against the listing: 103c68db reads stat 0x0f FIRST into the numerator and 103c694d stat 0x11 SECOND into the divisor; the guard at 103c6964 is ABS of the SECOND read against _DAT_104ce8c0 (1e-05) and the decompiler idiom resolves to a greater-than test, so it is a divide-by-zero guard on the DIVISOR and not a test on the numerator; the refusal answer _DAT_104454c4 is 0.0, confirmed out of the pinned image. Stat 0x0f is the wound counter, so this returns the DAMAGE fraction, the complement of HealthToPercent 0x1032fe60. Walked into docs/vtmb/combat-and-damage.md. |
| `0x103cc9b0` | CNPC_VWerewolf::GiveBaseFightingItems | 155 | 11 | `CNPC_VWerewolf#304` | — | `+0x026c` | 0d/1v/4c | `rule` | FElysiumNpc::GiveBaseFightingItems | Ported as the CNPC_VWerewolf species arm of slot 304, dispatched from inside the one slot method with no coined name. It REPLACES the base rather than extending it: 103cca32 runs Inventory_Find(item_w_werewolf_attacks) and, only when the item is ABSENT, gives that item and sets misc flag 0x10. It consults neither the slot-307 gate nor the slot-308 gate and never grants fists. 99 of its 155 bytes are the scope-trace frame. UNREACHABLE IN PLAY: CNPC_VWerewolf carries no entity classname in the census, so no spawned NPC can resolve to it; the arm is ported and exercised through SetRetailClassForTests. Walked into docs/vtmb/combat-and-damage.md. |
| `0x103cca80` | CNPC_VWerewolf::RemoveBaseFightingItems | 146 | 11 | `CNPC_VWerewolf#305` | — | `+0x026c` | 0d/0v/0c | `rule` | FElysiumNpc::RemoveBaseFightingItems | Ported as the CNPC_VWerewolf species arm of slot 305, dispatched from inside the one slot method with no coined name. It mirrors the base SHAPE with its own item: read GetMiscFlags, and if bit 0x10 is set remove item_w_werewolf_attacks via 0x1021fee0 and clear the 0x10 flag - the exact inverse of its own slot-304 grant, as the base pair is for item_w_fists. Same census gap as slot 304: unreachable in play, exercised through SetRetailClassForTests. Walked into docs/vtmb/combat-and-damage.md. |
| `0x103d6650` | CNPC_VWerewolf::GetHintEndpoint | 211 | 11 | — | — | `+0x026c` | 8d/0v/0c | `rule` | FElysiumNpc::GetHintEndpoint | CORRECTS the pack walk's fixed global point. A null hint answers DAT_1070d1b0/b4/b8, and staticinit_101370b0 writes ZERO into all three, so the answer is vec3_origin. Any other hint resolves its end entity through GetHintEndEntity (0x103d6390) and copies that entity's GetAbsOrigin (vtable +0x364) into the caller's Vector, with NO null check on the resolved entity - retail faults on an unresolvable end entity. Ported with a named crash guard that answers the same vec3_origin the null-hint arm answers. |
| `0x103d7090` | CNPC_VWerewolf::GetForwardHintForHint | 274 | 11 | — | — | `+0x026c` | 1d/0v/0c | `rule` | FElysiumNpc::GetForwardHintForHint | CORRECTS the pack walk's exemption run. The switch at 103d70dd holds FOURTEEN literals - 15000, 0x3a99, 0x3a9c, 0x3a9f, 0x3aa0, 0x3aa1 and 0x3aa3..0x3aaa - and 0x3aa2 is NOT among them, so 0x3a9f-0x3aaa would be fifteen. An exempt hint is handed straight back; every other type resolves its end entity via GetHintEndEntity (0x103d6390) and walks the global hint list from DAT_10925450 through the +0x5d8 next link for a hint of type 0x3a9c whose own end entity is the same object. On no match the loop cursor is null and retail DevWarnings with the format at 0x10662f98, which the pinned image reads as 'Could Not find forward hint for hint: %s (%s) ' - TWO %s, not one. The global hint list is a SEAM answering an empty list, so the no-match arm is the one reached. |
| `0x103d8300` | CNPC_VWerewolf::IsValidTeleportHint | 457 | 11 | — | — | `+0x026c`, `+0x08d8`, `+0x66e8` | 2d/0v/0c | `rule` | FElysiumNpc::IsValidTeleportHint | CORRECTS the pack walk's last gate. Seven ordered refusals: a null hint; field_0x66e8 bit 0x4; IsHintUnusable (0x102d14c0, family Hints, called); slot 566 FValidateHintType through vtable +0x8d8, VIRTUALLY, so a Werewolf takes its own species row; the six-type exclusion set 0x3aa3..0x3aa8, each its own CMP at 103d83c8-103d8430; and hint->+0x470 == 1 (an EQUALITY compare on the word slot 566 treats as a bit set) with field_0x66e8 bit 0x40. The final answer is the NEGATION of thunk_FUN_100b5190 on the resolved end entity, and 0x100b5190 is a seven-byte getter of +0xf4, which fields.md names m_bScriptHidden - not an entity-busy/occupied test. The endpoint seam answers false, which is the ADMITTING value because retail negates it. |
| `0x100e8100` | CDialog::process_npc_line | 357 | 12 | — | — | `+0x0004`, `+0x2830` | 1d/0v/0c | `rule` | FElysiumDlgConversation::EnterNpcLine | The two arms the port did not carry are now ported. (1) The row-fetch MISS at 100e8130 byte-copies the shared literal at 0x1054ca50 into the caller's buffer and returns 0, the used-default answer; the pinned image reads that cell as a SINGLE SPACE, and ElysiumDlgRetail::MissingNpcLineText is it, applied where the widget has a live conversation and no resolved NPC row. (2) 0x100e8060 is NOT a truncate/pad: it is the ellipsis normaliser, six replace-UNTIL-NO-MATCH passes over an 0x800 buffer through 0x100e7f70, with both pointer tables read out of the pinned image at 0x10561a88 and 0x10561aa0. CORRECTION: pass 3's replacement is ' ... ' WITH a trailing space, because 0x10561aa8 holds 0x10561b14, the same pointer pass 1 uses. It runs on the NPC subtitle ONLY - process_pc_line's own line_copy at 100e8875 has no such pass, so a PC choice keeps its authored spacing. The col-4/col-5 scheduling half and the gender/clan variant half were already carried and are unchanged. |
| `0x100e8520` | CDialog::process_pc_line | 883 | 12 | — | `+0x2834` | `+0x0004`, `+0x2830` | 1d/0v/0c | `rule` | FElysiumDlgConversation::EnterNpcLine | The packet flag words and the return code are now ported and tested as ElysiumDlgRetail::ClassifyPcRow. Three blocks in the listing's order, and the second is not an else of the first: an Auto-End row with the packet 0x30 test CLEAR and CDialogDependency::Test passing answers -1 and sets flag bit 0x10 when LookupSpeechFile hits (100e8600), and collapses the band to one entry at flags 0 / value -1 with the count forced to 1 and still answers 1 when it misses (100e85e0); an Auto-Link row with the same two gates sets flag bit 0x20 and answers 1, so the row STAYS in the band; a starting-condition row answers 0, which fill_packet drops WITHOUT raising +0x30e9; an ordinary row answers its initial 1. The gate, the automatic rows and DisplayText(bMale, ClanOffset) were already carried; the LookupSpeechFile arm remains the named divergence at ElysiumDialogueSession.h. |
| `0x1017c600` | FUN_1017c600 | 119 | 12 | — | `+0x1eb8`, `+0x1ec0` | `+0x02e0` | 1d/0v/0c +1 outside | `rule` | FElysiumPlayer::OpenBarterOrLoot | Read off the listing because the decompiled C folds the target pointer and the loot byte into one parameter. RET 0x14, five stack arguments: the target NPC, the loot byte and three ints. engine vtable +0x8c on the target's edict +0x2e0 is stored at +0x1eb8 and the loot byte at +0x1ec0; loot 0 calls CBaseCombatCharacter::SyncVendorInventory(target, a, b, c) and selects 0x10587ef4 showbarter, loot 1 calls 0x10324080(target) with NO extra arguments and selects 0x10587ee8 showloot; the tail is engine vtable +0xf4 ClientCommand on the PLAYER's own edict, so the window opens by console command and never by a direct call. Both command strings carry a trailing newline. The port has no barter or loot window: the two branch calls are counted seams and the command is recorded rather than dispatched. |
| `0x1026ce30` | CAI_BaseNPC::LeaveGrappleState | 100 | 12 | `CAI_BaseHumanoid#380`, `CAI_BaseNPC#380`, `CAI_ExpressiveNPC#380`, +10 more | — | `+0x0680`, `+0x1538`, `+0x153c`, `+0x5bf0` | 1d/0v/0c | `rule` | FElysiumNpc::BaseLeaveGrappleState | CAI_BaseNPC#380 base body beside the Troika override 0x102b5d90, ported under its own name. Four UNCONDITIONAL steps with no grapple-type gate: fire m_OnGrappleEnd with the +0x1538 handle as activator (null on a stale handle); CBaseCombatCharacter::LeaveGrappleState; slot 416 vt+0x680 SetForceFrequentThink(false); then 0x10007ea0, the saturating decrement of m_iIsOblivious +0x5bb4 clamped at 0 plus the squad reconnect 0x10009601. ElysiumNpc.cpp:790 gated the output fire and the oblivious pair on Grapple.Type == StealthKill and omitted SetForceFrequentThink; both fixed and the slot body now calls this. |
| `0x10277d90` | CAI_BaseNPC::FUN_10277d90 | 608 | 12 | `CAI_BaseHumanoid#582`, `CAI_BaseNPC#582`, `CAI_BaseNPCTroika#582`, +74 more | — | `+0x0270`, `+0x0274`, `+0x0280`, `+0x0370`, +2 more | 0d/0v/0c | `mechanism` | FElysiumNpc::ReportOverThinkLimit | the whole body is a developer report throttled by the global stamp _DAT_10920554 restamped to curtime plus _DAT_104454c0: the running task name from 0x1028a150 GetTask (INVALID_TASK when the schedule or index is out of range) through slot 449 TaskName, the schedule name at m_pSchedule+0x40, and six millisecond timers each scaled by g_ClockSpeedMillisecondsMultiplier, all into one DevWarning; then slot 220 GetOrigin, a world-space collision bounds computation for the overlay, an optional Slow text overlay behind its own cvar and a tail into 0x10280c80 when DAT_10920344 is live; nothing outside the diagnostic is written, so the seam is UE_LOG plus DrawDebugHelpers and the slot answers through it |
| `0x10278900` | CAI_BaseNPC::FUN_10278900 | 130 | 12 | `CAI_BaseHumanoid#574`, `CAI_BaseNPC#574`, `CAI_BaseNPCTroika#574`, +73 more | — | — | 0d/1v/1c | `rule` | hand:FElysiumNpc::GetShootEnemyDir | slot 574: 0x10278650 resolves the aim point first (the m_hShootTargetOverride +0x5ba8 origin when that handle is live, else with no enemy the body's own forward from slot 0x374 through AngleVectors 0x10139610, else the enemy-memory LKP 0x102dfed0 plus the enemy's BodyTarget(shootPos) minus its slot-217 origin with the +_DAT_104994e0 Z bonus applied when the enemy's stat 0x0b reads 5), the caller's shoot position param_2 is subtracted, and the delta is NORMALISED IN PLACE through 0x1057966c before the three floats are stored out; the decompiled C shows the raw delta because it misses the POP ESI at 10278969 that shifts the stack reads, so the slot returns a UNIT direction. |
| `0x1028de10` | CAI_BaseNPCTroika::FUN_1028de10 | 94 | 12 | `CAI_BaseNPCTroika#18`, `CGeneric_NPC#18`, `CNPCMaker#18`, +61 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::TraceMessage | slot 18, the NON-const overload: formats through 0x1028d990 into a 512-byte buffer (curtime as %6.2f, the indent level clamped at 0, a CONDS list, the 32-glyph PIS_PF_T_L mask over +0x5d8c, the 30-glyph RSCPFCNFIPCD mask over +0x14b8 and a NAV pair), then when the trace toggle DAT_10920534 is set appends the FORMATTED text to the NPC's own 16 KB ring at +0x1b4e through 0x1027ef20 (cursor +0x5b50, wrap ABOVE 0x3dff zero-fills the tail, sets +0x5b54 and resets the cursor to 0), and otherwise DevMsg's it. Ported in ElysiumNpcKernelDebug10.cpp; the ring is ABSENT per story 29b and the line goes to FDebugLine, with the cursor rule carried as the pure function DebugLogRingAdvance. The formatter 0x1028d990 is family Conditions10's row and is seamed rather than written twice. |
| `0x1028de90` | CAI_BaseNPCTroika::FUN_1028de90 | 125 | 12 | `CAI_BaseNPCTroika#17`, `CGeneric_NPC#17`, `CNPCMaker#17`, +61 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::TraceMessage | slot 17, the CONST twin of slot 18: the same 0x1028d990 format into a 512-byte buffer, but when DAT_10920534 is set it Q_strncpy's the RAW param_1 into a SECOND 512-byte buffer and rings THAT through the GLOBAL 0x1027ee20, so the ring never sees the formatted text and only the DevMsg arm does. The formatter still RUNS on the ring arm and its output is dropped, which is why it is called before the branch. Both asymmetries against slot 18 are retail's and are reproduced in ElysiumNpcKernelDebug10.cpp. |
| `0x1028df30` | CAI_BaseNPCTroika::FUN_1028df30 | 89 | 12 | `CAI_BaseNPCTroika#20`, `CGeneric_NPC#20`, `CNPCMaker#20`, +61 more | — | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::TraceMessageBare | slot 20: a null message does nothing at all, not even a DevMsg; otherwise DAT_10920534 set copies up to 0x200 bytes onto the stack and appends them to the NPC's OWN 16 KB ring through 0x1027ef20, and clear prints straight through DevMsg, with no member written on either arm. Byte-identical to slot 19 (0x1028dfb0, story 29c-1) except that slot 19 rings through the GLOBAL 0x1027ee20; that is the whole difference between the pair and the reason both exist. Ported in ElysiumNpcKernelDebug10.cpp. |
| `0x10292500` | CAI_BaseNPCTroika::NPCThinkDebugPre ‼ | 1782 | 12 | — | `+0x5b55` | `+0x026c`, `+0x0270`, `+0x0304`, `+0x0364`, +7 more | 1d/0v/0c | `rule` | FElysiumNpc::TroikaNPCThinkDebugPre | Troika's pre-think debug pass, read from the listing: a scope-trace push, then two witness overlays under curtime < the stamp -- m_flCriminalWitnessedTimer +0x63a4 draws a 3-unit box at GetAbsOrigin lifted by the collision maxs.z plus _DAT_10451ad0 with the remaining-time scalar clamped to 64.0 (_DAT_10451acc), m_flSupernaturalWitnessedTimer +0x63a8 the same box with the opposite corner order and _DAT_1044faa4 -- then, under the first ConVar (DAT_1092479c) being enabled, HasCondition(0x5a) traces eye-to-eye against m_hClosestPlayer +0x628c and DrawBBoxOverlays whatever the trace hit when it is neither the player nor null, and HasCondition(0x48) sprints "Blocked by %s" with GetDebugName of the blocker handle +0x5d90 above the head by _DAT_1044eb0c; under the second ConVar (DAT_109244c4) a 3-unit box at the head-adjusted eye position and a 2-unit box at the ideal, joined by a line; the third ConVar (DAT_1092435c) selects 0x1028e030 or 0x1028e060 by its int value 1 or 2; the tail clears +0x5b55 and dumps the 16 KB debug ring through 0x1027efb0 in 512-byte chunks from the +0x5b50 cursor. Only the +0x5b55 write touches state, and 29b records the ring and its three cursors ABSENT with a reason, so the port carries the arms through the Debug family's FDebugLine seam and the ring dump stays absent |
| `0x102a0290` | FUN_102a0290 | 400 | 12 | — | `+0x6004`, `+0x6008`, `+0x600c` | `+0x0364`, `+0x036c`, `+0x6004`, `+0x6008`, +1 more | 1d/0v/0c | `rule` | FElysiumNpc::ComputeKnockbackVelocity | Ported. 400 bytes, no slot, sole writer of m_KnockbackVelocity +0x6004. Confirmed: the 5.0 initialiser is DEAD on the null-source path (it is only read through t = raw * _DAT_104491b4, which that path does not take), and Z is multiplied by the XY factor and then REPLACED outright by the Z factor so the multiply is dead. Null source takes slot 219 GetAbsAngles through 0x10139550 and negates all three components with blend parameter _DAT_104454c0 = 1.0; with a source the parameter is GetRawAttackValue times _DAT_104491b4 = 0.1, and when 0x10344da0 says the source weapon id at +0x3ec lies in 139..147 the raw value is forced to 1.0 and the velocity is copied from +0x3bc..+0x3c4 (CalcAbsoluteVelocity first when m_iEFlags bit 12 is set), otherwise it is this body slot-217 origin minus the source. RECOVERED out of the pinned image: _DAT_1049a1d8 = 220.0, _DAT_1049a1dc = 400.0, _DAT_1049a1e0 = 200.0, _DAT_1049a1e4 = 310.0, _DAT_104491b4 = 0.1. Walked into docs/vtmb/combat-and-damage.md. |
| `0x102bf070` | CAI_BaseNPCTroika::UpdatePoseParameters | 528 | 12 | `CAI_BaseNPCTroika#314`, `CGeneric_NPC#314`, `CNPCMaker#314`, +61 more | `+0x0e6c`, `+0x0e70`, `+0x1064`, `+0x1068` | `+0x029c`, `+0x0304`, `+0x036c`, `+0x0e74`, +2 more | 0d/0v/0c | `rule` | hand:FElysiumNpc::UpdatePoseParameters | slot 314, 528 bytes. RECOVERED 2026-09-14 out of the pinned image's .rdata: _DAT_1049ae9c, the pitch bias, is 7.5f - the checklist left it unnamed. The aim target is m_hShootTargetOverride +0x5ba8 resolved and failing that slot 167 GetEnemy, and retail's goto pair means a LIVE override enters the aim arm even with a null enemy; m_iIsOblivious at 1 or more takes the no-aim arm even WITH a target, because the test is inside the aim arm and not in front of it. No-aim writes m_bAimWeaponAtTarget 0, +0x1064 = _DAT_104454c4 = 0.0 and +0x1068 = 0.0. Aim: slot 193 EyePosition, 0x10278650 for the aim point, the delta normalised, the flag 1, m_hWeaponAimTarget the target's handle, m_vWeaponAimOffset the aim point minus the TARGET's slot-217 origin, VectorAngles with the pitch biased by 7.5 and the yaw relative to slot 219, both clamped low-bound-first to minus 45 and 45. The tail CBaseCombatCharacter::UpdatePoseParameters runs on BOTH arms. |
| `0x102c79e0` | CAI_StandoffBehavior::vfunc22 | 359 | 12 | `CAI_StandoffBehavior#22` | `+0x001c` | `+0x0004`, `+0x001c` | 0d/0v/0c | `rule` | FElysiumNpc::StandoffTranslateActivity | PORTED 29d Motor10 (listing). CORRECTION: the log literal at 0x10601e9c is NPC in standoff lacks needed low aim activity (%s) - low AIM ACTIVITY, not low cover animation - and the %s is the active weapon's classname or the literal no weapon at 0x10601edc when GetActiveWeapon returns null. Arms in retail's order: the owner's m_pHintNode +0x5ddc existing with m_nHintType +0x5dc == 0x65 reads owner slot 569 (+0x8e4) into a local, reads it AGAIN to replace an incoming activity of 1, and sets +0x1c = 2 when +0x1c is 0 and the FIRST read was 8; +0x1c == 2 turns 1 into 8 and turns 9 into 0x12 when SelectHeaviestSequence(owner, 0x12, -1) is non-negative; +0x1c == 1 with activity 8 returns 1 (EAX still holds [ESI+0x1c]); +0x1c == 3 with activity 8 or 1 answers DAT_10925390 when weapon_smg1 (0x10601ef8) is owned and its sequence resolves, else DAT_10925388 when weapon_pistol (0x10601ee8) does, else logs through 0x1027e590 and answers 1; anything left tails to CAI_Behavior::vfunc22. DAT_10925390 and DAT_10925388 are in uninitialised .data (past the file's raw .data end) and are runtime-registered activity ids, so their VALUES stay unrecovered. |
| `0x102cdc50` | CAI_StandoffGoal::UpdateOnRemove | 60 | 12 | `CAI_StandoffGoal#180` | — | `+0x03cc`, `+0x0480` | 0d/0v/0c | `rule` | FElysiumNpc::StandoffGoalUpdateOnRemove | PORTED 29d Motor10 (listing). CORRECTION: the -1 in the stack block is at +0x14, not +0x0c. With E0 the entry ESP, SUB ESP,0x20 and PUSH ESI put the 0x20-byte block at E0-0x20; MOV [ESP+0xc] = block+0x08 = 0 and MOV [ESP+0x1c] = block+0x18 = 0 are written BEFORE the PUSH ECX, and the MOV [ESP+0x1c],0xffffffff at 102cdc72 is written AFTER it, so its address is block+0x14. The block is an inputdata_t and the three initialised words are exactly the inlined variant_t constructor: +0x08 iVal = 0, +0x14 eVal = INVALID_EHANDLE, +0x18 fieldType = FIELD_VOID. pActivator (+0x00), pCaller (+0x04) and nOutputID (+0x1c) are left UNINITIALISED - retail hands slot 243 three words of stack garbage. The block is dispatched to the goal's own slot 243 (vtable +0x3cc) only when bit 0 of m_flags +0x480 stands, and CBaseEntity::UpdateOnRemove is the tail either way. |
| `0x10358c40` | CNPC_Crow::vfunc394 | 13 | 12 | `CNPC_Crow#394` | — | — | 0d/0v/2c | `rule` | registry:394 | CONFIRMED against the listing 10358c40: PUSH 0x10628c14 "NPC_Crow.Gib" then the shared PAS emitter 0x101b0c10, MOV AL,1, RET. Registry row: class CNPC_Crow, slot 394, sound NPC_Crow.Gib, return true. |
| `0x10358ef0` | CNPC_Crow::vfunc490 | 11 | 12 | `CNPC_Crow#490` | — | — | 0d/0v/3c | `rule` | registry:490 | CONFIRMED against the listing 10358ef0: PUSH 0x10628c24 "NPC_Crow.Idle" then the shared PAS emitter 0x101b0c10, no return value. Registry row: class CNPC_Crow, slot 490, sound NPC_Crow.Idle. |
| `0x10358f10` | CNPC_Crow::vfunc489 | 11 | 12 | `CNPC_Crow#489` | — | — | 0d/1v/2c | `rule` | registry:489 | CONFIRMED against the listing 10358f10: PUSH 0x10628c34 "NPC_Crow.Alert" then 0x101b0c10. Registry row: class CNPC_Crow, slot 489, sound NPC_Crow.Alert. |
| `0x10358f30` | CNPC_Crow::vfunc491 | 11 | 12 | `CNPC_Crow#491` | — | — | 0d/0v/2c | `rule` | registry:491 | CONFIRMED against the listing 10358f30: PUSH 0x10628c48 "NPC_Crow.Pain" then 0x101b0c10, the CTakeDamageInfo argument ignored. Registry row: class CNPC_Crow, slot 491, sound NPC_Crow.Pain. |
| `0x10358f50` | CNPC_Crow::vfunc488 | 11 | 12 | `CNPC_Crow#488` | — | — | 0d/0v/2c | `rule` | registry:488 | CONFIRMED against the listing 10358f50: PUSH 0x10628c58 "NPC_Crow.Die" then 0x101b0c10, the CTakeDamageInfo argument ignored. Registry row: class CNPC_Crow, slot 488, sound NPC_Crow.Die. |
| `0x1035cf80` | CNPC_VAndreiBlood::Restore | 101 | 12 | `CNPC_VAndreiBlood#127` | — | — | 0d/0v/0c | `rule` | FElysiumNpc::AndreiBloodRestore | Slot 127, CNPC_VAndreiBlood#127 only. RETARGETED from registry:127 to a port method, on the same ground review5 retargeted its four siblings: the row is reached through family SaveRestore10's slot-127 arm table, which dispatches to a method, and a data row cannot express a base call. The reading is unchanged and confirms the walk: the listing has exactly ONE CALL between the scope-frame push and pop (1035cfd4, CNPC_VVampireBoss::Restore 0x103c5910) and the base's EAX survives untouched to the RET 0x4, so this species row carries NO restore-time datum of its own and returns the base's answer. Recording it as an empty row matters because omitting it would read as an unwalked body. Family SaveRestore10 had this row routed to VampireBossRestore as a placeholder with a comment saying the owning story would move it; the arm now names this body. |
| `0x10360e10` | CNPC_VAsianVampire::restore | 117 | 12 | `CNPC_VAsianVampire#127` | `+0x64b8` | — | 0d/0v/0c | `rule` | registry:127 | CNPC_VAsianVampire::restore: CNPC_VVampireBoss::Restore then m_fJumpGravity +0x64b8 = _DAT_104a9300. VALUE RECOVERED out of the pinned vampire.dll at file offset 0x4a9300: a float 2.0. Registry row: class CNPC_VAsianVampire, slot 127, JumpGravity 2.0. The port binds the member as FElysiumNpc::JumpGravity but no species row writes it, so it still reads 0. |
| `0x10365800` | CNPC_VBach::FValidateHintType | 38 | 12 | `CNPC_VBach#566` | — | — | 0d/0v/5c | `rule` | registry:566 | CNPC_VBach's FValidateHintType accepts hint types 17000 through 17005 (16999 < t and t < 0x426e) outright and otherwise FALLS THROUGH to CAI_BaseNPCTroika::FValidateHintType 0x10295c20 rather than answering false; the port's species table at ElysiumNpcKernelHints.cpp:366 has no CNPC_VBach row and no fall-through-to-base rule kind, so every Bach hint currently takes the base answer and the 17000..17005 acceptance is lost |
| `0x1036b170` | CNPC_VChangBros::Restore | 167 | 12 | `CNPC_VChangBros#127`, `CNPC_VChangBrosBlade#127`, `CNPC_VChangBrosClaw#127` | `+0x64b8` | — | 0d/0v/0c | `rule` | FElysiumNpc::ChangBrosRestore | Slot 127, shared by CNPC_VChangBros, CNPC_VChangBrosBlade and CNPC_VChangBrosClaw. RETARGETED from registry:127 for the same reason as 0x1035cf80. Reading confirms the walk and pins the order: the base CNPC_VVampireBoss::Restore 0x103c5910 runs first and its answer is stashed (1036b1e3 MOV EDI,EAX) and returned (1036b210 MOV EAX,EDI); then m_fJumpGravity +0x64b8 = _DAT_104ada44, issued between the FLD at 1036b1ce and the first call at 1036b1e5 so it lands first; then SetBodyEmitterName(0, chang_powerup_emitter at 0x10630e54), SetBodyEmitterName(1, the SAME literal) and SetBodyEmitterName(2, chang_spine_emitter at 0x10630e3c). Index 3 is never written and the base's ClearBodyEmitterNames is what empties it. RDATA read out of the pinned vampire.dll: _DAT_104ada44 = 2.3f at file offset 0x4ada44. Two sibling cells read while checking this one and recorded for the rows that own them: _DAT_104a9300 = 2.0f (0x10360e10) and _DAT_104c6148 = 2.0f (0x103ae7f0). |
| `0x1036cbd0` | CNPC_VChangBros::CheckForUnited | 196 | 12 | — | — | `+0x66ec` | 1d/0v/0c | `rule` | FElysiumNpc::CheckForUnited | false at once when GetOtherBrother is null, and false while curtime has not passed m_fLastUnitedAttackTime +0x66ec plus _DAT_104ada48; past both it is true UNLESS BOTH brothers' GetCurrHealthPercent are below _DAT_104ada4c. VALUES RECOVERED at 0x4ada48 and 0x4ada4c: 30.0 and 0.5. SENSE CORRECTED: the percent rises with damage, so below 0.5 is the HEALTHY half and two healthy brothers REFUSE the united attack - one hurt brother is what allows it. |
| `0x1037a270` | CNPC_VGargoyle::Touch | 372 | 12 | `CNPC_VGargoyle#175` | — | — | 0d/0v/0c | `rule` | FElysiumNpc::GargoyleTouch | Slot 175, CNPC_VGargoyle#175 only. RETARGETED from FElysiumNpc::Touch: slot 175's Troika-line body is CBaseEntity::Touch 0x100a4af0, a band 0-9 row with NO verdict (one of the 145 non-core CBaseEntity slots story 29c-1 set aside), so gen_kernel_shape still emits FElysiumNpc::Touch as a named stub and a definition of it here would be a duplicate symbol. The species body therefore lands as FElysiumNpc::GargoyleTouch and the base body as FElysiumNpc::BaseEntityTouch, the shape story 29c-1's cleanup used for CBaseEntity's own slot-153 body (BaseEntityIsMoving). Reading confirms the walk: two FClassnameIs compares of +0x26c, pillar at 0x1063a88c then central_pillar at 0x1063a894, both whole-name case-insensitive because neither literal carries the trailing star; on a match a CVDmg_t with SetSrc(this) and Set(1, 0x80, 10) - family 1 lethal, m_bdmgTypes 0x80 DMG_CLUB, m_iDiceAmt 10 - plus m_iToHitSuccesses +0x0c forced to 1 (1037a3ab writes ESP+0x34 where the descriptor is at ESP+0x18); wrapped by 0x101c26d0 with INFLICTOR = the pillar (1037a3a7 PUSH ESI, argument 0) and attacker = the gargoyle (1037a3a5 PUSH EBP) at damage 1.0, bits 0, ammo type -1; then slot 142 OnTakeDamage dispatched ON THE PILLAR (1037a3c1 CALL dword ptr [EDX + 0x238], ECX = ESI). BOTH paths end in CBaseEntity::Touch. The classname predicate is family Misc's GargoyleHitsPillar, called rather than restated. |
| `0x1037be80` | CNPC_VGhoulCroucher::OnVictimHitByMe | 172 | 12 | `CNPC_VGhoulCroucher#24` | — | `+0x026c`, `+0x6665` | 0d/0v/0c | `rule` | FElysiumNpc::OnVictimHitByMe | CNPC_VGhoulCroucher slot 24: the victim's player downcast is read at +0xa8, and when m_bSpawnBurning +0x6665 is set and that pointer is live it calls BurnPlayer(player, 10.0) first - 0x1037bf3c pushes 0x41200000; it then ALWAYS runs the Troika line 0x1029f8d0, the melee-move-record clear, unlike the Gargoyle and SabbatLeader arms which drop it. Added as the fourth row of VictimHitSpeciesRows in ElysiumNpcKernelMisc.cpp, which previously let a burning croucher fall to the bare Troika arm and never burn. |
| `0x1037bf60` | CNPC_VGhoulCroucher::StartTouch | 232 | 12 | `CNPC_VGhoulCroucher#174` | `+0x666c` | `+0x026c`, `+0x6665`, `+0x666c` | 0d/0v/0c | `rule` | FElysiumNpc::GhoulCroucherStartTouch | Slot 174, CNPC_VGhoulCroucher#174 only. RETARGETED from FElysiumNpc::StartTouch for the same reason as 0x1037a270: slot 174's Troika-line body CBaseEntity::StartTouch 0x100a49d0 is an unverdicted band 0-9 row and its generated stub still stands, so the species body is FElysiumNpc::GhoulCroucherStartTouch and the base is FElysiumNpc::BaseEntityStartTouch. Reading confirms the walk and NAMES its two offsets: CBaseEntity::StartTouch runs first; then OnDisturbed(toucher) when the toucher's +0xa8 is non-null OR its +0x94 is non-zero, and both words are already recovered - +0xa8 is CBaseEntity's player self-downcast cache and +0x94 its cached CAI_BaseNPC* - so the gate reads the toucher is the player, or the toucher is an NPC; then, when m_bSpawnBurning +0x6665 is set AND the +0xa8 pointer from that step is non-null (so an NPC toucher cannot burn) AND m_flNextTouchBurnTime +0x666c is STRICTLY behind curtime (FLD curtime / FCOMP / AND EAX,0x4100 / JNZ), restamp +0x666c = curtime + _DAT_1044ffd0 and call BurnPlayer(player, 5.0). RDATA: _DAT_1044ffd0 is a DOUBLE (1037c022 FADD double ptr) and reads 5.0 at file offset 0x44ffd0. The 5.0 is against the 10.0 (0x41200000) this class's own OnVictimHitByMe 0x1037be80 passes to the same BurnPlayer 0x1037c090, which family SpeciesMisc10 ported and which is called here rather than restated. DIVERGENCE, named: retail dereferences a null toucher at 1037bfe7 and faults; the port refuses the arm, and no world path can produce a null toucher. |
| `0x10395d00` | CNPC_VMingXiao::vfunc574 | 144 | 12 | `CNPC_VMingXiao#574` | — | — | 0d/0v/1c | `rule` | FElysiumNpc::GetShootEnemyDir | Ming Xiao's species arm of slot 574: identical to the base 0x10278900 except that the aim point returned by 0x10278650 has its Z raised by _DAT_1044eb0c before the caller's shoot position is subtracted; the delta is normalised in place through 0x1057966c and the UNIT direction stored out, which the decompiled C hides for the same stack-shift reason as the base body; this is a species branch on the port's one slot-574 body, not a second function |
| `0x103a6e80` | CNPC_VSabbatLeader::vfunc127 | 71 | 12 | `CNPC_VSabbatLeader#127` | `+0x6680`, `+0x6694` | — | 0d/0v/0c | `rule` | registry:127 | CONFIRMED against the listing 103a6e80. Species Restore for CNPC_VSabbatLeader: it returns CNPC_VVampireBoss::Restore's value, then writes m_pMonsterModelName +0x6680 = models/character/monster/Andrei/andrei.mdl (0x1064ead0), m_pszMonsterClassname +0x6694 = npc_VSabbatLeader (0x1064ccb4), and SetBodyEmitterName(0, Andrei_powerup_emitter) and (1, the same string at 0x1064e894). All four values RECOVERED verbatim out of the pinned image. Registry row: class CNPC_VSabbatLeader, slot 127, four values; the port carries none of these words. |
| `0x103ae7f0` | CNPC_VSheriffMan::vfunc127 | 59 | 12 | `CNPC_VSheriffMan#127` | `+0x64b8`, `+0x6680`, `+0x6694` | — | 0d/0v/0c | `rule` | registry:127 | CONFIRMED against the listing 103ae7f0. Species Restore for CNPC_VSheriffMan: CNPC_VVampireBoss::Restore, then m_pMonsterModelName +0x6680 = models/character/monster/manbat/manbat.mdl (0x10651230), m_pszMonsterClassname +0x6694 = npc_VSheriffMan (0x106508dc), m_fJumpGravity +0x64b8 = _DAT_104c6148 - a FLOAT, since 103ae811 is FLD dword, VALUE RECOVERED at 0x4c6148 as 2.0 - and finally the GLOBAL DAT_109340d8 = 0x15, which is not species data. That global is written by four NPCInit bodies too (Gargoyle, Hengeyokai, ManBat, SheriffMan) and read by seven functions outside the kernel; it is the one part a data table alone cannot carry and is still unported. Registry row: class CNPC_VSheriffMan, slot 127, three values plus that global. |
| `0x103c6a00` | FUN_103c6a00 | 16 | 12 | — | `+0x6698`? | — | 5d/0v/0c | `rule` | FElysiumNpc::RecordHealthPercent | one write: m_HealthPercentRecord +0x6698 = CNPC_VVampireBoss::GetCurrHealthPercent(this), the boss line's snapshot taken before a phase and read back by 0x103c6a20; five direct callers across the boss species. The port had no +0x6698 word; it now has one. |
| `0x103c6a20` | FUN_103c6a20 | 22 | 12 | — | — | `+0x6698`? | 3d/0v/0c | `rule` | FElysiumNpc::HealthPercentLostSinceRecord | returns CNPC_VVampireBoss::GetCurrHealthPercent(this) minus the snapshot m_HealthPercentRecord +0x6698, in that order and as a float10. SENSE CORRECTED: GetCurrHealthPercent 0x103c6830 is stat 0x0f (wounds) divided by stat 0x11 (cap), so it RISES with damage and this answers POSITIVE for a body that has LOST health - the earlier walk says negative. Every threshold that reads it (0.1 ChangBros, 0.0666667 SabbatLeader) is therefore a fraction of the bar lost since the mark. |
| `0x103d5590` | CNPC_VWerewolf::DrawDebugHintInfo | 1719 | 12 | — | — | `+0x026c`, `+0x1568` | 2d/0v/0c | `mechanism` | DrawDebugHelpers | pure overlay and nothing else: the hint's slot-217 origin and slot-219 angles, the hint groundpoint, the hint's targetname resolved through 0x100f7770 with an RTDynamicCast that picks up a second chained hint at +0x468, the hint endpoint, the mins and maxs for m_eHull +0x1568 from 0x102d6100 and 0x102d6120 lifted by _DAT_10451ad0, and a curtime-derived lerp parameter fed through fsin and fcos; five separate cvar gates each draw a box, a text line or one of two 16-unit markers interpolated along the hint-to-target and target-to-forward segments; no member is written and nothing outside the debug renderer is touched |
| `0x103d68d0` | CNPC_VWerewolf::GetHintTargetGroundpoint | 283 | 12 | — | — | `+0x026c`, `+0x6714`, `+0x6720` | 6d/0v/0c | `rule` | FElysiumNpc::GetHintTargetGroundpoint | a linear scan of m_HintData +0x6714 over the count at +0x6720 at stride 0x48, comparing the entity pointer at element+0x04 against the requested hint and returning the Vector at element+0x14; on a miss it DevWarnings Werewolf did not find the hint and falls back to GetGroundpoint of GetHintEndpoint, so the miss still answers a point; the port carries the sibling GetHintGroundpoint 0x103d6770 at ElysiumNpcKernelHints.cpp:683 but not this target variant, and six direct callers need it |
| `0x103d7210` | CNPC_VWerewolf::GetForwardYawForHint | 381 | 12 | — | — | `+0x026c` | 1d/0v/0c | `rule` | FElysiumNpc::GetForwardYawForHint | seeds the working direction with vec3_invalid DAT_10713de0 through DAT_10713de8, takes GetHintEndEntity and GetForwardHintForHint, forms the end origin minus the forward origin from slot 217 on each, normalises it and converts it to a yaw through 0x101d2c70; then switches on the hint type at +0x5dc - 0x3a9a and 0x3a9d add _DAT_10455050, 0x3a9b and 0x3a9e subtract it, 0x3aa3 and 0x3aa5 discard the computed yaw and take the hint's own slot-219 yaw instead - and finally compares the result against _DAT_10450568; the decompiler loses the float10 return storage so both tails read alike, and the listing must be checked for what that compare selects before porting; only the vec3_invalid sentinel is cited in the port (ElysiumNpcKernelMotor2.cpp:571) |
| `0x103d7dc0` | CNPC_VWerewolf::IsValidRandomMoveHint | 522 | 12 | — | — | `+0x026c`, `+0x08d8`, `+0x09a4`, `+0x66ec`, +1 more | 3d/0v/0c | `rule` | FElysiumNpc::IsValidRandomMoveHint | false unless all four of: the hint is non-null, 0x102d14c0 says it is not taken (its +0x5e8 disable flag clear, curtime past its lock time +0x5ec and its owner handle +0x5e0 dead), slot 566 FValidateHintType passes, and the type +0x5dc is not 0x3aa9; then by type - 0x3aa8 is true only when slot 617 EnemyCouldSeeHull at the hint endpoint answers FALSE; 0x3a9a, 0x3a9b and 0x3a99 are true when curtime minus m_flLastSeenByPlayerTime +0x66ec is at or below 0 or below _DAT_10452dc4, and otherwise only when the cached nearest node from 0x103d0ad0 has +0x94 equal to m_iRandomMoveHintNodeZone +0x670c; 0x3aa7, 0x3aa5, 15000 and 0x3aa3 are always false; every other type is true only when the per-NPC hint cooldown list 0x10366400 (handle and time pairs at +0x665c, count +0x6668) holds no live entry, a call that also evicts the expired row by swapping in the last |
| `0x103d8060` | CNPC_VWerewolf::IsValidMoveHint | 520 | 12 | — | — | `+0x026c`, `+0x08d8`, `+0x09a4`, `+0x6680` | 3d/0v/0c | `rule` | FElysiumNpc::IsValidMoveHint | false on a null hint, on 0x102d14c0 reporting the hint taken, on slot 566 FValidateHintType failing, and on types 0x3aa3 and 0x3aa9; type 0x3aa8 needs HasCondition(0x77) set AND slot 617 EnemyCouldSeeHull at the hint endpoint to answer false; type 0x3aa5 needs m_DoorState +0x6680 to be exactly 2; every other type needs the hint's end entity from GetHintEndEntity to have a clear byte at +0xf4 and the cooldown list 0x10366400 to answer false; only those three paths reach the true tail, and note the type set differs from the random-move twin 0x103d7dc0 in both membership and sense |
| `0x10098eb0` | CAI_BaseNPC::FUN_10098eb0 | 285 | 13 | `CAI_BaseHumanoid#255`, `CAI_BaseNPC#255`, `CAI_BaseNPCTroika#255`, +74 more | — | — | 0d/0v/0c | `mechanism` | FElysiumNpc::GetSkeleton | the skeleton build: the engine studiohdr through DAT_1070b22c+0x48, CalcPose for the base m_nSequence at m_flCycle with the pose-parameter array, then exactly FOUR overlay layers walked at stride 0x30 from +0x18 of the overlay block, each layer whose weight exceeds _DAT_104454c4 getting its own CalcPose followed by SlerpBones at that weight, and finally the bone-adjust pass 0x100c32e0 and the encoded-controller pass 0x100c2ce0; Unreal's UAnimInstance owns every step, only the base-then-four-layers order is retail's and it matches the engine's own layered blend |
| `0x100e7da0` | CDialog::fill_packet | 362 | 13 | — | `+0x2834`, `+0x30e9` | `+0x2834`, `+0x2838`, `+0x2848`, `+0x30e9` | 1d/0v/0c +1 outside | `rule` | FElysiumDlgConversation::EnterNpcLine | The band arithmetic is now ported and tested as ElysiumDlgRetail::FillPacketBand (Public/ElysiumDlg.h, Private/Scripting/ElysiumDlg.cpp): the COMPACTING index (the loop counter minus the number already dropped), -1 raising +0x30e9 AND dropping, anything below 1 dropping, the stored count reduced by the dropped rows, and the I-do-not-have-a-valid-reply substitution (0x1056368c) at flags 0 / value -1 with the count forced to 1. The port's divergence is CLOSED: ElysiumDlg.cpp gated that fallback on CandidateRows > 0, and 100e7e9a tests exactly two things - the flag is clear and the count is zero - so an NPC line with no authored PC rows at all reaches it too, because get_pc_responses answers 0 and the drop loop never runs. The gate is removed and CandidateRows with it. |
| `0x1026b4f0` | CAI_BaseNPC::FUN_1026b4f0 | 108 | 13 | `CAI_BaseHumanoid#538`, `CAI_BaseNPC#538`, `CAI_BaseNPCTroika#538`, +74 more | — | `+0x029c`, `+0x086c`, `+0x08f8` | 0d/0v/0c | `rule` | hand:FElysiumNpc::AimGun | slot 538; the whole body is gated on slot 167 GetEnemy being non-null and does nothing without an enemy; then slot 217 GetAbsOrigin feeds slot 389 Weapon_ShootPosition, the Vector that returns is handed to slot 574 GetShootEnemyDir with both trailing arguments 0, and the direction that comes back goes straight to slot 539 SetAim; no member is written here, the writes all happen inside those slots. |
| `0x1026d460` | FUN_1026d460 | 668 | 13 | — | — | `+0x02e0`, `+0x0378`, `+0x5b90` | 1d/0v/0c | `present` | ElysiumNpc.cpp:FElysiumNpc::NpcStep | RE-CONFIRMED against the decompiled C: the three local-player mute predicates (0x1017d630 scripted camera, 0x1017d680 camera track, 0x1017cf90 open conversation) checked first; the walk/run volume and distance pair taken from the template at +0x24/+0x28 or +0x2c/+0x30 when the debug cvar DAT_109203fc is live and from the four cvars otherwise; the DAT_10723944 surface-table and m_pSurfaceData +0x5b90 pair that silences a body standing on nothing; the distance-to-soundlevel map 0x10228350; the PAS attenuation _DAT_10449148 below 0x33 and 0x14/(d-0x32) at or above, which single-player never consumes; RandomInt(0,1) over the surface's +0x2c/+0x2e step pair; and EmitSound on channel 4 at pitch 100 with no CSoundEnt::InsertSound anywhere. ElysiumNpc.cpp carries every one of those arms against ElysiumFootsteps.h, correctly excludes the PAS cull and routes the species overrides through EElysiumFootstepPolicy. present HOLDS. |
| `0x10273070` | FUN_10273070 | 207 | 13 | — | `+0x5f2d` | `+0x036c`, `+0x1568`, `+0x5f2d` | 19d/0v/0c +2 outside | `rule` | FElysiumNpc::SetHullSizeNormal | PORTED 29d Motor10 (listing). The hull self-check runs UNCONDITIONALLY and before the gate: NAI_Hull::Bits(m_eHull) 0x102d6210 is called TWICE (1027307b and 10273090) so the test is (bits & NAI_Hull::GetUsedHullBits() 0x102f9950) != bits, i.e. this hull was never precached, and the six-line ERROR block names the entity through GetDebugName and the hull through NAI_Hull::Name 0x102d6230. Then, when m_fIsUsingSmallHull +0x5f2d is SET or the force byte is non-zero: UTIL_SetSize(this, mins 0x102d6100, maxs 0x102d6120) - the listing EVALUATES maxs first and mins second and then pushes mins before maxs - then m_fIsUsingSmallHull = 0 (the MOV at 1027312a sits BEFORE the physics JZ, so the clear happens on both arms) and SetupVPhysicsHull 0x10272f40 only when +0x36c is live. RET 0x4 with no value: the body is void. Port seams: RetailHullExtents (family Motor) answers the zero box, RetailHullBits answers 0, RetailUsedHullBits answers 0. |
| `0x10273180` | FUN_10273180 | 92 | 13 | — | `+0x5f2d` | `+0x036c`, `+0x1568`, `+0x5f2d` | 7d/0v/0c | `rule` | FElysiumNpc::SetHullSizeSmall | PORTED 29d Motor10. The small-hull twin, gate INVERTED (m_fIsUsingSmallHull CLEAR or force), no self-check, SetSize to 0x102d6140 / 0x102d6160, +0x5f2d = 1, SetupVPhysicsHull when +0x36c is live, and it returns 1 UNCONDITIONALLY including on the refused path. Confirmed against the listing; the checklist walk was already correct. |
| `0x10273720` | CAI_BaseNPC::FUN_10273720 | 36 | 13 | `CAI_BaseHumanoid#223`, `CAI_BaseNPC#223`, `CAI_BaseNPCTroika#223`, +74 more | — | `+0x0278`, `+0x036c` | 0d/2v/2c | `rule` | hand:FElysiumNpc::CreateVPhysics | slot 223: both slot 158 IsAlive (m_lifeState == 0) and a null physics object at +0x36c are required before 0x10272f40 builds the shadow, and that callee itself refuses when slot 94 GetMoveType answers 7, destroys any existing object first, calls VPhysicsInitShadow(true, false, null), sets the shadow mass from the model's own mass keyvalue or, when that is at or below _DAT_104454c4 (0.0), 90 for a male body and 65 for a female one, and sets its damping from the summed hull extents times _DAT_10449270 (0.5) squared; the slot returns true unconditionally, including when nothing was created. |
| `0x1027bb50` | CAI_BaseNPC::Precache | 118 | 13 | `CAI_BaseHumanoid#104`, `CAI_BaseNPC#104`, `CAI_ExpressiveNPC#104`, +6 more | — | `+0x0710`, `+0x5dec` | 7d/1v/11c | `rule` | FElysiumNpc::BasePrecache | CAI_BaseNPC#104 base body, a distinct retail function beside the Troika override 0x10298ad0 that owns slot 104 (which chains here after its own model and dialogue precaches), so it is ported under its own name. Three steps: m_spawnEquipment +0x5dec is precached through UTIL_PrecacheOther 0x101d0ec0 only when the pointer is non-null AND its first two bytes differ from the 2-byte literal at DAT_105399a0, a null pointer reading as the empty string DAT_106b8540; then slot 452 LoadedSchedules (vt+0x710) decides the rest - false prints DevMsg "ERROR: Rejecting spawn of %s as error in NPCs schedules" with CBaseEntity::GetDebugName, removes the entity through 0x101cd940 and RETURNS WITHOUT chaining the base at all; true falls through to CBaseCombatCharacter::Precache. The port has no carrier for either arm: ElysiumNpcKernelSchedule.cpp:296-303 answers LoadedSchedules true unconditionally by design so the reject arm is unreachable there, and slot 104 is still the generated stub at ElysiumNpcKernelSlots.cpp:692. |
| `0x1028ef20` | CAI_BaseNPCTroika::FUN_1028ef20 | 118 | 13 | `CAI_BaseNPCTroika#587`, `CGeneric_NPC#587`, `CNPC_ProneDialog#587`, +45 more | — | `+0x0128`, `+0x5b84`, `+0x5bb4`, `+0x6354`, +1 more | 0d/0v/1c | `rule` | hand:FElysiumNpc::CanWitnessSupernatural | slot 587, five refusals in this order, each answering false alone: IsKindred, m_iDialog non-zero, m_iIsOblivious greater than 0, m_bfNPCFrenziedFlags bit 0x10 (the does-not-witness bit, now named FElysiumNpcFlags::FrenziedDoesNotWitness), and IsBusyWithDiscipline; past all five it answers true when m_iPLSupernaturalFleeLevel is below 3 and otherwise returns whether m_iPLSupernaturalAttackLevel is below 3, so a body authored with flee 3 or more and attack 3 or more can never witness. The int argument is dead in every arm. Both thresholds are the RAW authored keyfields at +0x6354 and +0x6358, not the resolved-disable form. |
| `0x102b3e00` | CAI_BaseNPCTroika::FUN_102b3e00 | 485 | 13 | `CAI_BaseNPCTroika#472`, `CGeneric_NPC#472`, `CNPCMaker#472`, +59 more | `+0x14b8`, `+0x6088`, `+0x609c`, `+0x60a0`, +2 more | `+0x14b8`, `+0x14bc`, `+0x5fa4`, `+0x6081`, +5 more | 2d/0v/0c | `rule` | hand:FElysiumNpc::OnSeeEntity | slot 472, 485 bytes, Troika OnSeeEntity. Outer gate in retail order: m_bfAINPCFlags2 +0x14bc bit 0x4000000 CLEAR, the far byte +0x6081 set, and 0x102b3270(entity, false) true. Inside it, only when the entity's player pointer +0xa8 is non-null and 0x101671a0 admits it: read the ConVar object DAT_10924a6c slot 1, call 0x10269a20(this, 1), and if m_hBestSeeUnknown +0x609c already resolves to this entity RETURN with nothing written; else m_hBestSeeUnknown = the entity's handle and m_iEnemySightings +0x60a8 += 1; then if m_hLastSeeUnknown +0x60a0 resolves to the same entity, m_iSeeUnknownRepeatSightings += 1, m_bfAINPCFlags +0x14b8 &= 0xfebfffff (IGNORE_UNKNOWN 0x400000 and MADE_INITIAL_RESPONSE 0x1000000) and RETURN; otherwise m_OnUnknownVisionPlayer +0x5fa4 fires FIRST with the entity as activator, and ONLY THEN m_hLastSeeUnknown = m_hBestSeeUnknown, m_vecLastSeeUnknownPos +0x6088 = the resolved entity's slot-217 GetAbsOrigin, m_iSeeUnknownRepeatSightings = 0, m_flSeeUnknownRunTimer = curtime + RandomFloat(10.0, 20.0) and m_flSeeUnknownStartTimer = curtime + RandomFloat(5.0, 10.0). The refused path ORs 0x800000 into m_bfAINPCFlags, then releases m_hBestSeeUnknown only when it resolves to this entity; every fall-through tail clears m_bfAINPCFlags &= 0xfe9fffff (the two above plus LOOKED_AT_UNKNOWN 0x200000). The port carried every arm but fired OnUnknownVisionPlayer at the END of the new-unknown arm where retail fires it before all five writes: an observable order difference, now restored, with the body run from the slot. |
| `0x102b5d90` | CAI_BaseNPCTroika::LeaveGrappleState ‼ | 19 | 13 | `CAI_BaseNPCTroika#380`, `CGeneric_NPC#380`, `CNPCMaker#380`, +61 more | — | `+0x0998` | 0d/0v/0c | `present` | ElysiumNpc.cpp:FElysiumNpc::LeaveGrappleState | slot 380, read from the listing (the jump-table warning is a tail JMP, not a table): CALL 0x10005641 into CAI_BaseNPC::LeaveGrappleState 0x1026ce30, then JMP [vtable + 0x998] = slot 614 = 0x102c23f0, which stamps gpGlobals curtime into m_flNextThink +0x17c and the four think stamps +0x6244/+0x6248/+0x624c/+0x6250. present HOLDS for the slot body itself - it is exactly those two statements and the port has both - but the BASE half it calls was only partly ported, which is 0x1026ce30's row; ElysiumNpc.cpp:790 now calls BaseLeaveGrappleState and then ResetThinkTimers and cites both addresses. |
| `0x102c54c0` | FUN_102c54c0 | 43 | 13 | — | `+0x65f0`? | — | 2d/0v/0c | `rule` | FElysiumNpc::ResetFakeReloadCount | No slot; two direct callers, both AsianVampire reload arms. Resolves the owner char template (0x10207c40 = GetCharTemplate then 0x101d5e80 on the template manager DAT_10738d10), calls the engine random vtable +8 RandomInt with the template ints at +0x34 and +0x38, and stores the draw into m_iFakeReloadCount +0x65f0. The port binds the word; the two template columns have no counterpart in FElysiumClanTemplate and are a seam answering zero, and the roll still happens because retail's body has no arm that skips its only write. |
| `0x103482e0` | CAI_BaseNPC::FUN_103482e0 | 170 | 13 | `CAI_BaseHumanoid#326`, `CAI_BaseNPC#326`, `CAI_ExpressiveNPC#326`, +10 more | — | `+0x0278`, `+0x13bc`, `+0x13c0` | 1d/0v/0c +2 outside | `present` | ElysiumReactions.cpp:ElysiumReactions::IsKnockbackAllowed | CAI_BaseNPC#326 base body, a distinct retail function beside the Troika override 0x1029fec0 that owns slot 326; its recovered name is the knockback eligibility predicate (docs/vtmb/combat-and-damage.md, "Who may be knocked back"), dispatched at vt+0x518 from the melee commit 0x102579f0 to decide whether the victim plays a knockback reaction (vt+0x500) instead of the plain one (vt+0x504). Three arms in order: the NPC template record fetched by 0x101d5f10 over the table at DAT_10738d10 (indexed by CBaseCombatCharacter::GetCharTemplate, falling back to the lazily built DAT_10738e50) answers false at once when its byte +0x9e Disallow_Knockbacks is set; then it picks the CVStatList_t on this victim whose category tag +0x10 is 0, falling back to a lazily constructed global CVStatList_t(-1) at DAT_109f0b40 behind an atexit guard, and CVStatList_t::IsEqual(0x0f, 0x11) - Health == Max_Health read over damage-taken, i.e. dead - answers false; otherwise the answer is slot 158 (vt+0x278), which on CAI_BaseNPC is 0x100b4dc0, m_lifeState == 0. The port runs all three arms: ElysiumReactions::IsKnockbackAllowed (ElysiumReactions.cpp:127-142) ANDs Victim.DisallowsKnockbacks() (the authored +0x9e key) with the alive term IsAliveForCombat (ElysiumWeaponClasses.cpp:127), which is !IsInert() && !HasReportedDeath() and covers both the Health == Max_Health refusal and the lifeState test; no arm has a side effect, so retail's term order is unobservable and the reordering is not a divergence. The class bypass and hit-buildup terms that function also carries belong to 0x10344f80 and to the Troika twin 0x1029fec0, not to this body. |
| `0x1036cab0` | CNPC_VChangBros::CheckForTeleport | 217 | 13 | — | — | `+0x66b8`, `+0x66d0` | 1d/0v/0c | `rule` | FElysiumNpc::CheckForTeleport | CNPC_VChangBros, three arms in retail's order: m_ChangType +0x66b8 equal to 1 answers false outright; else the health delta 0x103c6a20 at or above _DAT_104ad9f8 answers true; else, only for type 0 and only with a live GetOtherBrother, curtime minus m_fFacingTime +0x66d0 strictly greater than GetFacingTimeToTeleport() answers true. VALUE RECOVERED at 0x4ad9f8: 0.1. SENSE CORRECTED: 0x103c6a20 is GetCurrHealthPercent minus the mark and GetCurrHealthPercent is wounds over cap, so it RISES with damage - the threshold is a tenth of the bar LOST since the mark, not gained. |
| `0x103a9d90` | CNPC_VSabbatLeader::CheckForJumpCondition | 165 | 13 | — | — | — | 1d/0v/0c | `rule` | FElysiumNpc::CheckForJumpCondition | CNPC_VSabbatLeader, three arms in retail order: 0x103c67f0(this, DAT_104c3cc8) answers true when curtime minus m_flLastAttackTime +0x5d9c is STRICTLY greater than the constant; else the health delta 0x103c6a20 at or above _DAT_104c3cc4 answers true; else the answer is PlayerDamagedEnoughThisRound. VALUES RECOVERED at 0x4c3cc8 and 0x4c3cc4: 8.0 and 0.0666667. The threshold is handed to 0x103c67f0 as an ARGUMENT, not compared inline. SENSE CORRECTED as for 0x1036cab0: the delta is positive for health LOST. |
| `0x103acac0` | FUN_103acac0 | 161 | 13 | — | — | `+0x0364`, `+0x6690`, `+0x6694`, `+0x6695` | 2d/0v/0c | `rule` | FElysiumNpc::ScurryingShouldDetect | CNPC_VScurrying detection test: a null target answers false; the distance between the two GetAbsOrigin results (slot 0x364, sqrt of the summed squares) must be strictly below m_flDetectionDistance +0x6690; then m_fIgnoreNosferatu +0x6694 rejects a target whose char template is Player_Nosferatu (0x103ad0f0); then m_fMustDetect +0x6695 rejects unless 0x103ad0a0 holds, which is true for any target that is not a player and for a player only while COND 0x5a SEE_PLAYER or COND 0x6f stands; passing all of them answers true. Not in the port; +0x6690, +0x6694 and +0x6695 are shape rows only. |
| `0x103c63c0` | CNPC_VVampireBoss::WaitForTransformation | 333 | 13 | — | — | `+0x13bc`, `+0x13c0`, `+0x1560`, `+0x66b0` | 1d/0v/0c | `rule` | FElysiumNpc::WaitForTransformation | CNPC_VVampireBoss task body, one gate then three writes: nothing until curtime passes m_flProteanTransformStartTime +0x66b0 plus _DAT_104ce8bc, strictly; then it selects the type-0 Attributes CVStatList_t from +0x13bc/+0x13c0 with the lazily built global fallback and calls CVStatList_t::Set(stat 0x0f, 0) - a FULL HEAL, because 0x0f is the wound counter and not current health; resolves m_hTransformPartner, RTDynamicCasts it and fires the partner's +0x6664 output with this as both activator and caller; then TaskComplete(0), which is 0x10273e80 and returns untouched while COND 0x5c TASK_FAILED stands. VALUE RECOVERED at 0x4ce8bc: 2.0. |
| `0x103d5130` | CNPC_VWerewolf::DrawDebugStatOverlays | 888 | 13 | `CNPC_VWerewolf#76` | — | `+0x026c`, `+0x6680`, `+0x668c`, `+0x6698`, +5 more | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugStatOverlays | CNPC_VWerewolf slot 76, added as the FOURTH body of the port's slot-76 dispatcher. In retail order: Not Seen Time with max(curtime - +0x66ec, 0.0) - the clamp against _DAT_104454c4 at 103d51aa is folded away by the decompiler and is RECOVERED here - then Player Distance reading +0x6264 m_flPlayerDist, the CACHED distance and not a computed range; then the tail into CNPC_VMingXiao 0x10366290; then the zone word +0x66e8 bit by bit 0x1, 0x2, 0x40, 0x80, 0x4, 0x800, 0x1000, 0x100; then conditions 0x77, 0x78, 0x79, 0x7b, 0x7a in that order; then m_DoorState +0x6680 printed closed/closing/open/opening with NO default arm at all; then DrawDebugHintInfo on a cvar-selected PLAYER hint (UTIL_PlayerByIndex(1) through 0x10172710 and an RTTI cast) and then on the NPC's own m_pMoveHint +0x66bc first, else m_pTeleportHint +0x66b0, else m_pLastUsedTeleportHint or m_pLastUsedMoveHint by two more cvars; then the last five entries of the schedule stack +0x668c with count +0x6698, printing INVALID SCHEDULE for a null row. |
| `0x103d7710` | CNPC_VWerewolf::InitializeHintData | 1178 | 13 | — | — | `+0x026c`, `+0x6714`, `+0x6720` | 1d/0v/0c | `rule` | FElysiumNpc::InitializeHintData | Runs only while the werewolf hint count +0x6720 is zero. It walks the global hint entity chain from DAT_10925450 through the +0x18 next link, growing the +0x6714 array one 0x48-byte CHintData_WW per entity, and per entity: CHintData_WW::Init, store the entity at +4, FindHintEndEntity into the handle at +0 (0xffffffff when absent), GetForwardYawForHint written back through the entity angles slot 0x104, GetGroundpoint of GetAbsOrigin raised by _DAT_104492a4 into +8, GetGroundpoint of GetHintEndpoint raised the same into +0x14, each validated against the 0x7f800000 exponent mask and on failure DevMsg has invalid ground point or invalid target ground point and fall back to the raw origin or raw endpoint; then it counts entities named by the hint target string through 0x100f7770 and DevWarnings unless the count is exactly 1; finally DevMsg the hint total. Unported. |
| `0x100e4780` | CDialog::NPCNotifyDoneTalking | 143 | 14 | — | — | `+0x0008`, `+0x2810` | 1d/0v/0c | `present` | ElysiumEntityWorldDialogue.cpp:FElysiumEntityWorld::UpdateDialogueAutomatic | CDialog::NPCNotifyDoneTalking: CallPendingNPCEventScript runs FIRST and unconditionally, outside the session test; then only if the session pointer +0x8 is live does the flag word +0x2810 pick the continuation - bit 0x20 takes Pick(0) and returns, else bit 0x10 takes Release, else nothing. The port matches arm for arm: UpdateDialogueAutomatic (ElysiumEntityWorldDialogue.cpp:202) calls FlushDialogueVoiceCompletion (line 143, the CallPendingNPCEventScript equivalent, gated only on the done-talking edge) as its FIRST statement, ahead of the session, turn-identity and voice gates, then takes ResolveAutomatic for the Pick(0) arm and EndDialogSession for the Release arm, with PlayerDialogSkip:161 the same edge for the skip key. The pack-04 row's stated divergence is WRONG - the port does not defer the flush into ResolveAutomatic; the second FlushPendingNpcAction there is idempotent because ElysiumDlg.cpp:1127 empties the buffer before the call. No divergence remains. |
| `0x100e4bd0` | CDialog::Pick | 628 | 14 | — | `+0x2830` | `+0x0004`, `+0x0008`, `+0x000c`, `+0x2830`, +2 more | 1d/0v/0c +2 outside | `present` | ElysiumDlg.cpp:FElysiumDlgConversation::Choose | Guarded on the live session +0x8. Index -2 hurries the owner through 0x102c0bb0 and returns; index -1 Releases and returns; otherwise the owner must not be mid-line (0x102c0aa0) and index+1 must not exceed m_iNumChoices +0x2834, then in order: 0x100e49b0 flushes the parked NPC script, goto_line_for_response writes the next line to +0x2830, a DevMsg, the index goes out as user message 5 on a reliable single-user filter, pc_charge_dependency on the row at +0x2848 plus index times 0x228, CallEventScript on +0x2964 plus index times 0x228, then fill_packet and message_send only when +0x2830 is non-zero, and Release when +0x2830 is zero or +0x30e9 is set. The port keeps that order (flush, charge, row script, link) in Choose and ResolveAutomatic; the -2 and -1 arms live in PlayerDialogSkip and EndDialogSession. |
| `0x1026a2c0` | CAI_BaseNPC::OnLooked | 624 | 14 | `CAI_BaseHumanoid#469`, `CAI_BaseNPC#469`, `CAI_ExpressiveNPC#469`, +10 more | `+0x5b68`, `+0x5b6c`, `+0x5b70`, `+0x5b74` | `+0x0098`, `+0x029c`, `+0x0650`, `+0x0654`, +3 more | 1d/0v/0c | `rule` | FElysiumNpc::BaseOnLooked | CAI_BaseNPC#469 base body, a distinct retail function beside the Troika override 0x102b39a0 that owns slot 469, so it is ported under its own name. In retail order: ClearCondition over the 6-entry table at 0x105c979c; resolve the entity to skip from the senses object at +0x98 - slot 0x928 normally, or when m_bfAINPCFlags2 bit 0x400000 is set the handle at +0x608c - and skip that entity outright before any arm; then per entity off the senses iterator 0x1030fc90/0x1030fd20: IRelationType slot 0x650; a player (+0xa8 non-null) raises COND 0x5a SEE_PLAYER and, when m_bIsBCCTargetable is set, stamps the player per-relation assessment through 0x1017ff40; then ONLY for relation != 4 (D_NU) the committed enemy (slot 0x29c) raises COND 0x46 SEE_ENEMY and the switch runs - D_HT (1) under D_CALM (flags2 0x10000) falls into the D_FR arm and is rejected there, otherwise IRelationPriority slot 0x654 splits it into 0x45 SEE_DISLIKE below 0, 0x43 SEE_HATE at 0 to 10 and 0x5b SEE_NEMESIS above 10, writing m_hLastSeenDislikeEnt, m_hLastSeenHateEnt or m_hLastSeenNemesisEnt and then calling slot 0x880 UpdateEnemyMemory with the target origin from slot 0x364; D_FR (2) writes m_hLastSeenFearEnt and raises 0x44 with the same 0x880 call; D_LI (3) and D_NU (4) raise nothing; D_ER (0) prints DevWarning level 2 "%s can't assess %s". ElysiumNpcCond::GatherSight (ElysiumNpcConditions.cpp:316-432) already carries the skip, SEE_PLAYER, the 0x1017ff40 stamp, the D_CALM divert, the three priority thresholds and the memory write in that order - MISSING is the relation != 4 gate and the skip-entity exclusion around SEE_ENEMY, which the port instead raises unconditionally in GatherCommittedEnemy (line 923) from Sighted().Contains(Enemy), plus the D_ER DevWarning; that partial carry is why this is rule and not present. |
| `0x1026ab50` | FUN_1026ab50 | 641 | 14 | — | — | `+0x0364`?, `+0x5f2d`? | 1d/0v/0c | `rule` | FElysiumNpc::HeadProbe | RunAI 0x1026f110 calls this between GatherConditions and PrescheduleThink (lifecycle.md:376). It runs only when the shrunk-hull byte +0x5f2d is set and +0x5f2c is set: it takes the navigator hull maxs (0x102d6120) and mins (0x102d6100) off m_pNavigator +0x1568, centres a box on GetAbsOrigin raised by _DAT_104454c0, halves the extents by _DAT_104454d0 and scales them by _DAT_104492dc, sets the two box flag bytes from the zero-delta test against _DAT_104454c4 and the _DAT_104492e0 extent tests, traces mask 0x200400b with the self filter 0x101d3190 and optionally draws it under a cvar, and on a clean trace (start-solid byte clear and fraction equal to _DAT_10449280 which is 1.0) calls 0x10273070, which restores the normal hull from the navigator, clears +0x5f2d and re-runs 0x10272f40 when +0x36c is set. Unported; no port field stands for +0x5f2d. |
| `0x1026fcf0` | CAI_BaseNPC::FUN_1026fcf0 | 405 | 14 | `CAI_BaseHumanoid#573`, `CAI_BaseNPC#573`, `CAI_BaseNPCTroika#573`, +74 more | — | `+0x0184`, `+0x029c`, `+0x0650` | 0d/1v/1c | `rule` | hand:FElysiumNpc::InnateWeaponLOSCondition | slot 573: the start point is param_1 plus m_vecViewOffset +0x0184, the ray runs from there to param_2 (0x1004f7a0 builds it), the trace uses mask 0x46004003 with the self filter 0x101d3190 and is optionally drawn under a cvar. Arms in order: fraction equal to _DAT_10449280 (a DOUBLE 1.0) answers true; the hit entity equal to GetEnemy slot 0x29c answers true; a hit entity that is null or whose +0x9c COMBAT CHARACTER is null raises COND 0x66 through 0x10269a20 and records the blocker into m_hEnemyOccluder +0x5d90 via 0x10270aa0, and answers false; otherwise IRelationType slot 0x650 equal to 1 (D_HT) answers true (the MOV AL,AL at 1026fe19 returns the relation's own low byte), and anything else raises COND 0x63 and answers false. CORRECTION to the earlier walk: it named +0x9c "the +0x27 word" and reported both condition raises as gated on trace bool bytes; the listing reads [ESP+0xb8] at 1026fe25 and 1026fe51, which with SUB ESP,0xa4 and two pushes is the THIRD ARGUMENT bSetConditions. There is no trace byte in either arm. |
| `0x102743c0` | CAI_BaseNPC::BestEnemy | 884 | 14 | `CAI_BaseHumanoid#478`, `CAI_BaseNPC#478`, `CAI_BaseNPCTroika#478`, +73 more | — | `+0x0324`, `+0x0364`, `+0x0650`, `+0x0654`, +4 more | 0d/1v/2c | `rule` | hand:FElysiumNpc::BestEnemy | slot 478: it walks the CAI_Memory list (slot 0x874, head +0xc, next +0x38, handle +0x24) rejecting in order FL_NOTARGET (GetFlags bit 0x8000, read as the sign of flags>>8), an entity whose NPC pointer +0x9c is live but whose m_bIsBCCTargetable +0x1480 is clear, self, not IsAlive slot 0x278, a relation slot 0x650 that is neither 1 D_HT nor 2 D_FR, and a HasEludedMe record 0x102e0210; arbitration seeds bestUnreachable true, bestPriority -1000 and bestDistance 0x10000000, and slot 0x848 is IsUnreachable (slot 530), so a reachable candidate against an unreachable incumbent wins OUTRIGHT with no priority and no distance compare, an unreachable candidate against a reachable incumbent is dropped, and the two matching cases fall to IRelationPriority slot 0x654 strictly greater and then, at equal priority, to the distance between the two slot-0x364 origins with a visibility bool taken from the senses seen set 0x1030fb10 or an FVisible slot-0x324 trace mask 0x2804091, the higher-priority replacement deliberately leaving the incumbent visibility byte in place. CORRECTIONS: the distance key is __ftol of the SUM OF SQUARES - 0x10431320 is plain __ftol, 39 bytes with no callees, and there is NO root; and the decompiled C loses the bestPriority write on the higher-priority arm, which the listing carries at 10274517. Slot 479 IsValidEnemy (vtable +0x77c) gates the outright reachability win, the higher-priority replacement AND the equal-priority replacement, and a candidate that fails it is dropped rather than demoted. ElysiumNpcEnemy.cpp carried the walk, the reachability split, the priority split, the distance and visibility tie-break and the stale-visibility quirk, but had NO FL_NOTARGET gate, NO m_bIsBCCTargetable read and NO IsValidEnemy gate, so the port could commit an enemy retail refuses; it now dispatches this slot. |
| `0x102767d0` | CAI_BaseNPC::DrawDebugTextOverlays | 2872 | 14 | `CAI_BaseHumanoid#124`, `CAI_BaseNPC#124`, `CAI_ExpressiveNPC#124`, +8 more | — | `+0x0224`, `+0x0268`, `+0x029c`, `+0x02e0`, +17 more | 2d/0v/0c | `rule` | FElysiumNpc::BaseDrawDebugTextOverlays | CAI_BaseNPC#124 base body, a distinct retail function beside the Troika override 0x1029d4e0 that owns slot 124 (which calls this first for its starting line index), so it is ported under its own name. Starts from the index CBaseCombatCharacter::DrawDebugTextOverlays returned. Under m_debugOverlays bit 0x80000 it emits three lines and adds 4: "Squad: %c %s" whose letter is O or X chosen by m_iSquadDisconnected < 1 with the squad name off +0x5da4 or the literal none; "Enemy: " completed from slot 0x29c GetEnemy using its +0x26c when non-null else +0x11c, left blank when there is no enemy; and a slot 0x888 line. Under bit 0x1 it emits, in order: one line per CAI_Memory record, walking the list head at slot 0x874 +0xc then +0x38 and resolving each record's handle at +0x24 through the entity table, printed with CBaseEntity::GetDebugName; the active-weapon line built from the two ammo indices at weapon +0x748 and +0x744, each -1 when negative else mapped through 0x103346c0, plus slot 0x570, or a bare line when GetActiveWeapon is null; a slot 0x658 line; a slot 0x65c line taken after 0x1027d990; then when +0x5c38 is non-null a "Schd: %s" line and either "Task: None" or "Task: %s (#%d)" from 0x1028a150 through slot 0x704, or under bit 0x100000 one line per task in the program (array at schedule+0x20, count at schedule+0x24); then the activity line - "Actv: INVALID" when m_Activity or m_IdealActivity is -1, "Actv: RESET" when m_Activity is 0, else "Actv: %s / %s" resolved through slots 0x5dc, 0x5f4 and 0x5e0 then SelectWeightedSequence and GetSequenceActivityName for current and ideal; two further conditional lines gated on two non-zero ints just past the decompiler's CAI_BaseNPC view; an "Enemy too far to attack" line while COND 0x55 stands; and finally the "Vel: %.1f %.1f %.1f Ang: %.1f %.1f %.1f" line, which calls CBaseEntity::CalcAbsoluteVelocity whenever m_iEFlags bit 0xc is set and prints only when +0x3bc/+0x3c0/+0x3c4 or m_vecAngVelocity differ from the static globals at DAT_1070d1b0 and DAT_1070d9d0. The return value is the running line index. No port carrier: slot 124 is the generated stub at ElysiumNpcKernelSlots.cpp:819 and ElysiumNpcKernelDebug2.cpp carries the CBaseEntity, ScriptedTarget and Hint siblings, not this body. |
| `0x10293ec0` | CAI_BaseNPCTroika::FUN_10293ec0 | 138 | 14 | `CAI_BaseNPCTroika#488`, `CNPCMaker#488`, `CNPCMaker_Fleshpile#488`, +52 more | — | — | 0d/0v/2c | `rule` | hand:FElysiumNpc::DeathSound | Slot 488, 138 bytes and the plain shape: a once-flag byte DAT_10923f0d guards the __strcmpi walk of the global VSound concept list DAT_1073dc40 with count DAT_1073dc3c for the concept "Death" (105d8c30), caching the matching entry's first word in DAT_10924d64 or 0xffffffff when absent, then calls the VSound play entry 0x101f5950 on the table object DAT_1073dc28 with that id. CORRECTION: the trailing three arguments are not a priority and a window - 0x101f5950 hands them straight to EmitSound as channel 2 (CHAN_VOICE), volume 1.0 and attenuation 1.25 (0x3fa00000), after picking a wav through 0x101f4600. Ported behind the two VSound seams; the concept list is empty here so the id is retail's own miss and the play entry takes retail's own table-out-of-bounds arm. |
| `0x10293f80` | CAI_BaseNPCTroika::FUN_10293f80 | 138 | 14 | `CAI_BaseNPCTroika#489`, `CNPCMaker#489`, `CNPCMaker_Fleshpile#489`, +52 more | — | — | 0d/0v/2c | `rule` | hand:FElysiumNpc::AlertSound | Slot 489, the plain shape over the concept "Target_Suspect" (105d8c38, UNDERSCORED - read out of the pinned image) through DAT_10924330 and DAT_109241f0, then the play entry with channel 2, volume 1.0 and attenuation 1.25. |
| `0x10294280` | CAI_BaseNPCTroika::FUN_10294280 | 138 | 14 | `CAI_BaseNPCTroika#490`, `CGeneric_NPC#490`, `CNPCMaker#490`, +53 more | — | — | 0d/0v/3c | `rule` | hand:FElysiumNpc::IdleSound | Slot 490, the plain shape over the concept "Idle_Calm" (105d8c60) through DAT_109240c0 and DAT_10924504, channel 2, volume 1.0, attenuation 1.25. Note what is absent: the Troika body carries NO FOkToMakeSound gate - the rate limit is slot 509's roll and the two species overrides that gate carry it themselves. |
| `0x10294340` | CAI_BaseNPCTroika::FUN_10294340 | 138 | 14 | `CAI_BaseNPCTroika#491`, `CNPCMaker#491`, `CNPCMaker_Fleshpile#491`, +51 more | — | — | 0d/0v/2c | `rule` | hand:FElysiumNpc::PainSound | Slot 491, the plain shape over the concept at 105d8c6c, RECOVERED from the pinned image as "Pain", through DAT_1092482c and DAT_1092442c. Species arm CNPC_VWerewolf::vfunc491 0x103d87a0 matches the same string through its own guard DAT_1093d634 and cache DAT_1093d6ec, inside a scope-trace frame whose string is the copy-pasted CNPC_VWerewolf::ExertHvySound (a retail mislabel), and passes attenuation 0 where the base passes 1.25; it does not chain. Ported as an IsRetailClass arm inside the one port method. |
| `0x10294400` | CAI_BaseNPCTroika::FUN_10294400 | 138 | 14 | `CAI_BaseNPCTroika#492`, `CGeneric_NPC#492`, `CNPCMaker#492`, +53 more | — | — | 0d/2v/3c | `rule` | hand:FElysiumNpc::FearSound | Slot 492, the plain shape over the concept "Fear_Start" (105d8c74) through DAT_10924934 and DAT_10924e88, channel 2, volume 1.0, attenuation 1.25. |
| `0x102944c0` | CAI_BaseNPCTroika::FUN_102944c0 | 160 | 14 | `CAI_BaseNPCTroika#493`, `CGeneric_NPC#493`, `CNPCMaker#493`, +53 more | — | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::LostEnemySound | Slot 493, the one hook with a roll in front: RandomInt(0,99) through the engine random vtable +8 must be below 0x19 (25 in 100) or the body returns silently, and the draw happens on EVERY call so the stream advances either way. Past it, the plain shape over the concept "Target_Lost" (105d8c84) through DAT_10923dd4 and DAT_109249c8, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294590` | CAI_BaseNPCTroika::FUN_10294590 | 147 | 14 | `CAI_BaseNPCTroika#494`, `CGeneric_NPC#494`, `CNPCMaker#494`, +53 more | — | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::FoundEnemySound | Slot 494: CBaseCombatCharacter::IsBusyWithDiscipline must be false or the body returns silently; then the plain shape over the concept "Target_Reacquired" (105d8c94) through DAT_109240c8 and DAT_10924a14. Slot 506 speaks the same concept through a different cache, which with a pure lookup is the same answer. |
| `0x10294660` | CAI_BaseNPCTroika::FUN_10294660 | 138 | 14 | `CAI_BaseNPCTroika#495`, `CNPCMaker#495`, `CNPCMaker_Fleshpile#495`, +52 more | — | — | 0d/1v/1c | `rule` | hand:FElysiumNpc::SurprisedSound | Slot 495, the plain shape over the concept "Surprised" (105d8cac) through DAT_10923f0c and DAT_10924f64, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294720` | CAI_BaseNPCTroika::FUN_10294720 | 138 | 14 | `CAI_BaseNPCTroika#496`, `CGeneric_NPC#496`, `CNPCMaker#496`, +54 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::TargetAcquiredSound | Slot 496, the plain shape over the concept "Target_Acquired" (105d8cb8) through DAT_10923dde and DAT_1092423c. Callers are 0d/0v/0c but it fills slot 496 on 57 census classes, so it is reached through the vtable and is not dead. |
| `0x10294870` | CAI_BaseNPCTroika::FUN_10294870 | 138 | 14 | `CAI_BaseNPCTroika#498`, `CGeneric_NPC#498`, `CNPCMaker#498`, +59 more | — | — | 0d/1v/1c | `rule` | hand:FElysiumNpc::FleeSound | Slot 498, the plain shape over the concept at 105d8cd0, RECOVERED from the pinned image as "Flee" - it sits one cell past the three-byte placeholder "???" at 105d8ccc - through DAT_10923dd5 and DAT_10924e84. |
| `0x10294930` | CAI_BaseNPCTroika::FUN_10294930 | 138 | 14 | `CAI_BaseNPCTroika#499`, `CGeneric_NPC#499`, `CNPCMaker#499`, +59 more | — | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::IdleAgitatedSound | Slot 499, the plain shape over the concept "Idle_Agitated" (105d8cd8) through DAT_10923ddd and DAT_10923e30, channel 2, volume 1.0, attenuation 1.25. |
| `0x102949f0` | CAI_BaseNPCTroika::FUN_102949f0 | 138 | 14 | `CAI_BaseNPCTroika#500`, `CGeneric_NPC#500`, `CNPCMaker#500`, +58 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::ExertHvySound | Slot 500, the plain shape over the concept "Exert_Heavy" (1057a1a0, which is NOT in the 105d8c30 block with the other sixteen) through DAT_109241ec and DAT_109240bc. Species arm CNPC_VWerewolf::vfunc500 0x103d8660 matches the same string through its own guard DAT_1093f99c and cache DAT_1093fa30 inside a scope-trace frame and passes attenuation 0 where the base passes 1.25; it does not chain. Ported as an IsRetailClass arm inside the one port method. |
| `0x10294ab0` | CAI_BaseNPCTroika::FUN_10294ab0 | 138 | 14 | `CAI_BaseNPCTroika#501`, `CGeneric_NPC#501`, `CNPCMaker#501`, +59 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::ExertLightSound | Slot 501, the plain shape over the concept "Exert_Light" (1057a1b0) through DAT_10923f0f and DAT_10924838, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294b70` | CAI_BaseNPCTroika::FUN_10294b70 | 138 | 14 | `CAI_BaseNPCTroika#502`, `CGeneric_NPC#502`, `CNPCMaker#502`, +59 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::RiledSound | Slot 502, the plain shape over the concept "Riled" (105d8ce8) through DAT_10924aac and DAT_10924244, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294c30` | CAI_BaseNPCTroika::FUN_10294c30 | 138 | 14 | `CAI_BaseNPCTroika#503`, `CGeneric_NPC#503`, `CNPCMaker#503`, +59 more | — | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::ComfortSound | Slot 503, the plain shape over the concept "Comfort" (105d8cf0) through DAT_1092497c and DAT_10924624, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294cf0` | CAI_BaseNPCTroika::FUN_10294cf0 | 138 | 14 | `CAI_BaseNPCTroika#504`, `CGeneric_NPC#504`, `CNPCMaker#504`, +59 more | — | — | 0d/0v/1c | `rule` | hand:FElysiumNpc::UpsetSound | Slot 504, the plain shape over the concept "Upset" (105d8cfc) through DAT_10924242 and DAT_10924fb4, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294db0` | CAI_BaseNPCTroika::FUN_10294db0 | 138 | 14 | `CAI_BaseNPCTroika#505`, `CGeneric_NPC#505`, `CNPCMaker#505`, +59 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::TargetGiveUpSound | Slot 505, the plain shape over the concept "Target_GiveUp" (105d8d04) through DAT_10923dd6 and DAT_10923d7c, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294e70` | CAI_BaseNPCTroika::FUN_10294e70 | 147 | 14 | `CAI_BaseNPCTroika#506`, `CGeneric_NPC#506`, `CNPCMaker#506`, +59 more | — | — | 0d/0v/0c | `rule` | hand:FElysiumNpc::Slot506 | Slot 506, byte-for-byte slot 494's shape: IsBusyWithDiscipline must be false, then the concept "Target_Reacquired" (the same string 105d8c94 slot 494 uses) through its own guard DAT_10924240 and cache DAT_10924830, channel 2, volume 1.0, attenuation 1.25. |
| `0x10294f40` | CAI_BaseNPCTroika::FUN_10294f40 | 302 | 14 | `CAI_BaseNPCTroika#507`, `CGeneric_NPC#507`, `CNPCMaker#507`, +59 more | `+0x10ec` | `+0x0128` | 0d/1v/2c | `rule` | hand:FElysiumNpc::FloatSound | Slot 507, the only hook with a computed fifth argument and a state write. Concept "Float" (105d8d14) behind bit 0 of DAT_1092488d into DAT_10924f68. The fifth argument comes from m_iDialog +0x0128: the term is 0xe when a dialogue name is set and 0 otherwise, the constant arm (term plus 0x42 at or below 0x32, the double 4.0 at 10449148) is unreachable for both values, so it is the INTEGER quotient 0x14 divided by (term plus 0x10) - 1.0 when idle and 0.0 while in dialogue - and it is EmitSound's attenuation, not a window maximum. CORRECTION: the re-arm term is Float_Sound_Info row 1 (1029502c pushes 1), which the authored table names FloatSoundMinDelay and sets to 5.0, truncated to an int by __ftol and added to the engine clock with FIADD, an INTEGER add; the earlier walk named neither the row nor the truncation. m_flNextFloatSoundTime +0x10ec is the write, and this is its first writer in the port - family Sounds' BaseShouldPlayFloatSound reads it. |
| `0x10298ad0` | CAI_BaseNPCTroika::Precache | 413 | 14 | `CAI_BaseNPCTroika#104`, `CNPC_ProneDialog#104`, `CNPC_VAnimal#104`, +34 more | `+0x64e8` | `+0x0024`, `+0x0128`, `+0x0350`, `+0x0980`, +1 more | 20d/1v/11c | `rule` | hand:FElysiumNpc::Precache | slot 104, Troika Precache in retail order: when the model keyfield (slot 0x24) is empty call SetModelName (slot 0x350) with "models/error/error.mdl"; PrecacheModel the keyfield through the engine (DAT_1070b22c+0x34) and hand the index to slot 0x28; when m_altEquipment (+0x1a98) is neither the string "0" at DAT_105399a0 nor the 15-byte "item_w_unarmed" UTIL_PrecacheOther it (0x101d0ec0); chain CAI_BaseNPC::Precache 0x1027bb50 (a direct thiscall with NO argument); when m_iDialog (+0x0128) is set Q_snprintf "sound/character/%s" into a 0x104 buffer, chop FOUR characters not five (buffer[strlen-4]=0, read off 10298bf8..10298c04), Q_strnlwr it over the NEW length and glob-precache that directory twice through 0x101d0f10 with the FOUR arguments (dir, ext, 1, 0) - ".wav" DAT_10598a30 then ".mp3" DAT_10548ed4, the third argument selecting the "*%s/%s" name format over dir+6; write +0x64e8 m_iDispositionModelIndex with the CDispositionTable row index built by 0x100ec640 (per stance: 3 idles, 3 fidgets, 3x3 transitions, a missing idle falling back to idle 1), which this port answers with EnsureStanceResolved; finally slot 608 SetAttackCoordinator with "Normal". Ported with its 23 species arms as story 29d family Precache10; the arm table is keyed on the override row's retail address so the three Chang forms and the two camera forms are one arm apiece. |
| `0x10298ce0` | CAI_BaseNPCTroika::SetModel | 50 | 14 | `CAI_BaseNPCTroika#105`, `CGeneric_NPC#105`, `CNPCMaker#105`, +59 more | `+0x64e8` | — | 2d/8v/15c | `rule` | hand:FElysiumNpc::SetModel | slot 105, 50 bytes, and the ORDER of its four calls is the whole body because each reads what the one before it wrote: CBaseCombatCharacter::SetModel, then SetHullSizeNormal(force=1) 0x10273070 (family Motor10's ported body, called not re-read), then SetDefaultEyeOffset 0x10274ca0 (family BaseHelpers'), then +0x64e8 from the disposition stance resolver 0x100ec640, which family Precache10 already reaches from slot 104 through EnsureStanceResolved so the two writers of that word cannot drift. The species prologue keys on the slot-105 override row's ADDRESS: CAI_BaseHumanoid 0x1025e510, CNPC_VGhoulCroucher 0x1037b1f0 and CNPC_VZombie 0x103e0540, the last two sharing one 119-byte body. |
| `0x10299da0` | CAI_BaseNPCTroika::IRelationType | 541 | 14 | `CAI_BaseNPCTroika#404`, `CGeneric_NPC#404`, `CNPCMaker#404`, +53 more | — | `+0x02a0`, `+0x0650`, `+0x628c`, `+0x647c` | 5d/9v/22c | `rule` | hand:FElysiumNpc::IRelationType | slot 404, 541 bytes. Self and null both answer D_ER first. Arm A: the candidate's +0x9c NPC carrying D_INSANE (flags2 0x20000) and m_hClosestPlayer resolving answers D_HT when slot 404 on that player (VIRTUAL, so a species arm re-enters) is D_HT or when slot 168 GetEnemy IS that player. Arm B: the candidate's +0x98 combat character's +0x647c follower boss, same pair. Arm C: my own +0x647c boss taken through its +0x9c; no boss chains CBaseCombatCharacter::IRelationType; boss == candidate answers D_LI; otherwise the boss's slot 404 toward the candidate, overridden to D_HT by the boss's slot 167 (CONST GetEnemy, not the 168 the earlier arms use) being the candidate, or by the candidate NPC's own slot 404 toward the boss or its slot 168. CORRECTION: 10299f84 is MOV EDI,EAX, so the fall-through answer is reassigned - for an NPC candidate the body returns the CANDIDATE's relation toward the boss, not the boss's toward the candidate as the checklist walk states. |
| `0x1029fbe0` | CAI_BaseNPCTroika::FUN_1029fbe0 | 205 | 14 | `CAI_BaseNPCTroika#330`, `CGeneric_NPC#330`, `CNPCMaker#330`, +58 more | — | `+0x0364` | 0d/0v/0c | `rule` | hand:FElysiumNpc::Slot330 | slot 330 void(float, FireBulletsInfo_t*), the near-miss flinch. delta = slot 217 GetAbsOrigin minus info m_vecSrc (+0x8/+0xc/+0x10) and its LENGTH comes out of 0x10137220, which family Senses10 recovered as VectorNormalize - a body that ANSWERS the length while normalising in place - and retail hands the UN-normalised delta to the reaction because the normalise writes through ECX into a second copy. The whole body is gated on info+0x98 (the firing weapon), info+0x94 (the attacker) and attacker+0x9c all non-null, in that order; then the weapon's CVDmg_t row 0x102517e0 gives two distance bands and a length below row+0x404 plays reaction kind 2, else below row+0x408 kind 0, both through 0x10344f80 whose three refusal gates are exactly ElysiumReactions IsKnockbackAllowed's. Beyond row+0x408 nothing happens. |
| `0x1029fec0` | CAI_BaseNPCTroika::FUN_1029fec0 | 81 | 14 | `CAI_BaseNPCTroika#326`, `CGeneric_NPC#326`, `CNPCMaker#326`, +61 more | — | `+0x6064` | 0d/0v/0c | `rule` | hand:FElysiumNpc::Slot326 | slot 326 bool(swingRecord, CBaseCombatCharacter*). The Troika arm can only ever NARROW the base 0x103482e0's answer: a false passes straight through, and a true becomes false unless m_iHitBuildupCount +0x6064 is at or below the int of the cvar DAT_109245e4 (+0x2c, 0 when defaulted) OR the SWING RECORD's byte +0xba is 2. +0xba is param_1's, the first argument, not the attacker's - the port reads FElysiumSwingRecord Ba, which ElysiumReactions KnockbackUnconditionalMarker already names as the 2. |
| `0x102b35b0` | CAI_BaseNPCTroika::QueryHearSound | 605 | 14 | `CAI_BaseNPCTroika#467`, `CGeneric_NPC#467`, `CNPCMaker#467`, +61 more | — | `+0x0310`, `+0x0770`, `+0x14b8`, `+0x5b84`, +1 more | 0d/0v/0c | `rule` | hand:FElysiumNpc::QueryHearSound | slot 467, refusals in retail order: NULL sound; cvar DAT_10924fba (ignore senses); cvar DAT_10924fb9 with the sound owner carrying a player (+0xa8); m_bfNPCFrenziedFlags (+0x5b84) bit 0x800 with the owner == m_hFriendPlayer (+0x60ac); owner == this; an owner that resolves to a live entity whose +0x9c combat char is NULL or which refuses 0x10146b20(cc, this), so a sound made by a live non-combat-character owner is not heard at all while a sound with NO owner handle skips the whole block; sound type (+0x4) == 4 with CStealthKillRules::InDeafZone(&DAT_1072c540, owner's player, this); then the distance test runs ONLY when m_bfAINPCFlags (+0x14b8) carries 0x20400 (COWERING 0x400 ¦ SLEEPING 0x20000) - distance from the ear position (slot 0x310) to the sound origin (+0x20/+0x24/+0x28) against slot 0x770 HearingSensitivity times the sound's INT volume (+0x8) times _DAT_1044bef8 (0.25), with CBaseEntity::AdjustSoundDistForStealth subtracting the owner's slot-30 reduction from that THRESHOLD and clamping at zero, and dist <= threshold hears - and everything else answers true. The ordinary radius test is NOT in this slot: it is CanHearSound 0x1030f7b0, the Listen-level cull that dispatches this slot, whose threshold is the same product without the 0.25. |
| `0x102b4760` | CAI_BaseNPCTroika::FUN_102b4760 | 668 | 14 | `CAI_BaseNPCTroika#594`, `CGeneric_NPC#594`, `CNPCMaker#594`, +61 more | `+0x6081` | `+0x0224`, `+0x0270`, `+0x0304`, `+0x0370`, +4 more | 0d/1v/1c | `rule` | hand:FElysiumNpc::Slot594 | slot 594: write m_bSeenInOuterBand +0x6081 = 0 FIRST, then read my eye point (slot 0x304) and the target's (slot 0x304); the range block runs only when (m_NPCState != 2 OR m_bEnemyWentOccluded +0x5bc5) AND m_flStealthVisionOverrideTime +0x6604 <= curtime - the 3D distance is compared against the TARGET's slot 28 (vt+0x70, its own stealth vision scalar) times m_flSeekDistInspection +0x63b8, beyond which it writes *out = 0 (only when a cell was passed) and returns false, and beyond _DAT_10457f54 (0.7) times that same product it instead sets the far byte +0x6081 = 1 and carries on; then when the target has a combat character (+0x9c) the concealment test 0x10146b20 refuses with *out = 0 and (only when m_debugOverlays < 0 and the target has a player) a +-2 debug box from the collision bounds; the success exit draws a +-3 box and returns true. CORRECTION: 102b479b reads [ESI+0x5bc5], which the shape map names m_bEnemyWentOccluded - the occlusion EDGE 0x10270180 writes - and NOT the ten-failure debounce the port's IsVisible was reading. This slot is the ONE writer of +0x6081, which slot 472 OnSeeEntity and both of its species arms read. |
| `0x102c0ca0` | CAI_BaseNPCTroika::FinishTalking | 573 | 14 | — | `+0x64c0`, `+0x64cc`, `+0x6554` | `+0x026c`, `+0x64c0`, `+0x64ec`, `+0x6554` | 3d/0v/0c +4 outside | `rule` | FElysiumNpc::FinishTalking | Scope-traced; LATCHES m_bIsTalking +0x64c0 at 102c0d12 before anything is written, then, if m_hDialogScene +0x6554 resolves: the partner's +0x498 byte clear takes UTIL_Remove (0x101cd940), set takes partner slot 0x3d4, ThinkSet(partner, 0x101c0b10, 0.0) and partner m_flNextThink +0x17c = curtime + _DAT_104493d0, which the pinned image reads as the DOUBLE 0.1 (102c0e29 FADD qword). Then unconditionally +0x6554 = -1, m_szDialogQue[0] at +0x64ec = 0, m_bIsTalking = 0, and +0x64cc = curtime - a STAMP, not a clear - followed by ResetScriptedSoundOverrideEnt. The LATCHED byte then picks the notify on the dialog singleton after UTIL_PlayerByIndex(1): clear takes CDialog::CallPendingNPCEventScript, set takes CDialog::NPCNotifyDoneTalking. The stamp write is why the port had to split m_bIsTalking from TalkingUntil; the shape map now binds +0x64c0 to the new bIsTalking byte. SaveRestore10's StopDialogOnRemove now calls this body instead of standing in for it. |
| `0x102d72f0` | CAI_TestHull::Spawn ‼ | 363 | 14 | `CAI_TestHull#103` | `+0x0210`, `+0x1568` | `+0x0108`, `+0x0174`, `+0x026c`, `+0x02b4` | 0d/0v/0c | `rule` | FElysiumNpc::TestHullSpawn | PORTED 29d Motor10 (listing). CORRECTION: the used-mask test is SIGNED - TEST EBX,EBX; JLE 0x102d7330 - so a zero OR NEGATIVE mask short-circuits to hull 0 WITHOUT the 22-miss fallback call, and the shipped .data initialiser for DAT_10610be8 is 0xffffffff which is exactly such a value. Otherwise m_eHull +0x1568 is the FIRST index 0..21 whose NAI_Hull::Bits (0x102d6210) intersects the mask, and 22 misses call AddUsedHullBits(0) - a no-op OR - before answering 0. Then 0x10273070(this, 0), SetSolid(SOLID_BBOX = 2) on m_Collision +0x270 under a CBaseEntity::SetSolid scope frame, AddSolidFlags(zero-extended word[+0x2b4] ¦ 4 FSOLID_NOT_SOLID) under a CBaseEntity::AddSolidFlags frame, slot 93 SetMoveType(MOVETYPE_FLY = 4, 0), m_iHealth +0x210 = 0x32 = 50, AddFlag(0x40000), byte [+0x5f44] = 0, and a TAIL JMP to slot 66 Hide so Hide's answer is Spawn's. DAT_10610be8's two writers are 0x102f9900 (clear it and DAT_1093412c) and 0x102f9920 (OR bits in), both ported. What +0x5f44 is at byte granularity stays unrecovered: the shape map binds that offset as an output block. |
| `0x1033b5f0` | CAI_BaseNPC::FUN_1033b5f0 | 354 | 14 | `CAI_BaseHumanoid#357`, `CAI_BaseNPC#357`, `CAI_BaseNPCTroika#357`, +74 more | `+0x1490`, `+0x1494` | `+0x0598`, `+0x0fec`, `+0x13bc`, `+0x13c0`, +2 more | 0d/0v/0c +1 outside | `rule` | hand:FElysiumNpc::Slot357 | Ported as slot 357. Two gates, both required: m_Activity +0x0fec equal to 0x132 and m_flNextFeedPulse +0x1490 below curtime. Below them: the type-0 list GetValue(stat 0x0e FaithPoints) against the stat DEFINITION maximum (0x10200370 over DAT_1074e658, then 0x10205840 on its +0x60) - strictly below it IncBase(0x0e), at or above it dispatch vt+0x598, which is slot 358 PrayerEnd 0x1033b890; then the cadence tail on EVERY pulse, m_flNextFeedPulse = curtime + m_flNextFeedDuration +0x1494, and while that duration is above _DAT_1047b868 it is reduced by _DAT_1049e890. RECOVERED out of the pinned image: BOTH cells are DOUBLES (the decompiler shows a float cast), reading 0.3 and 0.15. +0x1490 and +0x1494 are the FEED cadence pair; retail reuses them for the prayer. The activity gate reads the family Hints CurrentRetailActivityId seam, which answers -1, so the slot refuses - the retail answer for a body that is not praying. Walked into docs/vtmb/combat-and-damage.md. |
| `0x1034b160` | CNPCMaker::Precache | 468 | 14 | `CNPCMaker#104` | — | `+0x0024`, `+0x0270`, `+0x036c`, `+0x0370`, +1 more | 0d/1v/11c | `rule` | FElysiumNpcMaker::Precache | CNPCMaker slot 104, two symmetric arms: a NON-empty model keyfield (slot 0x24) -> PrecacheModel through the engine, chain CAI_BaseNPC::Precache 0x1027bb50, and UTIL_PrecacheOther m_iszNPCClassname (+0x665c) and return - an EMPTY classname instead Warns "%s at (%0.f %0.f %0.f) missing NPC" with GetDebugName and slot 0x370's origin, UTIL_Removes this, and then (only when the DAT_1070af4c cvar's vtable+4 test is false and its +0x2c >= 1) draws a "%s: BAD NPC Classname" overlay box at the collision mins/maxs; an EMPTY model keyfield takes the same shape with "missing model" and "%s: BAD MODEL NAME"; both error arms return early when the developer gate refuses ARGUED: the port models makers as a SEPARATE type - FElysiumNpcMaker final : public FElysiumEntity (ElysiumNpcMaker.h:9), registered for npc_maker, npc_maker_fleshpile and npc_maker_zombie in ElysiumNpcClasses.cpp:42 - and the overlay already lands all three maker slot-103 bodies (0x1034afe0, 0x1034c020, 0x1034cc60) on the ONE method FElysiumNpcMaker::Spawn. classes.md does stand CNPCMaker as a CAI_BaseNPCTroika with 621 slots, so slot 104 IS the NPC Precache virtual, but an FElysiumNpc arm would never run: no maker is an FElysiumNpc in this port. These are therefore NOT arms of FElysiumNpc::Precache but the three arms of one FElysiumNpcMaker::Precache, keyed on the maker's own classname exactly as FElysiumNpcMaker::IsFleshpileMaker already keys slots 617 and 139. The base CNPCMaker arm is the one that checks BOTH keyfields and draws the two developer overlay boxes, which the other two arms drop. |
| `0x1034c180` | CNPCMaker_Fleshpile::Precache | 169 | 14 | `CNPCMaker_Fleshpile#104` | — | `+0x0024`, `+0x0370`, `+0x665c` | 0d/1v/11c | `rule` | FElysiumNpcMaker::Precache | CNPCMaker_Fleshpile slot 104: a non-empty model keyfield (slot 0x24) -> PrecacheModel through the engine (DAT_1070b22c+0x34), chain CAI_BaseNPC::Precache 0x1027bb50, UTIL_PrecacheOther m_iszNPCClassname (+0x665c, "" when null) and return; an empty keyfield -> Warning "%s at (%0.f %0.f %0.f) missing model" built from GetDebugName and slot 0x370's origin, then UTIL_Remove(this); unlike CNPCMaker's it never checks the classname for emptiness and never draws the developer overlay box ARGUED: the port models makers as a SEPARATE type - FElysiumNpcMaker final : public FElysiumEntity (ElysiumNpcMaker.h:9), registered for npc_maker, npc_maker_fleshpile and npc_maker_zombie in ElysiumNpcClasses.cpp:42 - and the overlay already lands all three maker slot-103 bodies (0x1034afe0, 0x1034c020, 0x1034cc60) on the ONE method FElysiumNpcMaker::Spawn. classes.md does stand CNPCMaker as a CAI_BaseNPCTroika with 621 slots, so slot 104 IS the NPC Precache virtual, but an FElysiumNpc arm would never run: no maker is an FElysiumNpc in this port. These are therefore NOT arms of FElysiumNpc::Precache but the three arms of one FElysiumNpcMaker::Precache, keyed on the maker's own classname exactly as FElysiumNpcMaker::IsFleshpileMaker already keys slots 617 and 139. Relative to the base CNPCMaker arm the CNPCMaker_Fleshpile arm DROPS the empty-classname check and the developer overlay box entirely, keeping only the missing-model Warning and UTIL_Remove. |
| `0x1034cde0` | CNPCMaker_Zombie::Precache | 199 | 14 | `CNPCMaker_Zombie#104` | `+0x1a98`, `+0x5dec` | `+0x0024`, `+0x0370`, `+0x665c` | 0d/1v/11c | `rule` | FElysiumNpcMaker::Precache | CNPCMaker_Zombie slot 104: the Fleshpile shape plus three zombie facts - BEFORE chaining CAI_BaseNPC::Precache it zeroes m_altEquipment (+0x5dec) and m_spawnEquipment (+0x1a98), so an authored equipment keyfield is discarded, and AFTER UTIL_PrecacheOther(m_iszNPCClassname +0x665c) it also UTIL_PrecacheOthers the literal "item_w_zombie_fists"; an empty model keyfield takes the "missing model" Warning from GetDebugName and slot 0x370 then UTIL_Remove(this) ARGUED: the port models makers as a SEPARATE type - FElysiumNpcMaker final : public FElysiumEntity (ElysiumNpcMaker.h:9), registered for npc_maker, npc_maker_fleshpile and npc_maker_zombie in ElysiumNpcClasses.cpp:42 - and the overlay already lands all three maker slot-103 bodies (0x1034afe0, 0x1034c020, 0x1034cc60) on the ONE method FElysiumNpcMaker::Spawn. classes.md does stand CNPCMaker as a CAI_BaseNPCTroika with 621 slots, so slot 104 IS the NPC Precache virtual, but an FElysiumNpc arm would never run: no maker is an FElysiumNpc in this port. These are therefore NOT arms of FElysiumNpc::Precache but the three arms of one FElysiumNpcMaker::Precache, keyed on the maker's own classname exactly as FElysiumNpcMaker::IsFleshpileMaker already keys slots 617 and 139. Relative to the CNPCMaker_Fleshpile arm the CNPCMaker_Zombie arm ADDS two writes and one precache - m_altEquipment +0x5dec and m_spawnEquipment +0x1a98 are zeroed BEFORE the base chain, discarding an authored equipment keyfield, and item_w_zombie_fists is precached after the classname. |
| `0x103567e0` | CNPC_Bullseye::Spawn | 662 | 14 | `CNPC_Bullseye#103` | `+0x017c`, `+0x019c`, `+0x01fc`, `+0x03ec`, +1 more | `+0x0174`, `+0x019c`, `+0x01a0`, `+0x0204`, +3 more | 0d/0v/0c | `rule` | FElysiumNpc::BullseyeSpawn | CNPC_Bullseye slot 103 in order: Precache (vt+0x1a0); SetSize(-16..16) via 0x101cf390; slot 0x174 (0,0); SetBloodColor(0xf7); m_fEffects +0x17c = 0; m_flFieldOfView +0x19c = 0.5; m_flGravity +0x1fc = 0; SetBloodColor AGAIN with 0xf7 under spawnflag 0x80000 and -1 otherwise - the second call decides; AddFlag(0x2000); ThinkSet(LAB_100097fa, 0.0) with m_flNextThink = curtime + _DAT_104493d0, RECOVERED as a DOUBLE 0.1 out of the pinned image; SetSolid(2) and AddSolidFlags(or 0x10), plus or 4 only under spawnflag 0x10000; m_takedamage +0x3ec = 0 under spawnflag 0x20000 else 2; Relink; m_fEffects gains 0x40; PhysicsCheckWater; AddFlag2(0x10). CNPC_Bullseye carries no census classname so the arm is unreachable at runtime; driven by SetRetailClassForTests. |
| `0x10358ec0` | CNPC_Crow::Precache | 24 | 14 | `CNPC_Crow#104` | — | — | 0d/1v/11c | `rule` | FElysiumNpc::Precache | CNPC_Crow slot 104, 24 bytes and two calls whose ORDER is inverted from every other precache in this band: chain CAI_BaseNPC::Precache 0x1027bb50 FIRST, then PrecacheModel("models/crow.mdl") through the engine (DAT_1070b22c+0x34); no model keyfield is read, no fallback model is set and no sound is precached, so a crow's model is hard-coded and cannot be overridden by the map CONVENTION: species arm of slot 104's FElysiumNpc::Precache keyed on IsRetailClass(CNPC_Crow), which classes.md stands as a CAI_BaseNPC with entity classname npc_crow and ElysiumNpcKernelShape.cpp:4349 carries with CNPC_Crow_Classnames, so the lookup keys on it today; relative to the base the crow arm chains CAI_BaseNPC::Precache 0x1027bb50 FIRST rather than last and then hard-codes models/crow.mdl, reading no model keyfield at all. |
| `0x1035ade0` | CGeneric_NPC_bathack::Precache | 114 | 14 | `CGeneric_NPC_bathack#104` | — | — | 0d/1v/11c | `rule` | FElysiumNpc::Precache | CGeneric_NPC_bathack slot 104: PrecacheModel("models/bats.mdl") hard-coded, then PrecacheScriptSound (via *DAT_1070b248) over its OWN pointer table - npc/metropolice/alert1.wav (0x10629d80), surprise1.wav (0x10629d84), die1.wav (0x10629d88) - then a fixed 4-entry loop (offsets 0 to 0x10 step 4) over PTR_s_npc_citizen_pain1_wav at 0x10629d90, and only then chain CAI_BaseNPC::Precache 0x1027bb50; no model keyfield and no fallback, and the table is a separate copy of the same four names CGeneric_NPC and CGenericSabbat_NPC keep ARGUED: CGeneric_NPC_bathack is in the 77-class tree (classes.md, CAI_BaseNPC, 583 slots) AND carries the entity classname npc_generic_bathack (CGeneric_NPC_bathack_Classnames in ElysiumNpcKernelShape.cpp), so RetailClass() reaches it and the lookup keys on it exactly as on a CNPC_V*; the arm goes on slot 104's FElysiumNpc::Precache. Relative to the base it chains CAI_BaseNPC::Precache 0x1027bb50 LAST and hard-codes models/bats.mdl with its own private copy of the four metropolice and citizen sound tables, reading no model keyfield. |
| `0x1035b5d0` | CGenericSabbat_NPC::Precache | 233 | 14 | `CGenericSabbat_NPC#104` | — | `+0x0024`, `+0x0350` | 0d/1v/11c | `rule` | FElysiumNpc::Precache | CGenericSabbat_NPC slot 104: 0x10207e60 first, then when the model keyfield (slot 0x24) is empty SetModelName (slot 0x350) with the sabbat model at PTR 0x1062a000; PrecacheModel the keyfield through the engine; PrecacheScriptSound alert1/surprise1/die1 from its own table at 0x1062a004/0x1062a008/0x1062a00c and a fixed 4-entry loop (0 to 0x10 step 4) over the citizen pain table at 0x1062a014; chain CAI_BaseNPC::Precache 0x1027bb50 last; ElysiumNpcKernelSounds.cpp:200 already carries those four wav names as species DATA, but nothing in the port runs the empty-keyfield model fallback or this call order ARGUED: CGenericSabbat_NPC is in the 77-class tree (classes.md, CAI_BaseNPC, 583 slots) with entity classname npc_sabbat (CGenericSabbat_NPC_Classnames), so the lookup keys on it and the arm belongs on slot 104's FElysiumNpc::Precache. Relative to the base it ADDS the empty-keyfield SetModelName fallback to the sabbat model and its own copy of the alert, surprise, die and four pain tables, and chains CAI_BaseNPC::Precache 0x1027bb50 LAST. |
| `0x10369ff0` | CNPC_VCameraSecurity::FVisible | 44 | 14 | `CNPC_VCameraSecurity#201` | — | — | 0d/0v/7c | `rule` | FElysiumNpc::FVisible | CNPC_VCameraSecurity slot 201: resolve the linked CSecCamera (0x10369e70, walked at ElysiumNpcKernelDialogue.cpp:620 - name lookup, RTTI cast, the one-way +0x6668 latch that UTIL_Removes the NPC when a bound camera dies) and answer 1 only when the candidate carries a player (+0xa8) AND the link is non-null AND 0x1020cd00 is true - the camera's own sight test: the enabled byte at +0x7d8, then 0x1020cd30's 2D distance from camera to player compared against the camera's far radius (+0x794) and near radius (+0x790), its cone test 0x1020cc60 and finally a 0x4091 trace whose fraction must equal _DAT_10449280; every other case is 0, and the NPC's own eyes are never consulted CONVENTION: species arm of slot 201's FElysiumNpc::FVisible keyed on IsRetailClass(CNPC_VCameraSecurity); it REPLACES the Troika base 0x102b4630 outright - the NPC's own eyes are never consulted and the answer is the linked CSecCamera's enabled byte, far and near radius, cone test and 0x4091 trace. |
| `0x103c3cd0` | CNPC_VTzimisceRunner::NotifyChangeSizeSmall | 48 | 14 | `CNPC_VTzimisceRunner#335` | `+0x5f2c`, `+0x6672`, `+0x6674` | — | 0d/0v/0c | `rule` | FElysiumNpc::TzimisceRunnerNotifyChangeSizeSmall | CNPC_VTzimisceRunner slot 335, four writes in order: SetHullSizeSmall(force=1) via 0x10273180; the form byte +0x6672 = 1, which PreTranslate_TzimisceRunner 0x103c3e10 selects its four TZ activity variants on; m_bWantsLargeHull +0x5f2c = 0; and +0x6674 = the float the engine interface DAT_1070b22c returns from vtable +0x1dc called with 0. The base body 0x103415f0 is a pure no-op, so this arm adds the whole behaviour. NOT WIRED to slot 335: 0x103415f0 is a layer-0 row of band 0-9 with no verdict and merge_verdicts --band 10-18 refuses an out-of-band address, so the body lands under its recovered species name and the gap is reported. |
| `0x103c3d10` | CNPC_VTzimisceRunner::NotifyChangeSizeNormal | 71 | 14 | `CNPC_VTzimisceRunner#336` | `+0x5f2c`, `+0x6672` | `+0x6674` | 0d/0v/0c | `rule` | FElysiumNpc::TzimisceRunnerNotifyChangeSizeNormal | CNPC_VTzimisceRunner slot 336, the inverse and its guard: re-read the engine interface DAT_1070b22c vtable +0x1dc with 0 and compare against the value slot 335 cached at +0x6674 - ONLY when the two differ does it SetHullSizeNormal(force=1) via 0x10273070, clear the form byte +0x6672 and set m_bWantsLargeHull +0x5f2c = 1; an unchanged token leaves the runner small and the form byte set, so the restore is edge-triggered on the engine value, not on a request. The base 0x10341680 is the same pure no-op and takes an int argument this override ignores. NOT WIRED to slot 336 for the same band reason as 0x103c3cd0. |
| `0x103cb920` | CNPC_VWerewolf::CheckStuck | 1558 | 14 | — | — | `+0x026c`, `+0x028c`, `+0x0300`, `+0x0304`, +7 more | 2d/0v/0c | `rule` | FElysiumNpc::WerewolfCheckStuck | scope-traced and gated on slot 0x28c; probe 1 is a hull trace from GetAbsOrigin (slot 0x364) to origin + _DAT_10452dc4 in Z with mask 0x202400b, using the m_eHull (+0x1568) normal mins/maxs (0x102d6100/0x102d6120) and the navigator's (+0x5d40) filter, inside a CVProfile "CAI_MoveProbe::TraceHull" scope; CLEAR -> re-probe through the navigator (0x102a99e0) using the SMALL hull (0x102d6140/0x102d6160) when +0x5f2d is set and the third extent scaled by _DAT_10449154, and only when the fraction is below _DAT_104454c0 or either start-solid byte is set DevWarning "attempting alt unstuck..." and slot 0x360; BLOCKED -> up to three escalating world traces (0x1006dec0 with the 0x101d3190(this, 7) filter) reporting "Werewolf stuck!", then "Werewolf unstuck..." with slot 0x360 on the second, and on the third either 0x10269aa0(this, 0x77) succeeding -> "Werewolf teleported out from stuck", TeleportOut and slot 0x700 with "Werewolf stuck", or "Werewolf STUCK!!" and nothing; every exit but the teleport ends in SetHullSizeSmall(1) |
| `0x103ce750` | CNPC_VWerewolf::TaskFail | 479 | 14 | `CNPC_VWerewolf#448` | `+0x66a1`, `+0x66a4`, `+0x66c4`, `+0x66e8`, +2 more | `+0x026c`, `+0x0704`, `+0x5c38`, `+0x66b0`, +2 more | 0d/3v/43c | `rule` | FElysiumNpc::WerewolfTaskFail | CNPC_VWerewolf slot 448, joined to family Conditions10's seven-body slot-448 prologue as the eighth. The DIAGNOSTIC half runs only when the running schedule +0x5c38 equals one of 0x160, 0x161 or 0x162 resolved through 0x102cc1f0 - DevWarning Werewolf failed task with the task name from slot 0x704, the schedule name at schedule+0x40 and the failure text from 0x10316fa0, then Current MOVE Hint for m_pMoveHint, Current TELEPORT Hint for m_pTeleportHint and Current BREAK Hint, which is GATED on m_pBreakHint but PRINTS the TELEPORT hint - a retail copy-paste bug, reproduced. Then unconditionally SetHullSizeSmall(1), ClearMoveHint, ClearTeleportHint, m_pBreakHint +0x66c4 = 0, +0x66a4 = 0, +0x66a1 = 1, the Troika base 0x1029adb0, CheckStuck(NULL), +0x66e8 = 0, +0x6708 = -1, +0x670c = -1. OFFSETS CORRECTED from the datamap: +0x66b0 is m_pTeleportHint and +0x66bc is m_pMoveHint, which the earlier walk has swapped; also +0x66b4 m_pLastUsedTeleportHint, +0x66c0 m_pLastUsedMoveHint, +0x66c4 m_pBreakHint. |
| `0x103d8660` | CNPC_VWerewolf::vfunc500 | 245 | 14 | `CNPC_VWerewolf#500` | — | `+0x026c` | 0d/0v/0c | `rule` | FElysiumNpc::ExertHvySound | CNPC_VWerewolf slot 500, scope-traced as CNPC_VWerewolf::ExertHvySound: a one-shot lazy index lookup guarded by DAT_1093f99c bit 0 - walk the global sound-script table (DAT_1073dc40, count DAT_1073dc3c) comparing each entry's +0x4 name with __strcmpi against "Exert_Heavy", cache the matching script's first word in DAT_1093fa30 and store -1 when no entry matches - then EmitSound(&DAT_1073dc28, this, DAT_1093fa30, channel 2, volume 1.0, 0) on every call including the -1 one; the port's species sound table (ElysiumNpcKernelSounds.cpp) carries no CNPC_VWerewolf row CONVENTION: species arm of slot 500's FElysiumNpc::ExertHvySound keyed on IsRetailClass(CNPC_VWerewolf); against the Troika base 0x102949f0 the arm keeps the same script name Exert_Heavy and the same one-shot table walk but uses its OWN guard DAT_1093f99c and cache DAT_1093fa30, so a werewolf's lookup is independent of the base's, adds a scope-trace frame the base has none of, and passes 0 as EmitSound's last argument where the base passes 0x3fa00000. |
| `0x103d87a0` | CNPC_VWerewolf::vfunc491 | 245 | 14 | `CNPC_VWerewolf#491` | — | `+0x026c` | 0d/0v/2c | `rule` | FElysiumNpc::PainSound | CNPC_VWerewolf slot 491 (PainSound): the identical one-shot pattern with its own guard DAT_1093d634 and its own cache DAT_1093d6ec, matching the script name at DAT_105d8c6c - the same string CAI_BaseNPCTroika::PainSound 0x10294340 uses - over the global script table DAT_1073dc40/DAT_1073dc3c, then EmitSound(&DAT_1073dc28, this, DAT_1093d6ec, channel 2, volume 1.0, 0); its scope-trace string is the copy-pasted "CNPC_VWerewolf::ExertHvySound", a retail mislabel that a debug dump reproduces CONVENTION: species arm of slot 491's FElysiumNpc::PainSound keyed on IsRetailClass(CNPC_VWerewolf); against the Troika base 0x10294340 it matches the SAME script name at DAT_105d8c6c but through its own guard DAT_1093d634 and cache DAT_1093d6ec, drops the base's int* parameter, and pushes 0 as EmitSound's last argument where the base pushes 0x3fa00000; its scope-trace string is the copy-pasted CNPC_VWerewolf::ExertHvySound, a retail mislabel to reproduce. |
| `0x103d9f90` | CNPC_VWerewolf::SnapToAnimationPoint | 197 | 14 | — | `+0x66a8`, `+0x66dc`, `+0x66e0`, `+0x66e4`, +2 more | `+0x026c` | 2d/0v/0c | `rule` | FElysiumNpc::SnapToAnimationPoint | scope-traced, six writes and two calls in order: CBaseAnimating::MatchOriginAnglesToAnimation(Bip01, 1, 1) snaps origin AND angles onto the animation's root bone; SetHullSizeSmall(force=0) via 0x10273180, which therefore does nothing when +0x5f2d already says small; +0x66dc/+0x66e0/+0x66e4 set from DAT_1070d1b0/b4/b8 (vec3_origin), clearing the cached fake-hull position; +0x66a8 = 0; and both hint handles +0x6708 and +0x670c = -1, so the snap also drops whatever hint the werewolf was heading for. |
| `0x101a8de0` | CCineAISchedule::FUN_101a8de0 | 390 | 15 | `CCineAI#113`, `CCineAISchedule#113`, `CCineNPC#113` | `+0x5f58`, `+0x5f94` | `+0x5f44`, `+0x5f48`, `+0x5f4c`, `+0x5f54`, +2 more | 0d/0v/1c | `rule` | FElysiumScriptedSequence::Activate | CORRECTION to the walk's field naming. The three sequence-sound precaches at 0x10428880 run on m_iszPreIdle, then m_iszPostIdle, then m_iszPlay - and m_iszPreIdle is the field at +0x5f44 whose KEYFIELD is m_iszIdle (vtmb_fields CCineNPC), so it is the port's PreIdle and not the port's PreIdleAlt, whose keyfield is the literal m_iszPreIdle. Also recovered: 0x10428880 is PrecacheSequenceSounds - look the named sequence up on the actor's studio header, walk its anim events, and precache every event id below 5000 that is a sound event as a script sound, warning "Bad sound event %d in sequence %s" for an unterminated option string. The rest of the walk holds: CBaseEntity::Activate first; a FindEntityByName loop over m_iszEntity that stops at the first match carrying a CBaseAnimating at +0x94; a no-match arm that DevMsgs the divider, the "Could not find NPC %s in CCineNP..." line and the divider and SKIPS the precache; a no-model arm that DevMsgs the divider, "NPC %s has no model in CCineNPC::" and the divider; then m_hNextCine +0x5f94 from FindEntityByName(m_iszNextScript) or 0xffffffff, and a m_hNextCine that does not resolve CLEARS m_iszNextScript +0x5f58. Ported as story 29d family Lifecycle10. |
| `0x102658f0` | CAI_BaseNPC::FUN_102658f0 | 96 | 15 | `CAI_BaseHumanoid#402`, `CAI_BaseNPC#402`, `CAI_BaseNPCTroika#402`, +74 more | — | `+0x0134`, `+0x0628`, `+0x062c`, `+0x0638`, +1 more | 0d/1v/1c | `rule` | hand:FElysiumNpc::Event_Gibbed | slot 402, a two-gate body whose answer is always the FIRST gate's: slot 394 CorpseGib false -> slot 395 CorpseFade runs and the body returns 0; true -> slot 398 HasExplosiveGibs decides, and when it is false the body only UTIL_Removes this (0x101cd940) and returns the first gate's value, while when it is true it runs CBaseCombatCharacter::CreateSecondaryDiscParticles(m_vDiscBloodType +0xfd0), then slot 77 ScriptHide (vtable +0x134), then slot 395, and returns the first gate's value; so CorpseFade fires on every path except the remove one, and the disc particles are tied to the second gate alone. |
| `0x1026fbe0` | CAI_BaseNPC::FUN_1026fbe0 | 198 | 15 | `CAI_BaseHumanoid#562`, `CAI_BaseNPC#562`, `CAI_BaseNPCTroika#562`, +74 more | — | `+0x0804`, `+0x08f4` | 0d/0v/0c | `rule` | hand:FElysiumNpc::WeaponLOSCondition | slot 562 bool(ownerPos, targetPos, bSetConditions): with an active weapon the answer is the weapon's own slot 0x5b0(ownerPos, targetPos, bSetConditions); with no weapon read capabilities (slot 0x804) - without bit 0x20000 the answer is 0 and, only when bSetConditions, the cvar DAT_10924a6c's vtable+4 is touched and SetCondition(0x42) is set; with bit 0x20000 the answer is slot 0x8f4's (InnateWeaponLOSCondition 0x1026fcf0, slot 573); THEN, whatever that answer was, capabilities are re-read and bit 0x10000000 adds 0x10266b10 - which is NOT a plain cosine test but the PLAYER-IN-LINE-OF-FIRE walk over every client: dot(normalize(target-owner), normalize(WorldSpaceCenter-owner)) strictly above 0.92 AND the distance to the target strictly greater than the distance to that client, both terms unsquared because 0x10137220 (1057966c) is VectorNormalize and ANSWERS THE LENGTH. When it answers true the body sets condition 100 under bSetConditions and returns 0, overriding a weapon that said yes. |
| `0x10272490` | CAI_BaseNPC::SetActivityAndSequence | 243 | 15 | — | `+0x06f8`, `+0x0fec`, `+0x0ff4` | `+0x065d`, `+0x06f0`, `+0x0744`, `+0x0854`, +4 more | 2d/0v/0c | `rule` | FElysiumNpc::SetActivityAndSequence | CAI_BaseNPC SetActivityAndSequence, 243 bytes, the commit, and every one of its six effects is ordered against the others. +0xff4 the translated activity is written FIRST; a NEGATIVE sequence takes ResetSequence(0) and skips the cycle, the duration and the weapon activity but NOT the rest; otherwise the cycle word +0x6f8 is zeroed unless the sequence equals +0x6f0 with +0x65d set OR the old activity and the new one are both in the pair 9 and 0x13, retail's walk/run cycle carry; then ResetSequence, SequenceDuration and Weapon_SetActivity; then slot 533 EyeOffset(act, +0xff4) feeds SetViewOffset for EVERY request; then slot 465 OnChangeActivity fires when m_Activity differs from the NEW ACTIVITY while the listener 0x101f6010 fires on a DIFFERENT comparison, m_Activity against the TRANSLATED activity, and only while m_bKeepSound is clear; then m_Activity is overwritten and the navigator notified. |
| `0x1027bb20` | CAI_BaseNPC::PostConstructor | 27 | 15 | `CAI_BaseHumanoid#106`, `CAI_BaseNPC#106`, `CAI_BaseNPCTroika#106`, +74 more | — | `+0x06a0` | 0d/0v/0c +20 outside | `rule` | hand:FElysiumNpc::PostConstructor | Slot 106, 27 bytes whose entire content is an ORDER: 1027bb28 calls CBaseCombatCharacter::PostConstructor(name) to completion, then 1027bb31 dispatches this object's own vtable +0x6a0, which is 0x6a0/4 = slot 424 CreateComponents. The NPC-side post-construct pass therefore observes everything the base pass built and nothing it has not, and no state is written here directly. Ported as story 29d family SaveRestore10; slot 424 is story 29c-1 family Lifecycle's and is dispatched, not re-recovered. |
| `0x1027bc60` | CAI_BaseNPC::Save | 386 | 15 | `CAI_BaseHumanoid#126`, `CAI_BaseNPC#126`, `CAI_ExpressiveNPC#126`, +9 more | — | `+0x029c`, `+0x5b8c`, `+0x5c38`, `+0x5ce4`, +4 more | 2d/0v/0c | `rule` | FElysiumNpc::BaseSave | CORRECTION to the walk, from the listing. CAI_BaseNPC::Save encodes exactly TWO fields, not "4 timers from m_flExtendedBlockedByFriendTimer and 3 from m_flWaitFinished": thunk_FUN_101cf250 takes (float*, mode), and 1027bc6c PUSH 0x4 / LEA EBP,[ESI+0x5b8c] then 1027bc80 PUSH 0x3 / LEA EBX,[ESI+0x5db4] are mode 4 on m_flExtendedBlockedByFriendTimer and mode 3 on m_flWaitFinished. SECOND CORRECTION: 0x1023f040 / 0x1023f0c0 / 0x1023f060 are not a bit-vector init/copy/finish but CRC32 - init 0xffffffff, an unrolled table-driven byte loop over DAT_10496f58, final complement - so AIExtendedSaveHeader_t's last word is a CHECKSUM of the running schedule's task array (schedule+0x20 for schedule+0x24 << 3 bytes, an 8-byte Task_t each), not a copy of its interrupt bits. The header is version 1 as a 16-bit word; a flag word whose bit 0x1 is slot 0x29c GetEnemy non-null, bit 0x2 is m_hTargetEnt +0x5ce4 passing its 0x1fff index and >>13 serial check onto a live entity, and bit 0x4 is the navigator goal 0x102ee6a0; a 0x80-byte Q_strncpy of schedule+0x40; and that CRC. The no-schedule arm writes name[0] = 0 and a literal zero CRC, which CRC32 over zero bytes also produces. The motor pre-save 0x102e0b60 is GUARDED on m_pMotor +0x5d44 and the MoveAndShoot overlay pre-save 0x102e8aa0 at +0x5cf4 is not; both post halves run in the same order after CBaseCombatCharacter::Save, whose answer is this body's return. A base-class body beneath the Troika override 0x102993c0, so it takes its own port method name and no hand: prefix. Ported as story 29d family SaveRestore10. |
| `0x1028d6e0` | CAI_BaseNPCTroika::UpdateOnRemove | 102 | 15 | `CAI_BaseNPCTroika#180`, `CGeneric_NPC#180`, `CNPCMaker#180`, +58 more | — | `+0x0964`, `+0x658c`, `+0x6594`, `+0x65e8` | 4d/0v/1c | `rule` | hand:FElysiumNpc::UpdateOnRemove | Slot 180, the Troika UpdateOnRemove, read off the listing. Six steps: ClearHintNode(this, 5.0) at 1028d6e1 PUSH 0x40a00000 - a 5.0-second reuse delay where the base 0x1027ca30 passes 0.0, which is the whole difference between the two bodies; slot 0x964 (601) dispatched with this, only when m_pAttackCoordinator +0x65e8 is non-zero; 0x102b53d0 called as (0, "Leaving interesting place (UpdateOnRemove)") - the string at 0x105d87d4 read off 1028d702 SETTLES that 0x102b53d0 is the interesting-place release and NOT the grapple release the checklist's walk calls it, because +0x62ec is m_pInterestingPlace, +0x62e8 m_bInterestingPlaceArrived and +0x6304 m_eInterestingPlaceMode in the datamap; then IsInDialog 0x102c1170 gating the dialogue stop 0x102c0bb0; then 0x1029f5d0 on m_sppPatrolPath +0x658c and m_sppPatrolPathHunt +0x6594 in that order; then a tail jump to CAI_BaseNPC::UpdateOnRemove 0x1027ca30. Ported as story 29d family SaveRestore10 with three species arms (CNPC_VCop 0x10371a90, CNPC_VMingXiao 0x10391230, CNPC_VNewscaster 0x103a03a0). |
| `0x10295590` | CAI_BaseNPCTroika::NPC_EarlyTranslateActivity | 295 | 15 | `CAI_BaseNPCTroika#375`, `CGeneric_NPC#375`, `CNPCMaker#375`, +14 more | — | `+0x0804`, `+0x08e4`, `+0x08e8`, `+0x5b84`, +2 more | 4d/0v/5c | `rule` | hand:FElysiumNpc::NPC_EarlyTranslateActivity | slot 375, 295 bytes. CORRECTION to the earlier walk: BOTH delegates sit under ONE capability test. The listing puts 10295655 CALL [vt+0x804] and 1029565f TEST 0x8000000 in front of the reload arm AND the cover arm, so ACT_COVER is gated on 0x8000000 exactly as ACT_RELOAD_FAST is - the generated table had the ACT_COVER row predicated Always and is corrected with this body. Second correction: both frenzy rewrites goto past the ACT_FIDGET-to-ACT_IDLE test, so step 3 runs only when neither fired; unobservable, because 3 is in neither rewrite set, and recorded because the listing says so. Arms in order: the gait cvar DAT_10924d6c value 1 rewrites 9 and 0x1115 to 0x13 and value 2 rewrites 0x13 to 9; frenzy bit 0x40 rewrites the set {0x17, 9, 0x13, 0x16, 0x1115, 0x1121} to 0xf17 ELSE bit 0x20 rewrites {0x1115, 9, 0x16, 0x1121} to 0x13; 3 becomes 1; then under 0x8000000 request 0x55 RETURNS slot 570's answer and request 6, or 1 with m_afMemory bit 2 set, RETURNS slot 569's; otherwise the CBaseCombatCharacter tail, which is the identity. |
| `0x102984a0` | CAI_BaseNPCTroika::FUN_102984a0 | 674 | 15 | `CAI_BaseNPCTroika#531`, `CGeneric_NPC#531`, `CNPCMaker#531`, +61 more | `+0x5d30`, `+0x644c`, `+0x6450` | `+0x0364`, `+0x0740`, `+0x0804`, `+0x5bb0`, +4 more | 0d/0v/0c | `rule` | hand:FElysiumNpc::OnObstructingDoor | PORTED 29d Motor10 (listing). THREE CORRECTIONS. (1) m_hOpeningDoor is +0x5d24 (LEA EBP,[EDI+0x5d24] at 102984e7), not +0x644c - +0x644c is m_eAlternateAI and +0x6450 m_flAlternateAIExpireTimer. (2) The -1 nav-position arm at 10298725 writes the RESULT pointer (MOV EDX,[ESP+0x3c] = argument 4), not the move goal; the decompiler's *param_1 = 0 is wrong. (3) The successful-splice arm writes moveGoal->+0x28 = distClear (MOV ECX,[ESP+0x30] argument 1, MOV EAX,[ESP+0x38] argument 3 at 1029870e), not +0x28 of the block slot 246 returned. Signature bool OnObstructingDoor(AILocalMoveGoal_t*, CBaseDoor*, float distClear, AIMoveResult_t*). Arms: null door DevMsgs No door given to OnUpcomingDoor() (the retail NAME in the literal) and answers false; the gate is maxDist >= distClear with an unordered compare also running the body; the held door answers result 0 / 0x100f0e70(door) / true; the squad focus (GetSquadFocus 0x103166b0 under m_iSquadDisconnected <= 0 && m_pSquad) answers -2 / 0x100f0e90(door,1) / true; GetState() (slot 464) == 4 NPC_STATE_SCRIPT with CapabilitiesGet() (slot 513) NOT carrying the full 0xd00 runs StopScheduledMove 0x102bf770, m_eAlternateAI = 4, expire = curtime + 3.0 (_DAT_10449258) and m_hOpeningDoor = door when distClear < 64.0 (_DAT_10451acc), and EITHER way answers 0 / clear / true; 0x1027f550 refusing answers -2 / 0x100f0e90(door,4) / true; TEST AH,0xd means ANY of 0xd00 and none answers FALSE with no write; door slot 246 (+0x3d8) is asked for a nav point with (door+0x4f8 == 2) and -1 answers result 0 / clear / true; the pathfinder 0x10304130 (CAI_Pathfinder::BuildLocalRoute, VProf scope 0x10611514) FOUND stamps the door handle into waypoint+0x24 and splices at navigator+0x30 +0x24 through 0x10319f30 - a failed splice falls out FALSE with no write; NOT FOUND gives up with 0x100f0e70 and false for toggle state 0 or 2, else takes the door and tries 0x10298840, whose FAILURE writes -2 with 0x100f0e90(door,0x40) and true and whose SUCCESS resets m_hOpeningDoor to -1, clears and answers FALSE. 0x100f0e70 is door->+0x644 = 0 and 0x100f0e90 is door->+0x644 ¦= bits. 0x1027f550 is ported in full: null answers false with NO flag write, (CapabilitiesGet() & 0xd00) != 0xd00 ORs 0x8, door->+0x640 > curtime ORs 0x10. |
| `0x1029ca50` | CAI_BaseNPCTroika::DrawDebugGeometryOverlays | 2129 | 15 | `CAI_BaseNPCTroika#123`, `CGeneric_NPC#123`, `CNPC_ProneDialog#123`, +56 more | — | `+0x0050`, `+0x0054`, `+0x0058`, `+0x0224`, +15 more | 2d/0v/0c | `rule` | hand:FElysiumNpc::DrawDebugGeometryOverlays | slot 123, read from the LISTING. Four m_debugOverlays masks then three unmasked blocks, in order. 0x400000 draws three cones through 0x1029c4a0 - mine (arg3 0.0, 0x40, 0xc0, 0x40, len 0x32, arg8 1), the m_hClosestPlayer one with m_flFieldOfView divided by that player's slot 0x74 (arg3 2.0, 0xc0, 0x40, 0, len 100, arg8 0) and a third from 0x1029ca30 (arg3 4.0, 0xff, 0, 0x80, len 0x96, arg8 0); the second call OVERWRITES the length and fov registers, so the third cone uses the player-scaled pair whenever a player resolved. 0x1000 draws the collision box through NDebugOverlay::BoxDirection 0x10144cd0 ORIENTED by m_vecForward +0x6290 at 128/0/0 alpha 40 when mins != maxs, then a +-10 NDebugOverlay::BoxAngles 0x10142b80 at the Bip01 bone with that bone's ANGLES at 128/0/0 alpha 40, then the collision box widened by m_vecAttackExtents +0x50 coloured 255/128/0 alpha 20 when that vector is not vec3_origin, then the +0x156c hull's box (255, 255, 64, 10) when it differs from m_eHull +0x1568. 0x20000000 draws two NDebugOverlay::Circle rings about the axis (1,0,0) whose RADII are min(+0x8b8, +0x8bc) at 255/255/32 alpha 128 and max(+0x8c0, +0x8c4) at 128/128/16 alpha 128, each of HEIGHT 0x42000000 = 32.0 - the earlier walk had the radius and the height the other way round, and CNPC_VMingXiao#123's 100/150/200/300 bands with their constant 8.0 settle it - or 2000.0 and 0.0 when unarmed. Then under the cvar DAT_10924f24 five +-2 boxes at 255/32/32 alpha 1 for 0.1 s at the enemy's slot 197 BodyTarget, which is NOISY, so the five points differ; retail passes an UNINITIALISED stack vector as that call's posSrc. Then, when m_pHintNode +0x5ddc is set and its m_nHintType +0x5dc is one of 100, 0x65, 0x283c, 0x283d or 0x27d8, two 200-unit BoxDirection facings at 255/0/0 alpha 50 with extents (0,0,-10) to (200,0,10), drawn from the hint origin plus m_vDefaultEyeOffset; the first yaw is MINUS the hint's +0x454 and the second PLUS it, except for type 0x27d8, where leaning left gives a bare +43.0 and the hint's own value (the negation is jumped past) and not leaning left gives minus the hint's value and -43.0. Then the relationship-line walk, gated on +0x6658 and NOT +0x63fd: every entity within 4096.0 units (0x100f8490 with mask 0x820) whose +0x9c is an NPC and is not me gets a line whose channel weights come from slot 404 - 1 to (4,1,1), 2 to (4,4,1), 3 to (1,4,1), 4 to (1,1,4), default (4,0,0) - each multiplied by (slot 405 IRelationPriority + 2) * 6 clamped to 0 below and 0x3f above, and a null +0x9c ENDS the walk rather than skipping the entity. +0x6658 is layout.md's m_bUnread6658, written zero by the Troika constructor 0x1028d230 and touched by nothing else in the corpus, so the arm is DEAD in retail. Finally chain CAI_BaseNPC::DrawDebugGeometryOverlays 0x10275760. Ported in ElysiumNpcKernelDebug10_2.cpp with the species arms CNPC_VCop 0x10372f00, CNPC_VMingXiao 0x10399d40 and CNPCMaker 0x1034bd30 dispatched from the same method. |
| `0x1029d4e0` | CAI_BaseNPCTroika::DrawDebugTextOverlays | 3754 | 15 | `CAI_BaseNPCTroika#124`, `CGeneric_NPC#124`, `CNPCMaker#124`, +57 more | — | `+0x0001`, `+0x0224`, `+0x02e0`, `+0x0364`, +22 more | 4d/0v/0c | `rule` | hand:FElysiumNpc::DrawDebugTextOverlays | slot 124, 3754 bytes, read from the LISTING with every format string taken out of .rdata. Calls base CAI_BaseNPC::DrawDebugTextOverlays 0x102767d0 for the first free line and returns it unchanged unless m_debugOverlays bit 0 is set. DAT_1070b22c+0x8c is IndexOfEdict, not AddEntityTextOverlay: its answer is the first argument of NDebugOverlay::EntityText(index, line, text, 0.0, 255, 255, 255, 255) at 0x101434b0, which is why every block pushes eight dwords and cleans 0x20. Lines in order: "Seq: " plus the label, " / " and the activity name from GetSeqDesc(m_nSequence) or "(INVALID)"; "Cycle: %.2f"; when GetModelPtr is non-null three pose lines "move_yaw: %.3f", "aim_pitch: %.3f" and "aim_yaw: %.3" - the third string at 0x105d9c54 is MISSING its conversion character, a retail typo that drops the pose value it just computed; "ground speed: %.3f" from +0x654; "dist to player: %.3f" from +0x6264; "Disposition: %s" from 0x100ec410 over DAT_10924980; "pos: %5.1f, %5.1f, %5.1f" from slot 217 and "dir: %5.1f, %5.1f, %5.1f" from slot 219. Then, when m_debugOverlays and 0x10000000 is set and +0x5c38 is non-null, the condition dump: an "INT COND" header line first, then the +0x5c74 set mask and the +0x5c8c interrupt mask copied to the stack and walked ids 0 to 0x76, printing for each standing bit a 3-character abbreviation from slot 408 through "%s  " for the set arm and "!%s " for the interrupt arm, upper-cased for (10*4)/10 characters - and the two arms use OPPOSITE probes, the set arm upper-casing when HasCondition stands and the interrupt arm when it does NOT - flushed eight per line; then a "COND" header and the fixed 26-id watch list 0x40,0x46,0x01,0x47,0x48,0x49,0x4c,0x4f,0x51,0x08,0x5f,0x60,0x09,0x54,0x56,0x57,0x58,0x59,0x63,0x6d,0x6f,0x70,0x0a,0x0b,0x0c,0x0d through "%s ", eight per line, whose final flush is gated on the FIRST loop's saved count and 7 being unequal to 7 rather than on the watch list's own remainder, a retail bug that drops the last two entries whenever the bitfield loop ended with exactly seven pending. When the overlay bit is clear the block is replaced by a second "Enemy too far to attack" line on COND 0x55. Then "HG - %d : HB - %d" with m_LastHitGroup +0x1594 and the last CTakeDamageInfo's +0x40 (that is +0x664c, read by the four-byte 0x101c2a30). Then, gated on ConVar DAT_1092429c, the m_eAlternateAI switch 1..4 spelling EALTAI_OPEN_DOOR, EALTAI_WAIT_DOOR, EALTAI_BLOCKED_DOOR and EALTAI_SCRIPT_STOPPED_AT_DOOR with the REMAINING time m_flAlternateAIExpireTimer +0x6450 minus curtime, mode 0 and anything above 4 printing nothing. Finally the scene handle +0x6554: "Scene: %s time: %.2f" when the CChoreoScene at +0x4bc is live and "Scene: %s" when it is not, both of which RETURN out of the body, and "??? scene" otherwise; an unresolved handle prints nothing and does not advance the index. Returns the next free line index. Ported in ElysiumNpcKernelDebug10.cpp together with the base body 0x102767d0 and the five species arms CNPC_Crow 0x10358f90, CNPC_VHengeyokai 0x10383560, CNPC_VNewscaster 0x103a1250, CNPC_VTzimisce 0x103c08d0 and CNPC_VZombie 0x103e0e80, dispatched from the same method. |
| `0x1029e750` | CAI_BaseNPCTroika::FUN_1029e750 | 156 | 15 | `CAI_BaseNPCTroika#359`, `CGeneric_NPC#359`, `CNPCMaker#359`, +59 more | — | `+0x0650`, `+0x146c`, `+0x14bc`, `+0x5cc0` | 0d/0v/0c | `rule` | hand:FElysiumNpc::Slot359 | slot 359 (vt+0x59c), the Auspex aura index CBaseCombatCharacter::UpdateAuspex 0x100532b0 uses as DAT_10937160[idx + IsKindred*8], treating a negative as no aura. Arms in retail order: EntityUnselectable true gives -1; m_iCurFrenzyCount +0x146c greater than 0 gives 4; m_bfAINPCFlags2 +0x14bc bit 0x80000 D_MILDLY_CRAZY gives 3 - and note that test comes BEFORE the state switch, so a mildly-crazy body never reads its state; otherwise switch on m_NPCState: 2 and 0xb give 1; 3 and 0xe give 7; 5, 6, 8, 9, 10 and 0xd give 0; 7 gives -1; the default arm with a non-null observer whose slot 404 IRelationType answers 1 D_HT gives 2, else 5. Seven of the switch's retail states have no member in this runtime's EElysiumNpcState and those arms are ported UNREACHABLE. |
| `0x102ad340` | CAI_BaseNPCTroika::FUN_102ad340 | 629 | 15 | `CAI_BaseNPCTroika#460`, `CGeneric_NPC#460`, `CNPCMaker#460`, +58 more | `+0x14b8`, `+0x1b38`, `+0x1b3c`, `+0x1b40`, +4 more | `+0x0650`, `+0x14b8`, `+0x5cc0`, `+0x5cc4`, +5 more | 1d/0v/0c | `rule` | hand:FElysiumNpc::PreSelectIdealState | Ported as slot 460. CORRECTED: the COND_SEE_FEAR arm at 102ad3ef is a NESTED if, not an alternative - the INITIAL_FLEE bit 0x100 in m_bfAINPCFlags is armed only while m_NPCState is not already 8, and m_IdealNPCState = 8 is then written UNCONDITIONALLY (line 0x44b8). RECOVERED: +0x65cc is m_eForcedState, a raw NPC_STATE and not an order id - 0x102ae840 takes an NPC_STATE as its first argument and stores it there beside m_bForceStateChange and CHOOSE_NEW_SCHEDULE, and this slot is its only consumer; ElysiumAiScriptedSchedule.h described it as the director order id and is corrected at the declaration. The criminal bound is 0x1042fe90(0x3cf445af) = 2, computed and pinned by a test. Note the asymmetry at 102ad37a: the cover timer arms on HasInterruptCondition(0x46) and clears on plain HasCondition(0x48). Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x102b38b0` | CAI_BaseNPCTroika::QuerySeeEntity | 159 | 15 | `CAI_BaseNPCTroika#468`, `CGeneric_NPC#468`, `CNPCMaker#468`, +60 more | — | `+0x0650`, `+0x5b84`, `+0x60ac` | 0d/0v/1c | `rule` | hand:FElysiumNpc::QuerySeeEntity | slot 468, 159 bytes. Arms: return 0 when the debug byte DAT_10924fba (ignore-senses) is set, or when DAT_10924fb9 (ignore-players) is set and the candidate is non-null with a player record at +0xa8 (the null check sits inside that second arm only); then the frenzy-friend veto - when (m_bfNPCFrenziedFlags +0x5b84 and 0x800) == 0x800 and the candidate resolves equal to m_hFriendPlayer +0x60ac, return 0 - and it is AHEAD of the player arm, so a frenzied body does not see its friend at all; then any candidate carrying a player at +0xa8 returns 1 unconditionally, before any relationship test; otherwise dispatch IRelationType (slot 404, vt+0x650) and return 1 only for 1 (D_HT) and 2 (D_FR), and 0 for every other disposition including the default. |
| `0x102b39a0` | CAI_BaseNPCTroika::OnLooked | 36 | 15 | `CAI_BaseNPCTroika#469`, `CGeneric_NPC#469`, `CNPCMaker#469`, +61 more | `+0x60a8` | `+0x60a8` | 0d/0v/0c | `present` | hand:FElysiumNpc::OnLooked | slot 469, 36 bytes, two statements only: call the base CAI_BaseNPC::OnLooked (0x1026a2c0) FIRST, then if HasCondition(0x54 COND_NEW_ENEMY) still stands after it, increment m_iEnemySightings (+0x60a8) by one. The port now runs both halves through the slot itself: FElysiumNpc::BaseOnLooked is the base body (whose text is ElysiumNpcCond::GatherSight) and the increment is the slot's own tail. |
| `0x102b4630` | CAI_BaseNPCTroika::FVisible | 232 | 15 | `CAI_BaseNPCTroika#201`, `CGeneric_NPC#201`, `CNPCMaker#201`, +56 more | — | `+0x0948` | 2d/1v/7c | `rule` | hand:FElysiumNpc::FVisible | slot 201, 232 bytes. A one-shot static init guarded by bit 0 of DAT_10923e2c resolves the discipline id for Dominate_BrainWipe through 0x101e1590 and 0x101e1870 into DAT_10924074. Then: a null target returns false and does NOT write the blocker out-param; when DAT_10924fba (ignore-senses) is set, write 0 through the blocker if one was passed and return false; when DAT_10924fb9 (ignore-players) is set and the target has a player record at +0xa8, same blocker write and false; otherwise dispatch slot 594 (vt+0x948)(target, mask, 0, 0) - with BOTH trailing arguments forced to 0, so the Troika line never hands slot 594 a blocker cell - and return false when it fails; then return false when this NPC carries the Dominate_BrainWipe status (0x1033d2f0 on DAT_10924074); finally defer to CBaseEntity::FVisible for the trace itself. The asymmetry of the three blocker writes is the reason this is rule and not present; the generated signature spells the third parameter by value, so the port COUNTS the write with the target it was made for rather than delivering it. |
| `0x102b7fc0` | CAI_BaseNPCTroika::SelectScheduleRangedCombat | 677 | 15 | `CAI_BaseNPCTroika#605`, `CGeneric_NPC#605`, `CNPCMaker#605`, +18 more | `+0x1b30`, `+0x1b34` | `+0x04cc`, `+0x0978`, `+0x6078` | 0d/1v/6c | `rule` | hand:FElysiumNpc::SelectScheduleRangedCombat | Ported as slot 605, the Troika line and the arm a plain npc_VCop reaches. Seven arms in retail order, every return stamping AI_BaseNPCTroika.cpp and the line: m_bInMelee giving 0xe3 (0x5d98); COND 0x08 and slot 307 and slot 599 on GetEnemy giving 0xe3 (0x5db2); COND 0x3c giving 0xb8 (0x5db7); the weapon pre-pass 0x102b8620 whenever non-zero; then the split on neither COND 0x5f nor COND 0x08 with 0x101e3f50(&DAT_10739a4c, THIS) false - slot 606, else COND 0x2e giving 0xbd (0x5dff), else COND 0x60 giving 0xb1 (0x5e04), else 0; otherwise neither COND 0x2f nor COND 0x63 - dodge 0x102b7f40 giving 0xef (0x5dd8), else melee and RandomInt(0,99) < 0x19 and slot 599 giving 0xe8 (0x5ddc), else 0xf0 (0x5de0); else dodge giving 0xb8 (0x5de9), melee plus slot 599 giving 0xe8 (0x5ded), else 0xb9 (0x5df1). The door helper and the taunt prologue are NOT offered on this line. Note the discipline gate subject: this line passes THIS, the human arm passes the enemy +0x9c. Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x102c21c0` | CAI_BaseNPCTroika::CanTalk | 244 | 15 | `CAI_BaseNPCTroika#295`, `CGeneric_NPC#295`, `CNPCMaker#295`, +59 more | — | `+0x0128`, `+0x0278`, `+0x0650`, `+0x1088`, +3 more | 0d/2v/3c | `rule` | hand:FElysiumNpc::CanTalk | Slot 295's Troika-line body, 60 census classes, so hand: applies; CPayphone::CanTalk (0x101aaee0) is the one species override and is story 29c-1's PayphoneCanTalk, dispatched to. CORRECTS THREE FIELD NAMES in the pack walk, from the listing and from vtmb_fields CAI_BaseNPCTroika: m_iDialog is +0x128 (102c21d0, the authored dialogname key), m_bWillTalk is +0x1088 (102c221e) and m_bfNPCStateFlags is +0x5b64 (102c222c, bit 2) - the walk rotates all three. AND CORRECTS THE LAST GATE'S RECEIVER: 102c2291 is MOV EDX,[ESI] / PUSH EDI / MOV ECX,ESI / CALL [EDX+0x650], so slot 404 is dispatched on THIS NPC with the activator as the argument, not on the activator. Fourteen gates in the listing's order, every failure falling to XOR AL,AL at 102c22ad. Three seams stand for retail inputs with no source here - the activator's controller test 0x10175180, the cross predicate 0x10146b20 and the menu singleton 0x1023bd00 +0x4ac - and each answers the ADMITTING value. |
| `0x102c2ec0` | CAI_BaseNPCTroika::FUN_102c2ec0 | 830 | 15 | `CAI_BaseNPCTroika#589`, `CGeneric_NPC#589`, `CNPCMaker#589`, +61 more | — | `+0x13bc`, `+0x13c0` | 0d/0v/1c +1 outside | `rule` | hand:FElysiumNpc::SetScriptedDiscipline | Ported as slot 589. 830 bytes only because the type scan is inlined six times; the rule is short and the ORDER of its two-step writes is the behaviour because both intermediate states are observable. base = GetBase(list3, statId); a value ABOVE it looks up the type-2 buff list and, only when ITS GetBase is below the new value, Set(list2), then AddBase(list3, value - base) and only then Set(list3); a value BELOW it does Set(list3) FIRST and then SubBase(list3, base - value); an equal value writes nothing at all. Types 2 and 3 have no port container, so the seam records each write with its list type and the callers take the retail empty-global arm (DAT_109f0b40 reads 0). |
| `0x102c4a60` | CAI_BaseNPCTroika::CanBeFedUponBy | 77 | 15 | `CAI_BaseNPCTroika#342`, `CGeneric_NPC#342`, `CNPCMaker#342`, +61 more | — | `+0x0494`, `+0x63d8` | 0d/0v/0c | `rule` | hand:FElysiumNpc::CanBeFedUponBy | slot 342, three arms. m_bInvincible (+0x63d8) refuses at once (retail clears only the low byte of EAX so the upper bits are stale; the bool is false). Then slot 293 GetFollowerBoss: when it stands AND it IS the feeder, the feed is refused unless HasMiscFlag(0x40000), which is name 18 of the 22 at 0x10619ec8, No_Resist_Feeding - so your own follower or ghoul may only feed on you while that flag stands. Otherwise it defers to CBaseCombatCharacter::CanBeFedUponBy 0x10339800, which NEVER READS THE FEEDER: CanBeFedUpon (0x10339a90, char template +0x95 == 0), NOT_FEEDABLE (flags2 0x8000000) clear, no live grapple (m_GrapplePartner resolving with m_GrappleRole not -1 refuses), slot 158 IsAlive, and IsUnconscious (0x10341aa0, m_iMiscFlags bit 0). |
| `0x1034afe0` | CNPCMaker::Spawn | 261 | 15 | `CNPCMaker#103` | `+0x017c`, `+0x66b0`, `+0x66b8`, `+0x66c2` | `+0x01a0`, `+0x026c`, `+0x0270`, `+0x6664`, +2 more | 0d/0v/0c | `rule` | FElysiumNpcMaker::Spawn | CNPCMaker#103 only, not a Troika-line slot, 261 bytes, ported as the base arm of FElysiumNpcMaker::Spawn (ElysiumNpcMaker.cpp) keyed on the maker classname, the shape that class already uses for slots 104/139/617/618. Listing order: the scope frame naming CBaseEntity::SetSolid and the targetname; SetSolid(SOLID_NONE) on m_Collision at +0x270; m_cLiveChildren at +0x66b0 = 0; slot 104 Precache through vt+0x1a0; m_bInfChild +0x66c3 forcing m_bFade +0x66c2 = 1; then on !m_bDisabled +0x66c0 ThinkSet(0x1000696a, which resolves through the thunk table to 0x1034bbf0) and m_flNextThink +0x17c = m_flSpawnFrequency +0x6664 + curtime, else ThinkSet(0x1000572c, which resolves to 0x101c0b60, a bare RET) with NO next-think stamp; then Relink 0x1001514a; then m_flGround at +0x66b8 = 0 last on both paths. CORRECTIONS to the walk: m_cLiveChildren is +0x66b0 and m_flGround is +0x66b8 (vtmb_fields CNPCMaker) - the walk has the two offsets the wrong way round; and m_Collision is +0x270 (1034b04c LEA ECX,[ESI + 0x270]), not +0x17c, which is m_flNextThink. The port now carries SetSolid (a seam), the Precache dispatch and Relink (a counted seam), which it did not before. |
| `0x1034c020` | CNPCMaker_Fleshpile::Spawn | 261 | 15 | `CNPCMaker_Fleshpile#103` | `+0x017c`, `+0x66b0`, `+0x66b8`, `+0x66c2` | `+0x01a0`, `+0x026c`, `+0x0270`, `+0x6664`, +2 more | 0d/0v/0c | `rule` | FElysiumNpcMaker::Spawn | CNPCMaker_Fleshpile#103 only. Byte-for-byte 0x1034afe0 with exactly one difference: 1034c0d6 PUSH 0x10010dd4 instead of PUSH 0x1000696a, and 0x10010dd4 resolves through the thunk table to 0x1034c8b0, the fleshpile think. Same prologue, same +0x66b0/+0x66b8 order, same inert think 0x1000572c on the disabled path, same Relink and same m_flGround zero last. Ported as the fleshpile arm of FElysiumNpcMaker::Spawn, selected by IsFleshpileMaker(). Carries the same two offset corrections as 0x1034afe0. |
| `0x1034cc60` | CNPCMaker_Zombie::Spawn | 295 | 15 | `CNPCMaker_Zombie#103` | `+0x017c`, `+0x6348`, `+0x634c`, `+0x6350`, +5 more | `+0x01a0`, `+0x026c`, `+0x0270`, `+0x6664`, +2 more | 0d/0v/0c | `rule` | FElysiumNpcMaker::Spawn | CNPCMaker_Zombie#103 only, 295 bytes, ported as the zombie arm of FElysiumNpcMaker::Spawn. Same prologue as 0x1034afe0 (scope frame, SetSolid on +0x270, m_cLiveChildren +0x66b0 = 0, Precache vt+0x1a0, m_bInfChild forcing m_bFade). Difference one: the enabled path installs 0x10015c4e, which resolves to 0x1034d2d0, and stamps m_flNextThink = RandomFloat(1.0, 2.0) + m_flSpawnFrequency + curtime (1034cd20 PUSH 0x40000000 / 1034cd25 PUSH 0x3f800000, the jitter added before the frequency and the clock), while the disabled path calls ThinkSet(0) - a NULL think, not the inert 0x1000572c the other two install - and the two branches join at 1034cd51 so Relink and m_flGround +0x66b8 = 0 run on BOTH. Difference two: it writes 0xf423f (999999) into all five police thresholds on itself, after m_flGround and in offset order: m_iPLInvestigateLevel +0x6348, m_iPLCriminalFleeLevel +0x634c, m_iPLCriminalAttackLevel +0x6350, m_iPLSupernaturalFleeLevel +0x6354, m_iPLSupernaturalAttackLevel +0x6358. Same two offset corrections as 0x1034afe0. GAP, named: ElysiumNpcClasses.cpp registers no npc_maker_zombie leaf, so the arm is unreachable at runtime and is driven through SetZombieMakerForTests. |
| `0x10358f90` | CNPC_Crow::DrawDebugTextOverlays | 259 | 15 | `CNPC_Crow#124` | — | `+0x0224`, `+0x029c`, `+0x02e0`, `+0x5f4c`, +1 more | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugTextOverlays | CNPC_Crow#124 only, a species slot with no Troika-line entry, 259 bytes. Calls the base CAI_BaseNPC::DrawDebugTextOverlays (0x102767d0) for the starting line index and returns it unchanged when m_debugOverlays bit 0 is clear. Otherwise line N is Q_snprintf(morale: %d, m_nMorale at +0x5f50) pushed through AddEntityTextOverlay (DAT_1070b22c+0x8c) with colour 0, 0xff, 0xff, 0xff, and the return becomes N+1; then, only when GetEnemy (slot 167, vt+0x29c) is non-null, line N+1 is enemy: dist: %s %g built from the enemy's GetClassname and the cached enemy distance at +0x5f4c (retail evaluates the float argument BEFORE re-dispatching GetEnemy for the classname), and the return becomes N+2. No crow debug body exists in the port. CONVENTION: species arm of slot 124's FElysiumNpc::DrawDebugTextOverlays keyed on IsRetailClass(CNPC_Crow) (npc_crow, ElysiumNpcKernelShape.cpp:4349); relative to the base it chains CAI_BaseNPC::DrawDebugTextOverlays 0x102767d0 and NOT the Troika 0x1029d4e0, and ADDS two lines under m_debugOverlays bit 0 - morale, then the enemy classname with the cached distance - each advancing the returned line index. |
| `0x10359f70` | CGeneric_NPC::Precache | 223 | 15 | `CGeneric_NPC#104` | — | `+0x0024`, `+0x0350` | 0d/1v/11c | `rule` | FElysiumNpc::Precache | CGeneric_NPC#104 only - the Troika-line CGeneric_NPC, a different class from CGenericNPC whose 0x1034aa40 the port already carries as FElysiumNpc::GenericNpcPrecache, hence the distinct name. 223 bytes: read the model keyfield through slot 9 (vt+0x24) and, when the string pointer is null or the string is empty, SetModelName (vt+0x350) to models/character/npc/sabbat/sabb... (the -(s[0] != 0) and addr idiom yields a null pointer for an empty literal); then re-read the keyfield and PrecacheModel it with preload 0; then PrecacheScriptSound over npc/metropolice/alert1.wav, npc/metropolice/surprise1.wav and npc/metropolice/die1.wav, then the four-entry npc/citizen/pain1..4 table walked as 0x10 bytes in steps of 4; and only then call CAI_BaseNPCTroika::Precache (0x10298ad0) LAST, unlike every other Precache in this pack. Port ElysiumNpcKernelSounds.cpp:200 carries these four tables as vocalization data but no Precache body. ARGUED: CGeneric_NPC is in the 77-class tree (classes.md, CAI_BaseNPCTroika, 617 slots) with entity classname npc_generic (CGeneric_NPC_Classnames), so the lookup keys on it and the arm belongs on slot 104's FElysiumNpc::Precache with the other species arms; the coined name existed only to separate it from CGenericNPC 0x1034aa40, and IsRetailClass(CGeneric_NPC) makes that distinction without a second method. Relative to the base it chains CAI_BaseNPCTroika::Precache 0x10298ad0 LAST rather than first, which is this arm's one ordering fact. |
| `0x1035cb90` | CNPC_VAndreiBlood::Precache | 111 | 15 | `CNPC_VAndreiBlood#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VAndreiBlood#104 only, 111 bytes, and unlike CGeneric_NPC it calls the base CAI_BaseNPCTroika::Precache (0x10298ad0) FIRST. Then three PrecacheScriptSound calls in order - Character/Boss/Andrei/TeleportOut.wav, Character/Boss/Andrei/TeleportIn.wav, Character/Boss/Andrei/Summon.wav - then three particle-emitter precaches through DAT_1070b22c+0x44 with the preload flag 1: Andrei_Teleport_Out_Emitter, Andrei_Teleport_In_Emitter, Andrei_Summon_Emitter. The asset list, the sound-before-emitter split and the preload flag are the retail data a later story reads; no body in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VAndreiBlood); relative to the base it chains CAI_BaseNPCTroika::Precache 0x10298ad0 FIRST and then APPENDS its three teleport and summon sounds and three preload-1 emitters, changing nothing the base does. |
| `0x10360bc0` | CNPC_VAsianVampire::Precache | 106 | 15 | `CNPC_VAsianVampire#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VAsianVampire#104 only, 106 bytes. Pushes a scope-trace frame naming CNPC_VAsianVampire::Precache with two empty operands, calls CAI_BaseNPCTroika::Precache (0x10298ad0) first, then exactly one UTIL_PrecacheOther (0x101d0ec0) for the weapon entity item_w_avamp_blade, then pops the frame. That single weapon is the whole species payload; no body in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VAsianVampire); relative to the base it chains 0x10298ad0 FIRST and then APPENDS exactly one UTIL_PrecacheOther for item_w_avamp_blade - that single weapon is the whole species payload. |
| `0x103620d0` | CNPC_VAsianVampire::SelectScheduleRangedCombat | 546 | 15 | `CNPC_VAsianVampire#605` | `+0x1b30`, `+0x1b34` | `+0x04cc`, `+0x6078` | 0d/0v/6c | `rule` | FElysiumNpc::SelectScheduleRangedCombat | Ported as the CNPC_VAsianVampire species arm of slot 605, keyed on IsRetailClass and dispatched from inside the one slot method. Confirmed: it replaces the Troika base wholesale, answers 0xf0 where the base answers 0xb8 for COND 0x3c (line 0x289), and is the SHORTEST of the six - no dodge helper, no slot-606 arm, no cover-hint arm, and it offers NEITHER the door helper NOR the taunt prologue, only the weapon pre-pass. Arms: m_bInMelee giving 0xe3 (0x26a); the COND 0x08 triple giving 0xe3 (0x284); COND 0x3c giving 0xf0 (0x289); 0x102b8620; COND 0x5f or COND 0x08 with neither COND 0x2f nor COND 0x63 giving 0xf0 (0x2af); SEE_ENEMY and not 0x48 and not 0x60 giving 0xf0 (0x2e5); COND_ENEMY_UNREACHABLE taking GetJumpSchedule (0x2ce), else 0xe8 (0x2d2). Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x103637b0` | CNPC_VBach::Precache | 124 | 15 | `CNPC_VBach#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VBach#104 only, 124 bytes: CAI_BaseNPCTroika::Precache (0x10298ad0) first, then three UTIL_PrecacheOther (0x101d0ec0) calls for item_w_grenade_frag, item_w_katana and item_w_rem_m_700_bach in that order, then five PrecacheScriptSound calls for Character/Boss/Bach/bach_grenade, bach_shield, bach_camp, bach_holy_light and snipe_warn6. The weapons-before-sounds order and the exact five-sound set are the retail data; no body in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VBach); relative to the base it chains 0x10298ad0 FIRST and then APPENDS three weapons before five Character/Boss/Bach sounds - the weapons-before-sounds order is this arm's fact. |
| `0x1036ae60` | CNPC_VChangBros::Precache | 262 | 15 | `CNPC_VChangBros#104`, `CNPC_VChangBrosBlade#104`, `CNPC_VChangBrosClaw#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | Fills three species slots - CNPC_VChangBros#104, CNPC_VChangBrosBlade#104 and CNPC_VChangBrosClaw#104 - so all three Chang forms share this one 262-byte body. Scope-trace frame, CAI_BaseNPCTroika::Precache (0x10298ad0) first, then seven emitter precaches through DAT_1070b22c+0x44 with preload 1 in order: chang_teleport_in_emitter, chang_teleport_out_emitter, chang_powerup_emitter, chang_spine_emitter, chang_center_emitter, chang_blast_emitter, chang_ball_charge_emitter; then four UTIL_PrecacheOther calls: item_w_chang_claw, item_w_chang_blade, item_w_chang_energy_ball, item_w_chang_ghost; then pop the frame. Nothing in the port. CONVENTION: one body fills CNPC_VChangBros#104, CNPC_VChangBrosBlade#104 and CNPC_VChangBrosClaw#104, so the arm on slot 104's FElysiumNpc::Precache is keyed on IsRetailClass(CNPC_VChangBros) and DerivesFrom covers the two leaf forms; relative to the base it chains 0x10298ad0 FIRST and then APPENDS seven preload-1 emitters and four weapons. |
| `0x10372b70` | CNPC_VCop::IRelationType | 170 | 15 | `CNPC_VCop#404` | — | — | 0d/2v/22c | `rule` | FElysiumNpc::IRelationType | CNPC_VCop#404, a species arm of slot 404's port method, three arms in front of the Troika base. A null candidate answers 0 D_ER rather than reaching the base; the class-static provoker DAT_1093ac3c with expiry _DAT_1093aca8 (family Senses10's CopSuspectHandle and CopSuspectExpiry) answers 1 D_HT while curtime is below it; a candidate carrying a player record at +0xa8 answers 1 on 0x1017f8d0 (curtime below m_flHeightenedAlertExpireTimer +0x1d1c) or on 0x1017f770 (m_iCopsInPursuitCount +0x1d10) greater than 0; otherwise it chains 0x10299da0 through a DIRECT thunk, so the base body's own virtual +0x650 calls re-enter this arm. UNREACHABLE through the census dispatcher today: CNPC_VCop's classname list is null, so a spawned npc_VCop has a null RetailClass and correctly takes the Troika line. |
| `0x10378470` | CNPC_VGargoyle::Precache | 258 | 15 | `CNPC_VGargoyle#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VGargoyle#104 only, 258 bytes: CAI_BaseNPCTroika::Precache (0x10298ad0) first, then nine PrecacheModel calls with preload 1 for the gargoyle body variants at 0x1063a808 down to 0x1063a550 in descending address order, then a four-entry stomp sound table walked as 0x10 bytes in steps of 4 from PTR_s_character_monster_gargoyle_stomp (0x10639480), then a three-entry exert table walked as 0xc bytes from 0x10639490, then one PrecacheScriptSound for character/monster/gargoyle/roar2, then UTIL_PrecacheOther item_w_gargoyle_fist. The nine-model preload set and the 0x10 and 0xc table lengths are the retail data a later vocalization story reads; nothing in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VGargoyle); relative to the base it chains 0x10298ad0 FIRST and then APPENDS nine preload-1 body models in descending address order, a 0x10-byte stomp table, a 0xc-byte exert table, roar2 and item_w_gargoyle_fist. |
| `0x1037b1a0` | CNPC_VGhoulCroucher::Precache | 55 | 15 | `CNPC_VGhoulCroucher#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VGhoulCroucher#104 only, 55 bytes and the shortest Precache in the pack: CAI_BaseNPCTroika::Precache (0x10298ad0) first, then UTIL_PrecacheOther item_w_claws_ghoul, then two PrecacheModel calls with preload 0 for the two Malkavian ghoul models at 0x1063b088 and 0x1063b028. The two models are what makes the male/female split in its SetModel sibling (0x1037b1f0) reachable. Nothing in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VGhoulCroucher); relative to the base it chains 0x10298ad0 FIRST and then APPENDS only item_w_claws_ghoul and the two preload-0 Malkavian ghoul models that make its SetModel sibling's male/female split reachable - the shortest Precache arm in the band. |
| `0x1037b1f0` | CNPC_VGhoulCroucher::SetModel | 119 | 15 | `CNPC_VGhoulCroucher#105` | `+0x00b4`, `+0x00bc`, `+0x00c0` | `+0x00c0` | 0d/1v/15c | `rule` | FElysiumNpc::SetModel | CNPC_VGhoulCroucher slot 105, 119 bytes, byte-identical to CNPC_VZombie's 0x103e0540 and ported as one body told apart by the arm's address. The Troika base 0x10298ce0 runs FIRST - so the model write, the hull and the eye all happen before IsMale is ever asked - then CBaseCombatCharacter IsMale selects m_iszVSoundGroup +0xc0 Zombie_Male or Zombie_Female, then +0xbc takes the literal 2 unconditionally, then 0x101f55a0 over DAT_1073dc28 with the create flag CLEAR resolves the group into +0xb4. |
| `0x1037f960` | CNPC_VHengeyokai::Precache | 115 | 15 | `CNPC_VHengeyokai#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VHengeyokai#104 only, 115 bytes: CAI_BaseNPCTroika::Precache (0x10298ad0) first, then a four-entry stomp sound table walked as 0x10 bytes in steps of 4 from PTR_s_character_monster_hengeyokai_stomp (0x1063bd94), then a three-entry exert table walked as 0xc bytes from 0x1063bda4, then PrecacheModel of the Hengeyokai model at 0x1063f444 with preload 0, then the Hengeyokai_freeze_emitter through DAT_1070b22c+0x44 with preload 0 rather than the 1 that Andrei, Chang and ManBat use, then UTIL_PrecacheOther item_w_hengeyokai_fist. Nothing in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VHengeyokai); relative to the base it chains 0x10298ad0 FIRST and then APPENDS the 0x10-byte stomp and 0xc-byte exert tables, the model with preload 0, the freeze emitter with preload 0 rather than the 1 Andrei, Chang and ManBat use, and item_w_hengeyokai_fist. |
| `0x10386560` | CNPC_VHuman::SelectScheduleRangedCombat | 802 | 15 | `CNPC_ProneDialog#605`, `CNPC_VAndreiBlood#605`, `CNPC_VBatSwarm#605`, +36 more | `+0x1b30`, `+0x1b34`, `+0x6444` | `+0x029c`, `+0x04cc`, `+0x0978`, `+0x0984`, +2 more | 1d/4v/6c | `rule` | FElysiumNpc::SelectScheduleRangedCombat | Ported as the shared CNPC_VHuman arm of slot 605, keyed on IsRetailClass and inherited by 35 other species; it replaces the Troika base wholesale and never chains it. It differs from 0x102b7fc0 in three places and no more: a cover-hint arm in second position (an empty m_pShootAtHint +0x6444 filled from slot 609 vt+0x984 with argument 0, and when it is STILL empty the arm is skipped entirely, otherwise not COND 0x2f gives 0xec, line 0x6e4); two extra helpers in step 4, so the order is 0x102b8620 then 0x102b7370 SelectDoorObstructionSchedule then 0x102b7cf0; and the 0x101e3f50 discipline gate applied to the enemy +0x9c rather than to this body. Lines 0x6cf, 0x6e4, 0x6e9, 0x6ee, 0x736, 0x73b, 0x70f, 0x713, 0x717, 0x720, 0x724, 0x728. Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x10388bb0` | CNPC_VHunter::IRelationType | 109 | 15 | `CNPC_VHunter#404` | — | — | 0d/1v/22c | `rule` | FElysiumNpc::IRelationType | CNPC_VHunter#404, a species arm of slot 404's port method and strictly smaller than the cop's: a null candidate answers 0 D_ER, the hunter class's own static grudge DAT_1093b650 with expiry _DAT_1093b658 answers 1 D_HT, and everything else chains 0x10299da0. NO player-side arms at all - no heightened-alert timer and no cops-in-pursuit count - so a hunter's extra hostility comes only from that one shared thirty-second timer. |
| `0x1038aec0` | CNPC_VManBat::Precache | 280 | 15 | `CNPC_VManBat#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VManBat#104 only, 280 bytes and the longest Precache here: CAI_BaseNPCTroika::Precache (0x10298ad0) first, then a four-entry manbat model table walked as 0x10 bytes in steps of 4 from 0x10640ce0 with preload 0; then four emitter precaches with preload 1 in order Manbat_screechcone_emitter, Manbat_player_emitter, HUD_Manbat_emitter, Manbat_blast_player; then three three-entry sound tables each walked as 0xc bytes from 0x10640d10, 0x10640d1c and 0x10640d28; then two single PrecacheScriptSound calls at 0x10642b2c and 0x10642afc; then sheriff_teleport_emitter precached TWICE in a row with preload 1, a retail duplicate that is kept; then UTIL_PrecacheOther item_w_manbat_claw. The first four emitter names are exactly the four the screech-cone body 0x1038e9c0 spawns. Nothing in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VManBat); relative to the base it chains 0x10298ad0 FIRST and then APPENDS the four-model table, four preload-1 emitters, three 0xc-byte sound tables, two single sounds, sheriff_teleport_emitter TWICE - a retail duplicate the arm keeps - and item_w_manbat_claw. |
| `0x1038e9c0` | FUN_1038e9c0 | 1283 | 15 | — | `+0x6680`, `+0x6684`, `+0x66a0`, `+0x66a8` | `+0x0364`, `+0x0370`, `+0x6684`, `+0x669c`, +3 more | 1d/0v/0c | `rule` | FElysiumNpc::ManBatStartScreechCone | no vtable slot; one direct caller, CNPC_VManBat::StartTask 0x1038c390. Order: when the handle at +0x66a4 still resolves, UTIL_Remove that emitter and null it; spawn Manbat_screechcone_emitter at slot 217 GetAbsOrigin, store it at +0x66a4, attach at bone Bip01 Jaw mode 1 and start it. Then only for a target that carries a PLAYER record at +0xa8 AND a combat view at +0x9c: UTIL_ScreenShake around MY OWN slot 220 GetOrigin with 2.5, 0.2, 3.0, 0.0, 0, 0; the push 0x10344f80(victim, targetOrigin minus myOrigin, 2, 0); then, only when the slow stamp +0x6684 still equals _DAT_1044fab0, BeginSlowEntity(victim, 500.0); restamp +0x6684 = curtime + RandomFloat(15.0, 25.0); cache the victim's handle into +0x6680; when +0x669c holds nothing spawn Manbat_player_emitter at the target's origin on Bip01 Spine mode 1 and start it; the same for Manbat_blast_player at +0x66a8; then when inventory slot 0 resolves an item and +0x66a0 is stale spawn HUD_Manbat_emitter attached to THAT ITEM at mode 0xe with an empty bone, start it, and set the spawned emitter's +0x4a1 byte to 1; finally OR bit 0 into the player's +0x2454. VALUE RECOVERED: _DAT_1044fab0 is a DOUBLE 0.0, since 103c1db6 is FCOMP double ptr. |
| `0x10392660` | CNPC_VMingXiao::Precache | 255 | 15 | `CNPC_VMingXiao#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VMingXiao#104 only, 255 bytes: CAI_BaseNPCTroika::Precache (0x10298ad0) first, then eleven emitter precaches through DAT_1070b22c+0x44 all with preload 0 in this order - Ming_xiao_slimetrail_emitter, Ming_xiao_slimetrail_emitter2, Ming_xiao_tentacle_damage_emitter, Ming_xiao_tentacle_burst_emitter, Ming_xiao_death_emitter, Ming_xiao_death_emitter2, Ming_xiao_death_proxy_emitter, Ming_xiao_death_proxy_emitter2, Ming_xiao_vomit_emitter, Ming_xiao_transform_emitter, Ming_xiao_transform_emitter2; then a single PrecacheScriptSound from the move table at 0x1064339c; then three UTIL_PrecacheOther calls for item_w_mingxiao_melee, item_w_mingxiao_tentacle and item_w_mingxiao_spit. Nothing in the port. CONVENTION: species arm of slot 104 keyed on IsRetailClass(CNPC_VMingXiao); relative to the base it chains 0x10298ad0 FIRST and then APPENDS eleven emitters ALL with preload 0, one move sound and three weapons. |
| `0x103967d0` | CNPC_VMingXiao::SelectScheduleRangedCombat | 794 | 15 | `CNPC_VMingXiao#605` | `+0x1b30`, `+0x1b34`, `+0x6444` | `+0x04cc`, `+0x0978`, `+0x0984`, `+0x6078`, +1 more | 0d/0v/6c | `rule` | FElysiumNpc::SelectScheduleRangedCombat | Ported as the CNPC_VMingXiao species arm of slot 605. Confirmed: the human skeleton with three changes - there is NO COND 0x3c arm at all, there is NO 0x101e3f50 discipline gate in front of the slot-606 branch, and the dodge decision is INLINED. CORRECTED on the inline: it is only the FIRST arm of 0x102b7f40 (SelectWeightedSequence(0x10,-1) non-zero AND not COND_STOP_BACKUP 0x2c AND RandomInt(0,99) < 0x4b) - the discipline arm and the COND 0x3c arm of that helper are NOT inlined, which is why this is not a call to it. Lines 0xb12, 0xb27, 0xb2c, 0xb6e, 0xb73, 0xb49, 0xb4d, 0xb51, 0xb58, 0xb5c, 0xb60. Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x1039c220` | CNPC_VMingXiaoTentacle::Precache | 262 | 15 | `CNPC_VMingXiaoTentacle#104` | `+0x6664`, `+0x6668`, `+0x666c` | `+0x0024`, `+0x0350` | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VMingXiaoTentacle slot 104: if slot 9 GetClassname/name string is empty it first SetModelName (vtable +0x350, slot 212) to "models/character/monster/mingxia..." (0x1064a340, NULLed when the literal is empty); then the Troika base Precache 0x10298ad0; then three PrecacheModel calls (engine +0x34, preload 0) whose returned model indices are stored to m_iModeIndexTentacleToGrub +0x6664 and m_iModeIndexGrub +0x6668 (both from 0x1064a2f0) and m_iModeIndexGrubToProxy +0x666c (from 0x1064a2a0); then PrecacheParticle (+0x44) for Ming_xiao_tentacle_transform, Ming_xiao_baby_transform and Ming_xiao_baby_death emitters and two sounds from 0x106477c8/0x106477cc. The three model indices are state later bodies read. |
| `0x103a03e0` | CNPC_VNewscaster::Precache | 47 | 15 | `CNPC_VNewscaster#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VNewscaster slot 104: the Troika base Precache 0x10298ad0 first, then two directory sound precaches through 0x101d0f10 over "sound/character/conversations/newscaster..." (0x1064aa08), once with the extension pattern DAT_10548ed4 and once with DAT_10598a30; 0x101d0f10 rejects a name whose first 3 chars match DAT_105a0410 or whose first char matches DAT_105a040c, then walks FindFirst/FindNext on "%s/*%s" and precaches each non-directory hit. No field writes. |
| `0x103a2930` | CNPC_VPedestrian::IRelationType | 58 | 15 | `CNPC_VPedestrian#404` | — | — | 0d/0v/22c | `rule` | FElysiumNpc::IRelationType | CNPC_VPedestrian#404, a species arm of slot 404's port method, three arms: a null candidate answers 0 D_ER; a candidate whose +0x9c NPC carries D_INSANE (m_bfAINPCFlags2 0x20000) answers 2 D_FR WITHOUT consulting the relationship table at all; everything else chains 0x10299da0. Pedestrians fear the insane, and dialogue and the flee schedules read that through slot 404. |
| `0x103a38c0` | CNPC_VPedestrian::CreateCorpse | 220 | 15 | `CNPC_VPedestrian#301` | — | `+0x026c`, `+0x0270`, `+0x6660`, `+0x666c` | 0d/0v/0c | `rule` | FElysiumNpc::PedestrianCreateCorpse | CNPC_VPedestrian slot 301: BEFORE the base it snapshots the collision OBB through m_Collision vtable +4 into m_vecPreDeathMins +0x6660 and +8 into m_vecPreDeathMaxs +0x666c; then CBaseCombatCharacter::CreateCorpse; then ThinkSet(NULL, 0.0, NULL), which CLEARS the think function; then SetSolid(SOLID_NONE) on the collision under a CBaseEntity::SetSolid scope-trace frame. The order is load-bearing - the base resizes the hull. NOT WIRED to slot 301: the Troika-line body 0x1032c0e0 is a layer-14 row with no verdict, so gen_kernel_shape still emits its stub and there is no port method to hang the species case on; the body lands under its recovered name and the gap is reported. |
| `0x103a6ab0` | CNPC_VSabbatLeader::Precache | 355 | 15 | `CNPC_VSabbatLeader#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VSabbatLeader slot 104: scope-trace push, Troika base Precache 0x10298ad0, PrecacheModel(+0x34, preload 1) for "models/character/monster/Andrei_transformed..." and "models/character/npc/unique/hollywood...", then a 7-entry sound table at 0x1064c480 (byte loop to 0x1c) and a 3-entry table at 0x1064c49c (to 0xc), then seven single Character_Monster_Andrei_Transformed sounds, PrecacheParticle(+0x44, 1) for Andrei_powerup_emitter and Andrei_blast_emitter, and UTIL_PrecacheOther 0x101d0ec0 for "item_w_sabbatleader_attack" (spawn by classname, call its slot 104, remove). ElysiumNpcKernelSounds.cpp:234 already carries the two tables as data; the models, emitters and weapon precache are not ported. |
| `0x103ae540` | CNPC_VSheriffMan::Precache | 178 | 15 | `CNPC_VSheriffMan#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VSheriffMan slot 104: scope-trace push, Troika base Precache 0x10298ad0, PrecacheModel("models/character/monster/manbat...", preload 1), then PrecacheParticle(+0x44, 1) for sheriff_landblast_emitter once and sheriff_teleport_emitter TWICE with the identical string 0x10642adc (a retail duplicate, reproduce it), then UTIL_PrecacheOther("item_w_sheriff_sword"). No field writes. |
| `0x103afdb0` | CNPC_VSheriffMan::SelectScheduleRangedCombat | 984 | 15 | `CNPC_VSheriffMan#605` | `+0x1b30`, `+0x1b34` | `+0x04cc`, `+0x6078` | 0d/0v/6c | `rule` | FElysiumNpc::SelectScheduleRangedCombat | Ported as the CNPC_VSheriffMan species arm of slot 605, the longest of the six because it offers the three helpers as three separate tests rather than one chained condition. Its two own differences: NO slot-606 arm at all - where every other body reaches vt+0x978, the sheriff branch is COND_SEE_ENEMY and not 0x48 and not 0x60 which DECLINES (103aff56), otherwise COND_ENEMY_UNREACHABLE giving 0x15a (line 0x314), else 0xe8 (0x31a), and 0x15a is a number no other slot-605 body names; and the same inlined first-arm-only dodge as Ming Xiao at 0x4b. Lines 0x2b1, 0x2cb, 0x2d0, 0x314, 0x31a, 0x2ed, 0x2f1, 0x2f5, 0x2fc, 0x300, 0x304. Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x103b41e0` | CNPC_VTest::Precache | 194 | 15 | `CNPC_VTest#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VTest slot 104: precaches death1, alert1, idle1, then the 4-entry pain table at 0x10652194 (byte loop to 0x10), then fear1, lostenemy1, foundenemy1, surprise1 (called with ONE argument, a retail stack quirk) and knockout1, and only THEN the Troika base Precache 0x10298ad0 with a NULL argument. The base-last ordering is this body's distinguishing fact; ElysiumNpcKernelSounds.cpp:216 already carries the eight lists as data but nothing runs the precache or the ordering. |
| `0x103b8fa0` | CNPC_VTzimisce::Precache | 84 | 15 | `CNPC_VTzimisce#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VTzimisce slot 104: a 6-entry spiderchick sound table at 0x106530fc (byte loop to 0x18), a 3-entry table at 0x10653114 (to 0xc), UTIL_PrecacheOther 0x101d0ec0 for "item_w_tzimisce_melee", and only then the Troika base Precache 0x10298ad0 with the incoming argument. Base-last, like CNPC_VTest and unlike its siblings. Oracle: docs/vtmb/footsteps.md:902. |
| `0x103c1400` | CNPC_VTzimisceHeadClaw::Precache | 224 | 15 | `CNPC_VTzimisceHeadClaw#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VTzimisceHeadClaw slot 104: Troika base Precache 0x10298ad0 first, then PrecacheParticle(+0x44, 1) for Tzim2_powerup_emitter, Tzim2_blast_emitter, Tzim2_player_emitter and HUD_Tzim2_emitter (the four the slot-332 grab body later spawns by name), then three sound tables at 0x1065ca68 (to 8), 0x1065ca70 (to 8) and 0x1065ca78 (to 0xc), then the two single Slug sounds 0x1065d474 and 0x1065d43c, then UTIL_PrecacheOther for "item_w_tzimisce2_claw" and "item_w_tzimisce2_head". |
| `0x103c1d80` | CNPC_VTzimisceHeadClaw::vfunc332 | 956 | 15 | `CNPC_VTzimisceHeadClaw#332` | `+0x6674`, `+0x6678` | `+0x0364`, `+0x6678` | 0d/0v/0c | `rule` | FElysiumNpc::Slot332 | CNPC_VTzimisceHeadClaw slot 332 (the base 0x1014f890 is return;). OFFSETS CORRECTED from the listing: m_flSlowedExpire is +0x6678 (103c1db0), m_hSlowedEntity +0x6674 (103c1e02), the player emitter +0x667c (103c1dfc) and the HUD emitter +0x6680 (103c1ff2) - the earlier walk swapped the first two and shifted the last two. Guarded on a target that carries a PLAYER record at +0xa8 AND a combat view at +0x9c, so slot 332 is player-only. Steps: BeginSlowEntity(victim, 500.0) only while the expiry equals the DOUBLE 0.0 _DAT_1044fab0; m_flSlowedExpire = curtime + RandomFloat(5.0, 8.0), the bounds RECOVERED at 103c1dd5 as PUSH 0x41000000 then PUSH 0x40a00000; latch the victim's handle; with the particle handle dead spawn Tzim2_player_emitter at the victim's slot-217 origin, attach at bone Bip01 Spine mode 1 and start it; emit Character/Monster/TC_FatGuy/Sluge_Hit.wav on channel 4 then Sluge_Affected.wav on channel 3, both volume 1.0 attenuation 0.8 pitch 100 from the victim's slot-222 origin - BOTH STRINGS RECOVERED, the walk named neither; with inventory slot 0 holding an entity and the HUD handle dead spawn HUD_Tzim2_emitter attached to THAT ITEM at mode 0xe with an empty bone; finally the 2-D distance between the two slot-217 origins at or above _DAT_104cd108 raises SetCondition(0x35). VALUE RECOVERED at 0x4cd108: a DOUBLE 240.0, since 103c20dc is FCOMP double ptr. |
| `0x103c31e0` | CNPC_VTzimisceRunner::Precache | 137 | 15 | `CNPC_VTzimisceRunner#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VTzimisceRunner slot 104: Troika base Precache 0x10298ad0 first, then four sound tables walked as byte loops -- 0x1065d680 to 8 (2 entries), 0x1065d688 to 8 (2), 0x1065d690 to 0x10 (4 breath) and 0x1065d6a0 to 0xc (3 exert) -- then UTIL_PrecacheOther for "item_w_tzimisce3_claw". No field writes. |
| `0x103cb2a0` | CNPC_VWerewolf::Precache | 298 | 15 | `CNPC_VWerewolf#104` | `+0x00b4`, `+0x00bc`, `+0x00c0` | `+0x026c` | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VWerewolf slot 104: scope-trace push carrying GetDebugName from m_iName +0x26c ("NULL_ENTITY" when this is null); Troika base Precache 0x10298ad0; two directory sound precaches (0x101d0f10, pattern DAT_10598a30) over "sound/Character/Monster/Werewolf" and "sound/Area/Special/Observatory"; then three state writes -- +0xbc = 2, m_iszVSoundGroup +0xc0 = "Werewolf" (0x106618d8, NULLed when the literal is empty), and the sound-group table lookup 0x101f55a0(&DAT_1073dc28, this, group, 0) into +0xb4; then the 4-entry footstep table at 0x1065f4d0 (to 0x10), UTIL_PrecacheOther("item_w_werewolf_attacks"), and the two single sounds dev_ww_tele_out.wav and dev_ww_tele_in.wav. The +0xb4/+0xbc/+0xc0 triple is the same sound-group binding CNPC_VZombie::SetModel makes. |
| `0x103dd880` | CNPC_VYukie::IRelationType | 20 | 15 | `CNPC_VYukie#404` | — | — | 0d/0v/22c | `rule` | FElysiumNpc::IRelationType | CNPC_VYukie#404, a species arm of slot 404's port method, read off the listing because the C hides the return: MOV EAX,[ESP+4] then TEST EAX,EAX then JNZ, and a null candidate RETs with EAX still holding that null, i.e. 0 D_ER; anything else is JMP 0x10001adc, a tail jump to 0x10299da0 unchanged. The whole species override is the null guard. |
| `0x103df120` | CNPC_VZombie::Precache | 53 | 15 | `CNPC_VZombie#104` | — | — | 0d/0v/11c | `rule` | FElysiumNpc::Precache | CNPC_VZombie slot 104: Troika base Precache 0x10298ad0 first, then PrecacheParticle(+0x44, 0) for zombie_headshot_death_emitter and zombie_headshot_dmg_emitter, then UTIL_PrecacheOther 0x101d0ec0 for "item_w_zombie_fists". The two headshot emitters are the assets the zombie head-damage arm names. |
| `0x103e0540` | CNPC_VZombie::SetModel | 119 | 15 | `CNPC_VZombie#105` | `+0x00b4`, `+0x00bc`, `+0x00c0` | `+0x00c0` | 0d/0v/15c | `rule` | FElysiumNpc::SetModel | CNPC_VZombie slot 105, the same 119 bytes as CNPC_VGhoulCroucher's 0x1037b1f0 and the same one port body. Same three writes CNPC_VWerewolf Precache makes, rebound on every model change rather than once at precache. |
| `0x103e0fa0` | CNPC_VZombie::vfunc509 | 166 | 15 | `CNPC_VZombie#509` | — | `+0x06fc`, `+0x07ec`, `+0x07f8`, `+0x0fe8`, +2 more | 0d/0v/1c | `rule` | FElysiumNpc::ShouldPlayIdleSound | CNPC_VZombie#509, ported as the species arm of slot 509's one port method. It REPLACES the Troika body 0x10294040 wholesale - no IsInDialog refusal, no m_NPCState test and no SF_NPC_GAG test at all - and its own arms are m_bIsBCCTargetable clear, a live m_hDialogPartner +0xfe8, IsBusyWithDiscipline, then a weight of 999 that drops to 20 (a 1-in-21 roll) when the running schedule's slot-447 local id is 0x12f, in which case the float-sound arm is SKIPPED; otherwise slot 510 true plays slot 507 and returns FALSE, and only then does RandomInt(0, weight) equal to 0 decide. |
| `0x101aabf0` | CPayphone::NPCThink | 205 | 16 | `CPayphone#431` | `+0x017c`, `+0x06f0`, `+0x06f8` | `+0x03e8`, `+0x0fe8`, `+0x0ff0` | 0d/0v/0c | `rule` | FElysiumNpc::PayphoneThink | Slot 431, CPayphone#431 only; ported as FElysiumNpc::PayphoneThink, a prologue FElysiumNpc::Think dispatches and returns on, because CPayphone's slot 431 REPLACES the Troika NPCThink outright and never calls it. Listing order: slot 250 StudioFrameAdvance(0.0) with its float discarded (101aac07 FSTP ST0); then if m_hDialogPartner +0xfe8 resolves live, the dialogue upkeep tick 0x102c1400 unconditionally, then ONLY on an activity change SetIdealActivity(partner) + m_nSequence +0x6f0 = SelectWeightedSequence(that,-1) + ResetSequenceInfo 0x10090950, then on EVERY pass m_flCycle +0x6f8 = the partner's, then m_flNextThink = curtime + _DAT_10450aa4, and return; otherwise IsInDialog 0x102c1170 gates only the tick, SetIdealActivity(1 = ACT_IDLE) runs unconditionally, and m_flNextThink = curtime + _DAT_1044bef8. CORRECTIONS: +0xff0 is m_IdealActivity, not m_Activity, and +0x6f8 is m_flCycle (vtmb_fields CAI_BaseNPCTroika), so the mirror copies the partner's animation PHASE every pass and that is why the arm clocks at 0.01 s against the idle arm's 0.25 s. RDATA read out of the pinned vampire.dll: _DAT_10450aa4 = 0.01f (file offset 0x450aa4) and _DAT_1044bef8 = 0.25f (0x44bef8). SEAM: m_hDialogPartner has no writer in this runtime (the port carries the partner as the open session, not a handle), so the field is declared at its retail offset and answers nothing, which takes retail's own no-partner arm. 0x102c1400 is counted, not ported: it is no row of this family. |
| `0x10268900` | CAISound::FUN_10268900 | 1212 | 16 | `CAISound#185`, `CAI_BaseHumanoid#185`, `CAI_BaseNPC#185`, +79 more | — | `+0x0098`, `+0x00a8`, `+0x02e8` | 0d/0v/0c | `rule` | hand:FElysiumNpc::FireBullets | Slot 185, the shared FireBullets body, 1212 bytes. Order: 1 m_pBaseNPCTroika (+0x98) set, decrement its m_iFakeReloadCount (+0x65f0); 2 info+0x58 = GetAmmoDef()->Flags(info+0x8c) through DAT_1070ba0c vt+0xdc then 0x104276a0, which answers 0 for an out-of-range index; 3 default info+0x94 attacker to this; 4 push the two trace filters 0x101c2c60 and 0x101c2c30 and set DAT_1072cb48 = flags or 0x1000; 5 CORRECTION: 0x10138a90 is VectorVectors, not AngleVectors - it builds right = normalize(f.y, -f.x, 0) and up = normalize(cross(right, f)) from the SHOOTING DIRECTION at info+0x14, with a degenerate arm for f.x == f.y == 0 that answers right (1,0,0) and up (0, -f.z, 0) unnormalized; 6 open a per-victim tally; 7 for each of info[0] repeats, for each of info[1] bullets: copy the forward into info+0x20, add the UNSCALED spread offset 0x10268170 unless the ammo flag 0x2000000 is set - CORRECTION: +0xa8 is m_pPlayer, so for an NPC shooter the flag alone decides and the player arm is dead - then endpoint info+0x2c = info+0x08 plus dir times info+0x44; read the local player's type-3 stat list stat 3 (0x101cda50 then 0x102012d0, falling back to an empty CVStatList_t that answers 0), and when info+0xa0 bit 0 is set or the stat is above 2 and the shooter is not the player, latch the tracer flag and divide info+0xa4 by DAT_104994c8 (1.0 1.0 1.0 1.1 1.3 1.5, read out of the pinned image) indexed by the stat clamped to 0..5; 8 slot 186 (vt+0x2e8, whose whole retail body is return false) gates the emission: the tracer arm fires only when the flag latched AND info+0xa8 names a non-empty tracer, with 1.0/info[1] as the fraction, and REPLACES the trace pass; otherwise 0x10267b60 traces and info+0xbc is tallied; 9 after EACH repeat every tallied victim takes RangedDamagePerVictim 0x10268330 with hits/info[1] - the tally is never cleared, so repeat 2 re-pays repeat 1's victims; 10 0x10160560 on m_pPlayer, unreachable for an NPC. Ported verbatim with the ammo def, the stat join, the trace pass, the tracer effect and RangedDamagePerVictim as recording seams. |
| `0x102725d0` | CAI_BaseNPC::SetActivity | 95 | 16 | `CAI_BaseHumanoid#310`, `CAI_BaseNPC#310`, `CAI_ExpressiveNPC#310`, +10 more | `+0x0ff0` | `+0x0fec`, `+0x0ff0`, `+0x5ccc`, `+0x5cd0`, +1 more | 1d/2v/14c | `rule` | FElysiumNpc::BaseSetActivity | CAI_BaseNPC#310 base body, a distinct retail function beside the Troika override 0x10295750 that owns slot 310, so it is ported under its own name as wave 1's BasePrecache was. Two refusals then three writes: return at once when m_Activity already equals the request, and return when m_Activity is 2 ACT_TRANSITION unless the request is 0 ACT_RESET; otherwise m_IdealActivity is the request, ResolveActivityToSequence 0x10272130 fills the ideal triple, and SetActivityAndSequence commits it - re-reading m_IdealActivity rather than using the argument. |
| `0x102993c0` | CAI_BaseNPCTroika::Save | 656 | 16 | `CAI_BaseNPCTroika#126`, `CGeneric_NPC#126`, `CNPCMaker#126`, +58 more | — | `+0x607c`, `+0x6084`, `+0x60b0`, `+0x60dc`, +17 more | 3d/0v/0c | `rule` | hand:FElysiumNpc::Save | Slot 126, the Troika Save. It brackets CAI_BaseNPC::Save 0x1027bc60 with a sentinel encode 0x101cf250 and decode 0x101cf2f0 over eleven FIELD_TIME stamps and nine CAISound expiry stamps. Encode order and modes, from the listing's PUSH sequence at 102993c9..10299485: m_flCanSeekCoverTimer +0x607c mode 3, m_flSeeUnknownCheatVisionTime +0x6084 mode 2, m_flMeleeHeightDiffTimer +0x6274 mode 2, m_flOccludedReportTimeE/T/W +0x62cc/+0x62d0/+0x62d4 mode 3, m_flInterruptTime +0x632c mode 3, m_flNextInterestChangeTime +0x63d4 mode 2, m_flWeaponScareTime +0x63dc mode 2, m_flIgnoreCollisionTimer +0x6458 mode 4, m_flEyeFidgetTime +0x657c mode 4; then the nine CAISound +0x10 m_flExpireTime stamps at mode 2 through 0x101b9840, in order m_BestSound +0x60b0, m_InvestigateSound +0x60dc, m_LastSoundDanger +0x6108, m_LastSoundPhysicsDanger +0x6134, m_LastSoundCombat +0x6160, m_LastSoundBulletImpact +0x618c, m_LastSoundPlayer +0x61b8, m_LastSoundWorld +0x61e4, m_LastSoundFlinch +0x6210. CORRECTION: the decompiled C's decode pass renders its eleven pointers one slot out and its nine sound pointers two slots out through EBX and stack aliasing, but the pushed MODES at 1029956f..102995e9 read 3,2,2,3,3,3,3,2,2,4,4, identical to the encode list, so the decode walks the SAME fields in the SAME order with the SAME modes and nothing is shifted in the ported body. Between the passes: ISave vt+0x30 writes one bool (m_pPedestrianLink +0x630c != 0) and, when it is set, ISave vt+0x28 writes the two ints at +0x630c+4 and +0x630c+8. The return is the base body's result, stashed at [ESP+0x60] before the write pass and reloaded at 10299642. Ported as story 29d family SaveRestore10, with its four species arms as an OverrideOf dispatch inside the one method. |
| `0x102c0220` | CAI_BaseNPCTroika::FUN_102c0220 | 57 | 16 | `CAI_BaseNPCTroika#35`, `CGeneric_NPC#35`, `CNPCMaker#35`, +60 more | — | `+0x049c` | 0d/0v/0c | `rule` | hand:FElysiumNpc::Slot35 | slot 35, three arms: IsInDialog 0x102c1170 true answers 0; else slot 295 CanTalk - dispatched WITH the caller's argument (102c0232 PUSH ECX where ECX is [ESP+8]; the decompiled C drops the push) - false answers 0; else 102c024d computes 14 + (IsMale != 0), so 15 male and 14 female. A small id beside slot 34 GetHighlightMaterial, not a boolean. |
| `0x1034e320` | CScriptedTarget::Save | 53 | 16 | `CScriptedTarget#126` | — | `+0x5f64` | 0d/0v/0c | `rule` | FElysiumNpc::Save | CScriptedTarget slot 126: it brackets CAI_BaseNPC::Save 0x1027bc60 with the mode-3 sentinel encode 0x101cf250 and decode 0x101cf2f0 of exactly one field, m_flPauseDoneTime +0x5f64 (mode 3 = the value exactly equal to _DAT_104454c4 becomes 1e+11 on the way out and 0.0 on the way back), and returns the base's result. Same mechanism as the Troika Save 0x102993c0 over a single timer. |
| `0x103642f0` | CNPC_VBach::SelectScheduleRangedCombat | 413 | 16 | `CNPC_VBach#605` | `+0x1b30`, `+0x1b34`, `+0x6444`, `+0x6690` | `+0x0970`, `+0x5cc0` | 0d/0v/6c | `rule` | FElysiumNpc::SelectScheduleRangedCombat | Ported as the CNPC_VBach species arm of slot 605, and the one arm of the six that CHAINS: its classname tests fall through to the CNPC_VHuman body 0x10386560 through a direct non-virtual call and its own tail then rewrites that answer. COND 0x7b stamps +0x6690 with curtime + _DAT_10463584 (15.0) and answers 0x15a (line 700 decimal); GetActiveWeapon is fetched ONCE and COND 0x7a tested ONCE before the split; with no weapon COND 0x7a gives 0x158 (0x2da) and COND 0x79 gives 0x159 (0x2de); with a weapon and COND 0x7a a classname that is not item_w_katana gives 0x158 (0x2c8) and the katana itself DISPATCHES slot 604 (vt+0x970) and returns its answer; COND 0x79 with a classname that is not item_w_rem_m_700_bach gives 0x159 (0x2d3); then the human body; then UNCONDITIONALLY an m_NPCState +0x5cc0 that is neither 4 nor 0xc clears +0x6444; then a human answer of 0 with COND_SEE_ENEMY standing is rewritten to 0x15f (0x2ed). Conditions 0x79, 0x7a and 0x7b live above the base registrar 0x76 in a Bach-line table the census does not carry and are carried by NUMBER. Walked into docs/vtmb/npc-ai/conditions-and-states.md. |
| `0x10371a90` | CNPC_VCop::UpdateOnRemove | 51 | 16 | `CNPC_VCop#180` | `+0x0001`, `+0x6671` | `+0x0001`, `+0x6671` | 0d/0v/1c | `rule` | FElysiumNpc::UpdateOnRemove | CNPC_VCop slot 180, read off the listing (the C mis-renders the two bytes as this+1): if m_bCountedAlive +0x6671 is set, decrement the live-cop census DAT_1093acac; clear +0x6671; if +0x6672 is set, decrement the second census DAT_1093acb0; clear +0x6672; then tail-jump to the Troika UpdateOnRemove 0x1028d6e0. Both counters are program-visible: DAT_1093acac is written by CNPC_VCop::Spawn and read by CNPC_VCop::SelectSchedule and 0x103707e0/0x10370850, DAT_1093acb0 by CNPC_VCop::OnStateChange and SelectSchedule. |
| `0x10371ae0` | CNPC_VCop::vfunc472 | 54 | 16 | `CNPC_VCop#472` | — | `+0x0650`, `+0x6081` | 0d/0v/0c | `rule` | FElysiumNpc::OnSeeEntity | CNPC_VCop slot 472: when +0x6081 is clear and slot 404 IRelationType(seen) returns 1 (D_HT), 0x10370560 stamps the global cop suspect state -- _DAT_1093aca8 = curtime + _DAT_104492a8 and DAT_1093ac3c = the seen entity's handle -- but only when the seen entity has a non-null +0xa8; then the Troika OnSeeEntity 0x102b3e00 runs unconditionally. The same two globals are what CNPC_VCop::DrawDebugGeometryOverlays 0x10372f00 reads back to label a suspect. Oracle: docs/vtmb/npc-ai/senses.md:402. |
| `0x10372f00` | CNPC_VCop::DrawDebugGeometryOverlays | 1020 | 16 | `CNPC_VCop#123` | — | `+0x0224`, `+0x0270`, `+0x0364`, `+0x0650`, +2 more | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugGeometryOverlays | CNPC_VCop slot 123, debug-only but the arms expose the cop model: gated on m_debugOverlays +0x224 bit 0 AND m_hClosestPlayer +0x628c resolving; it bails when the collision OBB mins equal its maxs (+0x270 vtable +4 and +8), computes a text height of (maxZ - minZ) + _DAT_1045597c above GetAbsOrigin (slot 217), then switches on slot 404 IRelationType(closest player): case 1 (D_HT) re-runs the Troika IRelationType, prints DAT_105cc520, appends " Suspect" when that player equals DAT_1093ac3c and curtime is still below _DAT_1093aca8, appends " Alert" when the player's +0xa8 has curtime below its +0x1d1c (0x1017f8d0), appends " Count %d" when its +0x1d10 (0x1017f770) is positive, and appends " Pursuit" when the player equals m_hPursuitPlayer; case 2 prints DAT_105cc518, case 3 DAT_105cc510, case 4 DAT_105cc508, default DAT_10636728; then appends " %d %d" and draws; finally the Troika base overlay 0x1029ca50 always runs. |
| `0x10374ad0` | CNPC_VDog::NPC_EarlyTranslateActivity | 21 | 16 | `CNPC_VDog#375` | — | — | 0d/0v/5c | `rule` | FElysiumNpc::NPC_EarlyTranslateActivity | CNPC_VDog slot 375, 21 bytes: ACT_FIDGET 3 is preserved by returning with EAX still holding the request - one early return the Troika base never sees - and every other activity forwards to 0x10295590. Ported as a species arm of slot 375's one port method; the visual anim path's PreTranslate_Dog row carries the same rule as DATA and now answers the same live state through FElysiumNpc PreTranslatePredicate. |
| `0x103766d0` | CNPC_VFrenzyShadow::BestEnemy | 793 | 16 | `CNPC_VFrenzyShadow#478` | `+0x6664`, `+0x6668` | `+0x029c`, `+0x0324`, `+0x0364`, `+0x0654`, +6 more | 0d/0v/2c | `rule` | FElysiumNpc::BestEnemy | CNPC_VFrenzyShadow slot 478. Sticky arm first: with m_iHostileEnemyCount +0x6664 above 1, if the current GetEnemy (slot 167) has a +0x9c combat view whose GetFlags bit 15 is clear, whose +4 field_0x340 is set, whose slot 158 is true and whose slot 404 IRelationType against m_hFriendPlayer's +0xa8 is 1 (D_HT), and that enemy is neither eluded (IsEluded 0x102e0210 against GetEnemies slot 541) nor unreachable (slot 530), return it unchanged. Otherwise a full rescan: best = null, bestDist = 0x10000000, bestScore = 0, m_iHostileEnemyCount = 0; walk the memory list at GetEnemies()+0xc, resolve each +0x24 handle, take its +0x9c view, apply the same flags/alive/slot-158/not-eluded filter, then score -- not unreachable adds 0x40000000; IRelationType == 1 adds 0x20000000 and increments m_iHostileEnemyCount; either the senses' DidSeeEntity (0x1030fb10 on m_pSenses +0x60ac) or slot 201 FVisible(cand, 0x2804091, 0, 0) adds 0x10000000; slot 405 IRelationPriority multiplied by 0x1000000 is added last. A strictly higher score wins if slot 479 IsValidEnemy passes; an EQUAL score wins only when the 2D distance between origins (slot 217, __ftol of the squared-distance root) is strictly less than the incumbent's and IsValidEnemy passes. On exit, if the winner differs from the current GetEnemy it clears m_bFailedGrapple +0x6668. |
| `0x10383560` | CNPC_VHengeyokai::DrawDebugTextOverlays | 106 | 16 | `CNPC_VHengeyokai#124` | — | `+0x0024`, `+0x0224`, `+0x02e0` | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugTextOverlays | CNPC_VHengeyokai slot 124: the Troika base text overlay 0x1029d4e0 returns the next free line; then when m_debugOverlays +0x224 bit 0 is set it takes slot 9's string (NULL becomes the empty string DAT_106b8540), prints it through the engine text call (DAT_1070b22c +0x8c) at the entity text index +0x2e0 on that line in white (0xff,0xff,0xff,0xff), pops the message and returns line + 1. The return value is the line budget every later overlay body consumes, so the +1 is the contract. |
| `0x103854f0` | CNPC_VHuman::NPC_EarlyTranslateActivity | 565 | 16 | `CNPC_ProneDialog#375`, `CNPC_VAndreiBlood#375`, `CNPC_VAsianVampire#375`, +36 more | `+0x6410` | `+0x0804`, `+0x14b8`, `+0x14bc`, `+0x5b84`, +3 more | 1d/0v/5c | `rule` | FElysiumNpc::NPC_EarlyTranslateActivity | CNPC_VHuman slot 375, 565 bytes, 39 census classes, ported as the species arm of slot 375's one port method. CONFIRMED at the listing: for m_NPCState 3 or 0xb, goto LAB_10385611 - which leaves m_bAggressiveAnims CLEAR - requires (the state cvar dead OR its +0x2c int zero) AND m_afMemory bit 0x8000000 clear, so the flag is SET when the cvar is live and non-zero OR the memory bit stands. Also recovered: the outer four-term OR needs capability 0x40 AND cvar DAT_10924f74 live and non-zero AND m_bfAINPCFlags2 0x400; m_bfAINPCFlags 0x10000 FORCE_RELAXED_ANIMS beats everything below it; m_bfNPCFrenziedFlags 0x200 skips the state ladder and SETS the flag; state 2 sets it; and the no-weapon-or-NODRAW arm clears the flag and returns through the Troika body without touching either rewrite block. Aggressive clear rewrites 9 to 0x16 and 0x13 to 0x17 through 0x10295590; aggressive set rewrites 1 to 5 only with an active weapon whose slot 360 answer carries 0x6000, plus the six fixed pairs 0x3b to 0x3d, 0x3c to 0x3e, 0x9d to 0x9f, 0x9e to 0xa0, 0xa1 to 0xa3 and 0xa2 to 0xa4. |
| `0x103887d0` | CNPC_VHunter::vfunc472 | 54 | 16 | `CNPC_VHunter#472` | — | `+0x0650`, `+0x6081` | 0d/0v/0c | `rule` | FElysiumNpc::OnSeeEntity | CNPC_VHunter slot 472, the twin of CNPC_VCop's 0x10371ae0 over its own globals: when +0x6081 is clear and slot 404 IRelationType(seen) returns 1 (D_HT), 0x10387fd0 sets _DAT_1093b658 = curtime + _DAT_104492a8 and DAT_1093b650 = the seen entity's handle (with no +0xa8 guard, unlike the cop body); then the Troika OnSeeEntity 0x102b3e00 always runs. Oracle: docs/vtmb/npc-ai/senses.md:402. |
| `0x1038f020` | FUN_1038f020 | 493 | 16 | — | `+0x6684`, `+0x66a0`, `+0x66a8` | `+0x6680`, `+0x6684`, `+0x669c`, `+0x66a0`, +1 more | 2d/0v/0c | `rule` | FElysiumNpc::ManBatReleaseSlowedEntity | CNPC_VManBat's slowed-grab teardown, no slot; RunAI 0x1038e990 calls it with force 0 and Event_Killed 0x1038e8c0 with force 1. It runs only while 0x1038f290 says m_flSlowedExpire +0x6684 is above _DAT_1044fab0 (the DOUBLE 0.0) AND either force is set or the expiry has reached curtime; then it zeroes +0x6684, calls EndSlowEntity(victim's +0x9c, 500.0) only while the handle resolves with a combat view, UTIL_Removes the three effects at +0x669c, +0x66a8 and +0x66a0 in that order setting each to -1, clears bit 0 of the victim's +0xa8 +0x2454 word, and finally sets m_hSlowedEntity to -1 OUTSIDE that guard. The gate is family Misc's already-ported SlowedExpire, called rather than restated. |
| `0x10391230` | CNPC_VMingXiao::UpdateOnRemove | 26 | 16 | `CNPC_VMingXiao#180` | — | `+0x673c` | 0d/0v/1c | `rule` | FElysiumNpc::UpdateOnRemove | CNPC_VMingXiao slot 180: when m_eThrowableObjectMode +0x673c is non-zero it first drops the carried throwable through 0x10398fd0 -- UTIL_Remove the entity at the +0x6738 handle and set it to -1, set +0x6718 to -1, call 0x102c43b0(this, 0.75) and 0x10398d90(this, 0) -- then always the Troika UpdateOnRemove 0x1028d6e0. Without the drop the thrown prop outlives the boss. |
| `0x10399d40` | CNPC_VMingXiao::DrawDebugGeometryOverlays | 328 | 16 | `CNPC_VMingXiao#123` | — | `+0x0224`, `+0x0370` | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugGeometryOverlays | CNPC_VMingXiao slot 123: gated on m_debugOverlays +0x224 bit 0x20000000 (not bit 0 like its siblings), it draws four rings through +0x370 about the axis (1.0, 0.0, 0.0) at radii 100.0, 150.0, 200.0 and 300.0 with height 8.0 and colour (255, 32, 32, 128) and no depth test, popping the message after each; then the Troika base overlay 0x1029ca50 always runs. The four radii are MingXiao's recovered range bands and are the reason to port the body at all. |
| `0x103a03a0` | CNPC_VNewscaster::UpdateOnRemove | 16 | 16 | `CNPC_VNewscaster#180` | — | — | 0d/0v/1c | `rule` | FElysiumNpc::UpdateOnRemove | CNPC_VNewscaster slot 180: 0x103a0d50 tears the two story queues down first -- for each row of the main-story array (base +0x665c, count +0x6668, stride 0x28) it frees the row's first pointer and then the pointer pair at +4/+8 stepping by 8 up to 0x20, memmoves the tail down and decrements the count, then does the same for the side-story array (base +0x6670, count +0x667c), and finally clears the story-active byte +0x6690 -- then the Troika UpdateOnRemove 0x1028d6e0. The port already carries the cursors and the active flag as species words (ElysiumNpcKernelSpecies.inl:96-99); the teardown ordering is not ported. |
| `0x103a1250` | CNPC_VNewscaster::DrawDebugTextOverlays | 151 | 16 | `CNPC_VNewscaster#124` | — | `+0x026c` | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugTextOverlays | CNPC_VNewscaster slot 124: scope-trace push carrying GetDebugName from m_iName +0x26c ("NULL_ENTITY" when this is null, the empty string when the name is null); then the Troika base text overlay 0x1029d4e0 returns the first free line; then when m_debugOverlays +0x224 bit 0 is set it adds the lines 0x103a0ff0 prints (the "not playing VCD" case and the Main Stories / Side Stories queues) and returns base + that count, so the two bodies share one line budget. |
| `0x103ba290` | CNPC_VTzimisce::FVisible | 30 | 16 | `CNPC_VTzimisce#201` | — | — | 0d/0v/7c | `rule` | FElysiumNpc::FVisible | CNPC_VTzimisce slot 201: it forwards to the Troika base FVisible 0x102b4630 passing the caller's first three arguments unchanged but the FOURTH argument FORCED to 0, discarding whatever the caller supplied. The whole species override is that one-argument clamp, and it is observable -- every Tzimisce visibility test runs the base with that word zeroed. |
| `0x103bde40` | CNPC_VTzimisce::NPC_EarlyTranslateActivity | 102 | 16 | `CNPC_VTzimisce#375` | — | `+0x6688` | 0d/2v/5c | `rule` | FElysiumNpc::NPC_EarlyTranslateActivity | CNPC_VTzimisce slot 375. The polarity is the ZERO test: 0x103be130 gates on m_bfAINPCFlags +0x14b8 bit 5 CARRYING_BODY and, under it, m_bHeavyBodyTarget +0x6688 CLEAR returns 0xfd for request 1 and 0xff for 9 or 0x13 while SET returns 0xfc and 0xfe. The generated table's ids were right and its BodySideLeft predicate documented the opposite polarity; the generator's comment is corrected with this body. Every other request and the whole un-flagged case fall to the Troika pre-translate 0x10295590. |
| `0x103c08d0` | CNPC_VTzimisce::DrawDebugTextOverlays | 387 | 16 | `CNPC_VTzimisce#124` | — | `+0x0224`, `+0x02e0`, `+0x0364`, `+0x5c38`, +1 more | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugTextOverlays | CNPC_VTzimisce slot 124: the Troika base text overlay 0x1029d4e0 gives the first free line; then under m_debugOverlays +0x224 bit 0 the distance starts at _DAT_104454c4 and, when m_hPickupTarget +0x6670 resolves live, becomes the full 3D distance between that target's origin and mine (slot 217 on both, sqrt of dx*dx+dy*dy+dz*dz); when 0x103be130's carry bit is set AND that distance exceeds _DAT_1047a3ac it latches the distance into the global _DAT_1093d01c and strcpys the current schedule's name (+0x5c38 then +0x40) into the global buffer DAT_1093cd70; then Q_snprintf into a 0x200 buffer with "Body: %5.1f %5.1f %s" (live distance, latched distance, latched schedule name), prints at the entity text index +0x2e0 in white and returns line + 1. The two globals are a cross-NPC latch, not per-instance state. |
| `0x103c2230` | FUN_103c2230 | 494 | 16 | — | `+0x6674`, `+0x6678`, `+0x667c`, `+0x6680` | `+0x6674`, `+0x6678`, `+0x667c`, `+0x6680` | 2d/0v/0c | `rule` | FElysiumNpc::TzimisceHeadClawEndSlow | gate 0x103c24a0 - act only while m_flSlowedExpire +0x6678 is above 0.0 - then when the caller forces it or the timer has reached curtime: zero +0x6678; and ONLY while m_hSlowedEntity +0x6674 resolves with a +0x9c combat view, EndSlowEntity(victim, 500.0), set the handle to 0xffffffff and emit Character/Monster/TC_FatGuy/Sluge_Affected.wav on channel 3 from the VICTIM's slot 222 GetSoundEmissionOrigin at attenuation 0.8 - so a stale handle leaves m_hSlowedEntity standing. Then UTIL_Remove 0x101cd940 the two owned effects at +0x667c and +0x6680, each only while its handle's serial matches its slot, setting both to 0xffffffff. STRING CORRECTED: 0x1065d43c is Sluge_Affected.wav, not Sluge_Affect, and it is the same wav slot 332's second emit uses. |
| `0x103c3e10` | CNPC_VTzimisceRunner::NPC_EarlyTranslateActivity | 82 | 16 | `CNPC_VTzimisceRunner#375` | — | `+0x6672` | 0d/0v/5c | `rule` | FElysiumNpc::NPC_EarlyTranslateActivity | CNPC_VTzimisceRunner slot 375. It chains the Troika body 0x10295590 FIRST and only then remaps the TRANSLATED activity under a non-zero form byte +0x6672, so it is a POST-PASS on the base's answer and not a replacement - which is exactly why its slot-310 twin 0x103c3d80 remaps the REQUEST instead. The switch is on the incoming activity and not on a variant value: 1 and 0xf1 become 0x1134, 3 becomes 0x1135, 9 becomes 0x1136, 0x13 becomes 0x1137, everything else passes through. The generated PreTranslate_TzimisceRunner rows keyed RunnerVariantIs equals 0 to 3 with no From activity, which is the wrong key, and the generator is corrected with this body. |
| `0x103e0bc0` | CNPC_VZombie::FVisible | 202 | 16 | `CNPC_VZombie#201` | — | `+0x026c`, `+0x029c` | 0d/0v/7c | `rule` | FElysiumNpc::FVisible | Arm one - slot 167 GetEnemy()->+0x9c compared against the queried entity: on a match the answer is NOT 0x10146a80, which is discipline stat 8 at least 1 AND the entity's +0x14dc cloak byte, the seam the port already has as FElysiumCombatCharacter::IsObfuscatedForSenses. Arm two - every other entity falls through to the Troika FVisible 0x102b4630 with the fourth argument forced to 0. The remaining bytes are the scope-trace push and pop. CONVENTION: species arm of slot 201 keyed on IsRetailClass(CNPC_VZombie); relative to the Troika base 0x102b4630 it ADDS one arm in front - the current enemy is answered by the obfuscate test 0x10146a80 rather than by sight - and changes the base call itself by forcing the fourth argument to 0 for every other entity. |
| `0x103e0e80` | CNPC_VZombie::DrawDebugTextOverlays | 224 | 16 | `CNPC_VZombie#124` | — | `+0x0224`, `+0x02e0`, `+0x0728`, `+0x0910`, +1 more | 0d/0v/0c | `rule` | FElysiumNpc::DrawDebugTextOverlays | Calls the Troika slot-124 body 0x1029d4e0 for the starting text line, then under m_debugOverlays & 0x40000 walks condition bits 0 to 0xbf of the bitfield at +0x5c5c; each set bit is remapped local-to-global by 0x102ea280 over slot 580 GetClassScheduleIdSpace()+0x30 with base 1000000000, named through slot 458 ConditionName, formatted as Cond: %s into a 0x200 buffer and printed as entity text at the running line in 255,255,255,255; the line index increments per printed condition and is the return value. CONVENTION: species arm of slot 124 keyed on IsRetailClass(CNPC_VZombie); relative to the Troika base 0x1029d4e0 it chains the base FIRST for the starting line and then APPENDS, under m_debugOverlays & 0x40000, one Cond: %s line per set bit of the 0 to 0xbf condition bitfield at +0x5c5c, remapped local-to-global through slot 580 with base 1000000000 and named through slot 458. |
| `0x101ab1b0` | CPayphone::vfunc5 | 30 | 17 | `CPayphone#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CPayphone slot 5 is the MSVC scalar deleting destructor: ~CAI_BaseNPCTroika 0x1028d610 then CBaseEntity::operator delete 0x100aa7b0 only when bit 0 of the flag argument is set, returning this. |
| `0x102727d0` | CAI_BaseNPC::MaintainActivity | 230 | 17 | — | — | `+0x026c`, `+0x065c`, `+0x06f0`, `+0x0748`, +5 more | 4d/0v/0c | `rule` | FElysiumNpc::MaintainActivity | Gated on slot 466 ShouldMaintainActivity; inside, work happens only when m_Activity differs from m_IdealActivity OR m_nSequence from m_nIdealSequence. Activity 2 is the special arm - it waits for m_bSequenceFinished and then runs AdvanceToIdealActivity 0x102726a0 and nothing else, which is what lets a transition clip play out; every other activity re-resolves the ideal through 0x10272130 first and then advances. The body writes no field of its own. Family SaveRestore10 had declared MaintainActivity as a counting seam for the m_bForceMaintainActivity latch; that seam is now the entry point and runs this body, ported as BaseMaintainActivity, after its bookkeeping. |
| `0x1028d5e0` | CAI_BaseNPCTroika::vfunc5 | 30 | 17 | `CAI_BaseNPCTroika#5` | — | — | 0d/0v/0c | `mechanism` | FElysiumNpc::Slot5 | The Troika line's own scalar deleting destructor: ~CAI_BaseNPCTroika 0x1028d610 then CBaseEntity::operator delete 0x100aa7b0 under flag bit 0. The service is C++ object lifetime plus the engine allocator; the generated surface already declares it as Slot5(uint32) and the slot has to answer through that seam rather than tally a stub. |
| `0x10295750` | CAI_BaseNPCTroika::SetActivity | 612 | 17 | `CAI_BaseNPCTroika#310`, `CGeneric_NPC#310`, `CNPCMaker#310`, +59 more | `+0x5cd8` | `+0x02a0`, `+0x0364`, `+0x03ec`, `+0x0874`, +2 more | 2d/9v/14c | `rule` | hand:FElysiumNpc::SetActivity | slot 310, 612 bytes, three top arms. Request 0x1093: outside the 0x1093-0x1096 family hand 0x1093 to the base 0x102725d0 and stop; inside it with slot 251 IsActivityFinished false the body does NOTHING AT ALL - no base call - which is the arm the one-line walk did not name; inside and finished, m_bKeepSound 1, random(0,99) takes 0x1095 below 15, 0x1096 below 30, 0x1094 below 40 else 0x1093, then m_bKeepSound 0 on every one of the four exits. Request 0x1115: frenzy bit 0x40 forces 0xf17 unless it is already playing (and then does nothing rather than restarting), else bit 0x20 forces 0x13 on the same terms, else outside 0x1115-0x1117 hand 0x1115 down, else if finished the bounds start at 15 and 15 and widen to 8 and 0x18 only when slot 168 GetEnemy is non-null AND slot 541's memory carries a record for him (0x102dfa20), whose last known position and this NPC's own slot-217 origin are then read and DISCARDED. Any other request forwards to the base unchanged. |
| `0x1034af70` | CNPCMaker::vfunc5 | 65 | 17 | `CNPCMaker#5` | — | `+0x6668`, `+0x6680`, `+0x6698` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPCMaker slot 5, the scalar deleting destructor: destroys the three entity-output event lists m_OnLastNPCDied (+0x6668), m_OnNPCDied (+0x6680) and m_OnSpawnNPC (+0x6698) through 0x100cd2d0, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1034bfb0` | CNPCMaker_Fleshpile::vfunc5 | 65 | 17 | `CNPCMaker_Fleshpile#5` | — | `+0x6668`, `+0x6680`, `+0x6698` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPCMaker_Fleshpile slot 5, byte-identical to 0x1034af70: the three output lists at +0x6668, +0x6680 and +0x6698, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1034cbf0` | CNPCMaker_Zombie::vfunc5 | 65 | 17 | `CNPCMaker_Zombie#5` | — | `+0x6668`, `+0x6680`, `+0x6698` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPCMaker_Zombie slot 5, byte-identical to 0x1034af70: the three output lists at +0x6668, +0x6680 and +0x6698, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x10359f40` | CGeneric_NPC::vfunc5 | 30 | 17 | `CGeneric_NPC#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CGeneric_NPC slot 5: no members of its own, just ~CAI_BaseNPCTroika 0x1028d610 and operator delete 0x100aa7b0 under flag bit 0. |
| `0x1035c1f0` | CNPC_VVampireBoss::vfunc5 | 43 | 17 | `CNPC_VVampireBoss#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VVampireBoss slot 5: destroys the m_OnTransformComplete output list (+0x6664) through 0x100cd2d0, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1035cd00` | CNPC_VAndreiBlood::Destructor | 342 | 17 | — | `+0x0001`, `+0x19b0` | `+0x0001`, `+0x6664` | 1d/0v/0c | `rule` | FElysiumNpc::DestroyAndreiBlood | Ported verbatim. Listing order: restore its own vftable and the secondary at +0x19b0 (not portable, not observable); then under a CNPC_VAndreiBlood::Destructor scope frame UTIL_Remove 0x101cd940 the handle at +0x66e0 and then the one at +0x66e4, each only when the handle resolves live in the global table, writing 0xffffffff back ON THAT ARM ONLY; then destroy m_OnTransformComplete +0x6664; then tail-jump to ~CAI_BaseNPCTroika 0x1028d610. CORRECTION: the walk calls the pair the two owned entity handles that sit past the class tail because the decompiler renders them as this+1 and this[1].field_0x4; the listing (1035cd62, 1035cdd3) gives +0x66e0 and +0x66e4, and story 29c-1 family Damage had already recovered both as AndreiBloodEmitter (written by StartBloodEmitter 0x1035e1a0) and AndreiSummonEmitter (StartSummonEmitter 0x1035e3c0). The two removals are the whole retail-observable body. |
| `0x1035ec50` | CNPC_VAnimal::vfunc5 | 30 | 17 | `CNPC_VAnimal#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VAnimal slot 5: ~CAI_BaseNPCTroika 0x1028d610 then operator delete 0x100aa7b0 under flag bit 0, nothing of its own. |
| `0x10360410` | CNPC_VAsianVampire::vfunc5 | 43 | 17 | `CNPC_VAsianVampire#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VAsianVampire slot 5: the m_OnTransformComplete output list (+0x6664), then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x10362c90` | CNPC_VBach::vfunc5 | 30 | 17 | `CNPC_VBach#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VBach slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10366cb0` | CNPC_VBatSwarm::vfunc5 | 30 | 17 | `CNPC_VBatSwarm#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VBatSwarm slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103678b0` | CNPC_VBrujah::vfunc5 | 30 | 17 | `CNPC_VBrujah#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VBrujah slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103683f0` | CNPC_VCamera::vfunc5 | 30 | 17 | `CNPC_VCamera#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VCamera slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10369e40` | CNPC_VCameraSecurity::vfunc5 | 30 | 17 | `CNPC_VCameraSecurity#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VCameraSecurity slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x1036a1f0` | CNPC_VChangBros::vfunc5 | 43 | 17 | `CNPC_VChangBros#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VChangBros slot 5: the m_OnTransformComplete output list (+0x6664), then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1036eaf0` | CNPC_VChangBrosBlade::vfunc5 | 43 | 17 | `CNPC_VChangBrosBlade#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VChangBrosBlade slot 5: the m_OnTransformComplete output list (+0x6664), then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1036f2f0` | CNPC_VChangBrosClaw::vfunc5 | 43 | 17 | `CNPC_VChangBrosClaw#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VChangBrosClaw slot 5: the m_OnTransformComplete output list (+0x6664), then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1036fb50` | CNPC_VCombatman::vfunc5 | 30 | 17 | `CNPC_VCombatman#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VCombatman slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10370970` | CNPC_VCop::vfunc5 | 30 | 17 | `CNPC_VCop#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VCop slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10373570` | CNPC_VDog::vfunc5 | 30 | 17 | `CNPC_VDog#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VDog slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10376f20` | CNPC_VFrenzyShadow::vfunc5 | 30 | 17 | `CNPC_VFrenzyShadow#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VFrenzyShadow slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10376f50` | CNPC_VPlayerController::vfunc5 | 30 | 17 | `CNPC_VPlayerController#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VPlayerController slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103770b0` | CNPC_VGangrel::vfunc5 | 30 | 17 | `CNPC_VGangrel#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VGangrel slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10377b60` | CNPC_VGargoyle::vfunc5 | 43 | 17 | `CNPC_VGargoyle#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VGargoyle slot 5: destroys the m_OnGibDeath output list (+0x6664) through 0x100cd2d0, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1037aff0` | CNPC_VGhoulCroucher::vfunc5 | 54 | 17 | `CNPC_VGhoulCroucher#5` | — | `+0x6674`, `+0x668c` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VGhoulCroucher slot 5: destroys m_OnDisturbedByPlayer then m_OnDisturbed (the two output lists at +0x6674 and +0x668c) through 0x100cd2d0, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x1037c6a0` | CNPC_VGuard1::vfunc5 | 30 | 17 | `CNPC_VGuard1#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VGuard1 slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x1037e870` | CNPC_VHengeyokai::vfunc5 | 134 | 17 | `CNPC_VHengeyokai#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VHengeyokai slot 5: two inlined CUtlMemory teardowns over the block past the class tail - zero the count, and when the grow size is not -1 free the buffer with Plat_Free and zero the allocation count, the second pass first copying the buffer pointer to the saved-pointer word - then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x10381b50` | CNPC_VHengeyokai::NPC_EarlyTranslateActivity | 61 | 17 | `CNPC_VHengeyokai#375` | — | — | 0d/0v/5c | `rule` | FElysiumNpc::NPC_EarlyTranslateActivity | CNPC_VHengeyokai slot 375: under the carry-form bit 0x10381c80 (m_bfAINPCFlags +0x14b8 bit 5) request 1 returns 0x128 and 9 or 0x13 return 0x129, each immediately; every other request and the whole bit-clear case tail-call CNPC_VHuman 0x103854f0 - the HUMAN body, not the Troika one - which itself chains 0x10295590. |
| `0x103840a0` | CNPC_VHuman::vfunc5 | 30 | 17 | `CNPC_VHuman#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VHuman slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10386b20` | CNPC_VHumanCombatant::vfunc5 | 30 | 17 | `CNPC_VHumanCombatant#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VHumanCombatant slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10387750` | CNPC_VHumanCombatPatrol::vfunc5 | 30 | 17 | `CNPC_VHumanCombatPatrol#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VHumanCombatPatrol slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103880a0` | CNPC_VHunter::vfunc5 | 30 | 17 | `CNPC_VHunter#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VHunter slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10388e20` | CNPC_VLasombra::vfunc5 | 30 | 17 | `CNPC_VLasombra#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VLasombra slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103895a0` | CNPC_VMalkavian::vfunc5 | 30 | 17 | `CNPC_VMalkavian#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VMalkavian slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x10389df0` | CNPC_VManBat::vfunc5 | 30 | 17 | `CNPC_VManBat#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VManBat slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103910b0` | CNPC_VMingXiao::vfunc5 | 134 | 17 | `CNPC_VMingXiao#5` | `+0x665c`, `+0x6660`, `+0x6668`, `+0x666c` | `+0x665c`, `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VMingXiao slot 5: two inlined CUtlMemory teardowns over +0x665c (buffer), +0x6660 (allocation count), +0x6664 (grow size, -1 means an external buffer that must not be freed), +0x6668 (count) and +0x666c (saved pointer), then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x10391170` | CNPC_VBaseBoss::vfunc5 | 134 | 17 | `CNPC_VBaseBoss#5` | `+0x665c`, `+0x6660`, `+0x6668`, `+0x666c` | `+0x665c`, `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VBaseBoss slot 5, the same body as 0x103910b0: two CUtlMemory teardowns over the +0x665c block under the not -1 grow-size test, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x10395f80` | CNPC_VMingXiao::Save | 81 | 17 | `CNPC_VMingXiao#126` | — | `+0x66f4` | 0d/0v/0c | `rule` | FElysiumNpc::Save | CNPC_VMingXiao slot 126, a species-only slot, brackets the Troika Save with a sentinel pass over its own six m_rflRegrowTimers (+0x66f4, one float each). Order: the encode loop 0x101cf250(&timer[i], 4) for i = 0 to 5 ascending, mode 4 swapping an exact FLT_MAX for 1e+11 so retail's FIELD_TIME rebase cannot shift it; then CAI_BaseNPCTroika::Save 0x102993c0, whose int result is the return value; then the decode loop 0x101cf2f0(&timer[i], 4) over the same six in the same order. Six NPC offsets are written in place twice, and which field, which mode and the encode-before decode-after order are retail's rule around the archive call, so this is not a bare mechanism - the Troika Save 0x102993c0 and CScriptedTarget 0x1034e320, the same shape, are both verdicted rule. |
| `0x1039b0d0` | CNPC_VMingXiaoTentacle::vfunc5 | 30 | 17 | `CNPC_VMingXiaoTentacle#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VMingXiaoTentacle slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x1039ed50` | CNPC_VMingXiaoTentacle::Save | 53 | 17 | `CNPC_VMingXiaoTentacle#126` | — | `+0x6674` | 0d/0v/0c | `rule` | FElysiumNpc::Save | CNPC_VMingXiaoTentacle slot 126, species-only: 0x101cf250(&m_flPhaseExpireTimer +0x6674, 3) rewrites the field in place - mode 3 swaps a value exactly equal to _DAT_104454c4 (0.0) for 1e+11 against retail's FIELD_TIME rebase - then CAI_BaseNPCTroika::Save 0x102993c0 whose int result is returned, then 0x101cf2f0 on the same field with mode 3 restores it. One NPC offset written twice around the archive call under a retail-chosen mode, so rule and not mechanism, exactly like 0x1034e320. |
| `0x1039f640` | CNPC_VMoleman::vfunc5 | 30 | 17 | `CNPC_VMoleman#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VMoleman slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103a0270` | CNPC_VNewscaster::vfunc5 | 225 | 17 | `CNPC_VNewscaster#5` | `+0x665c`, `+0x6660`, `+0x6668`, `+0x666c`, +4 more | `+0x665c`, `+0x6664`, `+0x6670`, `+0x6678` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VNewscaster slot 5: four CUtlMemory teardowns, the +0x6670 block first (count +0x667c, allocation count +0x6674, grow size +0x6678, saved pointer +0x6680) then the +0x665c block (+0x6668, +0x6660, +0x6664, +0x666c), each freeing with Plat_Free only when the grow size is not -1, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x103a14e0` | CNPC_VNosferatu::vfunc5 | 30 | 17 | `CNPC_VNosferatu#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VNosferatu slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103a1e40` | CNPC_VPedestrian::vfunc5 | 30 | 17 | `CNPC_VPedestrian#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VPedestrian slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103a4320` | CNPC_VPlaceholder::vfunc5 | 30 | 17 | `CNPC_VPlaceholder#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VPlaceholder slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103a4d40` | CNPC_ProneDialog::vfunc5 | 30 | 17 | `CNPC_ProneDialog#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_ProneDialog slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103a50e0` | CNPC_VSabbatGunman::vfunc5 | 30 | 17 | `CNPC_VSabbatGunman#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VSabbatGunman slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103a5ce0` | CNPC_VSabbatLeader::vfunc5 | 43 | 17 | `CNPC_VSabbatLeader#5` | — | `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VSabbatLeader slot 5: the m_OnTransformComplete output list (+0x6664), then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x103abbe0` | CNPC_VScurrying::vfunc5 | 30 | 17 | `CNPC_VScurrying#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VScurrying slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103ad930` | CNPC_VRat::vfunc5 | 30 | 17 | `CNPC_VRat#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VRat slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103ae4f0` | CNPC_VSheriffMan::vfunc5 | 54 | 17 | `CNPC_VSheriffMan#5` | — | `+0x6664`, `+0x66b8` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VSheriffMan slot 5: destroys m_OnFinishTransformation then m_OnTransformComplete (the output lists at +0x6664 and +0x66b8) through 0x100cd2d0, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x103b1c60` | CNPC_VSheriffSwarm::vfunc5 | 30 | 17 | `CNPC_VSheriffSwarm#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VSheriffSwarm slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103b28f0` | CNPC_VStalker::vfunc5 | 30 | 17 | `CNPC_VStalker#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VStalker slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103b3970` | CNPC_VTaxiDriver::vfunc5 | 30 | 17 | `CNPC_VTaxiDriver#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VTaxiDriver slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103b3b60` | CNPC_VTest::vfunc5 | 30 | 17 | `CNPC_VTest#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VTest slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103b5370` | CNPC_VToreador::vfunc5 | 30 | 17 | `CNPC_VToreador#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VToreador slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103b5b10` | CNPC_VTremere::vfunc5 | 30 | 17 | `CNPC_VTremere#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VTremere slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103b6e90` | CNPC_VTzimisce::vfunc5 | 225 | 17 | `CNPC_VTzimisce#5` | `+0x665c`, `+0x6660`, `+0x6668`, `+0x666c`, +4 more | `+0x665c`, `+0x6664`, `+0x6690`, `+0x6698` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VTzimisce slot 5: four CUtlMemory teardowns, the +0x6690 block first (count +0x669c, allocation count +0x6694, grow size +0x6698, saved pointer +0x66a0) then the +0x665c block (+0x6668, +0x6660, +0x6664, +0x666c), Plat_Free only when the grow size is not -1, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x103c1340` | CNPC_VTzimisceHeadClaw::vfunc5 | 134 | 17 | `CNPC_VTzimisceHeadClaw#5` | `+0x665c`, `+0x6660`, `+0x6668`, `+0x666c` | `+0x665c`, `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VTzimisceHeadClaw slot 5: two CUtlMemory teardowns over the +0x665c block under the not -1 grow-size test, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x103c2810` | CNPC_VTzimisceHeadClaw::Save | 53 | 17 | `CNPC_VTzimisceHeadClaw#126` | — | `+0x6678` | 0d/0v/0c | `rule` | FElysiumNpc::Save | CNPC_VTzimisceHeadClaw slot 126, species-only: 0x101cf250(&m_flSlowedExpire +0x6678, 3) rewrites the field in place - mode 3 swaps the exact _DAT_104454c4 (0.0) sentinel for 1e+11 against retail's FIELD_TIME rebase - then CAI_BaseNPCTroika::Save 0x102993c0 whose int result is returned, then 0x101cf2f0 on the same field with mode 3. The field, the mode and the encode-then-decode order are retail, so rule not mechanism; this is the same +0x6678 slow stamp that 0x103c2230 gates its teardown on. |
| `0x103c3120` | CNPC_VTzimisceRunner::vfunc5 | 134 | 17 | `CNPC_VTzimisceRunner#5` | `+0x665c`, `+0x6660`, `+0x6668`, `+0x666c` | `+0x665c`, `+0x6664` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VTzimisceRunner slot 5: two CUtlMemory teardowns over the +0x665c block under the not -1 grow-size test, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x103c4920` | CNPC_VVampire::vfunc5 | 30 | 17 | `CNPC_VVampire#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VVampire slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103c77f0` | CNPC_VVentrue::vfunc5 | 30 | 17 | `CNPC_VVentrue#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VVentrue slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103ca7c0` | CNPC_VWerewolf::~CNPC_VWerewolf | 484 | 17 | — | `+0x19b0`, `+0x665c`, `+0x6660`, `+0x6668`, +8 more | `+0x026c`, `+0x665c`, `+0x6664`, `+0x668c`, +9 more | 1d/0v/0c | `rule` | FElysiumNpc::DestroyWerewolf | Ported verbatim. Listing order: the two vftable restores (+0x0 and +0x19b0, not portable); then under the scope frame DAT_1093fac4 = 0 and the werewolf_show_debug ConVar driven to 0 through ConVar slot 4, which is the only thing outside this object the body touches; then five outputs destroyed in this order - m_OnTeleportIn, m_OnTeleportOut, m_OnFinishCrushAnimation, m_OnBeginCrushAnimation, m_OnConditionDeathTriggered; then the +0x6714 vector walked BACKWARDS from its +0x6720 count destroying each 0x48-byte record with 0x103dc5b0, the count zeroed, 0x103dc220 run over it; then the CUtlMemory teardowns of the +0x6714, +0x668c and +0x665c blocks under the grow-size-is-not--1 test; then ~CAI_BaseNPCTroika. CORRECTION: +0x6714 / +0x6720 is not an unnamed vector, it is the werewolf's HINT-DATA ARRAY - CNPC_VWerewolf::InitializeHintData 0x103d7710 fills it and GetHintGroundpoint 0x103d6770, GetHintTargetGroundpoint 0x103d68d0, GetHintEndEntity 0x103d6390 and GetDataForHint 0x103d7620 read it - which family Hints already carries as WerewolfHintGroundpoints, and the 0x48 stride the walk quotes is that record's size. The port clears it in the recovered descending order. UNRECOVERED: what the +0x668c and +0x665c CUtlMemory blocks hold; the destructor is their only appearance in this closure. |
| `0x103dcf20` | CNPC_VWolfMorph::vfunc5 | 30 | 17 | `CNPC_VWolfMorph#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VWolfMorph slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103dd090` | CNPC_VYukie::vfunc5 | 30 | 17 | `CNPC_VYukie#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VYukie slot 5: ~CAI_BaseNPCTroika then operator delete under flag bit 0, nothing of its own. |
| `0x103de2f0` | CNPC_VZombie::vfunc5 | 43 | 17 | `CNPC_VZombie#5` | — | `+0x66e8` | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VZombie slot 5: destroys the m_OnAttackedVictim output list (+0x66e8) through 0x100cd2d0, then ~CAI_BaseNPCTroika and operator delete under flag bit 0. |
| `0x10290350` | FUN_10290350 | 421 | 18 | — | `+0x174c`?, `+0x65fa`? | `+0x0364`?, `+0x0700`? | 1d/0v/0c | `rule` | FElysiumNpc::RunAlternateAiDoorMode4 | Mode 4 of the door transaction, confirmed off the listing with its constants pinned. In order: m_bForceMaintainActivity +0x65fa := 1 across CAI_BaseNPC::MaintainActivity 0x102727d0 (a __thiscall with no dispatch site, so not a vtable slot) then := 0; when m_hOpeningDoor +0x5d24 still resolves and that door's m_toggle_state +0x4f8 is 0, stop the motor through 0x102bf7e0 and set m_eAlternateAI +0x644c to 0 - note this arm deliberately does NOT clear the door handle or m_bOpeningDoorWait; then a hull trace from slot 217 GetAbsOrigin to that origin plus m_vecForward +0x6290 scaled by _DAT_10451acc, recovered from the pinned image as 64.0, with mask 0x202400b and radius 100.0 through the filter at +0x5d40, and on a hit stop the motor, m_hOpeningDoor := -1, m_bOpeningDoorWait +0x5d30 := 0 and m_eAlternateAI := 0; last, the test is (curtime < m_flAlternateAIExpireTimer +0x6450) and the ELSE arm fires, so an expiry stamp EQUAL to curtime has expired and slot 0x700 (448) TaskFail is dispatched with 0xe and the same three fields cleared. Always answers true, so the transaction keeps the body. Ported as story 29d family SaveRestore10. |
| `0x102efaa0` | CAI_Navigator::MoveNormal | 538 | 18 | `CAI_Navigator#12` | — | — | 0d/1v/0c | `rule` | FElysiumNpc::NavigatorMoveNormal | PORTED 29d Motor10 (listing). THREE CORRECTIONS. (1) It returns an AIMoveResult_t, not a distance: the gate's refusal is MOV EAX,0xfffffffc = -4 AIMR_ILLEGAL (the decompiler prints -NAN because it typed the return float), the speed arm is XOR EAX,EAX = 0 AIMR_OK, and every other exit is the enact's own answer in EAX. (2) The ideal-speed test is speed <= _DAT_104454c4 (0.0f), not < 0.0: FCOMP then TEST AH,0x41 and JP takes C0 or C3. (3) The restore is TWO independent tests and the walk folds them: the FIRST is on the ideal speed SAVED before SetActivity ([ESP+0x14] at 102efc11) against _DAT_1044e658, the SECOND on how far the body actually moved against the same constant. _DAT_1044e658 is the double 0.01, read out of the pinned image at bytes 7b 14 ae 47 e1 7a 84 3f (docs/vtmb/npc-ai/lifecycle.md lists it as unrecovered). Order: the gate 0x102efd50; navigator slot 16 (+0x40) with a result seeded to -4, returning it when the override answers true; owner slot 248 GetIdealSpeed; save m_Activity +0xfec, m_nSequence +0x6f0 and slot 217 GetAbsOrigin; push the route's movement activity (0x102ee3f0 = nav->+0x30->+0x2c) through owner slot 310 SetActivity; re-read GetIdealSpeed and answer 0 when it is at or below 0.0 and m_Activity == 2; zero a 14-dword block and then a 31-dword block that CONTAINS it; build through navigator slot 17 (+0x44) and enact through navigator slot 15 (+0x3c) with the move info FIRST and this function's own stack word second; a non-zero enact returns; otherwise the two-test restore writes m_nSequence directly and re-issues the saved activity through slot 310; then navigator slot 6 (+0x18) when navigator+0x51 is clear. |
| `0x1035c270` | CNPC_VAndreiBlood::vfunc5 | 30 | 18 | `CNPC_VAndreiBlood#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VAndreiBlood slot 5: calls its class destructor 0x1035cd00 and then operator delete 0x100aa7b0 under flag bit 0 - the behaviour is all in 0x1035cd00, this is the ABI thunk. |
| `0x103acba0` | FUN_103acba0 | 1017 | 18 | — | — | `+0x0364`, `+0x0828`, `+0x1568`, `+0x5d34` | 1d/0v/0c | `rule` | FElysiumNpc::ScurryingFindFleeDestination | Given a threat position and a distance it first asks the navigator (+0x5d34) for the nearest node within 30000 units (0x102edae0 over 0x103008f0 and 0x102ee9c0). On success it jitters that node: whichever of the x or y deltas to the threat is larger picks the axis, the sign of that delta picks a 0..-60 or 0..60 band on it and the other axis gets -80..80, z is the node z plus slot 522 StepHeight times DAT_104454d0, and it tries up to 5 random points against CAI_BaseNPCTroika::IsAreaClear with mask 0x202400b, keeping the last one tried; the accepted point is written to the out vector when one was passed and it answers 1. On failure it marches instead: start at slot 217 GetAbsOrigin raised by StepHeight times DAT_104454d0, direction normalized away from the threat, hull-trace (collision bounds from +0x1568, mask 0x202400b) to start plus direction times the distance, and while the trace is blocked (fraction below DAT_104454c0 or either solid flag set) scale the distance and the direction by DAT_104454d0 and retry, answering 0 once the distance falls to DAT_104454c0 or below; a clear trace returns its endpoint. |
| `0x103c1cd0` | CNPC_VTzimisceHeadClaw::SetActivity | 55 | 18 | `CNPC_VTzimisceHeadClaw#310` | — | `+0x029c` | 0d/0v/14c | `rule` | FElysiumNpc::SetActivity | CNPC_VTzimisceHeadClaw slot 310, 55 bytes: request 9 ACT_WALK with a non-null slot 167 GetEnemy is rewritten to 0x1136 ACT_TZ_WALK2 before the Troika SetActivity 0x10295750; request 9 without an enemy and every other request forward unchanged. |
| `0x103c3d80` | CNPC_VTzimisceRunner::SetActivity | 110 | 18 | `CNPC_VTzimisceRunner#310` | — | `+0x6672` | 0d/0v/14c | `rule` | FElysiumNpc::SetActivity | CNPC_VTzimisceRunner slot 310, 110 bytes: with the form byte +0x6672 non-zero the REQUEST is remapped before the Troika SetActivity, tested in retail's own order 1, 9, 0x13, 3, 0xf1 and not in numeric order - 1 and 0xf1 to 0x1134, 9 to 0x1136, 0x13 to 0x1137, 3 to 0x1135; with the byte clear every request forwards unchanged. |
| `0x103ca790` | CNPC_VWerewolf::vfunc5 | 30 | 18 | `CNPC_VWerewolf#5` | — | — | 0d/0v/0c | `mechanism` | CRT:operator delete | CNPC_VWerewolf slot 5: calls ~CNPC_VWerewolf 0x103ca7c0 and then operator delete 0x100aa7b0 under flag bit 0 - the behaviour is all in 0x103ca7c0, this is the ABI thunk. |
| `0x103d0db0` | CNPC_VWerewolf::HasPath | 138 | 18 | — | — | `+0x026c`, `+0x5d34` | 12d/0v/0c +1 outside | `rule` | FElysiumNpc::WerewolfHasPath | 120 of its 138 bytes are the scope-trace push and pop; the body is ONE call, 0x102ee380(m_pNavigator +0x5d34, start, end). CORRECTION: the two navigator-cache stamps the earlier walk attributes to this body are inside 0x102ee380 - it writes nav+8 from the NPC's +0x156c hull and nav+0xc from the global frame word, repeats the pair on the path object 0x102ecc00, and only then forwards both Vectors to 0x102fdcc0. 12 direct callers plus one from outside the kernel; the void return is a decompile artifact of the forwarded call. 0x102fdcc0 is the seam this substrate has no path object for, so it answers false - retail's own answer for a navigator with no path. |
