# VtMB `.mdl` v2531 — struct map

The **skeletal half** — bones, sequences, the compressed animation tracks, skinning,
and attachments (for the `character/**` NPC models this parser raises on) — is
documented in `docs/vtmb/animation_and_movers.md`, Part A.

Authoritative layout for the VtMB studio-model format. Two VtMB-native references exist, and
they carry different weight depending on which part of the format is in question.

**Crowbar** (labels this format *"Source - Vampire: The Masquerade - Bloodlines"*, version 2531)
gives the best **header offsets**.
`Crowbar/Core/GameModel/SourceModel2531/SourceMdlFileData/SourceMdlFileData2531.vb` carries the
complete struct, and its offsets agree with the installed bytes end to end. Its *names* past
`SurfacePropIndex` are placeholders and in one case wrong — see the header table's provenance note.

**VAMPTools** (`$ELYSIUM_WORK_ROOT/research/reference-source/VAMPTools/MDLConverter`, atrblizzard)
owns the **geometry walk**. Its `optimize.h` and `src/Mesh.cpp` are hand-fitted to VtMB's disk
format — the author annotates every alignment fixup — and `Mesh.cpp` is the reference walk that
produces correct FBX. Its `inc/external/studio.h` is **not** reliable for header offsets: it omits
the `NumTransitions`/`TransitionIndex` pair and closes with modern-Source fields VtMB does not
have, so a field walk transcribed from it drifts by 8 bytes from `NumFlexDescs` onward.

**Both are hand-fits by third parties, and neither is a Troika artifact.** They disagree about the
end of the header and each is wrong there in a different way, so every field past
`SurfacePropIndex`@392 is settled against the shipped binaries instead — the same decompilation
provenance the bone and sequence fields carry.

**All offsets below are probe-verified** (`research/tooling/probes/probe_mdl.py`): headers,
materials, the mdl↔vtx lockstep walk, and all three vertex formats reconstruct valid
in-hull geometry with clean UVs on real scenery props (`pillars` 96 tris/unskinned,
`lampfloor` 1066 tris/skinned, `bookend` 530 tris/compressed). The header field
offsets are additionally **cross-confirmed against the game decompile** (`client.dll`
in Ghidra: `C_BaseAnimating::SetupBones` and its callees read `numbones@0xf0`,
`boneindex@0xf4`, `numlocalanim@0x108`, `localanimindex@0x10c`, bone stride `0xa0`,
`Flags@0x88`); the skeletal-animation decode (`docs/vtmb/animation_and_movers.md` Part A)
carries the same decompilation provenance.

## Files per model

A model is **2–3 sibling files** in the VPKs under `models/`:

| Ext | Role | Notes |
|---|---|---|
| `.mdl` | header + bones + materials + **embedded vertex data** | `IDST`, version **2531** |
| `.dx80.vtx` | strip/index data (triangle topology) → mesh-vertex IDs | the VtMB `.vtx` variant |
| `.phy` | vphysics collision (optional) | ~2/3 of models; **deferred** |

**Key divergences from modern Source (v44–49):**
- **No `.vvd`.** Vertex positions/normals/UVs are embedded *inside the `.mdl`*, per
  `StudioModel` (field `VertexIndex`). Modern Source splits them into `.vvd`.
- **`.vtx` is `.dx80.vtx` / `.dx7_2bone.vtx`**, never `.dx90.vtx`. One shipped model
  (`scenery/furniture/fishtank/fish07_school.mdl`) carries **only** the `.dx7_2bone.vtx`
  variant, so a reader that hard-requires `.dx80.vtx` drops it; the same parser reads both.
- **`MDLHeader.Name` is `char[128]`** (modern is `char[64]`), so `Length` sits at
  offset **140**, not 76. (Reading offset 76 lands mid-Name → the recon saw 0 there.)
  The widening is Troika's toolchain rather than this format: the compiled expression
  tables carry the same `char[128]` in place of Source's `char[64]`
  (`docs/vtmb/facial_animation.md` → "The compiled `.vfe` header").
- Structs carry extra padding vs. modern Source; the VtMB tool was compiled **x86**,
  so on-disk reserved space for runtime pointers is **4 bytes** each.

## `.mdl` — `MDLHeader` (default alignment; all fields 4-byte, no packing)

All `*Index` fields are byte offsets **from the header base (offset 0)** unless noted.
`Num*`/`*Index` pairs describe an array: `count` items of the named struct at `base + Index`.

| Off | Type | Field | Use |
|---|---|---|---|
| 0 | char[4] | `ID` | `"IDST"` |
| 4 | int | `Version` | **2531** |
| 8 | uint | `Checksum` | must match `.dx80.vtx` + `.phy` |
| 12 | char[128] | `Name` | internal model path |
| 140 | int | `Length` | file length |
| 144 | Vector | `GlobalScale` | always (0.5,0.5,0.5) |
| 156 | Vector | `EyePosition` | |
| 168 | Vector | `IllumPosition` | lighting origin |
| 180 | Vector | `HullMin` | movement hull (also used to de-scale UNSKINNED verts) |
| 192 | Vector | `HullMax` | |
| 204 | Vector | `ViewBBMin` | render bbox |
| 216 | Vector | `ViewBBMax` | |
| 228 | int | `Flags` | bit 4 (`0x10`) = `STUDIOHDR_FLAGS_STATIC_PROP` |
| 232 | float | `PhonemeFilterMin` | lower bound on the lipsync blend width, seconds — `0.065` on 57 rigged models, `0.080` on 33 |
| 236 | float | `PhonemeFilterMax` | upper bound — `0.100` (`Jeanette` `0.105`). `docs/vtmb/facial_animation.md` → "The phoneme envelope" |
| 240 | int | `NumBones` | |
| 244 | int | `BoneIndex` | `StudioBone[]` (160B each) |
| 248 | int | `NumBoneControllers` | **0 on every shipped model** — see below |
| 252 | int | `BoneControllerIndex` | `mstudiobonecontroller_t`, **24 B** |
| 256 | int | `NumHitBoxSets` | |
| 260 | int | `HitBoxSetIndex` | |
| 264 | int | `NumLocalAnims` | |
| 268 | int | `LocalAnimIndex` | |
| 272 | int | `NumLocalSeq` | |
| 276 | int | `LocalSeqIndex` | |
| 280 | int | `SequencesIndexed` | an init flag, not a count — `0` on all 4,445 |
| 284 | int | `NumSeqGroups` | **`1` on all 4,445**, so it cannot be an index |
| 288 | int | `SeqGroupIndex` | 16B records, record-relative offsets |
| 292 | int | `NumTextures` | **materials** — `StudioTexture[]` |
| 296 | int | `TextureIndex` | |
| 300 | int | `NumTextureSearchPaths` | |
| 304 | int | `TextureSearchPathIndex` | array of int offsets → search-path strings |
| 308 | int | `NumSkinRefs` | (skin table; static props index textures directly) |
| 312 | int | `NumSkinFamilies` | |
| 316 | int | `SkinIndex` | |
| 320 | int | `NumBodyParts` | geometry root |
| 324 | int | `BodyPartIndex` | `StudioBodyPart[]` (16B each) |
| 328 | int | `NumLocalAttachments` | |
| 332 | int | `LocalAttachmentIndex` | |
| 336 | int | `NumTransitions` | sequence node→node transition graph; `0` on all 4,445 |
| 340 | int | `TransitionIndex` | never authored — the value is a write cursor, see below |
| 344 | int | `NumFlexDescs` | the face — `docs/vtmb/facial_animation.md` |
| 348 | int | `FlexDescIndex` | |
| 352 | int | `NumFlexControllers` | |
| 356 | int | `FlexControllerIndex` | |
| 360 | int | `NumFlexRules` | |
| 364 | int | `FlexRuleIndex` | |
| 368 | int | `NumIKChains` | **239 models** — 4, or 8 on five — see below |
| 372 | int | `IKChainIndex` | `StudioIKChain[]` (16B each) |
| 376 | int | `NumMouths` | |
| 380 | int | `MouthIndex` | |
| 384 | int | `NumLocalPoseParameters` | **451 models**, 2 or 4 — see below |
| 388 | int | `LocalPoseParamIndex` | `StudioPoseParamDesc[]` (20B each) |
| 392 | int | `SurfacePropIndex` | offset to the physical-material name — see below |
| 396 | int | `NumSecondaryMotion` | `0..25` — `docs/vtmb/secondary_motion.md` |
| 400 | int | `SecondaryMotionIndex` | 28-byte records — `docs/vtmb/secondary_motion.md` |
| 404 | int | `NumIncludeModels` | `0..19` |
| 408 | int | `IncludeModelIndex` | `StudioModelGroup[]` |
| 412 | int | *unidentified* | `0` on all 4,445; no binary reads it — see below |
| 416 | int | *unidentified* | `0` on all 4,445; no binary reads it — see below |
| 420 | int | `Contents` | `CONTENTS_SOLID` (1) on 4,438 models, `0` on 7 — see below |

**The header is exactly 424 bytes.** `BoneIndex`@244 is `424` on **all 4,445** models — it is
the field's only value — so `StudioBone[0]` begins immediately after 420 and there is no header
slot beyond it. `BoneControllerIndex`@252 equals `424 + 160·NumBones` on 4,184 models, holding
across 116 distinct bone counts up to 343 bones; no other base (408, 416, 420, 428, 440) matches
on a single model and no other stride (156, 158, 162, 164) matches on more than three. Modern
Source's `virtualModel`, `szanimblocknameindex`, `numanimblocks`, `animblockindex`,
`bonetablebynameindex` and `numflexcontrollerui` have no room to exist here.

Reading past 420 as header is the trap this format sets, because the bone array's own fields
profile like plausible header slots: `StudioBone[0].pos`@456 is a float triple that reads as a
large int, and `parent`@428 plus the six `bonecontroller` ints at `+8..+28` read as `-1` on every
model with bones — which looks like an unused header block rather than the bone record it is.
The three models where those bytes differ are exactly the three with `NumBones == 0`
(`models/null.mdl`, `models/w_null.mdl`, `models/weapons/w_null.mdl`); with no bone 0 the space
is taken by `mstudiohitboxset_t[0]` and `StudioAnimDesc[0]`, which decode cleanly to hitbox set
`default` and a one-frame `@idle01` at 10 fps.

**VAMPTools declares these fields but places them wrongly.** Its `studio.h` names
`NumIKChains`, `NumMouths`, `NumLocalPoseParameters` and `SurfacePropIndex`, yet its struct omits
`NumTransitions`/`TransitionIndex` — it runs `LocalAttachmentIndex` straight into `NumFlexDescs`
— so a walk transcribed from it lands 8 bytes short from `NumFlexDescs` onward and puts
`SurfacePropIndex` at 384 rather than 392. Its tail after that field
(`BoneTableNameIndex`, `NumAnimblocks`, `AnimblockIndex`, then a byte quad
`{ConstDirectionalLightDOT, RootLOD, NumAllowedRootLODs, unused}`) is modern-Source boilerplate
never fitted to VtMB: those fields would close the struct roughly 24 bytes before the verified
424, and 396/400 is the secondary-motion table rather than an animblock pair.

**Crowbar carries the full struct**, including the transitions pair, in
`Crowbar/Core/GameModel/SourceModel2531/SourceMdlFileData/SourceMdlFileData2531.vb`, and its
offsets agree with the installed bytes throughout. Its **names** are a different matter. The
`studio.h` it quotes is headed "Bloodlines SDK source 2015-06-16" but is a community
reconstruction rather than a Troika artifact — it carries the reverse-engineer's own inline
annotations (eleven `// f64:` edit notes, and `MAXSTUDIOBONES 1024 // (Psycho-A: < 128)`, naming
the Unofficial Patch author). Where it was unsure it left placeholders: `gh[2]`, `numunk`/
`unkindex` for the secondary-motion pair, and `unhz[3]` for 412/416/420. At that last one it is
positively wrong — the transcript's tail is modern Source's `studio.h` commented out, with
`int contents` struck through and replaced by the placeholder, which is precisely the field the
disassembly proves is there.

**Neither reference is authoritative for the header tail.** VAMPTools asserts a byte quad whose
offsets never reach 420; Crowbar has the right widths and the wrong names. Both are hand-fits,
each wrong in a different way, so anything past `SurfacePropIndex`@392 is settled against the
binaries rather than against either header.

Each row is **profile-verified across all 4,445 installed models**, but note what that alone does
not establish: a slot holding a value inside the file's byte range is not evidence it is an
offset. Every row above is additionally anchored by decoding what it addresses.

**An empty array's index is the next write cursor, not a null.** `TransitionIndex`@340 equals
`BodyPartIndex`@324 on **4,445 of 4,445** models, and `BoneControllerIndex`@252 equals
`AttachmentIndex`@332 on **4,445 of 4,445** — the **transition and bone-controller** arrays are
unauthored corpus-wide, so each of their indices points at whatever studiomdl wrote next. A reader
must gate on the count; the index alone never reads as absent.

The equality is a write-cursor coincidence and says nothing about the arrays the two indices happen
to collide with. **Attachments in particular are authored**: `jeanette` declares three, and the
`bushhook` and `sledgehammer` wield models one apiece
(`docs/vtmb/animation_and_movers.md` A.6 and `docs/vtmb/wielded_weapons.md`).

**`SequencesIndexed`@280 / `NumSeqGroups`@284 / `SeqGroupIndex`@288 are the extra int that offsets
this format from modern Source**, which spends the same space on `numlocalnodes` /
`localnodeindex` / `localnodenameindex`. VtMB has no `localnodenameindex` at all. The three read
as `0`, `1` and a record offset on every model: a constant `1` cannot be an index, and `@288`
resolves to a 16-byte `{labelIndex, nameIndex, ...}` whose record-relative strings are label
`default` and filename `""` on **4,445 of 4,445** — a single demand-load sequence group, which is
what a model with no external sequence files carries. `@280` being an initialization flag is also
what the loader rewrite below is writing. There is no separate model-level node-table pair either:
`NumTransitions`@336/`TransitionIndex`@340 already **are** modern Source's `numlocalnodes`/
`localnodeindex` — an `n×n` byte adjacency matrix, `n = NumTransitions`, cell `[entry-1][exit-1]`
at `TransitionIndex + (entry-1)*n + (exit-1)` — addressed by each `StudioSeqDesc`'s own
`entrynode`@624/`exitnode`@628 (below). No model authors a transition graph: `NumTransitions` is
`0` on all 4,445 models and `entrynode`/`exitnode` are `0` on all 14,012 sequence descriptors.

**`Contents`@420 is a full `int`, read as one by the engine.** `engine.dll`'s
`CEngineTrace::ClipRayToCollideable` (`0x20069730`, identified by its `**studio**` literal at
`0x2019BFE8` and by writes landing on `trace_t` at the published offsets) reads it twice, both
32-bit:

```
200697C3  85 98 a4 01 00 00   test dword ptr [eax+0x1a4], ebx   ; pStudioHdr->contents & fMask
20069BB9  8b 88 a4 01 00 00   mov  ecx, dword ptr [eax+0x1a4]
20069BC5  89 4f 30            mov  dword ptr [edi+0x30], ecx    ; pTrace->contents = ...
```

`ebx` is the trace content mask whose `CONTENTS_HITBOX` bit (`0x40000000`) is tested two
instructions earlier, and the value is stored whole into `trace_t::contents`.

The trailing byte quad `{constdirectionallightdot, rootLOD, numAllowedRootLODs, unused}` — the
alternative reading, and the one VAMPTools' struct ends with — **is refuted**. It requires a
byte-sized load, `8A 80 A4 01 00 00` or `0F B6 80 A4 01 00 00`, and neither encoding occurs in
any shipped binary. A blind sweep of the `A4 01 00 00` displacement in `mod=10` form across
`engine.dll`, `StudioRender.dll`, `client.dll`, `vampire.dll`, `vphysics.dll`, `MaterialSystem.dll`,
`GameUI.dll` and `Vampire.exe` finds 241 dword-sized accesses, 18 `lea`, 37 x87 and exactly two
byte-sized — both stores in a `client.dll` routine that also writes `+0x1A8` and `+0x1AC`, past the
424-byte header, so a different struct. `constdirectionallightdot` is a StudioRender field, and
`StudioRender.dll` touches displacement `0x1A4` only as `[esp+0x1a4]` stack locals, never through a
pointer: under the byte reading the field would be dead code in the one module that consumes it.

**`StudioBone`+156 is the matching per-bone `Contents`**, the pair modern Source also carries. A
second engine routine (`0x200694D0`) returns `hdr->contents` for index 0 and `bone[index-1]+156`
otherwise. The field is `CONTENTS_SOLID` on **all 61,925 bones of all 4,445 models**.

The seven models with `Contents == 0` are compiled `$contents notsolid`:
`objects/projector`, `scenery/structural/libcolumn/libcolumn_{large,medium,small}`,
`scenery/structural/malkavian/malklifetube_trims` and
`weapons/handleclaws/{ground/g_,info/i_}handleclaws`. The columns are the case that looks wrong
and is not: all three are static props in `la_library_1` alone, and their `StaticPropLump_t.Solid`
is `SOLID_NONE` — the designer placed them non-solid and brush geometry carries their collision.
The two `handleclaws` are pickup and inventory display models with no world placement.

**`@412` and `@416` stay unidentified, and the corpus cannot settle them.** They are `0` on all
4,445 models and no shipped binary reads either offset through a studiohdr pointer, so there is no
disassembly evidence to constrain them either. Naming them by analogy to modern Source does not
work: modern Source places `contents` immediately *before* `numincludemodels`, whereas here it
follows `IncludeModelIndex`@408, so this tail is Troika's own ordering. Settling them needs a
Troika-side artifact or a model carrying a non-zero value, and the install offers neither.

`Name` holds the model's own path **without a `models/` prefix and with forward slashes**
(`character/npc/unique/society_of_leopold/average_vampire_hunter/average_vampire_hunter.mdl`),
so a caller joining it against the install prepends `models/`. Across all 4,445 installed
v2531 headers the field is never full: the longest name is **89** characters and **50** are 64
or longer, so any reader that keeps only 64 bytes of it silently drops the terminator. Two
64-character prefixes are shared by distinct models —
`scenery/furniture/DiningTableOldBeachHouse/DiningTableOldBeachHo{use,use_new}.mdl` and
`character/npc/common/shopkeeper/Shopkeeper_China_town_Female/chi{na_town_Female01,na_town_Female02}.mdl`
— so a 64-byte field renders those pairs identically, and an identity join keys on `Checksum`
rather than on this field. Names are mixed case, though no two of the 4,445 differ only by
case, so a lowercased lookup against the install index resolves each to exactly one file.

### The loaded image is the file, patched in place

The engine loads the `.mdl` **whole and unrelocated**: the runtime `studiohdr` pointer is
the file image at offset 0, so `pointer − studiohdr` is a file offset directly. Measured on
one `sp_theatre` capture that retained all **141** distinct runtime studio headers and
compared each against the patch-first installed file carrying the same `Checksum`: every
one of the 141 `Length`@140 values equals the installed file size exactly, and **42** images
are byte-identical to disk.

The other 99 differ only in narrow, structured ranges — the loader writes into the image
rather than into a copy. In one complete run the whole difference is 26,958 bytes:

| Range | Bytes | Models | What |
|---|---:|---:|---|
| the include-model bone remap arrays | 24,234 | 27 | written on **exactly** the 27 models that carry include models and on no other, zero exceptions |
| `StudioBone.Flags`@136 | 743 | 27 | one byte per bone, at the 160-byte stride |
| `StudioMesh.VertexData`@24 | 258 | 51 | the 36-byte runtime struct reserved on disk |
| `StudioSeqDesc`+0xc..0xf | 170×4 | 49 | one dword per sequence descriptor |
| `StudioModelGroup`+0x4..0xc, +0x14..0x1b, +0x44..0x4b | ~41 each | 27 | inside the include-model records |
| `MDLHeader.SequencesIndexed`@280 | 58 | 58 | the loader setting its own init flag |
| `MDLHeader.Flags`@228 | 45 | 45 | |

### Which of those bytes an evaluation actually dereferences

The loader-written ranges above matter because a pointer captured at runtime is a file
offset, so a byte the loader rewrote is *not* the byte on disk. Two complete `sp_theatre`
runs resolve every fired contribution to the exact spans retail read, and the animation
path touches **3,449,152** and **3,474,414** bytes across **34** and **35** owner images.

Per decoded cell those spans are, in the owner image:

| Range | Read by |
|---|---|
| `NumBones`@240, `BoneIndex`@244 | the selected-bone loop, before it examines the mask |
| `numframes`@12, `animindex`@48 of the `StudioAnimDesc` | the same, and nothing else in the 72-byte descriptor |
| `weight`@0 of each selected bone's 32-byte record | both channel decoders, first, returning on zero |
| `offset[c]`@4+4c for the channels that decoder owns | position takes 0–2, quaternion 3–6 |
| `pos`@32, `quat`@44, `posscale`@60, `rotscale`@72, per channel | see `animation_and_movers.md` A.4 for which, when |
| two bytes per RLE run stepped over, plus the keys bracketing the frame | the walk stops at the frame; it does not cover the track |

Nothing else in the animation path is claimed. In particular the remaining 64 bytes of
each `StudioAnimDesc`, and the bulk of each 764-byte `StudioSeqDesc` beyond
`numblends`@52, `groupsize`@572, `paramindex`@580 and the fired grid cells, are read by
nobody this capture observed — which is recorded as unknown rather than as inert. Other
consumers do reach into that bulk: `flags`@8, the transition fade triple at `+0x264` and the
melee custom block at `+0x2BC`…`+0x2F8` are read outside the pose-sampling path, and are mapped
below.

**`numframes`@12 is the format's only frame count, and `StudioSeqDesc` holds no second
copy.** A brute-force scan of all 764 bytes of `StudioSeqDesc` and all 72 bytes of
`StudioAnimDesc` on the `courtroom_bip1..7` models — every int in 4400–4800 and every
float in 145–160, the ranges that would hold that clip's 4,701-frame length or its
duration in seconds — returns `int@12 = 4701` and nothing else, bar one unrelated
bounding-box float on bip6. The controls behave identically: `ghoul_heather` 340,
`kueijin_part1_bip1` 3461, `snuff_bip1` 1331, `give_prince_key` 2021,
`prince_escort_male` 1801, `jack_vs_sabbat` 461 and `embrace_bips1` 1731 each reach a
single `StudioAnimDesc` through their sequence's base blend cell, with `numframes`@12 the
only count and `fps`@4 beside it. This is worth stating because the opposite is easy to
assume: a clip of that length is nonetheless one continuous authored animation rather than
a splice — on `courtroom_bip4.mdl` all **2,016** animated channels' RLE runs cover exactly
4,701 frames each, none truncated, run boundaries fall on multiples of 255 (the
run-length byte cap), and only 1 channel of the 2,016 starts a run at frame 4500.

### Which of those bytes an offline decoder does not read

Differencing the ranges above against the ranges `elysium_pipeline.formats.mdl_skel`
reads, measured by observing that decoder rather than by describing it: over the same
`sp_theatre` capture, **9,936 of 3,430,830** consumed bytes across all **34** owner
images are read by retail and by nothing offline. They are five fields:

| Field | Bytes | Owners | Read by retail for |
|---|---:|---:|---|
| `StudioAnimRecord.weight`@0 | 9,016 | 34 | the zero test that ends a bone's decode |
| `StudioSeqDesc.groupsize`@572 | 360 | 34 | the blend grid's axis extents |
| `StudioSeqDesc.paramindex`@580 | 360 | 34 | the pose parameter driving each axis |
| `StudioSeqDesc.numblends`@52 | 180 | 34 | how many cells the grid holds |
| `StudioSeqDesc.anim[16][16]`@56 | 20 | 2 | the fired cells beyond `[0][0]` |

The four sequence-descriptor figures are the same population reached by counting: the
run fires 45 distinct `(owner, sequence index)` identities, so 45×4, 45×8, 45×8; and the
20 grid bytes are ten 2-byte cells on the two `move_and_ranged` owners, which is the
blend-cell shortfall the source join measured as clips rather than as bytes.

**The weight is 1.0 wherever it is read.** All **2,254** decoded
`(owner, animation, bone)` triples in the capture carry `1.0`, so on this corpus the
absent read costs no output; what is absent is the zero test, and a record carrying zero
would decode as an ordinary one instead of the zero output retail writes.

The difference in the other direction is a property of the two walks rather than a
disagreement, and is measured at **7,993,786** bytes: an offline walk materialises a
whole RLE track where retail walks to the sampled frame (7,358,840 bytes), reads all 160
bytes of every bone where retail reads the bind and scale dwords a mask selected
(384,176), reads every sequence a model declares where a run fires a few dozen, and
follows the name strings no captured span claims. **35,858,024** bytes of the 34 images
are read by neither, and stay unknown.

None of the missing bytes fall in a loader-written range, so all of them are readable
from the installed file. Re-running the same decode against the installed image rather
than the captured one records the identical byte set on all 34 owners, which is what
says an offline read set is a property of the source rather than of the copy it was
handed.

**The `StudioBone.Flags` rewrite is purely additive.** Comparing all 2,286 captured bones
against their disk bytes, 627 across 27 models change — and every change *sets* bits that
were clear; **no bit is ever cleared**. The bits set are `0x4` (95 bones), `0x8` (600), and
`0x20`–`0x8000` (112–122 each), consistent with per-bone usage flags the loader computes
from hitboxes, attachments and vertex LODs. Their individual meanings are undecoded.

**`0x2` is never touched**: 27 bones carry it on disk and the same 27 carry it at runtime,
none disagreeing. So the split-inheritance rule
(`docs/vtmb/animation_and_movers.md` A.4a) reads a bit the loader leaves alone, and a disk
read of it is what the runtime uses. A decoder that reads `Flags` for any *other* bit is
reading a pre-load value.

**That region is not unreachable, and it is not a gap.** It is the include-model bone remap
array, one per include group, addressed by `StudioModelGroup`+0x10 *relative to the group
entry* — an index no header field points at directly, which is why a walk from the header
alone misses it. Measured over the 27 include-carrying models in one capture, holding the
runtime image against the installed file: the `+0x10` offset is byte-identical on disk in
**41 of 41** groups, so it is authored rather than loader-written; every one of the 41 array
bases lands inside the region; and the arrays span **173,320 of 173,320 bytes — 100.0% of
it**. An array holds one 56-byte record per bone of the *including* model. The 24,234 bytes
that differ from disk are the loader completing records it did not author whole, not a
separate structure.

The record layout is read from the consumer rather than from disk, and is owned by
`docs/vtmb/animation_and_movers.md` A.4b together with the remap rule that applies it.

(Header continues with flex/IK/mouth/pose/anim-block fields — irrelevant to static props;
the facial block starts at **344**, not the 336 a naive VAMPTools field walk gives, and is
mapped in `docs/vtmb/facial_animation.md`.)

**`+396` / `+400` are a count/index pair no public description of the format carries**, and
they address an array of 28-byte bone-chain dynamics records. The runtime consumes a first
moving bone, gravity, damping, a base-10 spring exponent and an angular limit; the one remaining
float at record +8 is not passed to the located client consumer. The shipped chains drive hair,
ponytails, manes and breasts. Modern Source has
`keyvalueindex`/`keyvaluesize` and `numlocalikautoplaylocks` at that offset, which is why a
decoder ported from the later format reads the bytes as a keyvalues offset and discards them.
`surfacepropindex` sits at `+392` and `NumIncludeModels` at `+404`, which brackets the pair
unambiguously. Layout, solver, render path, corpus and confidence ledger:
`docs/vtmb/secondary_motion.md`.

**`NumIncludeModels`@404 / `IncludeModelIndex`@408** reference external `.mdl`s
(the shared animation banks) via `StudioModelGroup[]`; static scenery props generally have
none, skeletal NPCs pull a recursive tree (`docs/vtmb/animation_and_movers.md` A.7). **`StudioModelGroup`
is 116 bytes** (`int FilenameIndex`@0 + `int LabelIndex`@4 + `int Filler[27]`, per VAMPTools);
`FilenameIndex` is **relative to the group-entry base** and yields a `models/`-rooted path.
Both offsets probe-verified (jeanette@404=1 → `shared/female/npc_allsequences.mdl`;
gangmember_male_2 the same). `elysium_pipeline.formats.mdl_skel.read_includes` reads them.

**`Filler` is not filler.** Most of it is disk padding the loader fills in, which is why the
rewrite table above lists three ranges inside these records. Read from the runtime image and
from the consumer that walks them (`docs/vtmb/animation_and_movers.md` A.4b):

| Off | On disk | At runtime |
|---|---|---|
| 0x04 | `0` | pointer to the loaded include model; its vtable slot `+0x4` returns that model's `studiohdr` |
| 0x08 | `0x7fffffff` sentinel | first virtual sequence index this group owns |
| 0x0c | `0` | how many virtual sequence indices it owns |
| 0x10 | authored offset | bone remap array, relative to the group entry (see above) |
| 0x14, 0x18 | `-1` | written by the loader; meaning undecoded |
| 0x44 | — | 24 `short` pose-parameter indices, remapping the caller's parameters into the include model's own |

A sequence index below `NumLocalSeq`@272 resolves against the model's own sequences;
otherwise the group whose `[0x08, 0x08+0x0c)` range contains it owns the sequence, and the
index becomes local to that owner.

### `mstudiobonecontroller_t` — 24 bytes, and never authored

`NumBoneControllers`@248 / `BoneControllerIndex`@252. The record is **24 bytes** (Source's is
56), with `type` at +4, `start`/`end` at +8/+12, and the **`inputfield` at +0x14** — the engine
looks a controller up by matching `inputfield` against the requested index, not by array
position.

**`NumBoneControllers` is 0 on all 4,255 `.mdl` in the retail VPKs and all 339 loose Unofficial
Patch models.** The lookup therefore returns null on every model in the game, and
`Studio_SetController` returns its input unchanged without writing anything. This is what makes
VtMB's NPC head turn inert: it is applied through bone controllers 0 and 1
(`docs/vtmb/facial_animation.md` → Gaze).

### `StudioIKChain` (16B, array at `IKChainIndex`) — 239 models

| Off | Type | Field |
|---|---|---|
| 0 | int | `NameIndex` — **relative to the chain record**, not to the header |
| 4 | int | `LinkType` (`0` on every chain) |
| 8 | int | `NumLinks` (`3` on every chain) |
| 12 | int | `LinkIndex` — **relative to the chain record** |

`StudioIKLink` is **28 bytes**: `int Bone`, `Vector KneeDir`, `Vector` (unused).

239 models carry chains: 234 declare four, named `rhand`, `lhand`, `rfoot` and `lfoot`, and five
declare eight — the same four names twice over (`skeleton_male`, `skeleton_female`, `nines`,
`beckett`, `sewer_guard`). A consumer keying chains by name therefore dedupes rather than
assuming one record per name. Each chain resolves to a parent→child triple:

```
rfoot: Bip01 R Thigh    -> Bip01 R Calf    -> Bip01 R Foot
lfoot: Bip01 L Thigh    -> Bip01 L Calf    -> Bip01 L Foot
rhand: Bip01 R UpperArm -> Bip01 R Forearm -> Bip01 R Hand
lhand: Bip01 L UpperArm -> Bip01 L Forearm -> Bip01 L Hand
```

(4 models spell the arm bone `ForeArm`.) All 239 validate: 2,928 link bone indices, every one
in range and anatomically ordered.

**The 28-byte stride is the one that has to be measured rather than assumed.** Strides 16 and
20 also yield in-range bone indices on every model — because at those strides every link past
the first reads as bone 0, the root, which is trivially valid. Only 28 produces chains whose
links are parent→child. A reader that validates on "indices are in range" accepts the wrong
stride silently.

**`KneeDir` is `(0,0,0)` on all 2,928 links**, so no model authors a pole vector and the
runtime derives its own.

### `StudioPoseParamDesc` (20B, array at `LocalPoseParamIndex`) — 451 models

| Off | Type | Field |
|---|---|---|
| 0 | int | `NameIndex` — **relative to the record** |
| 4 | int | `Flags` (`1` = looping) |
| 8 | float | `Start` |
| 12 | float | `End` |
| 16 | float | `Loop` (wrap width; `0` = no wrap) |

The authored set across the install:

| Name | Range | Loop | Flags | Models |
|---|---|---|---|---|
| `move_yaw` | −180 … 180 | 360 | 1 | 449 |
| `hit_yaw` | −180 … 180 | 360 | 1 | 451 |
| `move_yaw` | −180 … 180 | 0 | 0 | 2 |
| `aim_yaw` | −45 … 45 | 0 | 0 | 2 |
| `aim_pitch` | −45 … 45 | 0 | 0 | 2 |

These are the axes a blend grid is indexed by, stated by the model rather than inferred from
the grid: the name, the domain, and whether the domain wraps. `move_yaw` is movement
direction relative to facing; `hit_yaw` is an independent hit-reaction direction carried by
essentially the whole cast.

### `SurfacePropIndex`@392 — the physical material

A single offset **from the file base** to a NUL-terminated name — the surface the model is made
of, which is what selects impact sounds, decals, break behaviour and the physics material.
1,923 models carry a name; the most common are `metal` (431), `flesh` (396), `wood` (373),
`plaster` (123), `stone` (88), `plastic` (76), `metalpanel` (60), `concrete` (52) and `glass`
(43). Names are mixed case (`metal` and `Metal` both occur), so a lookup folds case.

The offset is file-base, not field-relative: reading at `392 + value` puts 2,240 of the 2,522
unnamed models past EOF and lands the other 282 mid-string inside the bone-name pool
(`01 L Finger42`, `ip01 L LowerEyelid`).

**The field points at the head of the model's string pool, and the name is the pool's first
entry.** Bone 0's name therefore begins at `SurfacePropIndex + len(name) + 1`, an identity that
holds on **4,440 of the 4,442 bone-carrying models**. The two exceptions
(`monitor_useable.mdl`, `monitor.mdl`) are Unofficial Patch recompiles that append the string
to the pool tail instead; their offsets still decode correctly.

**On 2,522 models the property is genuinely unset** — the offset is valid and in range, and the
byte it addresses is `\0`. There is no second encoding. The decisive evidence is the sibling
`.phy`, whose trailing KeyValues block carries its own `surfaceprop` written by the same
studiomdl run from the same QC command: across 2,422 pairs that are retail-consistent (both
from the VPKs, so patch shadowing cannot contaminate the join), **2,418 agree exactly**,
including 1,202 where both are empty, and **not one retail pair is `.mdl` empty with `.phy`
named**. A name hidden under another encoding in the `.mdl` would still appear in plain text in
the `.phy`, so that asymmetry is what rules it out.

Whether a model carries one is an authoring pattern rather than a format rule, and it tracks
content type rather than any header flag — `character` 78.4% named, `gibs` 75.9%, `scenery`
42.3%, `items` 35.1%, `cinematic` 7.5%, `weapons` 6.3%, `hands` 0 of 21, `editor` 0 of 10.
Neither `STUDIOHDR_FLAGS_STATIC_PROP` (41.2% named without the bit against 44.0% with it) nor
`VertexListType` predicts it.

The vocabulary is shipped game data: **`scripts/surfaceproperties.txt` in the VPKs defines 63
properties** (`docs/vtmb/surface_properties.md`), and **1,891 of the 1,923 names (98.3%) are exact
members** of it. Three entries
are VtMB's own — `gargoyle`, `ming_xiao`, `ming_xiao_tentacle` — and `gargoyle.mdl` and the
gargoyle gib models carry `gargoyle`, a round trip through game data that a misread pointer
could not produce. The 32 stragglers are authored values the table does not define (`cloth` ×14,
`leather`, `organic`, `railing`, `sign`), QC typos naming the model instead of a surface
(`cement_mixer`, `metaldetector`, `floorblock`), case near-misses (`kitchen_pans` against
`Kitchen_Pan`), and `wesp5` on two Unofficial Patch models. Several of the undefined values are
echoed verbatim in the sibling `.phy`, so the field decodes correctly even where its value names
no defined property.

### `StudioSeqDesc` — fields outside the pose-sampling path

`NumLocalSeq`@272 / `LocalSeqIndex`@276, 764-byte stride. The descriptor's overall layout,
its blend grid, its activity key and **everything the engine does with the fields below**
are owned by `docs/vtmb/animation_and_movers.md` A.3; recorded here are the on-disk facts
for the fields the pose-sampling capture above does not reach.

**The transition fade triple — `+0x264` / `+0x268` / `+0x26c`.** Three floats carrying the
sequence's authored transition fade duration, in seconds, read by the client's transition
blender.

| Off | Type | Field | Note |
|---|---|---|---|
| 612 (`0x264`) | float | transition fade duration, seconds | |
| 616 (`0x268`) | float | the same duration | **byte-identical to `+0x264` in 100% of shipped content** |
| 620 (`0x26c`) | float | the same duration | likewise |

**The transition-graph node pair — `+0x270` / `+0x274`.** `entrynode` / `exitnode`, 1-based,
`0` meaning "no node" — the two endpoints `CBaseAnimating::FindTransitionSequence` matches against
the model's `NumTransitions`@336/`TransitionIndex`@340 adjacency matrix. **`0` on all 14,012
sequence descriptors across all 4,445 v2531 models**, the same corpus-wide silence as
`NumTransitions`@336 itself. The consuming mechanism and the corpus census are
`docs/vtmb/animation_and_movers.md` → "NPC activity resolution, sequence choice and transition
commit".

| 624 (`0x270`) | int | `entrynode` | `0` on all 14,012 sequence descriptors |
| 628 (`0x274`) | int | `exitnode` | `0` on all 14,012 sequence descriptors |

Because the three never differ, whatever cycle-dependent interpolation the engine performs
across them always collapses to one value per sequence. Across 331 loose models / 5,836
sequences the triple reads `(0.2, 0.2, 0.2)` ×5,762, `(0.3, 0.3, 0.3)` ×58 and
`(0.5, 0.5, 0.5)` ×16. The non-default clips are deliberate rather than noise: the 0.3 s
sequences are dialogue clips in `nines_damagedw.mdl`, `Jeanette.mdl`, `Therese.mdl` and
`Tourette.mdl`, and the 0.5 s ones are lying-down and damaged stance idles in
`hannah.mdl`, `heatherneardeath.mdl`, `lacroix.mdl` and `vv.mdl`.

**That survey is a large sample, not the whole corpus.** A capture of VPK-resident models
observes a fourth value, `0.45`, on the female `stances.mdl`, which the loose-model sweep
does not contain — so the four-value distribution above is a lower bound on the authored
vocabulary. Sweeping every VPK-resident `.mdl` for the triple is what would close it. How
the value is consumed is `animation_and_movers.md`.

**`flags`@8 — the corpus census.** Across the same 5,836 sequences the field takes exactly
five values, and nothing else occurs: `0x0002` ×2,642, `0x0000` ×2,217, `0x0001` ×815,
`0x0014` ×118, `0x0003` ×44. Notably `0x14` (`0x4 | 0x10`) is the additive family's whole
population — **118 sequences carry it, every one named `*_delta`, and all 118 `*_delta`-named
sequences carry exactly `0x14`, zero exceptions across the 331 models** — so the two bits
never separate on shipped content. What each bit *means* is `animation_and_movers.md`.

**The trailing custom block is a melee attack — `+0x2BC` … `+0x2F8`.** The descriptor's last 64
bytes are authored content rather than load-time scratch, and they are what the melee path reads.
What each field *does* is `docs/vtmb/combat-and-damage.md`; the on-disk facts are here, and the
whole map is `docs/vtmb/animation_and_movers.md` A.3.

The block declares **two independent record arrays**, and confusing them is the easy error: the
188-byte contact records at 708/712 are where the swing *touches*, while the 24-byte envelope
records at 700/704 are the volume the cast-arm selector tests an enemy against. They never
overlap on any shipped descriptor. Neither 700/704 nor 716 carries a recovered studiomdl name;
the labels below are semantic.

| Off | Type | Field | Note |
|---|---|---|---|
| 700 (`0x2BC`) | int | envelope-record count — how many reach envelopes the attack declares | **574 descriptors declare them**, the same 574 that state 708/712/720. Counts run 2 to 459, the maximum on `andrei`'s `JumpFromBlood_Attack` — which declares only 17 contact records for the same clip, so the two arrays are not parallel. A further **7** single-`idle` scenery models (`projector`, the three `libcolumn*`, `malklifetube_trims`, `handleclaws/ground/info`) read count `768` at offset `768`: the same descriptor-tail-into-the-string-table corruption their `numautolayers`@660 shows, decoding to subnormal garbage, and excluded rather than believed |
| 704 (`0x2C0`) | int | envelope index — descriptor-relative base of the envelope array | 24-byte stride, six floats per record. Positive and in-bounds on all 574; zero overlap with the 188-byte array anywhere in the corpus |
| 708 (`0x2C4`) | int | `numswingcentres` — how many contact records the attack declares | **574 of the install's 14,012 descriptors declare 1,587 records across 53 models**. The shipped maximum is 17 (`andrei`'s `JumpFromBlood_Attack`); both runtime consumers clamp before walking, so a decoder gates at that clamp rather than believing the count |
| 712 (`0x2C8`) | int | `swingcentreindex` — descriptor-relative base of the record array | 188-byte stride, measured over all 1,587 records |
| 716 (`0x2CC`) | float | low reach — the near edge of the reach band whose far edge is 720 | **`FLT_MIN` (`0x00800000`) is the unset marker here, not `FLT_MAX`**, and covers 13,496 of 14,012. **516 state a value.** The two edges are authored independently: 502 descriptors state both, **72 state a `reach` with no low edge** (both sexes' `Fists_attack_W2`, `baseball`'s W/A/Center swings, `tireiron`, `claws`, `sledgehammer`, `andrei`, `creation1_full`, `creation2_full`) and **14 state a low edge with `reach` unset** (`mingxiao`'s `Spit Attack`, low 250). `low <= reach` holds on all 502 with no ties. Two genuine authored zeros — `werewolf`/`werewolf_damaged` `claw_attack_close`, low `0.0` against reach 114.9 |
| 720 (`0x2D0`) | float | `reach` — the swing's target-acquisition distance, Source units | `FLT_MAX` is studiomdl's unset marker and covers 13,431 of the 14,012 descriptors; **581 state a reach**, spanning 21.9 to 768.3 units. Seven single-`idle` scenery/prop models carry a wholly zeroed custom block, so a non-positive or non-finite value is unstated rather than a swing that can never reach — the 574 that survive that gate are the melee population the rest of this block is authored on |
| 724 (`0x2D4`) | int | attack button mask — which direction key selects this attack | **`-1` is the "not a candidate" marker**, on 13,898 descriptors. The whole install states exactly five other values, all Source-side `IN_*` usercmd bits: `0` (neutral, 36), `IN_FORWARD` `0x008` (24), `IN_BACK` `0x010` (18), `IN_MOVELEFT` `0x200` (16), `IN_MOVERIGHT` `0x400` (20) |
| 728 (`0x2D8`) | int | dodge activity enum | load-resolved from 732; `-1` on disk |
| 732 (`0x2DC`) | int | dodge activity name — descriptor-relative | `ACT_DODGE_DUCK` on all **12** descriptors that state one |
| 736 (`0x2E0`) | int | blocked-reaction enum | load-resolved from 740; `-1` on disk |
| 740 (`0x2E4`) | int | `szblockedreactionindex` — descriptor-relative name index | **`-1` is this index's unset marker**, not Source's usual `0`. **147 descriptors across 22 models name one**, and every one of them also states a `reach` |
| 744 (`0x2E8`) | int | chain successor name — descriptor-relative | a **sequence label**, not an activity |
| 748 (`0x2EC`) | int | alternate successor name — descriptor-relative | the flying-knockback wall branch, on **28** descriptors of `meleeshared_onehand` — the only combo carriers stating no reach, reaction or swing |
| 752/756/760 (`0x2F0`/`0x2F4`/`0x2F8`) | float ×3 | combo window: open, close, hold, in clip cycles | the unauthored reading is `(0.0, 1.0, 1.0)` — a chain window that gates nothing, over a hold that covers the whole clip — on 13,841 descriptors; the seven zeroed-block models read `(0.0, 0.0, 0.0)` |

**208 of the 14,012 descriptors author at least one of the combo fields**, all across the 22 shared
weapon banks.

**Two of these fields are name indices the DLL resolves at model load** into the enum slots beside
them — 732→728 and 740→736 — exactly as it fills `activity`@12 from `szactivitynameindex`@4, which
is why the runtime reads the enum and an offline decoder must read the name. **744 and 748 are the
exception**: they name sequence *labels* and are resolved case-insensitively through
`LookupSequence` at runtime, so no enum slot exists for them.

**The band's `-1` census is a loose-model figure, and it does not generalize.** Across the 5,836
loose-model descriptors, `+0x2D8`, `+0x2DC`, `+0x2E0`, `+0x2E8` and `+0x2EC` are `-1` without
exception — but the loose corpus contains no shared weapon bank, and **the weapon banks author
`+0x2DC`, `+0x2E8` and `+0x2EC`**. Only `+0x2D8` and `+0x2E0` are load-time-only slots corpus-wide.

**The 188-byte swing-contact record**, every offset record-relative:

| Off | Type | Field | Note |
|---|---|---|---|
| `0x00` / `0x04` | float | `start` / `end` — the contact window as a fraction of the clip cycle | unitless. **Four records state the window backwards** — the `fists_attack_heavy` and `fists_attack_heavy_old` swings of both player sexes, all four `start=0.302, end=0.0` |
| `0x08` | int | bone index | the **declaring model's** own table, so the bone *name* is the durable key across an include chain |
| `0x0C` / `0x18` | float ×3 | the contact segment's endpoints | bone-local, Source units |
| `0x24` | int | unidentified | `-1` everywhere but 18 records, which state `0` |
| `0x28`..`0x34` | int ×4 | per-bucket knockback candidate counts | `-1` on every record that fills all four buckets; on the other 639 the last three are stated and the first on 267 of them, and every stated magnitude equals that bucket's candidate count (`gargoyle`'s `-2` against its two candidates included) |
| `0x38`..`0x74` | int ×16 | knockback activity enum slots | `-1` on all 1,587; load-resolved from the names at `0x78` |
| `0x78` / `0x88` / `0x98` / `0xA8` | int ×4 each | four direction buckets × four candidate activity **name** indices | record-relative, `0` when unset (Source's usual convention, unlike `szblockedreactionindex`'s `-1`). **4,286 of the 4,306 stated buckets name exactly one activity**; only `gargoyle`'s twenty attack records put a second name in a bucket |
| `0xB8` | byte | the direction bucket 0 answers | with the four directions cycling `BACK, LEFT, FORWARD, RIGHT`, bucket `k` answers `(b8 + k) mod 4` on **948 of 948** records that fill every bucket — so the buckets are a rotation, not a fixed order |
| `0xBA` | byte | `2` marks the knockback unconditional | |
| `0xBB` | byte | — | `0` on all 1,587 |

`0xB8` and `0xBA` are both `0xFF` on the 639 records that fill fewer than four buckets.

**Autolayers in the capture population — `numautolayers`@660 / `autolayerindex`@664.**
**461 of 2,478 captured sequence descriptors carry autolayers, and every one of them is in
`move_and_ranged.mdl`** (male or female), 0–2 entries per sequence: `smith_ready` →
`smith_aim_layer` (a 3×3 grid on the pose parameters `aim_yaw`/`aim_pitch`),
`baseballbat_aggressive_run` → `baseballbat_bobble_layer`, `smith_aggressive_run` →
`smith_aim_layer` + `smith_bobble_delta`. An entry is four bytes holding a bare sequence
index and carries no weight, no envelope and no flags, unlike modern Source's
`mstudioautolayer_t` — so the composition rule is **not derivable from the format**. It is read
out of the pose accumulator instead, and `animation_and_movers.md` owns it, along with the layer
weight that stays open and the install-wide census.

### `StudioBodyPart` (16B, array at `BodyPartIndex`)
| Off | Type | Field | Note |
|---|---|---|---|
| 0 | int | `NameIndex` | rel. to bodypart base |
| 4 | int | `NumModels` | |
| 8 | int | `Base` | |
| 12 | int | `ModelIndex` | **rel. to bodypart base** → `StudioModel[]` |

### `StudioModel` (**224B**, array at bodypart + `ModelIndex`)
The **224-byte stride is measured**, off every multi-model bodypart in
the install (`mingxiao`, `cashbox`, `flashlight`, `jewelery_box`, `doorknob_round_brass`, …):
consecutive bodyparts' model arrays sit exactly `NumModels × 224` apart. 4,423 of the 4,444
models have one model per bodypart, so only a bodygroup's second and later models depend on
it (`mdl.py` still walks at 160 and is therefore correct for everything the prop path
touches). Details: `docs/vtmb/facial_animation.md`.
| Off | Type | Field | Note |
|---|---|---|---|
| 0 | char[128] | `Name` | the compiling `.smd`'s relative path, NUL-terminated, zero-padded. **128, not modern Source's 64** — the same widening `MDLHeader.Name` and the `.vfe` header carry. Three shipped records run past 64 chars (64/68/69: the `w_{m,f}_flamethrower` and `w_f_sledgehammer` wield models); every byte between the NUL and offset 136 is zero on all 4,567 records, so the modern-Source reading (`char[64]` + type/radius/attachment fields at 64–76) reads name text on those three and never a value on any other |
| 128 | int | `Type` | 0 on all 4,567 records |
| 132 | float | `BoundingRadius` | 0.0 on all 4,567 records |
| 136 | int | `NumMeshes` | |
| 140 | int | `MeshIndex` | **rel. to model base** → `StudioMesh[]` |
| 144 | int | `NumVertices` | unique verts in this model |
| 148 | int | `VertexIndex` | **rel. to model base** → embedded `StudioVertex[]` |
| 152 | int | `TangentsIndex` | |
| 156 | int | `VertexListType` | 0=SKINNED(`StudioVertex` 44B), 1=UNSKINNED, 2=COMPRESSED |
| 160 | float[3] | `PositionOffset` | de-quantization base (= `HullMin`, or 0 when SKINNED) |
| 172 | float[3] | `PositionScale` | de-quantization scale (= span/65535, or 1 when SKINNED) |
| 192 | int | `NumEyeballs` | **2** on every character model |
| 196 | int | `EyeballIndex` | **rel. to model base** → `StudioEyeball[]` (140B) |
| 200 | int | `NumClothDefinitions` | 60 of 4,567 model records are nonzero |
| 204 | int | `ClothDefinitionTableIndex` | **rel. to model base** → relative definition-record offsets |
| 208 | int | `NumClothCapsules` | authored renderer-cloth collision capsules |
| 212 | int | `ClothCapsuleIndex` | **rel. to model base** → 36B records; ignore when count is zero |
| 216 | int | `NumClothSpheres` | authored renderer-cloth collision spheres |
| 220 | int | `ClothSphereIndex` | **rel. to model base** → 20B records; ignore when count is zero |

`StudioRender`'s vertex accessor reads `pos = u16 · PositionScale + PositionOffset` from
these **per-model** fields rather than from the header hull; the two agree numerically on
every shipped model, so the hull-interpolation rule below is equivalent, but these are the
authoritative source.

The eyeball pair at 192/196 is fixed by its consumer: the eye pass resolves a record as
`model_base + *(int*)(model_base + 0xC4) + materialparam × 0x8C`. **301 models carry two
records each** — the whole character cast, players included. The record's layout and the
system it feeds are `docs/vtmb/facial_animation.md`.

The cloth fields are likewise fixed by StudioRender consumers rather than value shape. The +204
table selects particle/constraint definitions; +208/+212 and +216/+220 are transformed through
the current bone palette into capsule and sphere collision primitives before every cloth solve.
A zero primitive count can retain a nonzero offset, so count is the validity gate. The definition
record layout, solver and exact Jeanette/Sheriff payloads are `docs/vtmb/secondary_motion.md`.

#### Cloth collision records

`ClothCapsuleIndex` addresses this exact 36-byte record:

| Off | Type | Field |
|---|---|---|
| 0 | int | `BoneA` |
| 4 | int | `BoneB` |
| 8 | float | `Radius` |
| 12 | float[3] | `EndpointABind` |
| 24 | float[3] | `EndpointBBind` |

`ClothSphereIndex` addresses this exact 20-byte record:

| Off | Type | Field |
|---|---|---|
| 0 | int | `Bone` |
| 4 | float | `Radius` |
| 8 | float[3] | `CentreBind` |

The point fields are model bind-space coordinates. StudioRender transforms each point through
`currentBoneToWorld[bone] * StudioBone[bone].poseToBone` once per model draw; capsule endpoints
use their respective bones. Radius is not matrix-scaled. A host representation that attaches a
primitive directly to a bone first converts each bind-space point through that bone's
`poseToBone` inverse bind matrix. Runtime interpolation and collision behavior are
`docs/vtmb/secondary_motion.md`.

### `StudioMesh` (~60B, array at model + `MeshIndex`)
`VertexData` here is `MeshVertexData` (1 ptr + int[8] = 36B).
| Off | Type | Field | Note |
|---|---|---|---|
| 0 | int | `Material` | a **skinref**, not a texture index — resolve through the skin table (below) |
| 4 | int | `ModelIndex` | rel. offset back to owning model (negative) |
| 8 | int | `NumVertices` | verts in this mesh |
| 12 | int | `VertexOffset` | this mesh's first vert within the model's vertex array |
| 16 | int | `NumFlexes` | facial morph targets — `docs/vtmb/facial_animation.md` |
| 20 | int | `FlexIndex` | **rel. to the mesh base** → `StudioFlex[]` (32B) |
| 24 | int | `materialtype` | **1 = this mesh is an eyeball**, 0 otherwise |
| 28 | int | `materialparam` | which eyeball — index into the model's `StudioEyeball[]` |
| 32 | int | `MeshId` | runtime field; not needed by the offline geometry decode |
| 36 | float[3] | `Center` | runtime field; not needed by the offline geometry decode |
| 48 | int | `ClothSelectorIndex` | **rel. to mesh base** → `uint8[NumVertices]`; `0xFF` means ordinary skinning |
| 52 | int | `ClothPositionNormalIndex` | **rel. to mesh base** → `uint16[NumVertices]`; high bit reverses normal |
| 56 | int | `ClothTangentIndex` | **rel. to mesh base** → `uint16[NumVertices]` simulated tangent-output indices |

`materialtype`/`materialparam` are read by the hardware draw loop, which dispatches a
`materialtype == 1` mesh to the eye path and every other mesh to the ordinary one. **598 meshes
across 299 models are flagged**, always in pairs with `materialparam` 0 and 1
(`docs/vtmb/facial_animation.md`).

The final three fields are authored only for renderer cloth. All three are nonzero together on
**84 of 9,741** installed mesh records and never appear partially. Generated StudioRender vertex
routines read the selector first: `0xFF` preserves the ordinary skinned vertex; any in-range cloth
definition selects simulated position/normal/tangent output through +52/+56. Those maps are a
post-skin substitution, so the presence of valid biped weights on the same vertices does not imply
the surface is rigid. Carrier census and runtime behavior are `docs/vtmb/secondary_motion.md`.

### Embedded vertices — three formats keyed by `StudioModel.VertexListType`

Array at `model + VertexIndex`; mesh *m*, mesh-local id *i* → global index
`Mesh.VertexOffset + i`. **Scenery distribution (3,330 `models/scenery/**` .mdl):
SKINNED 60% / UNSKINNED 31% / COMPRESSED 9%** — all three are needed. Empirical
stride confirmed by `(TangentsIndex − VertexIndex) / NumVertices`. All three
**probe-validated** (positions land in `[HullMin,HullMax]`, UVs clean):

**`VertexListType == 0` — SKINNED `StudioVertex` (44B, exact).** `BoneWeight` is 12B — three
weight bytes, the selector byte, then four `short` bone indices — so the record is 12+12+12+8
(not the packed 42 the header comment implies):
| Off | Type | Field |
|---|---|---|
| 0 | byte[3] | `BoneWeight.Weight` — three stored; a fourth influence's weight is the shortfall `255 - sum` |
| 3 | byte | `BoneWeight.InfluenceSelector` — influence count is `selector % 5`, clamped to at least 1 |
| 4 | short[4] | `BoneWeight.Bone` — **four** slots, signed |
| 12 | float[3] | `VecPosition` (real Source coords) |
| 24 | float[3] | `VecNormal` |
| 36 | float[2] | `VecTexCoord` (u,v) |

The fourth slot and the selector are established from both sides. In the installed corpus,
`{selector % 5 == 4}` and `{Weight[0]+Weight[1]+Weight[2] < 255}` are **the same set exactly** —
1,012 vertices of 5,155,870, zero disagreement in either direction; `sum > 255` never occurs, and
the shortfall runs 13..64. A bone slot is zero for every index at or past the count, no index
inside the count is out of range or duplicated, and no weight inside the count is zero. In
`StudioRender.dll`, the frame blend helper loads the count with
`movzx ecx, byte [eax+3]` / `movzx ecx, byte [ecx+0x2c06c930]`, reads the fourth index with
`movsx edi, word ptr [eax+0xa]` (a **word** load, which is what fixes slot 3's width — the byte at
+11 is zero on every shipped vertex), and forms the fourth weight as `weightTable[255 - sum]` by
subtracting the running sum from `&weightTable[255]`. The count table at `0x2c06c930` is 256 bytes
of literal `i % 5`; the clamp to 1 lives in the consumer, whose dispatch falls through to the
one-matrix arm for both 0 and 1. **Standing: Verified.**

Both packed forms share one shape — quantized position, **one** packed-normal
slot, then **u16 u, u16 v** — settled by an install-wide sweep (all 1,082
UNSKINNED + 361 COMPRESSED models): the normal slot is bit-identical across all
three corners on ~35% of triangles (flat faces share a normal; real UVs never
do), and reading it as U collapses 100% of some models' UV triangles to zero
area — texture smeared into vertical streaks. With the layouts below, no model
is degenerate and the 3D→UV mapping is near-conformal (median log₁₀ singular
-value ratio 0.22, vs 0.85+ for every other channel pairing).

**`VertexListType == 1` — UNSKINNED `StudioVertex2` (12B, 6× u16).** Positions are
hull-normalized; **decode = `HullMin + (u16/65535)·(HullMax−HullMin)`** per axis
(VAMPTools' `·HullMin`-only form is wrong):
| Off | Type | Field |
|---|---|---|
| 0 | u16[3] | position scalars (x,y,z) → hull-interpolate |
| 6 | u16 | packed normal — byte offset into the `StudioRender` table |
| 8 | u16[2] | `VecTexCoord` (u,v) = raw/65535 |

**`VertexListType == 2` — COMPRESSED `StudioVertex3` (8B).** Byte-quantized
position, full-precision UVs:
| Off | Type | Field |
|---|---|---|
| 0 | byte[3] | position scalars → `HullMin + (b/255)·(HullMax−HullMin)` |
| 3 | byte | packed normal — index into the `StudioRender` table |
| 4 | u16[2] | `VecTexCoord` (u,v) = raw/65535 |

### Every model carries authored per-vertex normals

All three vertex formats store a normal, and the storage differs only in how it is addressed.

**SKINNED — `VecNormal`@24 is a plain `float[3]` and needs nothing.** No table, no unpacking,
no DLL. It is the format every character uses, and it accounts for **3,124 of the 4,567
`StudioModel` entries** in the install. Sampling 129,870 vertices across 3,027 models returns
**129,302 exactly unit-length** normals; the 568 exceptions are exactly `(0,0,0)` — degenerate
vertices in a handful of models (`w_f_holylight`, `toolboxtop_a`, `desk`), so a reader needs a
fallback for a zero-length normal but for nothing else.

**UNSKINNED (1,082 entries) and COMPRESSED (361) address a table compiled into
`StudioRender.dll`**, not the model: UNSKINNED's u16 is a *byte offset* into the 5,314-entry
unit-vector table at `0x2C06E008`, COMPRESSED's byte is an index into a second table at
`0x2C06D358` with stride 16. The u16 form is confirmed on real geometry — every offset on
`handrailbbrass` is divisible by 12, the index range is exactly `0..5313`, every decoded vector
is unit length, and the decoded normals sit at p50 14.1° / p99 31.0° from their own face
normals with none beyond 90°, which is what a correctly-decoded smoothed set looks like.
`elysium_pipeline.formats.mdl_skel.read_anorms` already extracts the table for the facial
pipeline (`docs/vtmb/facial_animation.md` → "The unit-vector table",
`research/tooling/probes/probe_facial.py --anorms`).

**The authored normals are not recoverable by averaging.** On a clean sphere they agree with a
recomputation to a fraction of a degree — Jack's eyeballs land within 0.03–0.42°, which is what
establishes that the stored vectors share the geometry's frame and sign. Everywhere the artist
authored smoothing they diverge sharply, measured over Jack's whole model:

| Recomputation | mean | median | p95 | max | >20° off |
|---|---|---|---|---|---|
| area-weighted (unnormalized face normals) | 14.75° | 8.10° | 46.9° | 116.8° | 28.4% |
| unweighted (unit face normals) | 11.73° | 1.24° | 44.2° | 115.2° | 25.5% |

The divergence is authored hard edges. 214 of the head's 1,382 vertices are co-located
duplicates — studiomdl's split vertices — and ~10% of those carry genuinely different normals
across the split, at nostrils, eye sockets, mouth corners and the hairline. Averaging over
adjacent faces cannot reproduce a split normal by construction, so neither weighting is a
better approximation than the other; both discard information the file states outright.

### Skin table — `short skinref[NumSkinFamilies][NumSkinRefs]` at `SkinIndex`

`skinTable[family][skinref]` → texture index. A mesh's `StudioMesh.Material` is a **skinref**;
the family row remaps it, which is how one model draws several skins — a traffic light's
red/walk/flashing/yellow, a doorknob's locked/unlocked, a laser emitter's armed/idle. The entity
picks the row with its `skin` keyfield/input (`m_nSkin`).

**Family 0 is the identity row on all 4,445 readable models in the install**, so reading
`StudioMesh.Material` as a texture index yields the correct skin-0 material — and nothing else.
201 models carry more than one family; 12 have an absent or unreadable table (treat as a single
identity family). `pipeline/src/elysium_pipeline/formats/mdl.py::skin_table` / `skin_families` decode it, and
`write_obj_scene` emits `props/<stem>.skins` for any model with alternate families.

Worked example — `models/scenery/street/StreetLight/StreetLight.mdl`, 15 textures / 9 skinrefs /
5 families:

| family | pedlight | lightred | lightyellow | lightgreen | state |
|---|---|---|---|---|---|
| 0 | `pedlight_stop` | `lightred_on` | `lightyellow` | `lightgreen` | red |
| 1 | `pedlight_Walk` | `lightred_OFF` | `lightyellow` | `lightgreen_ON` | green + walk |
| 2 | `pedlight_Flash` | `lightred_OFF` | `lightyellow` | `lightgreen_ON` | green + flashing |
| 3 | `pedlight_stop` | `lightred_OFF` | `lightyellow_ON` | `lightgreen` | amber |
| 4 | `pedlight_flash002` | `lightred_OFF` | `lightyellow` | `lightgreen` | all off + flashing |

The swap carries emission with it: `lightred_on` has a `$selfillum` mask, `lightred_OFF`
(basetexture `lightred`) has none — so the lamp's glow goes out with its colour.

### `StudioTexture` (material) — array at `TextureIndex`
| Off | Type | Field | Note |
|---|---|---|---|
| 0 | int | `NameIndex` | **rel. to the StudioTexture base** → material name string |
| 4 | int | `Flags` | |
| 8 | int | `Used` | |
| 12 | ptr | `material` | runtime (4B x86) |
| 16 | ptr | `clientmaterial` | runtime (4B x86) |

On-disk stride is smaller than modern Source's 64B (VtMB drops `unused[10]`); **probe
the stride** (candidates 20/24/64) by checking each `NameIndex` yields sane ASCII.
The name is a **material name** (e.g. `chinabldg04`), resolved to a `.vmt` via the
search paths — feeds straight into our existing VMT + TTH/TTZ resolver + PAKFILE.

Texture **search paths**: `NumTextureSearchPaths` ints at `TextureSearchPathIndex`;
each int is a header-relative offset to a path string (e.g. `models/scenery/…/`).

## `.dx80.vtx` — `OptimizedModel` (`#pragma pack(1)`, exact offsets)

Parallel tree to the `.mdl`; you walk both in lockstep. All `*Offset` fields are
**relative to the struct that owns them**.

### `FileHeader_t` (36B @0)
| Off | Type | Field |
|---|---|---|
| 0 | int | `version` (== **107** in VtMB, not the stock 7) |
| 4 | int | `vertCacheSize` |
| 8 | u16 | `maxBonesPerStrip` |
| 10 | u16 | `maxBonesPerTri` |
| 12 | int | `maxBonesPerVert` |
| 16 | int | `checkSum` (matches `.mdl`) |
| 20 | int | `numLODs` |
| 24 | int | `materialReplacementListOffset` |
| 28 | int | `numBodyParts` |
| 32 | int | `bodyPartOffset` |

### Nesting (all offsets relative to the owning header)
- `BodyPartHeader_t` (8B): `numModels`@0, `modelOffset`@4 → `ModelHeader_t[]`
- `ModelHeader_t` (8B): `numLODs`@0, `lodOffset`@4 → `ModelLODHeader_t[]`
- `ModelLODHeader_t` (12B): `numMeshes`@0, `meshOffset`@4, `switchPoint`@8 → `MeshHeader_t[]`
- `MeshHeader_t` (8B): `numStripGroups`@0 (u16), `stripGroupHeaderOffset`@4 → `StripGroupHeader_t[]`
- `StripGroupHeader_t` (**20B** — VtMB's compact form, probe-confirmed): `numVerts`@0 (u16),
  `numStrips`@4 (u16), `vertTableOffset`@8, `indexOffset`@12, `stripOffset`@16
- `StripHeader_t` (16B): `numIndices`@0 (u16), `indexOffset`@2 (u16), `numVerts`@4 (u16),
  `vertOffset`@6 (u16), `flags`@8 (byte), `spacing[7]`@9

**Vertex table**: array at `stripgroup + vertTableOffset`, one entry per
`StripGroupHeader_t.numVerts`, each yielding a **mesh-local vertex ID**. VtMB writes
it in **two forms**, selected by `StripGroupHeader_t.flags` at **+6** — bit `0x08` marks the
12-byte boned record, bit `0x10` the flat table:

| Form | Stride | ID | Used by |
|---|---|---|---|
| flat | 2B | `u16` @0 | retail's static props |
| record | 12B | `u16 origMeshVertID` @**10** | every non-static model, plus the patch's 22 recompiled static models |

The 12-byte record is all-`u16`, and is **not** stock Source's 9-byte
`byte boneWeightIndex[3]; byte numBones; ushort origMeshVertID; char boneID[3]`:

| Off | Type | Field |
|---|---|---|
| 0 | u16 | `boneWeightIndex` — row into the 65-row permutation table (below) |
| 2,4,6,8 | s16[4] | `boneID` — hardware palette slot, `0xffff` unused |
| 10 | u16 | `origMeshVertID` — mesh-local, the only field the pipeline reads |

The low bits of the flags byte are the render-path selector, so the observed values are
`0x09` = FLEXED|record, `0x0a` = HWSKINNED|record, `0x0b` = both|record, `0x0c` = flat with bit
`0x04`. `StripHeader_t` is likewise richer than a `spacing[7]` filler: u16 `numIndices`@0,
u16 `indexOffset`@2, u16 `numVerts`@4, u16 `vertOffset`@6, byte `numBones`@8, byte `flags`@9,
u16 `numBoneStateChanges`@10, int `boneStateChangeOffset`@12 relative to the strip header. A
`BoneStateChange` is 4 bytes: `u16 hardwareID`, `u16 newBoneID`.

The hardware palette resolves to ordinary `.mdl` bone order: over 60 character models,
`[BSC[slot] for slot in record.boneID]` equals the `.mdl` `StudioVertex.Bone[]` in order on
**232,780 of 232,782** vertices, across 672 non-identity bone-state-change tables. A decoder that
reads skin from the `.mdl` therefore needs no remap. **Standing: Verified.**

`STUDIOHDR_FLAGS_STATIC_PROP` does **not** predict the form: across the install's
4,425 models every flat model is static (3,278/3,278), but 22 static models are the
record form — all of them loose Unofficial Patch files (`vw_bus`, `paneltruck/truck`,
the `lightningbird`/`limo`/`yugo`/`copcar` vehicles, `libcolumn_*`, `projector`, …),
recompiled by the patch team with a tool that emits bone records regardless.

**Detect per stripgroup** (`mdl._vtable_form`): read `StripGroupHeader_t.flags`@+6 and take
bit `0x08` for the record form, `0x10` for the flat one. The distinctness heuristic that
predates the flags read — try both, keep the form whose IDs are all distinct and
`< StudioMesh.NumVertices` — agrees with the flag on every stripgroup measured, and remains a
useful cross-check: the wrong form reads bone bytes as IDs and collides or overshoots. (Reading
the flat form on a record-form model sends offsets past EOF: that is the
`unpack_from requires a buffer of at least …` failure.)

The ~405 models neither form fits are the skeletal `character/**` NPCs, which the prop
pipeline does not decode; `decode` raises on them rather than emitting garbage.
**Index table**: array of u16 at `stripgroup + indexOffset` — each indexes the vertex table.

## The walk (LOD0 only) — from `Mesh.cpp`

```
for bp in range(header.NumBodyParts):                 # mdl & vtx in lockstep
  mdl_part, vtx_part = mdl.bodypart(bp), vtx.bodypart(bp)
  for m in range(mdl_part.NumModels):
    model, vtx_model = mdl_part.model(m), vtx_part.model(m)
    vtx_lod = vtx_model.lod(0)                         # LOD0
    for mi in range(model.NumMeshes):
      mesh     = model.mesh(mi)                        # StudioMesh
      vtx_mesh = vtx_lod.mesh(mi)                      # MeshHeader_t
      material_name = texture(mesh.Material).name      # → .vmt → basetexture
      for sg in vtx_mesh.stripgroups:
        stride, off = detect_vtable_form(sg)           # (2,0) flat or (12,10) record
        vtable = bytes at sg.vertTableOffset
        itable = u16[] at sg.indexOffset
        for strip in sg.strips:
          for k in range(0, strip.numIndices, 3):      # tri list
            a,b,c = itable[strip.indexOffset + k + 0..2]
            for local_id in (a,b,c):
              mesh_vert_id = u16 at vtable + local_id*stride + off
              global_id    = mesh.VertexOffset + mesh_vert_id
              v = StudioVertex at model + model.VertexIndex + global_id*44   # SKINNED stride
              # v.VecPosition, v.VecNormal, v.VecTexCoord
```

Notes:
- **Winding:** VAMPTools emits `(a,c,b)` (reversed) for FBX; the world path is
  `cull_disabled`, so winding only affects normals/backfaces there. As stored, a triangle's
  geometric normal is `−(p1−p0) × (p2−p0)` — the stored `VecNormal` is anti-parallel to the
  unnegated cross product, so a check that compares the two without the sign reads ~180° and
  looks like a decode failure rather than a winding convention.
- **UV V:** VAMPTools flips to `1 - v` for FBX; our world exporter uses UVs as-is
  (Source convention).
- **Coordinates:** `pipeline/src/elysium_pipeline/formats/mdl_gltf.py` emits standard glTF 2.0
  as the on-demand inspection product — self-describing, so any glTF consumer reorients it into
  Unreal space with no manual `source_to_unreal` conversion (the one exemption to the `UE_`
  exporter convention; see `pipeline/CLAUDE.md`). The `sprp` per-instance origin/angles/scale
  still apply on top when placing the prop.

## Materials → our existing pipeline

`StudioTexture.name` (+ search paths) → `<name>.vmt` → `$basetexture` →
`<basetexture>.tth`/`.ttz`. Every step exists: `pipeline/src/elysium_pipeline/formats/vmt.py`,
`pipeline/src/elysium_pipeline/formats/tex_to_png.py`, PAKFILE resolution in `pipeline/src/elysium_pipeline/formats/bsp.py`. Model material resolution
reuses the world resolver verbatim.
