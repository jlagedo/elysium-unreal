{
  "version": 1,
  "topic": "animation_skinning",
  "consuming_document": "docs/vtmb/animation_and_movers.md",
  "research_id": "RE32",
  "program": "engine.dll",
  "binary": {
    "size": 1986620,
    "sha256": "9d00b2c1e5edbad052fb0b514634ba54574666e12d1b408c46ce141d51ed2313",
    "md5": "4c60ce9bc98fcf4c5253106b7d8468e1",
    "image_base": "0x20000000"
  },
  "lock_delay_seconds": 9,
  "questions": [
    "What does the fifth engine-private SetupBones callback argument point at? Its optionality is settled - CModelRender::DrawModel passes the address of a stack local and CModelRender::DrawModelShadow passes NULL for the same slot - but what the callee writes there is not.",
    "Which CModelRender state fields choose the StudioRender ordinary, flexed, and material-specific mesh branches?",
    "Which function does the model-loader interface slot +0x18 resolve to, and is it the complete set of paths that free a studio header from the model cache?",
    "What do the render info's two 16-bit fields at +0x08 and +0x0a select, and what are the dwords at +0x0c, +0x10, +0x14 and +0x1c?"
  ],
  "findings": [
    {
      "confidence": "confirmed",
      "fact": "CModelRender::RenderModel builds the studio draw's render info as eight dwords at +0x00 through +0x1c, and CStudioRender::DrawModel reads no further.",
      "evidence": "0x200a6191-0x200a61d5 fills the stack local passed as the first argument at 0x200a6210-0x200a6221: +0x00 the studio header, +0x04 the renderable plus 0xc0, two 16-bit fields at +0x08 and +0x0a defaulted to 0xffff and otherwise loaded from a 24-byte-stride per-instance table, dwords at +0x0c, +0x10, +0x14 and +0x1c, and the entity at +0x18. StudioRender 0x2c004fc7 reads +0x00, 0x2c005150 and 0x2c005110 zero-extend the two 16-bit fields, and 0x2c00511d-0x2c005119 pass +0x0c, +0x10 and +0x14 on.",
      "consequence": "The whole struct is a bounded 32-byte copy off a pointer the draw hook already holds, so the capture retains it verbatim; only +0x00 and +0x18 are decoded, and what the remaining fields mean stays an open question rather than a field name."
    },
    {
      "confidence": "hypothesis",
      "fact": "CEngineClient::UnloadModel dispatches a model release through the model-loader interface at vtable slot +0x18 with a flag of 8.",
      "evidence": "0x2001a7b0 pushes the VProf marker string \"CEngineClient::UnloadModel\" at 0x201976f4 and then calls (**(code **)(*(int *)0x201a6774 + 0x18))(model, 8). CModelLoader is present as an RTTI class with no methods attributed, its named members are FindModel 0x200b87a0, LoadModel 0x200b8960, GetExtraData 0x200bb6f0 and Map_IsValid 0x200bb8d0, and the Quake cache asserts \"Cache_Free: not allocated\" and \"Cache_UnlinkLRU: NULL link\" appear inlined across ten functions rather than in one Cache_Free body.",
      "consequence": "A model-cache free target for the census exists but is not yet pinned: the interface slot has to be resolved to a function and proved to be the complete set of free paths before a hook may be declared, the way the two engine draw frames were. Until then the census reports residency and a vanished count instead of an unload event."
    },
    {
      "confidence": "confirmed",
      "fact": "client.dll submits animated entities through VEngineModel006 method slot +0x8.",
      "evidence": "client.dll 0x10092f18-0x10092f51; VEngineModel006 acquisition at 0x100cb5e7-0x100cb5f3.",
      "consequence": "The SetupBones callback and final skinning seam cross from client.dll into engine.dll."
    },
    {
      "confidence": "confirmed",
      "fact": "The engine VEngineModel006 factory returns the static object at 0x20d63830.",
      "evidence": "registration 0x200a5c20, factory thunk 0x200a5c40.",
      "consequence": "The runtime-installed vtable can be recovered from the object's constructor."
    },
    {
      "confidence": "confirmed",
      "fact": "The VEngineModel006 object receives vtable 0x20174c54 and its +0x8 DrawModel slot is 0x200a6640.",
      "evidence": "constructor write at 0x200a5b76 and raw vtable dword at 0x20174c5c.",
      "consequence": "0x200a6640 is the engine-side continuation of C_BaseAnimating::InternalDrawModel."
    },
    {
      "confidence": "confirmed",
      "fact": "CModelRender::DrawModel is __thiscall with nine callee-cleaned dword stack arguments, returns int in EAX, and its relocatable prologue is nine bytes with no relative operand.",
      "evidence": "Both exits at 0x200a676b and 0x200a6971 are RET 0x24 preceded by XOR EAX,EAX and MOV EAX,0x1; 0x200a6640 is 83ec248b1528331720 (SUB ESP,0x24; MOV EDX,[0x20173328]), and nothing branches into 0x200a6640-0x200a6648.",
      "consequence": "The engine draw frame can be bracketed by an inline detour, which is what encloses a pose build and the StudioRender draw it feeds for every client class rather than only for C_BaseAnimating."
    },
    {
      "confidence": "confirmed",
      "fact": "Most studio draws do not pass through C_BaseAnimating::InternalDrawModel, including draws of fully animated cast models.",
      "evidence": "One complete sp_theatre capture records 399,198 CStudioRender::DrawModel calls against 90,747 C_BaseAnimating::InternalDrawModel calls; the unenclosed draws include character/npc/unique/Downtown/Lacroix/Lacroix.mdl, Sheriff.mdl and character/pc/male/brujah/armor0/brujah_Male_Armor_0.mdl.",
      "consequence": "The client draw entry is not the common parent of the pose build and the draw; CModelRender::DrawModel is, so draw attribution is scoped from engine.dll rather than client.dll."
    },
    {
      "confidence": "confirmed",
      "fact": "CModelRender::DrawModel invokes IClientRenderable::SetupBones through renderable vtable slot +0x3c with five staged arguments.",
      "evidence": "engine.dll 0x200a68a7-0x200a68ca; client public vtables place DrawModel and SetupBones 0x18 bytes apart, matching slots +0x24 and +0x3c.",
      "consequence": "The client bone-to-world matrices are produced directly into an engine-provided output buffer on the render submission path."
    },
    {
      "confidence": "confirmed",
      "fact": "VEngineModel006 slot +0x34 is a tail thunk to TStudioRender012 slot +0x40.",
      "evidence": "engine.dll 0x200a6f40-0x200a6f48.",
      "consequence": "StudioRender allocates or exposes the bone output buffer; the same four staged arguments remain on the stack before the client SetupBones call."
    },
    {
      "confidence": "confirmed",
      "fact": "CModelRender::RenderModel submits prepared model state through TStudioRender012 slot +0x58.",
      "evidence": "engine.dll 0x200a6208-0x200a6221.",
      "consequence": "The remaining poseToBone and vertex-skinning trace belongs in StudioRender.dll."
    },
    {
      "confidence": "confirmed",
      "fact": "Exactly two sites in engine.dll call TStudioRender012 slot +0x58, and no other loaded module holds the interface.",
      "evidence": "Only engine.dll and StudioRender.dll contain the string TStudioRender012; the interface pointer lives in engine global 0x20d63ef0, written only by its acquisition at 0x200a5a40 and cleared at 0x200a5aa1; tracking every one of the 72 reads of that global to the vtable register it feeds yields slot +0x58 at 0x200a6221 and 0x200a6ce4 only.",
      "consequence": "The set of routes to CStudioRender::DrawModel is closed, so a draw that no bracket encloses is a missing hook rather than an unknown path."
    },
    {
      "confidence": "confirmed",
      "fact": "CModelRender::DrawModelShadow is the second route to CStudioRender::DrawModel, and it is not reachable from CModelRender::DrawModel.",
      "evidence": "0x200a6990 opens the VProf marker CModelRender::DrawModelShadow at 0x201a6280 and calls slot +0x58 at 0x200a6ce4; it is VEngineModel006 vtable slot +0x44 (0x20174c98); the only other +0x58 site, 0x200a6221, sits in CModelRender::RenderModel 0x200a5f00, whose single direct caller is 0x200a6945 inside CModelRender::DrawModel.",
      "consequence": "Two sibling engine frames submit studio draws, so one draw bracket cannot enclose them all."
    },
    {
      "confidence": "confirmed",
      "fact": "CModelRender::DrawModelShadow is __thiscall with four callee-cleaned dword stack arguments, returns nothing, and its relocatable prologue is six bytes with no relative operand.",
      "evidence": "Both exits at 0x200a6e1f and 0x200a6e52 are RET 0x10 with EAX untouched; 0x200a6990 is 8b1528331720 (MOV EDX,[0x20173328]); nothing branches into 0x200a6990-0x200a6996 and no data word points inside it; the client call site at 0x100d7bc5 stages four arguments.",
      "consequence": "The shadow draw frame can be bracketed by an inline detour on the same terms as the ordinary one."
    },
    {
      "confidence": "confirmed",
      "fact": "The shadow frame builds its own pose: CModelRender::DrawModelShadow calls IClientRenderable::SetupBones through the same renderable slot +0x3c, staging the bone buffer from VEngineModel006 slot +0x34, boneMask 0x15e and an LOD-shifted maxBones, and passing NULL for the fifth argument.",
      "evidence": "engine.dll 0x200a6c4e-0x200a6c7b stages PUSH 0 / PUSH float / PUSH edi / PUSH 0x15e / PUSH eax, against 0x200a68a7-0x200a68ca in CModelRender::DrawModel which stages LEA ECX,[ESP+0x20] in the same slot.",
      "consequence": "A pose build is not evidence of the ordinary draw frame; the fifth argument is an optional caller-supplied pointer rather than a required one."
    },
    {
      "confidence": "confirmed",
      "fact": "The client shadow manager is what enters the shadow frame, through VEngineModel006 slot +0x44.",
      "evidence": "client.dll holds the interface at global 0x104a57d8 and calls slot +0x44 only at 0x100d7bc5, inside the routine that immediately before sets the shadow material variables $shadowdir, $toppoint, $bottompoint, $topcolor and $bottomcolor at 0x100d7af9-0x100d7b85.",
      "consequence": "The unbracketed draws are shadow submissions of actors that the ordinary frame also draws, not draws of unseen actors."
    }
  ],
  "functions": [
    {
      "address": "200a6640",
      "label": "c_modelrender_drawmodel",
      "confidence": "confirmed",
      "role": "VEngineModel006 slot +0x8; engine-side animated-model draw entry reached from client C_BaseAnimating::InternalDrawModel.",
      "depth": 2,
      "cap": 180,
      "asm_count": 3200
    },
    {
      "address": "200a59e0",
      "label": "init_studiorender",
      "confidence": "confirmed",
      "role": "Loads StudioRender.dll and acquires TStudioRender012 into engine global 0x20d63ef0.",
      "depth": 2,
      "cap": 100,
      "asm_count": 900
    },
    {
      "address": "200a5f00",
      "label": "c_modelrender_rendermodel",
      "confidence": "confirmed",
      "role": "CModelRender::RenderModel; calls TStudioRender012 slot +0x58 at 0x200a6221 and is reached only from CModelRender::DrawModel at 0x200a6945.",
      "depth": 2,
      "cap": 160,
      "asm_count": 2600
    },
    {
      "address": "200a6990",
      "label": "c_modelrender_drawmodelshadow",
      "confidence": "confirmed",
      "role": "VEngineModel006 slot +0x44; the second engine draw frame, entered from the client shadow manager, which builds its own pose and calls TStudioRender012 slot +0x58 at 0x200a6ce4.",
      "depth": 2,
      "cap": 180,
      "asm_count": 3200
    },
    {
      "address": "200a6f40",
      "label": "c_modelrender_get_bone_buffer",
      "confidence": "confirmed",
      "role": "VEngineModel006 slot +0x34 tail-forwards to TStudioRender012 slot +0x40, returning the bone output buffer used as SetupBones argument one.",
      "depth": 2,
      "cap": 120,
      "asm_count": 1400
    }
  ],
  "relationships": [
    {
      "from": "10092970",
      "to": "200a6640",
      "confidence": "confirmed",
      "evidence": "cross-DLL VEngineModel006 +0x8 dispatch"
    },
    {
      "from": "100d7bc5",
      "to": "200a6990",
      "confidence": "confirmed",
      "evidence": "cross-DLL VEngineModel006 +0x44 dispatch from the client shadow manager"
    },
    {
      "from": "200a6640",
      "to": "200a5f00",
      "confidence": "confirmed",
      "evidence": "the single direct call at 0x200a6945"
    }
  ]
}
