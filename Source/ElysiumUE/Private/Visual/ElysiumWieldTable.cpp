#include "ElysiumWieldTable.h"

// A pure data holder: the bake fills its properties and the runtime reads them. This translation
// unit exists so the type has one, beside its generated reflection code.
